# 🔍 系统完整检查清单 - 2026年3月3日

## ✅ 已完成项目

### 1. 数据库配置 ✅
- [x] MySQL 服务器已安装并运行
- [x] 数据库 `rwa_protocol` 已创建
- [x] 用户 `rwa_user` 已创建并授权
- [x] 所有表结构已导入
- [x] `.env` 文件配置正确

### 2. 后端服务 ✅
- [x] 后端服务已启动（端口 3001）
- [x] 数据库连接成功
- [x] API 路由正常工作（`/api/stats/global` 返回 200）
- [x] 管理后台可以访问数据

### 3. 智能合约 ✅
- [x] 合约已部署到 Hardhat 本地网络
- [x] 合约地址已配置在 `frontend/lib/contracts/addresses.ts`
- [x] 合约关系已配置（StRWA、RWAToken、StakingContract 等）

---

## ⚠️ 需要检查的项目

### 1. Hardhat 本地节点（必须）

**检查方法：**
```powershell
# 检查 8545 端口是否被占用
netstat -ano | findstr :8545
```

**如果未运行，需要启动：**
```bash
cd "E:\MyRWA_Project\rwa aura"
npx hardhat node
```

**重要：** 这个窗口必须一直开着，前端才能连接区块链。

---

### 2. 前端服务（必须）

**检查方法：**
- 访问 `http://localhost:3000`
- 如果无法访问，需要启动前端：

```bash
cd frontend
npm run dev
```

---

### 3. MetaMask 配置（必须）

**需要配置：**
1. 添加 Hardhat 本地网络到 MetaMask
   - 网络名称：`Hardhat Local`
   - RPC URL：`http://127.0.0.1:8545`
   - 链 ID：`31337`
   - 货币符号：`ETH`

2. 导入测试账户（使用 Hardhat 默认账户）
   - 私钥：`0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`
   - 或使用 Hardhat 默认账户列表

---

### 4. 前端功能测试清单

#### 4.1 首页 (`/`)
- [ ] 页面正常加载
- [ ] 统计数据正常显示
- [ ] 导航菜单正常

#### 4.2 仪表板 (`/dashboard`)
- [ ] 连接钱包后显示用户数据
- [ ] 显示质押总额
- [ ] 显示累计收益
- [ ] 显示节点等级

#### 4.3 质押页面 (`/stake`)
- [ ] 可以输入质押金额
- [ ] 可以授权 USDT
- [ ] 可以执行质押交易
- [ ] 交易成功后显示 stRWA 余额

#### 4.4 提取页面 (`/withdraw`)
- [ ] 显示可提取的 RWA 数量
- [ ] 可以提取 RWA
- [ ] 可以解锁 stRWA（通过 SwapContract）
- [ ] 显示交易状态

#### 4.5 兑换页面 (`/swap`)
- [ ] USDT ↔ RWA 兑换功能正常
- [ ] stRWA ↔ RWA 兑换功能正常
- [ ] 显示实时余额
- [ ] 显示兑换比例

#### 4.6 抽奖页面 (`/lucky`)
- [ ] 可以购买彩票
- [ ] 显示奖池信息
- [ ] 显示中奖历史

#### 4.7 管理后台 (`/admin`)
- [x] 全局统计正常显示
- [ ] 用户管理功能正常
- [ ] 质押统计正常
- [ ] 奖励统计正常

---

## 🚀 下一步操作步骤

### 步骤 1：启动 Hardhat 本地节点

```bash
cd "E:\MyRWA_Project\rwa aura"
npx hardhat node
```

**保持这个窗口一直开着！**

---

### 步骤 2：确认前端服务运行

```bash
cd frontend
npm run dev
```

访问 `http://localhost:3000` 确认前端正常。

---

### 步骤 3：配置 MetaMask

1. 打开 MetaMask
2. 点击网络下拉菜单 → "添加网络"
3. 填写：
   - 网络名称：`Hardhat Local`
   - RPC URL：`http://127.0.0.1:8545`
   - 链 ID：`31337`
   - 货币符号：`ETH`
4. 保存

5. 导入测试账户：
   - 点击账户图标 → "导入账户"
   - 选择"私钥"
   - 输入：`ac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80`
   - 导入

---

### 步骤 4：测试基础功能

1. **连接钱包**
   - 访问 `http://localhost:3000`
   - 点击"连接钱包"
   - 选择 MetaMask
   - 确认连接

2. **测试质押**
   - 访问 `/stake`
   - 输入质押金额（例如：100 USDT）
   - 点击"授权"（如果是第一次）
   - 点击"质押"
   - 在 MetaMask 确认交易
   - 等待交易确认

3. **测试查看数据**
   - 访问 `/dashboard`
   - 确认显示质押总额、收益等数据

---

## 📝 常见问题排查

### 问题 1：前端无法连接钱包

**可能原因：**
- MetaMask 未安装
- 未切换到 Hardhat Local 网络
- Hardhat 节点未运行

**解决方法：**
1. 确认 Hardhat 节点在运行（`npx hardhat node`）
2. 在 MetaMask 切换到 Hardhat Local 网络
3. 刷新前端页面

---

### 问题 2：交易失败

**可能原因：**
- 账户余额不足
- 合约地址配置错误
- 未授权代币

**解决方法：**
1. 检查账户余额（Hardhat 默认账户有 10000 ETH）
2. 确认合约地址配置正确
3. 先执行授权操作

---

### 问题 3：前端显示错误

**可能原因：**
- 合约地址未配置
- 网络不匹配
- RPC 连接失败

**解决方法：**
1. 检查 `frontend/lib/contracts/addresses.ts` 中的地址
2. 确认 MetaMask 连接到 Hardhat Local（链 ID 31337）
3. 确认 Hardhat 节点在运行

---

## ✅ 完成标准

系统完全就绪的标准：
- [x] 数据库连接正常
- [x] 后端服务运行正常
- [x] 管理后台可以访问
- [ ] Hardhat 节点运行中
- [ ] 前端服务运行中
- [ ] MetaMask 配置完成
- [ ] 可以连接钱包
- [ ] 可以执行质押交易
- [ ] 可以查看数据

---

**当前状态：** 后端和管理后台已完成，需要启动 Hardhat 节点和测试前端功能。

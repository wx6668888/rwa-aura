# 复投功能说明

## 当前状态

### ❌ 问题：复投功能尚未实现

**现状：**
1. **合约层面**：`StakingContract.sol` 中**没有** `reinvest` 函数
2. **合约层面**：**没有** `reinvestmentCount` mapping 来记录复投次数
3. **前端层面**：`reinvest-rewards-card.tsx` 中的 `reinvestCount` 是**硬编码为 0**
4. **前端层面**：只有显示逻辑，没有实际的复投功能

### 📋 复投逻辑设计（根据项目方案）

根据项目文档，复投逻辑应该是：

| 复投次数 | 额外奖励 | 说明 |
|---------|---------|------|
| 第1次复投 | +5% | 首次复投获得 5% 额外奖励 |
| 第3次复投 | +10% | 第3次复投获得 10% 额外奖励 |
| 第5次复投 | +15% | 第5次复投获得 15% 额外奖励 |
| 第10次+复投 | +20% | 第10次及以上复投获得 20% 额外奖励 |

**复投定义：**
- 用户将收益（RWA 或 USDT）重新质押
- 复投金额 = 原始收益 + 复投奖励
- 复投奖励根据复投次数计算

---

## 为什么复投后没有产生加成？

**原因：**
1. 合约中没有复投功能，所以无论用户如何操作，复投次数都是 0
2. 前端只是显示，没有实际的复投逻辑
3. 用户可能只是"再次质押"，而不是"复投"（复投需要将收益重新质押）

---

## 解决方案

### 方案1：实现完整的复投功能（推荐）

**需要实现：**

1. **合约层面**：
   ```solidity
   // 在 StakingContract.sol 中添加
   mapping(address => uint256) public reinvestmentCount;
   
   function reinvest(uint256 rwaAmount, uint256 usdtAmount) external {
       // 检查余额
       require(users[msg.sender].rwaPending >= rwaAmount, "Insufficient RWA");
       require(users[msg.sender].usdtRewards >= usdtAmount, "Insufficient USDT");
       
       // 计算复投奖励
       uint256 count = reinvestmentCount[msg.sender];
       uint256 bonusRate = getReinvestmentBonus(count);
       
       // 转换为 USDT 等值
       uint256 totalUSDT = usdtAmount + (rwaAmount * rwaPrice / 1e18);
       uint256 bonus = totalUSDT * bonusRate / 100;
       
       // 扣除余额
       users[msg.sender].rwaPending -= rwaAmount;
       users[msg.sender].usdtRewards -= usdtAmount;
       
       // 更新复投次数
       reinvestmentCount[msg.sender]++;
       
       // 重新质押（本金 + 奖励）
       stake(totalUSDT + bonus, users[msg.sender].referrer);
   }
   ```

2. **前端层面**：
   - 添加复投按钮/功能
   - 从合约读取 `reinvestmentCount`
   - 调用 `reinvest` 函数

### 方案2：临时方案 - 基于质押历史判断（不推荐）

通过检测用户是否将收益重新质押来判断复投，但这种方法不够准确。

---

## 下一步行动

1. ✅ **确认需求**：复投功能是否需要立即实现？
2. ⏳ **实现合约**：在 `StakingContract.sol` 中添加复投功能
3. ⏳ **更新前端**：添加复投 UI 和逻辑
4. ⏳ **测试**：测试复投功能是否正常工作

---

## 当前复投显示逻辑

**前端显示逻辑（`reinvest-rewards-card.tsx`）：**
- `reinvestCount = 0`（硬编码）
- `currentBonusRate = 5%`（因为 count < 1）
- 显示"再复投 1 次可解锁 +5% 加成率"

**但实际上：**
- 用户再次质押并不会增加复投次数
- 因为合约中没有复投功能
- 所以复投次数永远是 0

---

## 建议

如果复投功能是必需的，建议：
1. 先实现合约层面的复投功能
2. 然后更新前端，从合约读取复投次数
3. 添加复投按钮，让用户可以真正进行复投

如果暂时不需要复投功能，可以：
1. 在 UI 上标注"即将推出"或"开发中"
2. 或者暂时隐藏复投奖励卡片

# 实时收益功能测试指南

## 🔄 如何验证新逻辑是否生效

### 步骤1：清除缓存并刷新

1. **硬刷新浏览器**：
   - Windows: `Ctrl + Shift + R`
   - Mac: `Cmd + Shift + R`
   - 或者按 `F12` 打开开发者工具，右键刷新按钮，选择"清空缓存并硬性重新加载"

2. **或者重启前端服务**：
   ```bash
   # 停止前端服务（Ctrl + C）
   # 然后重新启动
   cd frontend
   npm run dev
   ```

### 步骤2：测试切换页面后的行为

#### 测试场景：

1. **打开仪表板**：
   - 访问 http://localhost:3000/dashboard
   - 记录当前 RWA 余额（例如：0.000123 RWA）

2. **等待 10 秒**：
   - 观察数字是否每秒增长
   - 记录 10 秒后的余额（例如：0.000456 RWA）

3. **切换到其他页面**：
   - 点击导航栏，访问 http://localhost:3000/stake
   - 等待 10 秒

4. **返回仪表板**：
   - 点击导航栏，返回 http://localhost:3000/dashboard
   - **关键检查**：余额应该继续从刚才的数字增长，而不是重新开始

#### 预期行为（新逻辑）：

```
时间 0s:  仪表板显示 0.000123 RWA
时间 10s: 仪表板显示 0.000456 RWA (增长了 0.000333)
时间 10s: 切换到质押页面
时间 20s: 返回仪表板
时间 20s: 仪表板显示 0.000789 RWA (继续增长，不是从 0.000123 重新开始)
```

#### 旧逻辑的问题：

```
时间 0s:  仪表板显示 0.000123 RWA
时间 10s: 仪表板显示 0.000456 RWA
时间 10s: 切换到质押页面
时间 20s: 返回仪表板
时间 20s: 仪表板显示 0.000456 RWA (❌ 重新开始，没有继续增长)
```

### 步骤3：检查浏览器控制台

1. 按 `F12` 打开开发者工具
2. 切换到 Console 标签
3. 查看是否有错误信息
4. 如果有 TypeScript 错误，可能需要重启服务

### 步骤4：验证计算准确性

#### 手动计算：

假设你质押了 10,000 USDT：

- 每日收益：10,000 × 0.8% = 80 RWA
- 每秒收益：80 ÷ 86400 = 0.000926 RWA
- 10 秒收益：0.000926 × 10 = 0.00926 RWA
- 60 秒收益：0.000926 × 60 = 0.0556 RWA

#### 验证步骤：

1. 记录初始余额：例如 0.000000 RWA
2. 等待 60 秒
3. 记录新余额：例如 0.055600 RWA
4. 计算增长：0.055600 - 0.000000 = 0.0556 RWA
5. 对比预期：应该约等于 0.0556 RWA

## 🐛 如果还是有问题

### 问题1：数字不增长

**可能原因**：
- 没有质押 USDT
- 钱包未连接
- 链上数据为 0

**解决方案**：
1. 确保已连接钱包
2. 确保已质押 USDT（最低 100 USDT）
3. 刷新页面重新获取链上数据

### 问题2：切换页面后重置

**可能原因**：
- 浏览器缓存了旧代码
- Next.js 热重载失败

**解决方案**：
1. 硬刷新浏览器（Ctrl + Shift + R）
2. 重启前端服务
3. 清除浏览器缓存

### 问题3：数字增长太快或太慢

**可能原因**：
- 计算逻辑错误
- 质押金额读取错误

**解决方案**：
1. 检查浏览器控制台是否有错误
2. 验证质押金额是否正确显示
3. 手动计算预期收益并对比

## 📊 调试技巧

### 在浏览器控制台查看实时数据

打开浏览器控制台（F12），在 Console 中输入：

```javascript
// 查看当前组件状态（需要 React DevTools）
// 1. 安装 React DevTools 扩展
// 2. 切换到 Components 标签
// 3. 找到 EarningsCard 组件
// 4. 查看 hooks 中的状态值
```

### 添加临时日志

如果需要更详细的调试信息，可以在代码中添加 `console.log`：

```typescript
// 在 useEffect 中添加
useEffect(() => {
  console.log('Base time:', baseTimeRef.current)
  console.log('Base balance:', baseBalanceRef.current)
  console.log('Per second earnings:', perSecondEarnings)
  
  const interval = setInterval(() => {
    const elapsedSeconds = (Date.now() - baseTimeRef.current) / 1000
    const newBalance = baseBalanceRef.current + (perSecondEarnings * elapsedSeconds)
    console.log('Elapsed seconds:', elapsedSeconds)
    console.log('New balance:', newBalance)
    setLiveRwaBalance(newBalance)
  }, 1000)
  
  return () => clearInterval(interval)
}, [isConnected, totalStakedNum, perSecondEarnings, rwaPendingNum])
```

## ✅ 成功标志

如果以下所有条件都满足，说明功能正常：

1. ✅ RWA 余额每秒增长
2. ✅ 切换页面后返回，余额继续增长（不重置）
3. ✅ 增长速度符合预期（每日 0.8%）
4. ✅ 刷新页面后，从最新的链上数据开始继续增长
5. ✅ 提现后，余额更新并从新数据开始增长

## 🎯 核心改进

### 旧逻辑（有问题）：
```typescript
// 每次组件挂载都从链上数据开始
setInterval(() => {
  setLiveRwaBalance(prev => prev + perSecondEarnings)
}, 1000)
```

### 新逻辑（已修复）：
```typescript
// 记录基准时间和基准余额
const baseTimeRef = useRef<number>(Date.now())
const baseBalanceRef = useRef<number>(rwaPendingNum)

// 基于时间差计算累计收益
setInterval(() => {
  const elapsedSeconds = (Date.now() - baseTimeRef.current) / 1000
  const newBalance = baseBalanceRef.current + (perSecondEarnings * elapsedSeconds)
  setLiveRwaBalance(newBalance)
}, 1000)
```

### 关键区别：

- **旧逻辑**：每秒加一个固定值，切换页面后重置
- **新逻辑**：基于时间差计算，切换页面后继续累计

## 📝 总结

新逻辑通过 `useRef` 记录基准时间和基准余额，确保即使组件重新挂载，也能基于原始基准继续计算累计收益。这样就解决了切换页面后重置的问题。

如果还有问题，请提供：
1. 浏览器控制台的错误信息
2. 具体的测试步骤和观察到的行为
3. 质押金额和预期收益

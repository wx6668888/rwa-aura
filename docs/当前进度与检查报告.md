# 当前进度与检查报告

## 一、当前做到哪一步了

### 已完成（与现机制一致）

| 层级 | 状态 | 说明 |
|------|------|------|
| **合约** | ✅ | 最低提现 100、8% 手续费拆分、紧急退出仅锁仓 USDT 且按天数比例、无 3×/15% 推荐封顶 |
| **前端-提现** | ✅ | RWA 收益卡 + 本金提取区分离、MIN_WITHDRAW=100、withdraw-mode 8%/120% stRWA |
| **前端-紧急** | ✅ | 仅展示未到期锁仓 USDT、用合约 getEmergencyWithdrawPreview 算可退额 |
| **后端** | ✅ | EventMonitor 处理所有提现/本金/紧急事件并 sync 用户与 RWA 状态；withdrawal_fee_rate=0.08；PriceOracleService 最低 100 |
| **知识库-中英** | ✅ | 提现/冷却/紧急/stRWA/推荐计算已更新；多语言关键条用英文兜底 |
| **知识库-多语言** | ✅ | ja/ko/hi/pt/es/ru/fr/ar 的 withdraw-cooldown-fee 已改为 8%、100、92 RWA，并去掉 3×/15% 表述 |
| **i18n** | ✅ | 中英 FAQ a5/a6/a7/a8/a10、各语言 feeCharge 与 notice、minWithdraw、withdrawUModeDesc 已对齐 |
| **文档** | ✅ | `知识库完整文案-中文.md`、`用户端整体逻辑.md` 已按新机制更新；公告中提现相关已改 8% |

### 本次检查中已修改的残留

1. **多语言知识库**（ja/ko/hi/pt/es/ru/fr/ar）  
   - `withdraw-cooldown-fee`：5% → 8%，95 RWA → 92 RWA，最低 10/8 USDT → 100，并补充 stRWA 模式说明。  
   - ja/ko 的 `referral-reward-calc`：删除「个人累计 3 倍上限、1 日 15% 上限」。

2. **i18n**  
   - ko/es/hi/fr/pt/ru/ja 的 `feeCharge`：由「5% 手续费（销毁）」改为「立即手续费 (8%)」等对应翻译。  
   - 韩语 `notice`：由 5% 销毁改为 8% 拆分 + stRWA 说明。

3. **公告**（`announcements-content-zh.ts`）  
   - 「手续费 = 提现金额 × 5%」→「8%（3% 回购/销毁、3% Treasury、2% 社区池）」；  
   - 「提现手续费 5% 直接销毁」→「立即到账提现 8% 手续费（3% 回购/销毁…）」。

4. **文档**（`用户端整体逻辑.md`）  
   - RWA 提现卡片说明改为：仅收益；本金在「本金提取」区按类型操作；冷却 + 8% 手续费 / stRWA 0%+120% 锁 30 天。

---

## 二、无需改动的「5%」等（业务设计保留）

- **抽奖池 5% 进国库**：仍为协议设计，各语言知识库与 i18n 保留。  
- **节点等级比例**（如 L2 5%、L3 8%）：节点表与 `backend/src/models/types.ts` 等保留。  
- **税费/流动性 5%**：治理/税务说明中的比例，与提现费无关。  
- **0.01～0.05 BNB 建议**：Gas 建议，非提现费率。  
- **Reinvest 5%～20%**：复投阶梯奖励，非提现费。

---

## 三、建议后续（可选）

| 项 | 说明 |
|----|------|
| **历史/设计文档** | `项目逻辑与用户层审查报告.md`、`灵活退出机制-梳理与改进方案.md`、`FRONTEND_PAGES_AUDIT.md`、`用户提现逻辑完整说明.md`、`锁仓与提现逻辑说明.md` 等仍含旧「5%、10 RWA、50% 紧急」等，属历史设计文档，不影响运行；若需统一可批量替换为「以当前合约与知识库为准」。 |
| **本地验证** | 在项目根目录执行 `npm run compile && npm test`，在 `frontend` 执行 `npx next build --webpack`，确认通过。 |
| **前端 dev** | 在 `frontend` 下执行 `npm run dev` 启动 http://localhost:3000/。 |

---

## 四、结论

- **合约、前端、后端、中英与多语言知识库、i18n、公告与核心文档**已与当前机制一致（最低 100、8% 拆分、stRWA 0%+120% 锁 30 天、紧急仅锁仓 USDT、无 3×/15% 推荐封顶）。  
- 本次检查已修掉多语言知识库、i18n、公告和用户端逻辑文档中的提现/手续费残留。  
- 剩余可选工作仅为历史设计类文档的文案统一与本地构建/测试验证。

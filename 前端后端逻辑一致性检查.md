# 🔍 前端后端逻辑一致性检查

## 📊 当前状态分析

### 后端逻辑（DailyYieldService.ts）

**计算方式**：
```typescript
// 每日收益计算
const usdtYield = totalStaked × 0.008  // 0.8% 日收益率
const rwaYield = usdtYield / 0.85      // 转换为 RWA（RWA 价格 0.85）
```

**发放时间**：
- ✅ 每天 **00:00 UTC** 统一发放一次
- ✅ 不是实时计算，不是从质押时间开始计算
- ✅ 所有用户同时收到收益

**关键特性**：
- 使用 `total_staked`（总质押金额）
- 固定 0.8% 日收益率
- RWA 价格固定 0.85
- **没有锁仓期限倍数**

---

### 前端逻辑（rwa-withdraw-card.tsx）

**计算方式**：
```typescript
// 实时计算收益
const baseDailyRate = 0.008  // 0.8%
const adjustedDailyRate = baseDailyRate * lockMultiplier  // 根据锁仓期限调整
const dailyUSDTYield = stakeAmount * adjustedDailyRate
const dailyRWAYield = dailyUSDTYield / rwaPrice  // rwaPrice = 0.85
const perSecondRWAYield = dailyRWAYield / 86400

// 从质押时间到现在的收益
const elapsedSeconds = currentTime - stakeTime
const stakeRWA = perSecondRWAYield * elapsedSeconds
```

**锁仓期限倍数**：
- flexible: 1.0
- 30天: 1.3
- 90天: 1.6
- 180天: 2.0
- 365天: 2.5

**关键特性**：
- ✅ 实时计算（每秒更新）
- ✅ 基于每笔质押分别计算
- ✅ 从质押时间开始计算
- ✅ **有锁仓期限倍数**
- ✅ RWA 价格 0.85

---

## ❌ 发现的不一致问题

### 1. **锁仓期限倍数**

**前端**：有锁仓期限倍数（1.0, 1.3, 1.6, 2.0, 2.5）
**后端**：没有锁仓期限倍数

**影响**：
- 前端显示的收益会高于后端实际分发的收益
- 用户可能看到 100 RWA，但实际只能提取 50 RWA（如果锁仓期限是 flexible）

---

### 2. **计算时间点**

**前端**：从质押时间开始实时计算
**后端**：每天 00:00 UTC 统一发放

**影响**：
- 前端显示的是"实时累计收益"
- 后端实际分发的是"每日固定收益"
- 如果用户在 12:00 质押，前端会显示 12 小时的收益，但后端要到第二天 00:00 才发放

---

### 3. **计算基础**

**前端**：基于每笔质押分别计算
**后端**：基于 `total_staked`（总质押金额）

**影响**：
- 如果用户有多笔质押，前端会分别计算每笔的收益
- 后端只基于总质押金额计算一次

---

## ✅ 应该的一致性

### 方案 1：前端只显示链上数据（推荐）

**逻辑**：
- 前端只显示链上的 `rwaPending`
- 不进行实时计算
- 显示提示："收益每天 00:00 UTC 统一发放"

**优点**：
- ✅ 与后端完全一致
- ✅ 不会误导用户
- ✅ 简单可靠

**缺点**：
- ❌ 用户体验稍差（看不到实时收益）

---

### 方案 2：前端实时计算，但标注为"预估"

**逻辑**：
- 前端实时计算收益（带锁仓倍数）
- 但明确标注为"预估收益"
- 显示"实际可提取"和"预估收益"两个值

**优点**：
- ✅ 用户体验好
- ✅ 信息透明

**缺点**：
- ❌ 需要维护两套逻辑
- ❌ 可能造成混淆

---

### 方案 3：后端支持锁仓期限倍数

**逻辑**：
- 后端在计算收益时考虑锁仓期限
- 前端实时计算与后端逻辑一致

**优点**：
- ✅ 完全一致
- ✅ 用户体验好

**缺点**：
- ❌ 需要修改后端逻辑
- ❌ 需要修改合约（可能需要）

---

## 🎯 推荐方案

### 短期方案（立即修复）

**采用方案 1**：
1. 前端只显示链上的 `rwaPending`
2. 移除实时计算逻辑
3. 添加提示："收益每天 00:00 UTC 统一发放"

### 长期方案（优化）

**采用方案 3**：
1. 后端支持锁仓期限倍数
2. 前端实时计算与后端逻辑一致
3. 显示"实际可提取"和"预估收益"

---

## 📝 需要修复的代码

### 1. rwa-withdraw-card.tsx

**当前**：
```typescript
// 实时计算收益
const availableRWA = totalRwaEarning.toFixed(6)  // 显示实时计算的收益
const onChainAvailable = rwaPendingNum  // 链上实际可提取的
```

**修复后**：
```typescript
// 只显示链上数据
const availableRWA = rwaPendingNum.toFixed(6)  // 只显示链上数据
const onChainAvailable = rwaPendingNum  // 链上实际可提取的
```

### 2. stat-cards.tsx

**同样需要修复**，移除实时计算逻辑

---

## ⚠️ 当前问题

1. **前端显示 100 RWA**，但实际链上可能只有 50 RWA（如果后端还没分发）
2. **锁仓期限倍数**导致前端显示高于后端实际分发
3. **实时计算**导致前端显示与后端不一致

---

## ✅ 建议

**立即修复**：
- 前端只显示链上的 `rwaPending`
- 移除实时计算逻辑
- 添加提示说明收益发放时间

**后续优化**：
- 后端支持锁仓期限倍数
- 前端实时计算与后端逻辑一致

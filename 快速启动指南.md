# 🚀 RWA 项目快速启动指南

**项目状态**: ✅ 100% 完成  
**版本**: v1.0.0

---

## 📋 前置要求

- Node.js >= 18
- npm 或 yarn
- MySQL 5.7+ 或 8.0+
- MetaMask 钱包（用于前端测试）

---

## 🎯 快速启动（3步）

### 步骤 1: 启动 Hardhat 本地节点

```bash
# Windows 用户（推荐）
双击运行: start-hardhat-with-deploy.bat

# 或命令行
npx hardhat node
```

**等待**: 节点启动后，在另一个终端继续。

---

### 步骤 2: 部署合约（如果使用 start-hardhat-with-deploy.bat，此步已自动完成）

```bash
# 新终端
npx hardhat run scripts/deploy-all.ts --network localhost
```

**记录**: 复制输出的合约地址，稍后需要更新前端配置。

---

### 步骤 3: 启动前端

```bash
# 新终端
cd frontend
npm install  # 如果还没安装
npm run dev
```

**访问**: http://localhost:3000

---

## 🔧 完整启动（包含后端）

### 1. 数据库准备

```bash
# 创建数据库
mysql -u root -p
CREATE DATABASE rwa_protocol CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;

# 运行数据库脚本
mysql -u root -p rwa_protocol < backend/src/config/database.sql
```

### 2. 配置环境变量

#### 后端 `.env`

```env
# 数据库
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=rwa_protocol

# 网络
BSC_TESTNET_RPC_URL=http://127.0.0.1:8545
BACKEND_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# 合约地址（部署后更新）
STAKING_CONTRACT_ADDRESS=0x...
RWA_TOKEN_ADDRESS=0x...
```

#### 前端 `.env.local`

```env
NEXT_PUBLIC_CHAIN_ID=31337
NEXT_PUBLIC_BSC_TESTNET_RPC_URL=http://127.0.0.1:8545
```

### 3. 启动服务

```bash
# 终端 1: Hardhat 节点
npx hardhat node

# 终端 2: 部署合约
npx hardhat run scripts/deploy-all.ts --network localhost

# 终端 3: 后端服务
cd backend
npm install
npm run dev

# 终端 4: 前端
cd frontend
npm install
npm run dev
```

---

## ✅ 验证安装

### 1. 检查合约部署

访问 Hardhat 节点输出，确认所有合约已部署。

### 2. 检查前端

访问 http://localhost:3000，应该能看到：
- ✅ 首页正常加载
- ✅ 钱包连接功能正常
- ✅ 导航菜单正常

### 3. 检查后端

查看后端终端，应该看到：
- ✅ 数据库连接成功
- ✅ EventMonitor 已启动
- ✅ 所有服务正常运行

---

## 🧪 运行测试

```bash
# 运行所有测试
npm test

# 运行特定测试
npx hardhat test test/FullIntegration.test.ts
npx hardhat test test/Security.test.ts
npx hardhat test test/TreasuryContract.test.ts
```

---

## 📝 测试流程

### 1. 质押测试

1. 连接钱包（MetaMask）
2. 切换到 Hardhat Local 网络
3. 导入测试账户（私钥在 Hardhat 节点输出中）
4. 访问 `/stake` 页面
5. 输入质押金额（≥100 USDT）
6. 选择锁仓期限
7. 授权并质押

**预期结果**:
- ✅ 质押成功
- ✅ 50% 进入 Treasury
- ✅ 50% 进入合约
- ✅ stRWA 已铸造

### 2. 奖励测试

1. 等待后端计算奖励（或手动触发）
2. 访问 `/dashboard` 页面
3. 查看奖励余额

**预期结果**:
- ✅ RWA 奖励正确显示
- ✅ USDT 奖励正确显示

### 3. 提现测试

1. 访问 `/withdraw` 页面
2. 选择提现模式（提U 或 持RWA）
3. 输入提现金额
4. 确认提现

**预期结果**:
- ✅ 提现成功
- ✅ 手续费正确扣除

---

## 🐛 常见问题

### 问题 1: 合约地址未找到

**解决**: 更新 `frontend/lib/contracts/addresses.ts` 中的合约地址。

### 问题 2: 数据库连接失败

**解决**: 检查 `.env` 中的数据库配置。

### 问题 3: 前端无法连接钱包

**解决**: 
1. 确认 MetaMask 已安装
2. 确认已切换到正确的网络
3. 检查浏览器控制台错误

### 问题 4: 后端服务无法启动

**解决**:
1. 检查 Node.js 版本（需要 >= 18）
2. 检查数据库是否运行
3. 检查环境变量配置

---

## 📚 更多文档

- `部署准备清单.md` - 详细部署指南
- `项目完成总结.md` - 项目总结
- `项目最终完成报告.md` - 完成报告
- `RWA项目最优完整方案.md` - 完整方案

---

## 🎉 开始使用

一切就绪后，您可以：

1. **测试质押功能** - 访问 `/stake`
2. **查看收益** - 访问 `/dashboard`
3. **测试提现** - 访问 `/withdraw`
4. **计算收益** - 访问 `/calculator`

**祝您使用愉快！** 🚀

# 🔧 数据库连接配置指南 - 2026年3月3日

## 📋 当前问题

**错误信息**：
```
Access denied for user 'rwa_user'@'localhost' (using password: YES)
```

**原因**：
- 数据库用户 `rwa_user` 不存在
- 或者密码不正确
- 或者数据库 `rwa_protocol` 不存在

---

## ✅ 解决方案

### 方案 1：使用自动配置脚本（推荐）

**Windows PowerShell**：
```powershell
cd backend
.\setup-database.ps1
```

脚本会：
1. ✅ 检查 MySQL 是否安装
2. ✅ 创建数据库 `rwa_protocol`
3. ✅ 创建用户 `rwa_user`（需要您输入密码）
4. ✅ 初始化数据库表结构
5. ✅ 创建 `.env` 文件

---

### 方案 2：手动配置

#### 步骤 1：创建数据库和用户

**使用 MySQL 命令行**：
```sql
-- 登录 MySQL（需要 root 权限）
mysql -u root -p

-- 创建数据库
CREATE DATABASE IF NOT EXISTS rwa_protocol CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户（请修改密码）
CREATE USER IF NOT EXISTS 'rwa_user'@'localhost' IDENTIFIED BY 'your_password_here';

-- 授予权限
GRANT ALL PRIVILEGES ON rwa_protocol.* TO 'rwa_user'@'localhost';
FLUSH PRIVILEGES;

-- 退出
EXIT;
```

#### 步骤 2：初始化数据库表结构

```bash
cd backend
mysql -u rwa_user -p rwa_protocol < src/config/database.sql
```

#### 步骤 3：配置环境变量

**创建或编辑 `backend/.env` 文件**：
```env
# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=rwa_user
DB_PASSWORD=your_password_here  # 替换为您在步骤1中设置的密码
DB_NAME=rwa_protocol

# Server Configuration
PORT=3001

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
```

---

### 方案 3：使用现有数据库

如果您已经有 MySQL 数据库，可以：

1. **使用现有数据库**：
   - 修改 `backend/.env` 中的 `DB_NAME` 为您的数据库名

2. **使用现有用户**：
   - 修改 `backend/.env` 中的 `DB_USER` 和 `DB_PASSWORD`

3. **初始化表结构**：
   ```bash
   mysql -u your_user -p your_database < backend/src/config/database.sql
   ```

---

## 🔍 验证数据库连接

### 1. 检查 MySQL 服务

**Windows**：
```powershell
Get-Service -Name MySQL* | Select-Object Name, Status
```

**如果服务未运行**：
```powershell
Start-Service MySQL80  # 或您的 MySQL 服务名
```

### 2. 测试数据库连接

```bash
mysql -u rwa_user -p rwa_protocol
```

如果能够登录，说明用户和数据库配置正确。

### 3. 检查表结构

```sql
USE rwa_protocol;
SHOW TABLES;
```

应该看到以下表：
- `users`
- `stakes`
- `rewards`
- `referral_relations`
- `node_level_history`
- `rwa_stakes`
- `department_volumes`
- `system_config`

---

## 🚀 启动后端服务

配置完成后，启动后端服务：

```bash
cd backend
npm run dev
```

**成功启动的标志**：
```
✅ Database connection established
✅ Listening on port 3001
```

**如果仍然看到数据库连接错误**：
1. 检查 `.env` 文件中的密码是否正确
2. 确认 MySQL 服务正在运行
3. 确认数据库和用户已创建
4. 确认用户有权限访问数据库

---

## 📝 常见问题

### Q1: MySQL 服务未运行

**解决方案**：
```powershell
# Windows
Start-Service MySQL80

# 或使用服务管理器
services.msc
# 找到 MySQL 服务并启动
```

### Q2: 忘记 root 密码

**解决方案**：
1. 停止 MySQL 服务
2. 使用 `--skip-grant-tables` 启动 MySQL
3. 重置 root 密码
4. 重启 MySQL 服务

### Q3: 用户已存在但密码错误

**解决方案**：
```sql
-- 使用 root 登录
mysql -u root -p

-- 重置用户密码
ALTER USER 'rwa_user'@'localhost' IDENTIFIED BY 'new_password';
FLUSH PRIVILEGES;

-- 更新 .env 文件中的密码
```

### Q4: 数据库已存在但表不存在

**解决方案**：
```bash
cd backend
mysql -u rwa_user -p rwa_protocol < src/config/database.sql
```

---

## 🎯 快速检查清单

- [ ] MySQL 服务正在运行
- [ ] 数据库 `rwa_protocol` 已创建
- [ ] 用户 `rwa_user` 已创建
- [ ] 用户有权限访问数据库
- [ ] 数据库表结构已初始化
- [ ] `backend/.env` 文件已创建
- [ ] `.env` 文件中的密码正确
- [ ] 后端服务可以成功连接数据库

---

**完成时间**：2026年3月3日  
**状态**：待配置数据库

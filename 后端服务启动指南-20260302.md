# 🚀 后端服务启动指南 - 2026年3月2日

**目的**: 配置并启动后端服务，实现自动发放 RWA 奖励

**状态**: ✅ 配置完成，准备启动

---

## 📋 前置要求

### 1. 数据库 (MySQL)

**需要安装 MySQL**:
- MySQL 5.7+ 或 MySQL 8.0+
- 创建数据库和用户

**安装 MySQL** (如果还没有):
```bash
# Windows: 下载 MySQL Installer
# https://dev.mysql.com/downloads/installer/

# 或使用 Docker
docker run --name mysql-rwa -e MYSQL_ROOT_PASSWORD=rootpassword -e MYSQL_DATABASE=rwa_protocol -e MYSQL_USER=rwa_user -e MYSQL_PASSWORD=rwa_password -p 3306:3306 -d mysql:8.0
```

---

### 2. Redis (可选，用于价格缓存)

**安装 Redis** (如果还没有):
```bash
# Windows: 下载 Redis for Windows
# https://github.com/microsoftarchive/redis/releases

# 或使用 Docker
docker run --name redis-rwa -p 6379:6379 -d redis:latest
```

---

## 🔧 配置步骤

### Step 1: 创建后端 `.env` 文件

**在 `backend` 目录下创建 `.env` 文件**:

```bash
cd backend
cp .env.example .env
```

**编辑 `.env` 文件**，填入以下配置：

```env
# ============================================
# Network Configuration
# ============================================
BSC_RPC_URL=http://127.0.0.1:8545

# ============================================
# Contract Addresses (Hardhat Local)
# ============================================
STAKING_CONTRACT_ADDRESS=0xFD471836031dc5108809D173A067e8486B9047A3
RWA_TOKEN_ADDRESS=0x7bc06c482DEAd17c0e297aFbC32f6e63d3846650
USDT_TOKEN_ADDRESS=0x7969c5eD335650692Bc04293B07F5BF2e7A673C0
ST_RWA_ADDRESS=0xc351628EB244ec633d5f21fBD6621e1a683B1181
SWAP_CONTRACT_ADDRESS=0xB0D4afd8879eD9F52b28595d31B441D079B2Ca07

# ============================================
# Backend Account (Must match contract's backendAddress)
# ============================================
# ⚠️ IMPORTANT: Use deployer's private key
BACKEND_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# ============================================
# Database Configuration
# ============================================
DB_HOST=localhost
DB_PORT=3306
DB_USER=rwa_user
DB_PASSWORD=rwa_password
DB_NAME=rwa_protocol

# ============================================
# Redis Configuration
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379

# ============================================
# Service Configuration
# ============================================
PORT=3001
CONFIRMATION_BLOCKS=12
POLL_INTERVAL=5000
MAX_REWARD_PER_CALL=10000000000000000000000
DAILY_YIELD_RATE=0.008
PRICE_ORACLE_CACHE_TTL=300
PANCAKE_ROUTER_ADDRESS=0x10ED43C718714eb63d5aA57B78B54704E256024E
LOG_LEVEL=info
```

---

### Step 2: 创建数据库

**连接到 MySQL**:
```bash
mysql -u root -p
```

**创建数据库和用户**:
```sql
CREATE DATABASE IF NOT EXISTS rwa_protocol;
CREATE USER IF NOT EXISTS 'rwa_user'@'localhost' IDENTIFIED BY 'rwa_password';
GRANT ALL PRIVILEGES ON rwa_protocol.* TO 'rwa_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

---

### Step 3: 初始化数据库表

**运行数据库迁移**:
```bash
cd backend
npm run migrate:up
```

**或手动执行 SQL**:
```bash
mysql -u rwa_user -p rwa_protocol < src/config/database.sql
```

---

### Step 4: 安装依赖

**安装后端依赖**:
```bash
cd backend
npm install
```

---

### Step 5: 同步用户数据到数据库

**重要**: 后端服务需要从数据库读取用户信息。如果用户已经质押，需要先同步到数据库。

**创建同步脚本** (可选):
```bash
# 手动插入测试用户
mysql -u rwa_user -p rwa_protocol

INSERT INTO users (address, total_staked, is_active, first_stake_time, node_level)
VALUES ('0x70997970C51812dc3A010C7d01b50e0d17dc79C8', 11000000000000000000000, TRUE, UNIX_TIMESTAMP('2026-03-02 09:35:48'), 1);
```

---

## 🚀 启动服务

### 方式 1: 开发模式（推荐）

```bash
cd backend
npm run dev
```

**输出示例**:
```
✅ Database connected
✅ Redis connected
✅ HTTP server listening on port 3001
✅ Event monitor started
✅ Scheduler started
🚀 Backend service is running
```

---

### 方式 2: 生产模式

```bash
cd backend
npm run build
npm start
```

---

## 📊 服务功能

### 1. 定时任务

**每日收益计算** (每天 00:00 UTC):
- 自动计算所有活跃用户的 0.8% 日收益
- 更新数据库 `rwa_pending`
- 调用合约 `updateUserRewards` 更新链上余额

**价格刷新** (每 5 分钟):
- 从 PancakeSwap 获取 RWA/USDT 价格
- 缓存到 Redis

**节点等级同步** (每小时):
- 检查并更新用户节点等级

---

### 2. 事件监听

**监听质押事件**:
- 自动监听链上 `StakeEvent`
- 处理推荐关系绑定
- 计算并发放推荐奖励
- 更新团队业绩

---

### 3. API 接口

**健康检查**:
```
GET http://localhost:3001/health
```

**用户信息**:
```
GET http://localhost:3001/api/user/:address
```

**奖励历史**:
```
GET http://localhost:3001/api/rewards/:address
```

---

## ⚠️ 重要提示

### 1. 后端地址必须匹配

**合约中的 `backendAddress` 是 `immutable`**，部署时设置。

**当前后端地址**: `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` (Deployer)

**`.env` 中的 `BACKEND_PRIVATE_KEY` 必须对应这个地址的私钥**。

---

### 2. 数据库同步

**后端服务从数据库读取用户信息**，如果用户已经质押但数据库中没有记录，需要先同步。

**解决方案**:
- 运行 `EventMonitor` 监听历史事件并同步
- 或手动插入用户数据

---

### 3. 定时任务时间

**每日收益计算**: 每天 00:00 UTC（北京时间 08:00）

**如果需要立即测试**:
- 可以手动触发: `GET http://localhost:3001/api/trigger/daily-yield`
- 或修改 cron 表达式为更频繁的时间（测试用）

---

## 🔍 验证服务运行

### 1. 检查服务状态

**查看日志**:
```bash
# 应该看到:
✅ Database connected
✅ Redis connected
✅ HTTP server listening on port 3001
✅ Event monitor started
✅ Scheduler started
```

---

### 2. 测试 API

**健康检查**:
```bash
curl http://localhost:3001/health
```

**用户信息**:
```bash
curl http://localhost:3001/api/user/0x70997970C51812dc3A010C7d01b50e0d17dc79C8
```

---

### 3. 检查定时任务

**等待定时任务执行**，或手动触发:
```bash
# 如果 API 支持手动触发
curl -X POST http://localhost:3001/api/trigger/daily-yield
```

---

## 📝 下一步

1. ✅ **配置完成** - 已创建 `.env.example` 和配置指南
2. ⏳ **安装 MySQL** - 如果还没有安装
3. ⏳ **创建数据库** - 运行迁移脚本
4. ⏳ **启动服务** - `npm run dev`
5. ⏳ **验证运行** - 检查日志和 API

---

## 🐛 常见问题

### 问题 1: 数据库连接失败

**错误**: `Database connection failed`

**解决方案**:
- 检查 MySQL 是否运行
- 检查 `.env` 中的数据库配置
- 检查用户权限

---

### 问题 2: Redis 连接失败

**错误**: `Redis connection failed`

**解决方案**:
- Redis 是可选的，如果不需要价格缓存可以暂时不配置
- 或安装并启动 Redis

---

### 问题 3: 合约调用失败

**错误**: `Only backend can call`

**解决方案**:
- 检查 `BACKEND_PRIVATE_KEY` 是否对应正确的地址
- 检查合约中的 `backendAddress` 是否匹配

---

### 问题 4: 用户数据不存在

**错误**: `No users found`

**解决方案**:
- 确保数据库中有用户数据
- 运行 `EventMonitor` 同步历史事件
- 或手动插入测试用户

---

**配置完成后，启动服务即可实现自动发放奖励！**

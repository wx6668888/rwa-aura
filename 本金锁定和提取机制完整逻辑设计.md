# 🔒 本金锁定和提取机制完整逻辑设计

## 📋 核心设计理念

### 用户预期

**用户选择锁仓期限时，期望**：
1. ✅ 锁仓期限越长，收益倍数越高（已实现）
2. ✅ 锁仓期结束后，可以提取本金（**未实现**）
3. ✅ 提前解锁需要支付惩罚（可选）
4. ✅ 灵活锁仓可以随时提取（可选）

---

## 🎯 完整逻辑设计

### 1. 质押时的资金分配和锁定

#### 1.1 资金分配（保持不变）

```
用户质押 10,000 USDT，选择 180天锁仓

资金分配：
├─ 50% (5,000 USDT) → Treasury
│   └─ 状态：永久锁定，不返还
│   └─ 用途：实体投资
│
└─ 50% (5,000 USDT) → Staking Contract
    └─ 状态：根据锁仓期限锁定
    └─ 用途：用户本金，锁仓期结束后可提取
```

#### 1.2 本金锁定逻辑

**数据结构**:
```solidity
struct LockedPrincipal {
    uint256 stakeId;           // 质押ID
    uint256 principalAmount;   // 本金金额（合约中的50%，18 decimals）
    uint256 lockStartTime;     // 锁仓开始时间
    uint256 lockEndTime;       // 锁仓结束时间
    bool isWithdrawn;          // 是否已提取
    uint256 lockPeriod;        // 锁仓期限（天数）
}

mapping(address => LockedPrincipal[]) public userLockedPrincipals;
```

**质押时的处理**:
```solidity
function stake(uint256 amount, address referrer, uint256 lockPeriod) {
    // ... 现有逻辑（资金分配）...
    
    // 如果锁仓期限 > 0，锁定本金
    if (lockPeriod > 0) {
        userLockedPrincipals[msg.sender].push(LockedPrincipal({
            stakeId: stakeId,
            principalAmount: contractAmount,  // 合约中的50%
            lockStartTime: block.timestamp,
            lockEndTime: block.timestamp + (lockPeriod * 1 days),
            isWithdrawn: false,
            lockPeriod: lockPeriod
        }));
    } else {
        // 灵活锁仓：本金不锁定，但需要单独管理
        // 可以随时提取，但需要24小时冷却期
    }
}
```

---

### 2. 锁仓期结束后的本金提取

#### 2.1 正常提取（锁仓期已满）

**逻辑**:
```
用户质押 10,000 USDT，选择 180天锁仓

180天后：
├─ 锁仓期结束 ✅
├─ 本金状态：解锁，可以提取
├─ 可提取金额：5,000 USDT（合约中的50%）
└─ 提取方式：调用 withdrawPrincipal(lockIndex)
```

**提取函数**:
```solidity
function withdrawPrincipal(uint256 lockIndex) external nonReentrant {
    LockedPrincipal storage lock = userLockedPrincipals[msg.sender][lockIndex];
    
    // 1. 检查锁仓期是否结束
    require(block.timestamp >= lock.lockEndTime, "Lock period not ended");
    
    // 2. 检查是否已提取
    require(!lock.isWithdrawn, "Already withdrawn");
    
    // 3. 检查合约余额
    uint256 usdtAmount = lock.principalAmount / PRECISION_MULTIPLIER;
    require(
        usdtToken.balanceOf(address(this)) >= usdtAmount,
        "Insufficient contract balance"
    );
    
    // 4. 检查冷却期（24小时）
    require(
        block.timestamp >= users[msg.sender].lastWithdrawTime + WITHDRAWAL_COOLDOWN,
        "Cooldown active"
    );
    
    // 5. 更新状态
    lock.isWithdrawn = true;
    users[msg.sender].totalStaked -= lock.principalAmount;
    totalStaked -= lock.principalAmount;
    users[msg.sender].lastWithdrawTime = block.timestamp;
    
    // 6. 转账给用户
    usdtToken.safeTransfer(msg.sender, usdtAmount);
    
    emit PrincipalWithdrawn(msg.sender, lockIndex, usdtAmount, block.timestamp);
}
```

---

#### 2.2 提前解锁（可选功能）

**逻辑**:
```
用户质押 10,000 USDT，选择 180天锁仓
90天后提前提取

提前解锁惩罚：
├─ 锁仓期限：180天
├─ 已锁仓时间：90天
├─ 剩余时间：90天
├─ 提前解锁惩罚率：根据剩余时间计算
└─ 惩罚金额：principalAmount × penaltyRate
```

**惩罚率表**:
```
剩余时间比例 → 惩罚率
≥ 75% → 50%  (剩余时间 ≥ 75%)
≥ 50% → 35%  (剩余时间 ≥ 50%)
≥ 25% → 25%  (剩余时间 ≥ 25%)
< 25% → 15%  (剩余时间 < 25%)
```

**提前解锁函数**:
```solidity
function earlyUnlockPrincipal(uint256 lockIndex) external nonReentrant {
    LockedPrincipal storage lock = userLockedPrincipals[msg.sender][lockIndex];
    
    // 1. 检查锁仓期是否未结束
    require(block.timestamp < lock.lockEndTime, "Lock period already ended");
    
    // 2. 检查是否已提取
    require(!lock.isWithdrawn, "Already withdrawn");
    
    // 3. 计算提前解锁惩罚
    uint256 remainingTime = lock.lockEndTime - block.timestamp;
    uint256 totalLockTime = lock.lockEndTime - lock.lockStartTime;
    uint256 remainingRatio = (remainingTime * 100) / totalLockTime;
    
    uint256 penaltyRate;
    if (remainingRatio >= 75) {
        penaltyRate = 50;  // 50%
    } else if (remainingRatio >= 50) {
        penaltyRate = 35;  // 35%
    } else if (remainingRatio >= 25) {
        penaltyRate = 25;  // 25%
    } else {
        penaltyRate = 15;  // 15%
    }
    
    uint256 penalty = (lock.principalAmount * penaltyRate) / 100;
    uint256 withdrawableAmount = lock.principalAmount - penalty;
    
    // 4. 更新状态
    lock.isWithdrawn = true;
    users[msg.sender].totalStaked -= lock.principalAmount;
    totalStaked -= lock.principalAmount;
    
    // 5. 转账给用户（扣除惩罚）
    uint256 usdtAmount = withdrawableAmount / PRECISION_MULTIPLIER;
    usdtToken.safeTransfer(msg.sender, usdtAmount);
    
    // 6. 处理惩罚资金
    uint256 penaltyIn6Decimals = penalty / PRECISION_MULTIPLIER;
    // 50% 销毁，30% 奖励池，20% Treasury
    usdtToken.safeTransfer(address(0x000000000000000000000000000000000000dEaD), penaltyIn6Decimals * 50 / 100);
    // 剩余30%留在合约（奖励池），20%转到Treasury
    usdtToken.safeTransfer(treasuryAddress, penaltyIn6Decimals * 20 / 100);
    
    emit EarlyUnlockPrincipal(msg.sender, lockIndex, usdtAmount, penalty, block.timestamp);
}
```

---

#### 2.3 灵活锁仓的本金提取

**逻辑**:
```
用户质押 10,000 USDT，选择灵活锁仓

本金状态：
├─ 本金：5,000 USDT（合约中的50%）
├─ 锁定状态：未锁定
├─ 提取条件：24小时冷却期
└─ 提取方式：调用 withdrawFlexiblePrincipal()
```

**灵活锁仓提取函数**:
```solidity
function withdrawFlexiblePrincipal(uint256 amount) external nonReentrant {
    UserInfo storage user = users[msg.sender];
    
    // 1. 计算灵活锁仓的本金（总质押 - 已锁定的本金）
    uint256 totalLocked = 0;
    for (uint256 i = 0; i < userLockedPrincipals[msg.sender].length; i++) {
        if (!userLockedPrincipals[msg.sender][i].isWithdrawn) {
            totalLocked += userLockedPrincipals[msg.sender][i].principalAmount;
        }
    }
    
    uint256 flexiblePrincipal = user.totalStaked - totalLocked;
    require(flexiblePrincipal >= amount, "Insufficient flexible principal");
    
    // 2. 检查冷却期
    require(
        block.timestamp >= user.lastWithdrawTime + WITHDRAWAL_COOLDOWN,
        "Cooldown active"
    );
    
    // 3. 更新状态
    user.totalStaked -= amount;
    totalStaked -= amount;
    user.lastWithdrawTime = block.timestamp;
    
    // 4. 转账给用户
    uint256 usdtAmount = amount / PRECISION_MULTIPLIER;
    usdtToken.safeTransfer(msg.sender, usdtAmount);
    
    emit FlexiblePrincipalWithdrawn(msg.sender, amount, block.timestamp);
}
```

---

### 3. 多笔质押的管理

#### 3.1 场景示例

```
用户质押记录：
├─ 质押1：10,000 USDT，30天锁仓（已结束，可提取）
├─ 质押2：20,000 USDT，180天锁仓（进行中，未结束）
└─ 质押3：5,000 USDT，灵活锁仓（可随时提取）

本金状态：
├─ 锁定本金：20,000 USDT（质押2）
├─ 可提取本金：10,000 USDT（质押1）
└─ 灵活本金：5,000 USDT（质押3）
```

#### 3.2 查询函数

```solidity
function getLockedPrincipals(address user) external view returns (
    uint256[] memory stakeIds,
    uint256[] memory amounts,
    uint256[] memory lockStartTimes,
    uint256[] memory lockEndTimes,
    bool[] memory canWithdraw,
    bool[] memory isWithdrawn
) {
    LockedPrincipal[] storage locks = userLockedPrincipals[user];
    uint256 length = locks.length;
    
    stakeIds = new uint256[](length);
    amounts = new uint256[](length);
    lockStartTimes = new uint256[](length);
    lockEndTimes = new uint256[](length);
    canWithdraw = new bool[](length);
    isWithdrawn = new bool[](length);
    
    for (uint256 i = 0; i < length; i++) {
        stakeIds[i] = locks[i].stakeId;
        amounts[i] = locks[i].principalAmount;
        lockStartTimes[i] = locks[i].lockStartTime;
        lockEndTimes[i] = locks[i].lockEndTime;
        canWithdraw[i] = !locks[i].isWithdrawn && 
                         block.timestamp >= locks[i].lockEndTime;
        isWithdrawn[i] = locks[i].isWithdrawn;
    }
}

function getFlexiblePrincipal(address user) external view returns (uint256) {
    UserInfo storage userInfo = users[user];
    uint256 totalLocked = 0;
    
    for (uint256 i = 0; i < userLockedPrincipals[user].length; i++) {
        if (!userLockedPrincipals[user][i].isWithdrawn) {
            totalLocked += userLockedPrincipals[user][i].principalAmount;
        }
    }
    
    return userInfo.totalStaked - totalLocked;
}
```

---

### 4. 与现有收益机制的关系

#### 4.1 收益计算不受影响

**逻辑**:
```
本金提取不影响收益计算：
├─ 收益基于 totalStaked 计算
├─ 提取本金后，totalStaked 减少
├─ 收益计算自动调整（基于剩余的 totalStaked）
└─ 已提取的本金不再产生收益
```

**示例**:
```
用户质押 10,000 USDT，选择 180天锁仓

第1-180天：
├─ totalStaked: 10,000 USDT
├─ 每日收益: 10,000 × 0.008 × 2.0 = 160 USDT/天
└─ 累计收益: 160 × 180 = 28,800 USDT

第180天：提取本金 5,000 USDT
├─ totalStaked: 5,000 USDT（剩余）
├─ 每日收益: 5,000 × 0.008 × 2.0 = 80 USDT/天
└─ 说明：已提取的本金不再产生收益
```

---

#### 4.2 收益和本金提取的独立性

**逻辑**:
```
用户可以：
├─ 提取本金（不影响收益提取）
├─ 提取收益（不影响本金提取）
└─ 同时提取本金和收益（独立操作）

限制：
├─ 本金提取：需要锁仓期结束 + 24小时冷却
├─ 收益提取：需要 24小时冷却
└─ 两者独立，互不影响
```

---

### 5. 完整流程示例

#### 场景：用户质押并提取本金

**第1天：质押**
```
用户质押 10,000 USDT，选择 180天锁仓

操作：
1. 用户调用 stake(10000, referrer, 180)
2. 资金分配：
   - 5,000 USDT → Treasury（永久锁定）
   - 5,000 USDT → 合约（锁定180天）
3. 创建 LockedPrincipal：
   - stakeId: 1
   - principalAmount: 5,000 USDT
   - lockStartTime: Day 1
   - lockEndTime: Day 181
   - isWithdrawn: false
4. 开始产生收益（基于 10,000 USDT）
```

**第1-180天：锁仓期间**
```
用户状态：
├─ 本金：5,000 USDT（锁定中，不可提取）
├─ 收益：每天产生 160 USDT（10,000 × 0.008 × 2.0）
├─ 累计收益：160 × 180 = 28,800 USDT
└─ 可以提取收益，但不能提取本金
```

**第181天：锁仓期结束**
```
用户状态：
├─ 本金：5,000 USDT（解锁，可以提取）✅
├─ 收益：28,800 USDT（可以提取）✅
└─ 提取本金：
   1. 调用 withdrawPrincipal(0)
   2. 检查：锁仓期已结束 ✅
   3. 检查：24小时冷却期 ✅
   4. 转账：5,000 USDT 给用户
   5. 更新：totalStaked = 5,000 USDT（剩余）
```

**第182天及以后：提取本金后**
```
用户状态：
├─ 本金：已提取 5,000 USDT ✅
├─ 剩余质押：5,000 USDT（Treasury部分，永久锁定）
├─ 收益：继续产生（基于剩余的 5,000 USDT）
└─ 每日收益：5,000 × 0.008 × 2.0 = 80 USDT/天
```

---

### 6. 特殊情况处理

#### 6.1 多笔质押，部分提取

```
用户质押记录：
├─ 质押1：10,000 USDT，30天锁仓（已结束）
├─ 质押2：20,000 USDT，180天锁仓（进行中）
└─ 质押3：5,000 USDT，灵活锁仓

提取操作：
├─ 提取质押1的本金：10,000 USDT ✅
├─ 质押2的本金：20,000 USDT（仍锁定）⏳
└─ 质押3的本金：5,000 USDT（可随时提取）✅

收益计算：
├─ 基于剩余质押：20,000 + 5,000 = 25,000 USDT
└─ 每日收益：25,000 × 0.008 × (加权平均倍数)
```

#### 6.2 提前解锁

```
用户质押 10,000 USDT，选择 180天锁仓
90天后提前提取

提前解锁：
├─ 已锁仓时间：90天
├─ 剩余时间：90天（50%）
├─ 提前解锁惩罚：35%
├─ 惩罚金额：5,000 × 35% = 1,750 USDT
├─ 可提取金额：5,000 - 1,750 = 3,250 USDT
└─ 惩罚分配：
   - 销毁：875 USDT（50%）
   - 奖励池：525 USDT（30%）
   - Treasury：350 USDT（20%）
```

---

## 📊 数据结构设计

### 完整的数据结构

```solidity
// 锁定本金信息
struct LockedPrincipal {
    uint256 stakeId;           // 质押ID
    uint256 principalAmount;   // 本金金额（18 decimals）
    uint256 lockStartTime;     // 锁仓开始时间
    uint256 lockEndTime;       // 锁仓结束时间
    bool isWithdrawn;          // 是否已提取
    uint256 lockPeriod;        // 锁仓期限（天数）
}

// 用户信息（需要添加字段）
struct UserInfo {
    uint256 totalStaked;        // 总质押金额
    uint256 rwaPending;         // RWA 待提取
    uint256 usdtRewards;        // USDT 奖励
    uint256 lastWithdrawTime;   // 最后提取时间
    address referrer;           // 推荐人
    uint256 firstStakeTime;     // 首次质押时间
    uint8 nodeLevel;            // 节点等级
    bool isActive;              // 是否活跃
    // 新增：灵活锁仓本金（可选）
    uint256 flexiblePrincipal;  // 灵活锁仓本金（18 decimals）
}

// 映射
mapping(address => LockedPrincipal[]) public userLockedPrincipals;
```

---

## 🔄 与现有机制的集成

### 1. 与收益计算的关系

**逻辑**:
```
收益计算基于 totalStaked：
├─ 提取本金后，totalStaked 减少
├─ 收益计算自动调整
└─ 已提取的本金不再产生收益

示例：
- 质押：10,000 USDT
- 提取本金：5,000 USDT
- 剩余质押：5,000 USDT
- 收益计算：基于 5,000 USDT
```

### 2. 与紧急提取的关系

**逻辑**:
```
紧急提取（emergencyWithdraw）：
├─ 提取所有剩余本金（50%）
├─ 扣除已领取的 USDT 奖励
├─ 停止所有收益
└─ 与正常本金提取独立

正常本金提取（withdrawPrincipal）：
├─ 提取单笔质押的本金
├─ 不影响其他质押
├─ 不影响收益提取
└─ 收益继续产生（基于剩余质押）
```

---

## ✅ 实现要点

### 1. 关键函数

1. **`stake`**: 修改，添加本金锁定逻辑
2. **`withdrawPrincipal`**: 新增，提取锁仓期结束的本金
3. **`earlyUnlockPrincipal`**: 新增（可选），提前解锁
4. **`withdrawFlexiblePrincipal`**: 新增，提取灵活锁仓本金
5. **`getLockedPrincipals`**: 新增，查询锁定本金列表
6. **`getFlexiblePrincipal`**: 新增，查询灵活锁仓本金

### 2. 事件

```solidity
event PrincipalLocked(
    address indexed user,
    uint256 indexed stakeId,
    uint256 principalAmount,
    uint256 lockEndTime,
    uint256 timestamp
);

event PrincipalWithdrawn(
    address indexed user,
    uint256 indexed lockIndex,
    uint256 amount,
    uint256 timestamp
);

event EarlyUnlockPrincipal(
    address indexed user,
    uint256 indexed lockIndex,
    uint256 amount,
    uint256 penalty,
    uint256 timestamp
);
```

### 3. 前端集成

**需要添加**:
1. 本金锁定列表显示
2. 本金提取界面
3. 锁仓期倒计时
4. 提前解锁选项（可选）

---

## 🎯 总结

### 完整逻辑流程

```
质押
  ↓
资金分配（50/50）
  ↓
如果锁仓期限 > 0：
  → 锁定本金（创建 LockedPrincipal）
  → 存储锁仓信息
  ↓
锁仓期间：
  → 本金锁定，不可提取
  → 收益正常产生
  ↓
锁仓期结束：
  → 本金解锁，可以提取
  → 调用 withdrawPrincipal 提取
  → 更新 totalStaked
  → 收益继续产生（基于剩余质押）
```

### 关键特性

1. ✅ **本金锁定**：锁仓期限实际锁定本金
2. ✅ **到期提取**：锁仓期结束后可以提取本金
3. ✅ **提前解锁**：可选功能，需要支付惩罚
4. ✅ **灵活锁仓**：可以随时提取（24小时冷却）
5. ✅ **多笔管理**：支持多笔质押，分别管理
6. ✅ **收益独立**：本金提取不影响收益提取

---

**这就是完整的本金锁定和提取机制逻辑！** 🚀

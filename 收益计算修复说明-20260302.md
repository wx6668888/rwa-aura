# 收益计算修复说明

## 问题分析

### 问题1：链上数据一直没有更新

**原因：**
- 收益的计算和发放是通过**后端服务**完成的，不是链上自动计算
- 后端服务需要调用 `StakingContract.updateUserRewards()` 来更新链上的 `rwaPending`
- 目前后端服务没有运行，所以链上数据一直是 `0.248039 RWA`（这是之前手动调用 `updateUserRewards` 的结果）

**解决方案：**
1. **短期**：运行 `scripts/manual-update-rewards.ts` 手动更新奖励
2. **长期**：启动后端服务，自动定时更新奖励

### 问题2：计算方式不对（未考虑锁仓期限）

**原因：**
- 合约的 `stake` 函数没有接收锁仓期限参数
- 合约没有存储每笔质押的锁仓信息
- 前端虽然选择了锁仓期限，但没有传递给合约
- 之前的计算逻辑统一使用 0.8% 日收益率，没有考虑锁仓期限的倍数

**解决方案：**
1. ✅ 创建了 `useUserStakes` hook：从链上事件获取每笔质押记录
2. ✅ 修改了 `earnings-card.tsx`：根据每笔质押分别计算收益，考虑锁仓期限倍数
3. ✅ 修改了 `stake-action-panel.tsx`：质押成功后，将锁仓期限保存到 localStorage

## 修复内容

### 1. 新增 `useUserStakes` Hook

**文件：** `frontend/hooks/useUserStakes.ts`

**功能：**
- 从链上 `StakeEvent` 事件中获取用户的所有质押记录
- 从 localStorage 中读取每笔质押的锁仓期限
- 返回质押列表，包括：stakeId、amount、timestamp、lockPeriod

### 2. 修改 `earnings-card.tsx`

**主要改动：**
- 使用 `useUserStakes` 获取质押记录
- 根据每笔质押分别计算收益：
  - 灵活锁仓：0.8% × 1.0 = 0.8%
  - 30天锁仓：0.8% × 1.3 = 1.04%
  - 90天锁仓：0.8% × 1.6 = 1.28%
  - 180天锁仓：0.8% × 2.0 = 1.6%
  - 365天锁仓：0.8% × 2.5 = 2.0%
- 每笔质押的收益 = 质押金额 × 调整后的日收益率 × 经过天数

### 3. 修改 `stake-action-panel.tsx`

**主要改动：**
- 导入 `decodeEventLog` 和 `stakingContractABI`
- 在质押交易确认后，从 receipt 中解析 `StakeEvent` 事件
- 获取 `stakeId`，并将锁仓期限保存到 localStorage
- Key 格式：`stake_{stakingAddress}_{stakeId}_lockPeriod`

## 使用说明

### 对于新质押

1. 用户在质押页面选择锁仓期限
2. 质押成功后，锁仓期限自动保存到 localStorage
3. Dashboard 页面会根据锁仓期限正确计算收益

### 对于已有质押（需要手动设置）

由于之前的质押没有保存锁仓期限，需要手动设置：

**方法1：通过浏览器控制台**

```javascript
// 设置第一笔质押（1000 USDT，假设是灵活锁仓）
localStorage.setItem('stake_0xFD471836031dc5108809D173A067e8486B9047A3_0_lockPeriod', 'flexible')

// 设置第二笔质押（10000 USDT，假设是 90 天锁仓）
localStorage.setItem('stake_0xFD471836031dc5108809D173A067e8486B9047A3_1_lockPeriod', '90')
```

**方法2：创建修复脚本**

可以创建一个脚本来自动设置已有质押的锁仓期限。

## 收益计算示例

假设用户有两笔质押：
- 质押1：1000 USDT，灵活锁仓，质押时间：2026-03-02 09:35:48
- 质押2：10000 USDT，90天锁仓，质押时间：2026-03-02 09:36:58

**计算方式：**

1. **质押1（灵活锁仓）：**
   - 日收益率：0.8% × 1.0 = 0.8%
   - 每日 USDT 收益：1000 × 0.008 = 8 USDT
   - 每日 RWA 收益：8 / 0.85 = 9.41 RWA
   - 每秒 RWA 收益：9.41 / 86400 = 0.0001089 RWA
   - 经过时间：假设 1 小时 = 3600 秒
   - 收益：0.0001089 × 3600 = 0.392 RWA

2. **质押2（90天锁仓）：**
   - 日收益率：0.8% × 1.6 = 1.28%
   - 每日 USDT 收益：10000 × 0.0128 = 128 USDT
   - 每日 RWA 收益：128 / 0.85 = 150.59 RWA
   - 每秒 RWA 收益：150.59 / 86400 = 0.001742 RWA
   - 经过时间：假设 1 小时 = 3600 秒
   - 收益：0.001742 × 3600 = 6.27 RWA

3. **总收益：** 0.392 + 6.27 = 6.662 RWA

## 注意事项

1. **链上数据更新**：前端显示的是实时计算的收益，但实际可提取金额以链上 `rwaPending` 为准
2. **锁仓期限存储**：锁仓期限存储在 localStorage，如果用户清除浏览器数据，需要重新设置
3. **后端服务**：长期来看，需要启动后端服务自动更新链上数据

## 下一步

1. ✅ 修复计算逻辑（已完成）
2. ⏳ 手动设置已有质押的锁仓期限
3. ⏳ 启动后端服务，自动更新链上数据
4. ⏳ 考虑将锁仓期限存储到后端数据库（更可靠）

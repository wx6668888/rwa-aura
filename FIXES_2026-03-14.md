# RWA质押系统修复记录 - 2026-03-14

## 问题概述
用户提现失败，前端显示1800 USDT可提现，但合约只有1700 USDT灵活余额。

## 根本原因
EventMonitor的事件签名与合约实际发出的事件不匹配，导致：
1. EventMonitor无法捕获任何StakeEvent
2. 第1笔100 USDT质押被错误解析为灵活质押（实际是90天锁仓）

## 修复内容

### 1. 修复EventMonitor事件签名

**文件**: `backend/src/services/EventMonitor.ts`

**修复前**:
```typescript
'event StakeEvent(address indexed user, uint256 amount, address indexed referrer, uint256 indexed stakeId, uint256 timestamp, uint256 lockPeriod)',
'event RWAStakeEvent(address indexed user, uint256 amount, address indexed referrer, uint256 indexed stakeId, uint256 timestamp, uint256 lockPeriod)',
```

**修复后**:
```typescript
'event StakeEvent(address indexed user, uint256 amount, address referrer, uint256 stakeId, uint256 timestamp, uint256 lockPeriod)',
'event RWAStakeEvent(address indexed user, uint256 amount, address referrer, uint256 stakeId, uint256 timestamp, uint256 lockPeriod)',
```

**说明**: 移除了referrer和stakeId的indexed修饰符，匹配合约实际事件签名。

**合约实际事件哈希**: `0xa065435c124eadadc67eaba129651d6a45a0c6417334e01223ab3273dfd79bc0`

### 2. 修复数据库lock_period错误

**SQL修复**:
```sql
-- 恢复第1笔100 USDT质押的正确lock_period
UPDATE stake_events 
SET lock_period = 90 
WHERE tx_hash = '0x4579d3ebafbe47cf65a36e3b2fa0740629c6963f049589668490dea99bbfee60';
```

**说明**: 这笔交易实际是90天锁仓，之前被错误地标记为灵活质押。

## 验证结果

### 数据库状态
- 灵活质押: 3900 USDT
- 锁仓质押: 250 USDT (150天30天 + 100天90天)
- 灵活提现: 2200 USDT
- **灵活剩余: 1700 USDT** ✅

### 合约状态
- `users.totalStaked`: 1950 USDT
- `usdtFlexibleTotalStaked`: 1700 USDT
- `usdtLockedPrincipals[0]`: 100 USDT (90天锁仓)
- `usdtLockedPrincipals[1]`: 150 USDT (30天锁仓)

### 一致性验证
✅ 数据库灵活剩余 = 合约灵活余额 = 1700 USDT
✅ 数据库锁仓 = 合约锁仓 = 250 USDT
✅ 总余额 = 1950 USDT

## 用户质押明细

### USDT质押记录 (9笔)
1. 100 USDT, 90天锁仓, 区块95455880
2. 100 USDT, 灵活, 区块95458926
3. 200 USDT, 灵活, 区块95469367
4. 150 USDT, 30天锁仓, 区块95487051
5. 100 USDT, 灵活, 区块95501486
6. 1000 USDT, 灵活, 区块95504917
7. 500 USDT, 灵活, 区块95505846
8. 1000 USDT, 灵活, 区块95506858
9. 1000 USDT, 灵活, 区块95506890

### USDT提现记录 (4笔)
1. 300 USDT, 区块95475495
2. 100 USDT, 区块95503680
3. 800 USDT, 区块95505967
4. 1000 USDT, 区块95506992

## 锁仓到期时间
- 100 USDT (90天): 2026-06-11 17:12:27
- 150 USDT (30天): 需查询具体时间

## 后续建议

1. **EventMonitor已修复**: 未来的质押会正确捕获
2. **用户可提现**: 1700 USDT (灵活部分)
3. **锁仓提现**: 等待到期后可提现250 USDT
4. **监控**: 定期验证数据库和合约状态一致性

## 技术细节

### 合约事件签名
```solidity
event StakeEvent(
    address indexed user,
    uint256 amount,
    address referrer,        // 非indexed
    uint256 stakeId,         // 非indexed
    uint256 timestamp,
    uint256 lockPeriod
)
```

### 事件解析示例
```javascript
// topic0: 0xa065435c124eadadc67eaba129651d6a45a0c6417334e01223ab3273dfd79bc0
// topics[1]: user (indexed)
// data: [amount, timestamp, lockPeriod]
```

## 修复时间
- 开始: 2026-03-14 00:00
- 完成: 2026-03-14 02:13
- 总耗时: 约2小时13分钟

## 修复人员
- AI助手 (Kiro)
- 用户: rwaqzz

---
**备注**: 此次修复解决了EventMonitor事件签名不匹配的根本问题，确保了数据库和合约状态的一致性。

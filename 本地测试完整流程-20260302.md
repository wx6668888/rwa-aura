# 🧪 本地测试完整流程 - 2026年3月2日

**环境**: 本地 Hardhat 网络  
**优势**: 无需测试币，完全免费，快速迭代  
**状态**: 准备执行

---

## 🚀 快速开始（3步）

### Step 1: 启动本地节点

**打开新终端窗口**，执行：
```bash
npx hardhat node
```

**保持终端运行**，不要关闭！

**预期输出**:
```
Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/

Accounts
========
Account #0: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 (10000 ETH)
...
```

---

### Step 2: 部署合约

**在当前终端**，执行：
```bash
npx hardhat run scripts/deploy-all.ts --network localhost
```

**预期输出**:
- 4个合约地址
- 配置完成确认

---

### Step 3: 测试功能

**测试质押和 stRWA**:
```bash
npx hardhat run scripts/test-stake.ts --network localhost
```

**测试互换功能**:
```bash
npx hardhat run scripts/test-swap.ts --network localhost
```

---

## 📋 完整测试流程

### 1. 启动本地节点

```bash
# 终端1: 启动节点（保持运行）
npx hardhat node
```

---

### 2. 部署所有合约

```bash
# 终端2: 部署合约
npx hardhat run scripts/deploy-all.ts --network localhost
```

**保存输出的合约地址**，后续测试需要。

---

### 3. 初始化 SwapContract 池子（可选）

如果需要测试互换功能，需要先初始化池子：

```bash
# 使用 Hardhat console
npx hardhat console --network localhost
```

然后在 console 中：
```javascript
// 获取合约实例
const SwapContract = await ethers.getContractFactory("SwapContract");
const swapContract = SwapContract.attach("0x..."); // 使用部署的地址

const RWAToken = await ethers.getContractFactory("RWAToken");
const rwaToken = RWAToken.attach("0x...");

const StRWA = await ethers.getContractFactory("StRWA");
const stRWA = StRWA.attach("0x...");

// 初始化池子
const amount = ethers.parseEther("10000");
await rwaToken.approve(await swapContract.getAddress(), amount);
await stRWA.mint(await deployer.getAddress(), amount);
await stRWA.approve(await swapContract.getAddress(), amount);
await swapContract.initializePool(amount, amount);
```

---

### 4. 功能测试

#### 测试1: 质押功能

```bash
npx hardhat run scripts/test-stake.ts --network localhost
```

**验证**:
- ✅ 用户成功质押 USDT
- ✅ 用户收到 stRWA（50%）
- ✅ 用户信息更新正确

---

#### 测试2: 互换功能

```bash
npx hardhat run scripts/test-swap.ts --network localhost
```

**验证**:
- ✅ stRWA → RWA 互换成功
- ✅ RWA → stRWA 互换成功
- ✅ 池子余额更新正确

---

#### 测试3: 提现功能

创建测试脚本或使用 Hardhat console 测试提现功能。

---

## 🎯 本地测试优势

1. **无需测试币** - Hardhat 自动提供 10000 ETH
2. **即时执行** - 无需等待区块确认
3. **完全控制** - 可以重置、回滚
4. **免费测试** - 无任何成本
5. **完整功能** - 与真实网络功能一致

---

## 📊 测试检查清单

### 部署验证
- [ ] 所有合约部署成功
- [ ] 合约关系配置正确
- [ ] 合约地址已保存

### 功能测试
- [ ] 质押功能正常
- [ ] stRWA 铸造正常（50%）
- [ ] 提现功能正常（提U模式：70%）
- [ ] 提现功能正常（持RWA模式：120%）
- [ ] stRWA → RWA 互换正常
- [ ] RWA → stRWA 互换正常

### 集成测试
- [ ] 完整流程测试
- [ ] 边界情况测试
- [ ] 错误处理测试

---

## 🔧 常用命令

### 启动节点
```bash
npx hardhat node
```

### 部署合约
```bash
npx hardhat run scripts/deploy-all.ts --network localhost
```

### 运行测试
```bash
npx hardhat test
```

### 使用 Console
```bash
npx hardhat console --network localhost
```

---

## ⚠️ 注意事项

1. **保持节点运行**
   - 部署和测试时，Hardhat 节点必须运行
   - 关闭节点会丢失所有状态

2. **合约地址**
   - 每次重启节点，地址会变化
   - 需要重新部署或使用固定地址

3. **状态重置**
   - 重启节点 = 重置所有状态
   - 适合测试不同场景

---

## 🎉 开始测试

**立即执行**:

1. **启动节点**（新终端）:
   ```bash
   npx hardhat node
   ```

2. **部署合约**（当前终端）:
   ```bash
   npx hardhat run scripts/deploy-all.ts --network localhost
   ```

3. **测试功能**:
   ```bash
   npx hardhat run scripts/test-stake.ts --network localhost
   npx hardhat run scripts/test-swap.ts --network localhost
   ```

---

**本地测试完全可以满足开发需求，无需测试币！**

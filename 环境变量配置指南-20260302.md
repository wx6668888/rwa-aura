# ⚙️ 环境变量配置指南

**日期**: 2026年3月2日  
**用途**: 测试网部署配置

---

## 📋 必需配置

### 1. 创建/更新 `.env` 文件

在项目根目录创建或更新 `.env` 文件：

```env
# ============================================
# 网络配置
# ============================================
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545/
BSC_MAINNET_RPC_URL=https://bsc-dataseed.binance.org/

# ============================================
# 账户配置（⚠️ 重要：使用测试网专用账户）
# ============================================
PRIVATE_KEY=your_testnet_private_key_here

# ============================================
# 系统地址（测试网）
# ============================================
TREASURY_ADDRESS=your_treasury_address
BACKEND_ADDRESS=your_backend_address
LIQUIDITY_FUND_ADDRESS=your_liquidity_address

# ============================================
# 代币地址（测试网）
# ============================================
USDT_TOKEN_ADDRESS=0x337610d27c682E347C9cD60BD4b3b107C9d34dDd

# ============================================
# 初始流动性（可选）
# ============================================
INITIAL_LIQUIDITY_RWA=10000
INITIAL_LIQUIDITY_ST_RWA=10000

# ============================================
# BSCScan API（用于合约验证，可选）
# ============================================
BSCSCAN_API_KEY=your_bscscan_api_key
```

---

## 🔐 安全注意事项

### ⚠️ 重要警告

1. **不要使用主网私钥**
   - 使用专门的测试网账户
   - 测试网私钥泄露风险较低，但仍需谨慎

2. **不要提交 `.env` 到 Git**
   - 确保 `.env` 在 `.gitignore` 中
   - 使用 `.env.example` 作为模板

3. **私钥格式**
   - 不要包含 `0x` 前缀（Hardhat 会自动添加）
   - 或者包含 `0x` 前缀也可以

---

## 📝 配置步骤

### Step 1: 准备测试网账户

1. **创建新钱包**（如果还没有）
   - 使用 MetaMask 创建新账户
   - 或使用其他钱包工具

2. **获取测试网 BNB**
   - 访问: https://testnet.bnbchain.org/faucet-smart
   - 输入钱包地址
   - 获取 0.1-0.2 BNB

3. **导出私钥**
   - 在 MetaMask 中导出私钥
   - 或使用其他方式获取

---

### Step 2: 配置 `.env` 文件

1. **复制模板**（如果 `.env` 不存在）
   ```bash
   # 如果有 .env.example，复制它
   cp .env.example .env
   ```

2. **编辑 `.env` 文件**
   - 填入 `PRIVATE_KEY`
   - 填入系统地址
   - 检查其他配置

3. **验证配置**
   ```bash
   npx hardhat run scripts/check-balance.ts --network bscTestnet
   ```

---

## ✅ 配置验证

### 检查清单

- [ ] `.env` 文件已创建
- [ ] `PRIVATE_KEY` 已配置
- [ ] `BSC_TESTNET_RPC_URL` 已配置
- [ ] 系统地址已配置
- [ ] 账户余额 ≥ 0.1 BNB
- [ ] 网络连接正常

---

## 🚀 配置完成后

执行以下命令验证：

```bash
# 1. 检查网络连接
npx hardhat run scripts/check-network.ts --network bscTestnet

# 2. 检查账户余额
npx hardhat run scripts/check-balance.ts --network bscTestnet

# 3. 如果都正常，执行部署
npx hardhat run scripts/deploy-all.ts --network bscTestnet
```

---

## 📞 需要帮助？

如果遇到问题：

1. **私钥错误**
   - 检查私钥格式
   - 确认私钥对应正确的账户

2. **余额不足**
   - 从水龙头获取更多 BNB
   - 检查账户地址是否正确

3. **网络连接失败**
   - 检查 RPC URL
   - 尝试其他 RPC 端点

---

**配置完成后，继续执行部署！**

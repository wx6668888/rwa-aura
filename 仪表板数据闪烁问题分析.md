# 仪表板数据闪烁问题分析

## 问题描述

页面刷新时,数据会闪烁变化:
1. 初始显示: 个人质押 25709, 团队总质押 25709, 总留存 0
2. 然后变为: 个人质押 7210, 团队总质押 7210, 总留存 25709

## 问题原因

### 数据来源不一致

仪表板页面使用了两个不同的数据源:

1. **链上数据** (useStakingContract)
   - 来源: 直接从智能合约读取
   - 加载速度: 快 (几百毫秒)
   - 数据: `getUserStakeInfo()` 和 `rwaStakes()`
   - 返回值: 10497 USDT + 17896 RWA ≈ 25709 USDT

2. **后端 API 数据** (useLevelInfo)
   - 来源: 后端数据库 `/api/user/:address/level-info`
   - 加载速度: 慢 (1-2秒)
   - 数据: `cumulativePersonalStake`, `teamVolume`, `teamRetained`
   - 返回值: 可能是 7210 USDT (数据库中的历史累计)

### 加载顺序

```
时间轴:
0ms    - 页面加载,显示默认值 0
100ms  - 链上数据加载完成,显示 25709 (链上实时数据)
1500ms - 后端 API 数据加载完成,显示 7210 (数据库历史数据)
```

### 数据不一致的原因

后端数据库的 `cumulative_personal_stake` 字段可能:
1. 没有及时更新
2. 只统计了有锁仓的质押(灵活锁仓不计入)
3. 数据库与链上不同步

## 当前代码逻辑

### stat-cards.tsx

```typescript
// 链上数据 (快)
const personalTotalStaked = usdtStaked + rwaStakedInUSDT  // 25709

// 后端数据 (慢)
const { levelInfo, loading } = useLevelInfo()
const cumulativePersonal = levelInfo.cumulativePersonalStakeUsdt  // 7210
const teamVolumeOnly = levelInfo.teamVolumeUsdt  // 0
const teamTotalStaked = cumulativePersonal + teamVolumeOnly  // 7210

// 显示逻辑
const displayPersonal = isBackendDataReady ? cumulativePersonal : personalTotalStaked
const displayTeamTotal = isBackendDataReady ? teamTotalStaked : personalTotalStaked
```

问题: `isBackendDataReady` 的判断逻辑可能有问题,导致在后端数据加载完成后,显示了不正确的值。

## 解决方案

### 方案 1: 优先使用链上数据 (推荐)

```typescript
// 始终使用链上数据作为个人质押
const displayPersonal = personalTotalStaked  // 25709 (链上实时)

// 团队总质押 = 后端团队数据 + 链上个人数据
const displayTeamTotal = levelInfoLoading 
  ? personalTotalStaked  // 加载中,临时显示个人数据
  : (teamVolumeOnly + personalTotalStaked)  // 加载完成,使用后端团队+链上个人
```

优点:
- 个人质押始终准确(链上实时)
- 团队总质押包含最新的个人数据
- 避免数据库不同步问题

### 方案 2: 修复后端数据同步

确保后端数据库的 `cumulative_personal_stake` 字段及时更新:

1. 检查 EventMonitor 是否正常监听质押事件
2. 检查 TeamVolumeService 是否正确更新数据
3. 运行数据同步脚本

```bash
# 检查后端日志
cd backend
npm run dev

# 查看数据库
mysql -u root -p
USE rwa_staking;
SELECT address, cumulative_personal_stake, team_volume, team_total_deposited 
FROM users 
WHERE address = '0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266';
```

### 方案 3: 添加加载状态显示

在数据加载期间显示加载动画,避免闪烁:

```typescript
if (levelInfoLoading) {
  return <StatCard label="个人质押" value="..." />
}
```

## 推荐实施步骤

1. **立即修复**: 实施方案 1,使用链上数据作为个人质押
2. **排查问题**: 检查后端数据库,找出 7210 的来源
3. **长期优化**: 确保后端数据同步机制正常工作

## 测试验证

修复后,刷新页面应该:
- 个人质押: 始终显示 25709 (链上实时)
- 团队总质押: 始终显示 25709 (个人+下级,当前无下级)
- 总留存: 显示 25709 (后端数据)
- 无闪烁,数据稳定

## 相关文件

- `frontend/components/dashboard/stat-cards.tsx` - 统计卡片组件
- `frontend/components/dashboard/portfolio-card.tsx` - 投资组合卡片
- `frontend/hooks/useLevelInfo.ts` - 后端数据 hook
- `frontend/hooks/useStakingContract.ts` - 链上数据 hook
- `backend/src/routes/api.ts` - API 路由
- `backend/src/services/EventMonitor.ts` - 事件监听服务

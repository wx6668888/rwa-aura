# 锁仓期结束后用户资金位置说明

## ❓ 核心问题

**如果用户选择质押1天（或最短锁仓期限），锁仓期结束后，用户的资金在哪里？**

---

## 📊 资金分配机制

### 质押时的资金分配

**重要**：无论选择什么锁仓期限，资金分配规则都是一样的

```
用户质押 10,000 USDT，选择锁仓期限（例如：灵活锁仓/30天/90天/180天/365天）

资金分配：
├─ 50% (5,000 USDT) → Treasury（储备金）
│   └─ 用途：实体投资
│   └─ 状态：永久锁定，不返还给用户
│   └─ 说明：这部分资金用于投资，产生回报后回流到奖励池
│
└─ 50% (5,000 USDT) → Staking Contract（锁仓本金池）
    └─ 用途：用户本金
    └─ 状态：根据锁仓期限锁定
    └─ 说明：锁仓期结束后，用户可以提取这部分本金
```

---

## 🔍 不同锁仓期限的资金状态

### 场景1：灵活锁仓（无锁仓要求）

**质押时**：
```
用户质押 10,000 USDT，选择灵活锁仓

资金分配：
├─ 5,000 USDT → Treasury（永久锁定）
└─ 5,000 USDT → 合约（可随时提取）
```

**锁仓期结束后（立即）**：
```
用户资金状态：
├─ Treasury部分：5,000 USDT（永久锁定，不返还）
├─ 合约部分：5,000 USDT（可以立即提取）
└─ 收益：每天自动到账（RWA代币）

用户可以：
✅ 立即提取合约中的 5,000 USDT 本金
✅ 继续持有，继续获得收益
✅ 提取已获得的 RWA 收益
```

---

### 场景2：30天锁仓

**质押时**：
```
用户质押 10,000 USDT，选择30天锁仓

资金分配：
├─ 5,000 USDT → Treasury（永久锁定）
└─ 5,000 USDT → 合约（锁定30天）
```

**第30天：锁仓期结束**：
```
用户资金状态：
├─ Treasury部分：5,000 USDT（永久锁定，不返还）
├─ 合约部分：5,000 USDT（锁仓期结束，可以提取）
└─ 收益：30天 × 日收益 = 累计收益

用户可以：
✅ 提取合约中的 5,000 USDT 本金
✅ 继续持有，继续获得收益（但收益倍数会降低）
✅ 提取已获得的 RWA 收益
```

---

### 场景3：180天锁仓

**质押时**：
```
用户质押 10,000 USDT，选择180天锁仓

资金分配：
├─ 5,000 USDT → Treasury（永久锁定）
└─ 5,000 USDT → 合约（锁定180天）
```

**第180天：锁仓期结束**：
```
用户资金状态：
├─ Treasury部分：5,000 USDT（永久锁定，不返还）
├─ 合约部分：5,000 USDT（锁仓期结束，可以提取）
└─ 收益：180天 × 日收益 = 累计收益

用户可以：
✅ 提取合约中的 5,000 USDT 本金
✅ 继续持有，继续获得收益（但收益倍数会降低）
✅ 升级到365天锁仓，获得更高收益倍数
✅ 提取已获得的 RWA 收益
```

---

### 场景4：365天锁仓

**质押时**：
```
用户质押 10,000 USDT，选择365天锁仓

资金分配：
├─ 5,000 USDT → Treasury（永久锁定）
└─ 5,000 USDT → 合约（锁定365天）
```

**第365天：锁仓期结束**：
```
用户资金状态：
├─ Treasury部分：5,000 USDT（永久锁定，不返还）
├─ 合约部分：5,000 USDT（锁仓期结束，可以提取）
└─ 收益：365天 × 日收益 = 累计收益

用户可以：
✅ 提取合约中的 5,000 USDT 本金
✅ 继续持有，继续获得收益（但收益倍数会降低）
✅ 提取已获得的 RWA 收益
```

---

## 💡 关键点说明

### 1. Treasury 的 50% 资金

**重要**：无论锁仓期多长，Treasury 的 50% 资金都是**永久锁定，不返还**的

```
原因：
- 这部分资金用于实体投资
- 投资回报会回流到奖励池，分配给用户
- 这是项目的可持续性保障
```

**用户需要明确知道**：
- ✅ 投入 10,000 USDT，只有 5,000 USDT 可以提取
- ✅ 另外 5,000 USDT 用于投资，不返还
- ✅ 但可以通过投资回报获得更多收益

---

### 2. 合约中的 50% 资金

**锁仓期间**：
- 资金锁定在合约中
- 不能提取
- 但可以继续获得收益

**锁仓期结束后**：
- 资金自动解锁（状态变更）
- 用户可以提取
- 或者继续持有，继续获得收益

---

### 3. 锁仓期结束后的状态

**自动解锁**：
```
锁仓期结束后，合约会自动更新状态：
├─ lockEndTime <= block.timestamp
├─ isLocked = false（自动解锁）
└─ 用户可以提取本金
```

**用户操作**：
```
用户需要主动调用提取函数：
├─ withdrawPrincipal() - 提取本金
└─ 或者继续持有，继续获得收益
```

**注意**：锁仓期结束后，资金不会自动转账给用户，需要用户主动提取

---

## 🔄 完整流程示例

### 示例：用户 Alice 质押 10,000 USDT，选择 30天锁仓

#### 第1天：质押

```
Alice 操作：
1. 在前端选择质押 10,000 USDT
2. 选择锁仓期限：30天
3. 点击"确认质押"

合约执行：
├─ 从 Alice 钱包扣除 10,000 USDT
├─ 5,000 USDT → Treasury（永久锁定）
├─ 5,000 USDT → 合约（锁定30天）
├─ 记录锁仓信息：
│   ├─ lockStartTime: 第1天
│   ├─ lockEndTime: 第31天
│   └─ isLocked: true
└─ 开始计算收益（日收益 × 1.3倍）

Alice 的资金状态：
├─ 总投入：10,000 USDT
├─ Treasury：5,000 USDT（不返还）
├─ 合约：5,000 USDT（锁定30天）
└─ 可提取本金：0 USDT（锁仓期间）
```

---

#### 第1-30天：锁仓期间

```
每日收益：
├─ 基础日收益：10,000 × 0.8% = 80 RWA/天
├─ 锁仓倍数：1.3x（30天锁仓）
├─ 实际日收益：80 × 1.3 = 104 RWA/天
└─ 累计收益：104 × 30 = 3,120 RWA

Alice 的资金状态：
├─ Treasury：5,000 USDT（不返还）
├─ 合约：5,000 USDT（锁定中，不能提取）
├─ RWA收益：3,120 RWA（可以随时提取）
└─ 可提取本金：0 USDT（锁仓期间）
```

---

#### 第31天：锁仓期结束

```
合约自动更新状态：
├─ lockEndTime <= block.timestamp（第31天）
├─ isLocked = false（自动解锁）
└─ 用户可以提取本金

Alice 的资金状态：
├─ Treasury：5,000 USDT（永久锁定，不返还）
├─ 合约：5,000 USDT（已解锁，可以提取）
├─ RWA收益：3,120 RWA（可以随时提取）
└─ 可提取本金：5,000 USDT（锁仓期结束）

Alice 可以选择：
选项1：提取本金
├─ 调用 withdrawPrincipal()
├─ 提取 5,000 USDT
└─ 停止获得收益

选项2：继续持有
├─ 不提取本金
├─ 继续获得收益（但收益倍数降低到 1.0x）
└─ 可以随时提取本金

选项3：升级锁仓
├─ 调用 extendLock(OneEighty) 或 extendLock(ThreeSixtyFive)
├─ 重新锁定，获得更高收益倍数
└─ 锁仓期重新计算
```

---

## 📋 合约实现逻辑

### 数据结构

```solidity
// 锁仓信息
struct LockInfo {
    LockPeriod period;        // 锁仓期限
    uint256 lockStartTime;     // 锁仓开始时间
    uint256 lockEndTime;       // 锁仓结束时间
    uint256 principalAmount;   // 本金金额（合约中的50%）
    uint256 treasuryAmount;    // Treasury金额（永久锁定）
    bool isLocked;             // 是否锁定中
    bool isWithdrawn;          // 是否已提取本金
}

mapping(address => LockInfo[]) public userLocks;
```

### 提取本金函数

```solidity
function withdrawPrincipal(uint256 lockIndex) external nonReentrant {
    LockInfo storage lock = userLocks[msg.sender][lockIndex];
    
    // 检查锁仓期是否结束
    require(block.timestamp >= lock.lockEndTime, "Lock period not ended");
    
    // 检查是否已提取
    require(!lock.isWithdrawn, "Already withdrawn");
    
    // 检查是否有本金可提取
    require(lock.principalAmount > 0, "No principal to withdraw");
    
    // 更新状态
    lock.isWithdrawn = true;
    lock.isLocked = false;
    
    // 转换精度（18位 -> 6位）
    uint256 usdtAmount = lock.principalAmount / PRECISION_MULTIPLIER;
    
    // 转账给用户
    usdtToken.safeTransfer(msg.sender, usdtAmount);
    
    emit PrincipalWithdrawn(msg.sender, lockIndex, usdtAmount, block.timestamp);
}
```

### 检查锁仓状态函数

```solidity
function getLockStatus(address user, uint256 lockIndex) external view returns (
    bool isLocked_,
    bool canWithdraw_,
    uint256 remainingTime_,
    uint256 principalAmount_,
    uint256 treasuryAmount_
) {
    LockInfo storage lock = userLocks[user][lockIndex];
    
    isLocked_ = lock.isLocked && block.timestamp < lock.lockEndTime;
    canWithdraw_ = !lock.isWithdrawn && block.timestamp >= lock.lockEndTime;
    
    if (block.timestamp < lock.lockEndTime) {
        remainingTime_ = lock.lockEndTime - block.timestamp;
    } else {
        remainingTime_ = 0;
    }
    
    principalAmount_ = lock.principalAmount;
    treasuryAmount_ = lock.treasuryAmount;
}
```

---

## ✅ 总结

### 锁仓期结束后，用户资金的位置

**答案**：

1. **Treasury 的 50%**：
   - 位置：Treasury 地址（多签钱包或合约）
   - 状态：永久锁定，不返还
   - 用途：实体投资

2. **合约中的 50%**：
   - 位置：Staking Contract 地址
   - 状态：锁仓期结束后自动解锁
   - 用途：用户本金，可以提取

3. **收益（RWA代币）**：
   - 位置：Staking Contract（用户余额）
   - 状态：可以随时提取
   - 用途：用户收益

---

### 用户需要知道的关键点

1. ✅ **投入 10,000 USDT，只有 5,000 USDT 可以提取**
   - 另外 5,000 USDT 在 Treasury，不返还
   - 但可以通过投资回报获得更多收益

2. ✅ **锁仓期结束后，本金不会自动转账**
   - 需要用户主动调用 `withdrawPrincipal()` 提取
   - 或者继续持有，继续获得收益

3. ✅ **锁仓期结束后，收益倍数会降低**
   - 如果继续持有但不重新锁定，收益倍数降为 1.0x
   - 如果重新锁定，可以获得更高收益倍数

4. ✅ **收益可以随时提取**
   - RWA 收益不受锁仓期限限制
   - 可以随时提取已获得的收益

---

### 前端显示建议

**用户界面应该清晰显示**：

```
您的质押信息：
├─ 总投入：10,000 USDT
├─ Treasury：5,000 USDT（永久锁定，用于投资）
├─ 合约本金：5,000 USDT
│   ├─ 锁仓状态：已解锁 ✅
│   ├─ 可提取：5,000 USDT
│   └─ [提取本金] 按钮
├─ 累计收益：3,120 RWA
│   └─ [提取收益] 按钮
└─ 收益倍数：1.0x（锁仓期结束后）
    └─ [重新锁定] 按钮（可选）
```

---

**这样用户就能清楚地知道：锁仓期结束后，资金在哪里，可以做什么操作。**

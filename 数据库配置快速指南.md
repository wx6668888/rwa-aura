# 🚀 数据库配置快速指南

## ⚠️ 当前问题

后端服务无法连接数据库，错误信息：
```
Access denied for user 'rwa_user'@'localhost' (using password: YES)
```

---

## ✅ 快速解决方案

### 方法 1：使用自动配置脚本（最简单）

```powershell
cd backend
.\setup-database.ps1
```

脚本会自动：
1. ✅ 检查 MySQL 是否安装
2. ✅ 创建数据库 `rwa_protocol`
3. ✅ 创建用户 `rwa_user`（需要您输入密码）
4. ✅ 初始化所有表结构
5. ✅ 创建 `.env` 配置文件

---

### 方法 2：手动配置（3步）

#### 步骤 1：创建数据库和用户

打开 MySQL 命令行（需要 root 权限）：
```sql
mysql -u root -p
```

执行以下 SQL：
```sql
CREATE DATABASE IF NOT EXISTS rwa_protocol CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER IF NOT EXISTS 'rwa_user'@'localhost' IDENTIFIED BY 'rwa_password_123';
GRANT ALL PRIVILEGES ON rwa_protocol.* TO 'rwa_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### 步骤 2：初始化表结构

```bash
cd backend
mysql -u rwa_user -prwa_password_123 rwa_protocol < src/config/database.sql
```

#### 步骤 3：创建 .env 文件

在 `backend` 目录下创建 `.env` 文件：
```env
DB_HOST=localhost
DB_PORT=3306
DB_USER=rwa_user
DB_PASSWORD=rwa_password_123
DB_NAME=rwa_protocol
PORT=3001
CORS_ORIGIN=http://localhost:3000
```

---

## 🔍 验证配置

### 1. 检查 MySQL 服务

```powershell
Get-Service -Name MySQL*
```

如果未运行，启动服务：
```powershell
Start-Service MySQL80
```

### 2. 测试数据库连接

```bash
mysql -u rwa_user -prwa_password_123 rwa_protocol
```

如果能够登录，说明配置正确。

### 3. 检查表结构

```sql
USE rwa_protocol;
SHOW TABLES;
```

应该看到 8 个表。

---

## 🚀 启动后端服务

配置完成后：

```bash
cd backend
npm run dev
```

**成功标志**：
```
✅ Database connection established
✅ Listening on port 3001
```

---

## ❓ 常见问题

### Q: MySQL 服务未运行

**解决**：
```powershell
Start-Service MySQL80
```

### Q: 忘记 root 密码

**解决**：重置 MySQL root 密码（需要管理员权限）

### Q: 用户已存在但密码错误

**解决**：
```sql
ALTER USER 'rwa_user'@'localhost' IDENTIFIED BY 'new_password';
FLUSH PRIVILEGES;
```

然后更新 `.env` 文件中的密码。

---

**配置完成后，刷新前端管理后台页面即可看到数据！**

# 团长激励机制完整方案

## 🎯 核心目标

1. **吸引团长**：让团长愿意长期拉人，而不是短期割一波
2. **质量考核**：防止刷人头，只奖励有效业绩
3. **长期激励**：团长越做越久，收益越高
4. **分阶段调整**：前期高激励吸引，后期稳定运营

---

## 📊 三层激励体系

### 激励结构

```
团长总收益 = 直推奖励 + 级差奖励 + 管理分红 + 团队业绩奖励 + 特殊权益
```

---

## 💰 激励一：直推奖励（即时激励）

### 1.1 基础直推奖励

#### 奖励规则

| 节点等级 | 直推奖励率 | 说明 |
|---------|-----------|------|
| V1 | 0.5% | 新用户首存的0.5% |
| V2 | 0.8% | 新用户首存的0.8% |
| V3 | 1.2% | 新用户首存的1.2% |
| V4 | 1.5% | 新用户首存的1.5% |
| V5 | 2.0% | 新用户首存的2.0% |

#### 发放机制

```solidity
// 直推奖励：30%即时 + 70%线性释放60天
struct DirectReferralReward {
    uint256 instantReward;      // 即时奖励（30%）
    uint256 lockedReward;       // 锁定奖励（70%）
    uint256 releaseStartTime;   // 释放开始时间
    uint256 releaseDuration;    // 释放时长（60天）
    uint256 releasedAmount;     // 已释放金额
}

mapping(address => DirectReferralReward[]) public referralRewards;

function processDirectReferralReward(
    address referrer,
    address newUser,
    uint256 stakeAmount
) internal {
    UserInfo storage refInfo = users[referrer];
    uint8 nodeLevel = refInfo.nodeLevel;
    
    // 获取奖励率
    uint256 rewardRate = directReferralRates[nodeLevel];
    uint256 totalReward = stakeAmount * rewardRate / 10000;
    
    // 30%即时发放
    uint256 instantReward = totalReward * 30 / 100;
    usdtToken.safeTransfer(referrer, instantReward);
    
    // 70%线性释放60天
    uint256 lockedReward = totalReward - instantReward;
    referralRewards[referrer].push(DirectReferralReward({
        instantReward: instantReward,
        lockedReward: lockedReward,
        releaseStartTime: block.timestamp,
        releaseDuration: 60 days,
        releasedAmount: 0
    }));
    
    emit DirectReferralRewardPaid(referrer, newUser, totalReward);
}

// 提取线性释放的奖励
function claimVestedRewards() external {
    DirectReferralReward[] storage rewards = referralRewards[msg.sender];
    uint256 totalClaimable = 0;
    
    for (uint i = 0; i < rewards.length; i++) {
        DirectReferralReward storage reward = rewards[i];
        
        if (block.timestamp >= reward.releaseStartTime) {
            uint256 elapsed = block.timestamp - reward.releaseStartTime;
            uint256 releaseProgress = elapsed > reward.releaseDuration 
                ? reward.releaseDuration 
                : elapsed;
            
            uint256 claimable = reward.lockedReward * releaseProgress / reward.releaseDuration;
            uint256 newClaimable = claimable - reward.releasedAmount;
            
            if (newClaimable > 0) {
                totalClaimable += newClaimable;
                reward.releasedAmount = claimable;
            }
        }
    }
    
    if (totalClaimable > 0) {
        usdtToken.safeTransfer(msg.sender, totalClaimable);
        emit VestedRewardsClaimed(msg.sender, totalClaimable);
    }
}
```

#### 示例计算

**场景**：V3团长 Alice 推荐新用户 Bob 质押 10,000 USDT

```
Alice 的直推奖励：
├─ 奖励率：1.2%（V3等级）
├─ 奖励金额：10,000 × 1.2% = 120 USDT
├─ 即时发放：120 × 30% = 36 USDT（立即到账）
└─ 线性释放：120 × 70% = 84 USDT（60天释放）

释放计划：
├─ 第1天：36 USDT（即时）
├─ 第2-61天：每天 84/60 = 1.4 USDT
└─ 总计：120 USDT
```

---

### 1.2 复投奖励加成

#### 奖励规则

当直推用户复投时，团长获得额外奖励：

| 复投次数 | 额外奖励率 | 说明 |
|---------|-----------|------|
| 第1次复投 | +0.2% | 额外0.2%奖励 |
| 第2次复投 | +0.3% | 额外0.3%奖励 |
| 第3次复投 | +0.5% | 额外0.5%奖励 |
| 第4次及以上 | +0.5% | 额外0.5%奖励 |

#### 实现逻辑

```solidity
mapping(address => mapping(address => uint256)) public referralReinvestmentCount;

function processReinvestmentReward(
    address referrer,
    address user,
    uint256 reinvestAmount
) internal {
    uint256 count = referralReinvestmentCount[referrer][user];
    
    uint256 bonusRate = 0;
    if (count == 0) bonusRate = 20;      // 0.2%
    else if (count == 1) bonusRate = 30; // 0.3%
    else if (count == 2) bonusRate = 50; // 0.5%
    else bonusRate = 50;                 // 0.5%
    
    uint256 bonus = reinvestAmount * bonusRate / 10000;
    
    // 即时发放50%，线性释放50%
    uint256 instant = bonus * 50 / 100;
    usdtToken.safeTransfer(referrer, instant);
    
    // 记录线性释放
    referralRewards[referrer].push(DirectReferralReward({
        instantReward: instant,
        lockedReward: bonus - instant,
        releaseStartTime: block.timestamp,
        releaseDuration: 30 days,
        releasedAmount: 0
    }));
    
    referralReinvestmentCount[referrer][user]++;
    
    emit ReinvestmentRewardPaid(referrer, user, bonus);
}
```

#### 示例计算

**场景**：V4团长 Charlie 的直推 David 第2次复投 5,000 USDT

```
Charlie 的复投奖励：
├─ 复投次数：第2次
├─ 额外奖励率：0.3%
├─ 奖励金额：5,000 × 0.3% = 15 USDT
├─ 即时发放：15 × 50% = 7.5 USDT
└─ 线性释放：15 × 50% = 7.5 USDT（30天释放）
```

---

## 💎 激励二：级差奖励（保留原有机制）

### 2.1 级差奖励规则

| 节点等级 | 级差奖励率 | 说明 |
|---------|-----------|------|
| V1 | 5% | 对下级质押的5% |
| V2 | 10% | 对下级质押的10% |
| V3 | 15% | 对下级质押的15% |
| V4 | 20% | 对下级质押的20% |
| V5 | 50% | 对下级质押的50% |

**注意**：级差奖励使用压级逻辑，防止重复计算

#### 示例计算

**场景**：用户 Eve 质押 10,000 USDT，推荐链：Eve → Frank(V2) → Grace(V4) → Henry(V5)

```
级差奖励计算：
├─ Frank (V2, 10%)：
│   └─ 级差：10% - 0% = 10%
│   └─ 奖励：10,000 × 10% = 1,000 USDT
│
├─ Grace (V4, 20%)：
│   └─ 级差：20% - 10% = 10%
│   └─ 奖励：10,000 × 10% = 1,000 USDT
│
└─ Henry (V5, 50%)：
    └─ 级差：50% - 20% = 30%
    └─ 奖励：10,000 × 30% = 3,000 USDT

总计：1,000 + 1,000 + 3,000 = 5,000 USDT（50%上限）
```

---

## 🏆 激励三：管理分红（月度奖励）

### 3.1 业绩分数计算

#### 计算规则

```solidity
struct TeamPerformance {
    uint256 directReferrals;       // 直推人数
    uint256 teamVolume;            // 团队总业绩（有效）
    uint256 activeMembers;         // 活跃成员数（30天内有操作）
    uint256 lockedMembers;         // 锁仓成员数
    uint256 reinvestmentCount;     // 团队复投次数
    uint256 performanceScore;      // 业绩分数
}

function calculatePerformanceScore(address manager) public view returns (uint256) {
    TeamPerformance memory perf = getTeamPerformance(manager);
    
    uint256 score = 
        perf.directReferrals * 10 +              // 每个直推 10分
        perf.teamVolume / 1000 +                 // 每1000 USDT 1分
        perf.activeMembers * 5 +                 // 每个活跃成员 5分
        perf.lockedMembers * 15 +                // 每个锁仓成员 15分（更高权重）
        perf.reinvestmentCount * 3;              // 每次复投 3分
    
    return score;
}
```

### 3.2 分红等级表

| 业绩分数 | 月度分红比例 | 说明 |
|---------|------------|------|
| 0 - 100 | 0% | 无分红 |
| 101 - 500 | 团队业绩的 0.3% | 初级团长 |
| 501 - 2000 | 团队业绩的 0.6% | 中级团长 |
| 2001 - 5000 | 团队业绩的 1.0% | 高级团长 |
| 5001 - 10000 | 团队业绩的 1.5% | 超级团长 |
| 10000+ | 团队业绩的 2.0% | 顶级团长 |

### 3.3 分红发放机制

```solidity
// 月度分红池
uint256 public monthlyDividendPool;
uint256 public lastDividendDistribution;
uint256 public constant DIVIDEND_INTERVAL = 30 days;

// 分红资金来源
function addToDividendPool(uint256 amount) external {
    monthlyDividendPool += amount;
}

// 分配分红
function distributeManagementDividend() external onlyOwner {
    require(block.timestamp >= lastDividendDistribution + DIVIDEND_INTERVAL, "Too soon");
    
    uint256 totalPool = monthlyDividendPool;
    if (totalPool == 0) return;
    
    // 获取所有V2+用户
    address[] memory managers = getManagers(); // nodeLevel >= 2
    uint256 totalScore = 0;
    
    // 计算总分数
    for (uint i = 0; i < managers.length; i++) {
        totalScore += calculatePerformanceScore(managers[i]);
    }
    
    if (totalScore == 0) return;
    
    // 按分数分配
    for (uint i = 0; i < managers.length; i++) {
        address manager = managers[i];
        uint256 score = calculatePerformanceScore(manager);
        
        if (score > 0) {
            uint256 dividend = totalPool * score / totalScore;
            
            // 发放分红
            usdtToken.safeTransfer(manager, dividend);
            
            emit ManagementDividendPaid(manager, dividend, score);
        }
    }
    
    // 清零分红池
    monthlyDividendPool = 0;
    lastDividendDistribution = block.timestamp;
}
```

#### 示例计算

**场景**：V4团长 Ivy 的团队数据

```
Ivy 的团队数据：
├─ 直推人数：20人
├─ 团队总业绩：150,000 USDT（有效）
├─ 活跃成员：35人（30天内有操作）
├─ 锁仓成员：18人（选择锁仓）
└─ 团队复投次数：25次

业绩分数计算：
├─ 直推分数：20 × 10 = 200分
├─ 团队量分数：150,000 / 1000 = 150分
├─ 活跃成员分数：35 × 5 = 175分
├─ 锁仓成员分数：18 × 15 = 270分
└─ 复投分数：25 × 3 = 75分

总分数：200 + 150 + 175 + 270 + 75 = 870分

月度分红：
├─ 业绩等级：501-2000（中级团长）
├─ 分红比例：0.6%
└─ 月度分红：150,000 × 0.6% = 900 USDT

年度分红：900 × 12 = 10,800 USDT
```

---

## 🎁 激励四：团队业绩奖励（季度奖励）

### 4.1 季度业绩奖励

#### 奖励规则

每季度根据团队业绩增长给予额外奖励：

| 季度业绩增长 | 奖励比例 | 说明 |
|------------|---------|------|
| 0 - 10% | 0% | 无奖励 |
| 10% - 30% | 团队增长的 0.5% | 基础奖励 |
| 30% - 50% | 团队增长的 1.0% | 良好奖励 |
| 50% - 100% | 团队增长的 1.5% | 优秀奖励 |
| 100%+ | 团队增长的 2.0% | 卓越奖励 |

#### 实现逻辑

```solidity
struct QuarterlyPerformance {
    uint256 quarterStartVolume;   // 季度开始时的团队量
    uint256 quarterEndVolume;     // 季度结束时的团队量
    uint256 growthRate;           // 增长率
    uint256 rewardAmount;         // 奖励金额
    bool isClaimed;               // 是否已领取
}

mapping(address => mapping(uint256 => QuarterlyPerformance)) public quarterlyPerformance;
uint256 public currentQuarter;

function calculateQuarterlyReward(address manager) public view returns (uint256) {
    QuarterlyPerformance storage perf = quarterlyPerformance[manager][currentQuarter];
    
    if (perf.quarterStartVolume == 0) return 0;
    
    uint256 growth = perf.quarterEndVolume > perf.quarterStartVolume
        ? perf.quarterEndVolume - perf.quarterStartVolume
        : 0;
    
    uint256 growthRate = growth * 100 / perf.quarterStartVolume;
    
    uint256 rewardRate = 0;
    if (growthRate >= 100) rewardRate = 200;      // 2.0%
    else if (growthRate >= 50) rewardRate = 150;  // 1.5%
    else if (growthRate >= 30) rewardRate = 100;  // 1.0%
    else if (growthRate >= 10) rewardRate = 50;   // 0.5%
    
    return growth * rewardRate / 10000;
}

// 季度结算
function settleQuarter() external onlyOwner {
    currentQuarter++;
    
    address[] memory managers = getManagers();
    
    for (uint i = 0; i < managers.length; i++) {
        address manager = managers[i];
        TeamPerformance memory perf = getTeamPerformance(manager);
        
        quarterlyPerformance[manager][currentQuarter] = QuarterlyPerformance({
            quarterStartVolume: perf.teamVolume,
            quarterEndVolume: 0,
            growthRate: 0,
            rewardAmount: 0,
            isClaimed: false
        });
    }
}
```

#### 示例计算

**场景**：V5团长 Jack 的季度业绩

```
季度开始：团队量 200,000 USDT
季度结束：团队量 350,000 USDT

增长计算：
├─ 增长量：350,000 - 200,000 = 150,000 USDT
├─ 增长率：150,000 / 200,000 = 75%
└─ 奖励等级：50%-100%（优秀奖励）

季度奖励：
├─ 奖励比例：1.5%
└─ 奖励金额：150,000 × 1.5% = 2,250 USDT

年度奖励：2,250 × 4 = 9,000 USDT
```

---

## 🌟 激励五：特殊权益（长期激励）

### 5.1 节点等级权益

#### 权益表

| 节点等级 | 特殊权益 | 说明 |
|---------|---------|------|
| V1 | 无 | 基础权益 |
| V2 | 优先提现 | 提现排队优先 |
| V3 | 降低手续费 | 提现手续费-2% |
| V4 | 额外分红 | 月度分红+20% |
| V5 | 项目方分红 | 参与项目方收益分配 |

### 5.2 V5特殊权益：项目方分红

#### 分红规则

V5团长可以参与项目方收益分配：

```solidity
// 项目方收益分配
struct ProjectRevenue {
    uint256 liquidityWithdraw;    // 流动性撤出收益
    uint256 treasuryReturn;       // Treasury投资回报
    uint256 operationPool;        // 运营池收益
    uint256 totalRevenue;         // 总收益
}

// V5分红比例
uint256 public constant V5_DIVIDEND_RATE = 10; // 项目方总收益的10%分配给V5团长

function distributeV5Dividend() external onlyOwner {
    ProjectRevenue memory revenue = getProjectRevenue();
    uint256 totalV5Dividend = revenue.totalRevenue * V5_DIVIDEND_RATE / 100;
    
    // 获取所有V5团长
    address[] memory v5Managers = getV5Managers();
    uint256 totalScore = 0;
    
    // 计算总分数
    for (uint i = 0; i < v5Managers.length; i++) {
        totalScore += calculatePerformanceScore(v5Managers[i]);
    }
    
    if (totalScore == 0) return;
    
    // 按分数分配
    for (uint i = 0; i < v5Managers.length; i++) {
        address manager = v5Managers[i];
        uint256 score = calculatePerformanceScore(manager);
        
        uint256 dividend = totalV5Dividend * score / totalScore;
        usdtToken.safeTransfer(manager, dividend);
        
        emit V5DividendPaid(manager, dividend, score);
    }
}
```

#### 示例计算

**场景**：项目方总收益 10,000,000 USDT，3个V5团长

```
V5分红池：
├─ 项目方总收益：10,000,000 USDT
├─ V5分红比例：10%
└─ V5分红总额：10,000,000 × 10% = 1,000,000 USDT

V5团长分配：
├─ 团长A（分数：5000）：1,000,000 × 5000 / 15000 = 333,333 USDT
├─ 团长B（分数：6000）：1,000,000 × 6000 / 15000 = 400,000 USDT
└─ 团长C（分数：4000）：1,000,000 × 4000 / 15000 = 266,667 USDT
```

---

## 📊 有效业绩考核（防刷机制）

### 6.1 有效业绩定义

```solidity
// 有效业绩条件
struct ValidPerformance {
    bool isLocked;              // 是否锁仓≥30天
    bool isActive;              // 7天内是否有操作
    bool hasReinvested;         // 是否有复投行为
    uint256 stakeDuration;      // 质押时长
    uint256 minStakeAmount;     // 最低质押额（100 USDT）
}

function isValidPerformance(address user) public view returns (bool) {
    UserInfo storage userInfo = users[user];
    
    // 条件1：锁仓≥30天 或 质押≥30天
    bool condition1 = userInfo.lockTier >= LockTier.TIER_30 || 
                     (block.timestamp - userInfo.firstStakeTime) >= 30 days;
    
    // 条件2：7天内有操作
    bool condition2 = (block.timestamp - userInfo.lastActivityTime) <= 7 days;
    
    // 条件3：有复投行为（至少1次）
    bool condition3 = reinvestmentCount[user] > 0;
    
    // 条件4：最低质押额
    bool condition4 = userInfo.totalStaked >= 100 * 10**18; // 100 USDT
    
    return condition1 && condition2 && condition3 && condition4;
}

// 只统计有效业绩
function getValidTeamVolume(address manager) public view returns (uint256) {
    address[] memory teamMembers = getTeamMembers(manager);
    uint256 validVolume = 0;
    
    for (uint i = 0; i < teamMembers.length; i++) {
        if (isValidPerformance(teamMembers[i])) {
            validVolume += users[teamMembers[i]].totalStaked;
        }
    }
    
    return validVolume;
}
```

---

## 🎯 分阶段团长激励调整

### 阶段一：启动期（0-3个月）

**激励策略：高激励吸引**

| 激励类型 | 参数 | 说明 |
|---------|------|------|
| 直推奖励 | +50% | 所有等级奖励+50% |
| 级差奖励 | 保持不变 | V1-V5 |
| 管理分红 | +100% | 分红比例翻倍 |
| 季度奖励 | +50% | 季度奖励+50% |
| 有效业绩要求 | 降低 | 只需锁仓≥7天 |

**示例**：
- V3团长直推奖励：1.2% × 1.5 = **1.8%**
- 管理分红：0.6% × 2 = **1.2%**

---

### 阶段二：成长期（3-6个月）

**激励策略：稳定激励**

| 激励类型 | 参数 | 说明 |
|---------|------|------|
| 直推奖励 | 基准 | 恢复正常 |
| 级差奖励 | 保持不变 | V1-V5 |
| 管理分红 | 基准 | 恢复正常 |
| 季度奖励 | 基准 | 恢复正常 |
| 有效业绩要求 | 标准 | 锁仓≥30天 |

---

### 阶段三：成熟期（6个月+）

**激励策略：长期激励**

| 激励类型 | 参数 | 说明 |
|---------|------|------|
| 直推奖励 | -20% | 降低20% |
| 级差奖励 | 保持不变 | V1-V5 |
| 管理分红 | +50% | 分红+50%（鼓励长期） |
| 季度奖励 | +100% | 季度奖励翻倍 |
| V5特殊权益 | 开启 | 参与项目方分红 |

---

## 📈 完整示例：团长收益计算

### 场景：V4团长 Kevin 的月度收益

**团队数据**：
- 直推人数：15人
- 团队总业绩：120,000 USDT（有效）
- 活跃成员：25人
- 锁仓成员：12人
- 团队复投：20次

**本月新用户**：
- 新用户A：质押 5,000 USDT（首存）
- 新用户B：质押 8,000 USDT（首存）
- 新用户C：复投 3,000 USDT（第2次）

**收益计算**：

#### 1. 直推奖励

```
新用户A：
├─ 奖励：5,000 × 1.5% = 75 USDT
├─ 即时：75 × 30% = 22.5 USDT
└─ 线性：75 × 70% = 52.5 USDT（60天释放）

新用户B：
├─ 奖励：8,000 × 1.5% = 120 USDT
├─ 即时：120 × 30% = 36 USDT
└─ 线性：120 × 70% = 84 USDT（60天释放）

新用户C（复投）：
├─ 奖励：3,000 × 0.3% = 9 USDT
├─ 即时：9 × 50% = 4.5 USDT
└─ 线性：9 × 50% = 4.5 USDT（30天释放）

直推奖励总计：
├─ 即时：22.5 + 36 + 4.5 = 63 USDT
└─ 线性：52.5 + 84 + 4.5 = 141 USDT（逐步释放）
```

#### 2. 级差奖励

```
假设本月团队级差奖励：2,000 USDT
（来自下级质押的级差分配）
```

#### 3. 管理分红

```
业绩分数：
├─ 直推：15 × 10 = 150分
├─ 团队量：120,000 / 1000 = 120分
├─ 活跃成员：25 × 5 = 125分
├─ 锁仓成员：12 × 15 = 180分
└─ 复投：20 × 3 = 60分

总分数：635分
业绩等级：501-2000（中级团长）
分红比例：0.6%

月度分红：120,000 × 0.6% = 720 USDT
```

#### 4. 季度奖励（假设）

```
季度增长：50,000 USDT
增长率：50,000 / 100,000 = 50%
奖励等级：30%-50%（良好奖励）

季度奖励：50,000 × 1.0% = 500 USDT
月度平均：500 / 3 = 167 USDT
```

**Kevin 的月度总收益**：

```
├─ 直推奖励（即时）：63 USDT
├─ 直推奖励（线性）：141 USDT（逐步释放）
├─ 级差奖励：2,000 USDT
├─ 管理分红：720 USDT
└─ 季度奖励（平均）：167 USDT

月度总计：63 + 2,000 + 720 + 167 = 2,950 USDT
年度总计：2,950 × 12 = 35,400 USDT

加上线性释放：141 × 12 = 1,692 USDT
年度总收益：35,400 + 1,692 = 37,092 USDT
```

---

## ✅ 团长激励总结

### 核心机制

1. **直推奖励**：即时30% + 线性70%（60天）
2. **级差奖励**：V1-V5，最高50%
3. **管理分红**：月度发放，最高2%团队业绩
4. **季度奖励**：季度业绩增长奖励，最高2%
5. **特殊权益**：V5参与项目方分红（10%）

### 防刷机制

- 有效业绩考核（锁仓+活跃+复投+最低质押）
- 线性释放（防止短期刷单）
- 质量权重（锁仓成员权重更高）

### 分阶段调整

- **前期**：高激励吸引（+50-100%）
- **中期**：稳定激励（基准）
- **后期**：长期激励（管理分红+50%，季度奖励+100%）

---

**这个方案既保证了团长的积极性，又通过质量考核防止刷单。您觉得还需要调整哪些参数？**

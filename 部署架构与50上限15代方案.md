# 部署架构与50%上限15代方案

## 🏗️ 部署架构分析

### 问题一：哪些部署在区块链上？

#### 链上部署（智能合约）

**必须部署在链上的部分**：

1. **RWAToken.sol** - 代币合约
   - 代币发行和转账
   - 交易税机制
   - 白名单管理
   - ✅ **必须链上**

2. **StakingContract.sol** - 质押合约
   - 用户质押USDT
   - 资金分配（50/50）
   - 用户余额管理
   - 提现功能
   - ✅ **必须链上**

3. **StRWA.sol** - 锁仓RWA合约（新增）
   - stRWA代币发行
   - 解锁机制
   - ✅ **必须链上**

4. **TreasuryContract.sol** - 储备金管理合约（新增）
   - 投资记录
   - 回报分配
   - ✅ **必须链上**

#### 链下部署（后端服务）

**部署在服务器上的部分**：

1. **RewardEngine.ts** - 奖励计算引擎
   - 级差奖励计算（复杂算法）
   - 推荐链查询
   - ✅ **链下计算，链上更新**

2. **NodeLevelService.ts** - 节点等级服务
   - 升级条件检查
   - 业绩统计
   - ✅ **链下计算，链上更新**

3. **DailyYieldService.ts** - 日收益服务
   - 每日收益计算
   - ✅ **链下计算，链上更新**

4. **数据库（MySQL）**
   - 用户信息
   - 推荐关系
   - 奖励记录
   - ✅ **链下存储**

5. **EventMonitor.ts** - 事件监听
   - 监听链上事件
   - 处理质押事件
   - ✅ **链下服务**

---

## 🔄 完整数据流

### 用户质押流程

```
1. 用户调用 StakingContract.stake()
   ├─ 链上：USDT转账（50% Treasury，50% Contract）
   ├─ 链上：发出 StakeEvent 事件
   └─ 链上：更新用户余额

2. EventMonitor 监听事件（链下）
   ├─ 链下：查询数据库，检查是否已处理
   ├─ 链下：存入数据库（stakes表）
   └─ 链下：建立推荐关系（referral_relations表）

3. RewardEngine 计算奖励（链下）
   ├─ 链下：查询推荐链（从数据库）
   ├─ 链下：计算级差奖励（复杂算法）
   └─ 链下：验证50%上限

4. 更新合约状态（链上）
   ├─ 链上：调用 StakingContract.updateUserRewards()
   ├─ 链上：更新用户USDT奖励余额
   └─ 链上：发出 RewardsUpdated 事件

5. 更新数据库（链下）
   ├─ 链下：记录奖励到 rewards 表
   └─ 链下：更新用户统计
```

### 为什么大部分计算在链下？

**原因**：
1. **Gas成本**：复杂计算在链上非常昂贵
2. **性能**：链下计算更快
3. **灵活性**：可以随时调整算法，无需升级合约
4. **数据查询**：推荐链查询在链上成本极高

**安全性保证**：
- 关键状态更新在链上（余额、质押额）
- 链下计算结果需要链上验证（50%上限检查）
- 使用事件日志保证可追溯性

---

## 💰 问题二：50%上限覆盖15代方案

### 当前问题

**现有设计**：
- 50%上限主要分配给前5代
- 6-15代如果前5代已满，无法获得奖励

### 改进方案：50%上限，但覆盖15代

#### 核心思路

**级差逻辑 + 深度权重**：
- 前5代：正常级差，权重高
- 6-10代：级差减半，权重中
- 11-15代：级差再减半，权重低
- 总奖励不超过50%

#### 实现逻辑

```solidity
// 深度权重配置
struct DepthWeight {
    uint256 weight;        // 权重（基点制）
    uint256 maxPercentage; // 该深度段最大百分比
}

mapping(uint256 => DepthWeight) public depthWeights;

function initializeDepthWeights() internal {
    // 1-5代：权重100%，最大40%
    for (uint i = 1; i <= 5; i++) {
        depthWeights[i] = DepthWeight({
            weight: 10000,      // 100%
            maxPercentage: 40   // 最大40%
        });
    }
    
    // 6-10代：权重50%，最大7%
    for (uint i = 6; i <= 10; i++) {
        depthWeights[i] = DepthWeight({
            weight: 5000,       // 50%
            maxPercentage: 7    // 最大7%
        });
    }
    
    // 11-15代：权重25%，最大3%
    for (uint i = 11; i <= 15; i++) {
        depthWeights[i] = DepthWeight({
            weight: 2500,       // 25%
            maxPercentage: 3    // 最大3%
        });
    }
}

// 计算级差奖励（覆盖15代）
function calculateDifferentialRewards15Generations(
    uint256 stakeAmount,
    address userAddress
) internal returns (RewardDistribution[] memory) {
    address[] memory ancestors = getAncestors(userAddress);
    
    RewardDistribution[] memory rewards = new RewardDistribution[](ancestors.length);
    uint256 maxAllocatedPercentage = 0;
    uint256 totalRewards = 0;
    
    // 按深度段统计已分配百分比
    uint256 allocated1_5 = 0;   // 1-5代已分配
    uint256 allocated6_10 = 0;  // 6-10代已分配
    uint256 allocated11_15 = 0; // 11-15代已分配
    
    for (uint i = 0; i < ancestors.length && i < 15; i++) {
        address ancestor = ancestors[i];
        uint256 depth = i + 1;
        
        // 获取深度权重
        DepthWeight storage depthWeight = depthWeights[depth];
        
        UserInfo storage ancInfo = users[ancestor];
        uint8 nodeLevel = ancInfo.nodeLevel;
        uint256 basePercentage = nodeRewardPercentages[nodeLevel];
        
        // 计算级差
        uint256 differential = basePercentage > maxAllocatedPercentage 
            ? basePercentage - maxAllocatedPercentage 
            : 0;
        
        if (differential > 0) {
            // 应用深度权重
            uint256 adjustedPercentage = differential * depthWeight.weight / 10000;
            
            // 检查深度段上限
            bool canAllocate = false;
            if (depth <= 5) {
                if (allocated1_5 + adjustedPercentage <= depthWeight.maxPercentage) {
                    canAllocate = true;
                    allocated1_5 += adjustedPercentage;
                }
            } else if (depth <= 10) {
                if (allocated6_10 + adjustedPercentage <= depthWeight.maxPercentage) {
                    canAllocate = true;
                    allocated6_10 += adjustedPercentage;
                }
            } else if (depth <= 15) {
                if (allocated11_15 + adjustedPercentage <= depthWeight.maxPercentage) {
                    canAllocate = true;
                    allocated11_15 += adjustedPercentage;
                }
            }
            
            if (canAllocate) {
                uint256 rewardAmount = stakeAmount * adjustedPercentage / 10000;
                
                rewards[i] = RewardDistribution({
                    beneficiary: ancestor,
                    amount: rewardAmount,
                    percentage: adjustedPercentage,
                    nodeLevel: nodeLevel,
                    depth: depth
                });
                
                maxAllocatedPercentage = basePercentage;
                totalRewards += rewardAmount;
            }
        }
        
        // 检查总奖励是否超过50%上限
        if (totalRewards > stakeAmount * 50 / 100) {
            break;
        }
    }
    
    return rewards;
}
```

---

## 📊 完整示例：50%上限覆盖15代

### 场景：用户Alice质押10,000 USDT，推荐链15代

**推荐链结构**：
```
Alice (质押者)
    ↓ 第1代
Bob (V2, 10%)
    ↓ 第2代
Charlie (V3, 15%)
    ↓ 第3代
David (V4, 20%)
    ↓ 第4代
Eve (V5, 50%)
    ↓ 第5代
Frank (V1, 5%)
    ↓ 第6代
Grace (V2, 10%)
    ↓ 第7代
Henry (V3, 15%)
    ↓ ... (继续到第15代)
```

**奖励计算**：

#### 第1-5代（权重100%，最大40%）

```
第1代：Bob (V2, 10%)
├─ 级差：10% - 0% = 10%
├─ 深度权重：100%
├─ 实际奖励：10,000 × 10% × 100% = 1,000 USDT
└─ 1-5代已分配：10%

第2代：Charlie (V3, 15%)
├─ 级差：15% - 10% = 5%
├─ 深度权重：100%
├─ 实际奖励：10,000 × 5% × 100% = 500 USDT
└─ 1-5代已分配：15%

第3代：David (V4, 20%)
├─ 级差：20% - 15% = 5%
├─ 深度权重：100%
├─ 实际奖励：10,000 × 5% × 100% = 500 USDT
└─ 1-5代已分配：20%

第4代：Eve (V5, 50%)
├─ 级差：50% - 20% = 30%
├─ 深度权重：100%
├─ 实际奖励：10,000 × 30% × 100% = 3,000 USDT
└─ 1-5代已分配：50%（达到上限，但只用了40%的深度段上限）

第5代：Frank (V1, 5%)
├─ 级差：5% - 50% = 0%（被压级）
└─ 实际奖励：0 USDT

1-5代总计：1,000 + 500 + 500 + 3,000 = 5,000 USDT（50%）
```

**注意**：虽然总奖励达到50%，但1-5代深度段上限是40%，所以第4代可能无法获得全部30%

#### 优化方案：动态分配

**如果前5代已经达到50%，6-15代无法获得奖励**

**解决方案**：调整分配策略

```
策略A：前5代优先，但限制在40%
├─ 1-5代：最大40%
├─ 6-10代：最大7%
├─ 11-15代：最大3%
└─ 总计：50%

策略B：动态平衡
├─ 如果前5代未满40%，优先分配
├─ 如果前5代已满40%，分配给6-15代
└─ 总上限：50%
```

---

## 🎯 最终方案：50%上限覆盖15代

### 分配策略

| 深度段 | 权重 | 最大百分比 | 说明 |
|--------|------|-----------|------|
| 1-5代 | 100% | 40% | 主要奖励池 |
| 6-10代 | 50% | 7% | 次要奖励池 |
| 11-15代 | 25% | 3% | 补充奖励池 |
| **总计** | - | **50%** | **总上限** |

### 分配逻辑

```solidity
function calculateRewardsWith15Generations(
    uint256 stakeAmount,
    address userAddress
) internal returns (RewardDistribution[] memory) {
    address[] memory ancestors = getAncestors(userAddress);
    
    uint256 allocated1_5 = 0;   // 1-5代已分配（最大40%）
    uint256 allocated6_10 = 0;  // 6-10代已分配（最大7%）
    uint256 allocated11_15 = 0; // 11-15代已分配（最大3%）
    uint256 totalAllocated = 0; // 总分配（最大50%）
    
    RewardDistribution[] memory rewards = new RewardDistribution[](ancestors.length);
    uint256 maxAllocatedPercentage = 0;
    
    for (uint i = 0; i < ancestors.length && i < 15; i++) {
        address ancestor = ancestors[i];
        uint256 depth = i + 1;
        
        // 计算级差
        uint256 basePercentage = getNodeRewardPercentage(ancestor);
        uint256 differential = basePercentage > maxAllocatedPercentage 
            ? basePercentage - maxAllocatedPercentage 
            : 0;
        
        if (differential > 0) {
            uint256 weight = getDepthWeight(depth);
            uint256 adjustedPercentage = differential * weight / 10000;
            
            // 检查深度段上限和总上限
            bool canAllocate = false;
            uint256 segmentAllocated = 0;
            uint256 segmentMax = 0;
            
            if (depth <= 5) {
                segmentAllocated = allocated1_5;
                segmentMax = 40; // 40%
            } else if (depth <= 10) {
                segmentAllocated = allocated6_10;
                segmentMax = 7;  // 7%
            } else {
                segmentAllocated = allocated11_15;
                segmentMax = 3;  // 3%
            }
            
            // 检查两个条件：深度段上限 + 总上限
            if (segmentAllocated + adjustedPercentage <= segmentMax &&
                totalAllocated + adjustedPercentage <= 50) {
                canAllocate = true;
            }
            
            if (canAllocate) {
                uint256 rewardAmount = stakeAmount * adjustedPercentage / 10000;
                
                rewards[i] = RewardDistribution({
                    beneficiary: ancestor,
                    amount: rewardAmount,
                    percentage: adjustedPercentage,
                    nodeLevel: getNodeLevel(ancestor),
                    depth: depth
                });
                
                // 更新统计
                if (depth <= 5) {
                    allocated1_5 += adjustedPercentage;
                } else if (depth <= 10) {
                    allocated6_10 += adjustedPercentage;
                } else {
                    allocated11_15 += adjustedPercentage;
                }
                
                totalAllocated += adjustedPercentage;
                maxAllocatedPercentage = basePercentage;
            }
        }
        
        // 如果总分配达到50%，停止
        if (totalAllocated >= 50) {
            break;
        }
    }
    
    return rewards;
}
```

---

## 📋 完整示例：50%上限覆盖15代

### 场景：用户质押10,000 USDT，推荐链15代

**假设推荐链**：
- 第1代：V2 (10%)
- 第2代：V3 (15%)
- 第3代：V4 (20%)
- 第4代：V5 (50%)
- 第5代：V1 (5%)
- 第6代：V2 (10%)
- 第7代：V3 (15%)
- ... (继续到第15代)

**奖励分配**：

```
第1-5代（权重100%，最大40%）：
├─ 第1代：10% × 100% = 10% → 1,000 USDT
├─ 第2代：5% × 100% = 5% → 500 USDT
├─ 第3代：5% × 100% = 5% → 500 USDT
├─ 第4代：20% × 100% = 20% → 2,000 USDT（限制在40%内）
└─ 第5代：0%（被压级）

1-5代总计：10% + 5% + 5% + 20% = 40%（4,000 USDT）

第6-10代（权重50%，最大7%）：
├─ 第6代：10% × 50% = 5% → 500 USDT（但被压级，实际0%）
├─ 第7代：15% × 50% = 7.5% → 但只能分配7%（上限）
└─ 实际分配：7% → 700 USDT

6-10代总计：7%（700 USDT）

第11-15代（权重25%，最大3%）：
├─ 可以分配：3% → 300 USDT

11-15代总计：3%（300 USDT）

总分配：40% + 7% + 3% = 50%（5,000 USDT）✅
```

---

## ✅ 方案总结

### 部署架构

**链上部署**：
- ✅ RWAToken.sol
- ✅ StakingContract.sol
- ✅ StRWA.sol（新增）
- ✅ TreasuryContract.sol（新增）

**链下部署**：
- ✅ RewardEngine.ts（计算，链上更新）
- ✅ NodeLevelService.ts（计算，链上更新）
- ✅ DailyYieldService.ts（计算，链上更新）
- ✅ 数据库（MySQL）

### 50%上限覆盖15代

**分配策略**：
- 1-5代：最大40%（权重100%）
- 6-10代：最大7%（权重50%）
- 11-15代：最大3%（权重25%）
- **总上限：50%**

**优势**：
- ✅ 覆盖15代，激励更深层推荐
- ✅ 总奖励不超过50%，保证可持续性
- ✅ 前5代获得主要奖励（40%）
- ✅ 6-15代获得补充奖励（10%）

---

**这个方案既保证了50%上限，又覆盖了15代。您觉得还需要调整哪些参数？**

# 🚀 后端服务快速启动指南 - 2026年3月2日

**目的**: 快速启动后端服务，实现自动同步链上数据

---

## ⚡ 快速启动（3步）

### Step 1: 配置数据库（必需）

**选项 A: 使用 Docker（推荐，最简单）**

```bash
# 启动 MySQL 容器
docker run --name mysql-rwa \
  -e MYSQL_ROOT_PASSWORD=rootpassword \
  -e MYSQL_DATABASE=rwa_protocol \
  -e MYSQL_USER=rwa_user \
  -e MYSQL_PASSWORD=rwa_password \
  -p 3306:3306 \
  -d mysql:8.0

# 等待 MySQL 启动（约 10 秒）
Start-Sleep -Seconds 10

# 初始化数据库表
cd backend
mysql -h 127.0.0.1 -P 3306 -u rwa_user -prwa_password rwa_protocol < src/config/database.sql
```

**选项 B: 使用本地 MySQL**

```bash
# 1. 确保 MySQL 已安装并运行
# 2. 创建数据库和用户
mysql -u root -p

# 在 MySQL 中执行：
CREATE DATABASE IF NOT EXISTS rwa_protocol;
CREATE USER IF NOT EXISTS 'rwa_user'@'localhost' IDENTIFIED BY 'rwa_password';
GRANT ALL PRIVILEGES ON rwa_protocol.* TO 'rwa_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;

# 3. 初始化表结构
cd backend
mysql -u rwa_user -prwa_password rwa_protocol < src/config/database.sql
```

---

### Step 2: 配置 Redis（可选，但推荐）

**使用 Docker（推荐）:**

```bash
docker run --name redis-rwa -p 6379:6379 -d redis:latest
```

**或使用本地 Redis:**

确保 Redis 服务已启动在 `localhost:6379`

---

### Step 3: 启动后端服务

```bash
cd backend
npm run dev
```

**预期输出:**

```
============================================================
RWA Protocol Backend Service
============================================================

Testing database connection...
✅ Database connected
Connecting to Redis...
✅ Redis connected
✅ HTTP server listening on port 3001
Starting event monitor...
✅ Event monitor started
Starting scheduler...
✅ Scheduler started

============================================================
🚀 Backend service is running
============================================================
```

---

## 📊 服务功能

### 1. 自动同步链上数据

- **事件监听**: 自动监听 `StakeEvent`，同步用户质押数据到数据库
- **实时更新**: 每 5 秒轮询一次链上事件

### 2. 定时任务

- **每日收益计算**: 每天 00:00 UTC（北京时间 08:00）自动计算并发放 0.8% 日收益
- **价格刷新**: 每 5 分钟刷新 RWA/USDT 价格
- **节点等级同步**: 每小时检查并更新用户节点等级

### 3. API 接口

**健康检查:**
```bash
curl http://localhost:3001/health
```

**用户信息:**
```bash
curl http://localhost:3001/api/user/0x70997970C51812dc3A010C7d01b50e0d17dc79C8
```

---

## ⚠️ 重要提示

### 1. 数据库必须配置

后端服务**必须**连接数据库才能启动。如果数据库未配置，服务会启动失败。

### 2. Redis 可选但推荐

Redis 用于价格缓存。如果未配置 Redis，价格服务可能无法正常工作，但其他功能不受影响。

**临时跳过 Redis（仅用于测试）:**

修改 `backend/src/index.ts` 第 119-121 行，注释掉 Redis 连接：

```typescript
// Connect price oracle (Redis)
// logger.info('Connecting to Redis...');
// await this.priceOracleService.connect();
// logger.info('✅ Redis connected');
```

### 3. 合约地址必须匹配

确保 `backend/.env` 中的合约地址与当前部署的合约地址一致。

**当前地址（Hardhat Local）:**
- `STAKING_CONTRACT_ADDRESS=0xFD471836031dc5108809D173A067e8486B9047A3`
- `RWA_TOKEN_ADDRESS=0x7bc06c482DEAd17c0e297aFbC32f6e63d3846650`
- `USDT_TOKEN_ADDRESS=0x7969c5eD335650692Bc04293B07F5BF2e7A673C0`

---

## 🔍 验证服务运行

### 1. 检查服务状态

```bash
curl http://localhost:3001/health
```

**成功响应:**
```json
{
  "status": "ok",
  "timestamp": "2026-03-02T10:00:00.000Z"
}
```

### 2. 检查日志

查看后端服务控制台输出，应该看到：
- ✅ Database connected
- ✅ Redis connected（如果配置了）
- ✅ HTTP server listening on port 3001
- ✅ Event monitor started
- ✅ Scheduler started

### 3. 测试自动同步

1. 在前端进行一笔质押
2. 等待 5-10 秒
3. 检查后端日志，应该看到事件被处理

---

## 🐛 常见问题

### 问题 1: 数据库连接失败

**错误**: `Database connection failed`

**解决方案:**
- 检查 MySQL 是否运行: `docker ps` 或 `Get-Service MySQL`
- 检查 `.env` 中的数据库配置
- 检查数据库用户权限

### 问题 2: Redis 连接失败

**错误**: `Redis connection failed`

**解决方案:**
- Redis 是可选的，可以暂时跳过（见上方提示）
- 或启动 Redis: `docker start redis-rwa`

### 问题 3: 合约调用失败

**错误**: `Only backend can call`

**解决方案:**
- 检查 `BACKEND_PRIVATE_KEY` 是否对应正确的地址
- 检查合约中的 `backendAddress` 是否匹配

---

## 📝 下一步

1. ✅ **配置数据库** - 使用 Docker 或本地 MySQL
2. ✅ **配置 Redis**（可选）
3. ✅ **启动服务** - `npm run dev`
4. ✅ **验证运行** - 检查健康检查 API
5. ✅ **测试同步** - 在前端质押，观察后端日志

**配置完成后，后端服务将自动同步链上数据并定时发放收益！**

# 🔧 授权问题排查指南

**问题**: 点击授权后没反应

---

## 🔍 可能的原因

### 1. 合约地址未更新（已修复 ✅）

**问题**: 前端使用的合约地址是旧的

**解决**: 已更新 `frontend/lib/contracts/addresses.ts` 中的合约地址

**需要操作**:
1. 重启前端服务器
2. 清除浏览器缓存
3. 重新加载页面

---

### 2. MetaMask 未弹出确认窗口

**检查**:
- MetaMask 是否已安装并解锁
- 是否切换到 Hardhat Local 网络
- MetaMask 扩展是否被浏览器阻止

**解决**:
1. 检查浏览器控制台是否有错误
2. 检查 MetaMask 是否显示待处理交易
3. 尝试刷新页面

---

### 3. 网络连接问题

**检查**:
- Hardhat 节点是否正在运行
- 前端是否连接到正确的 RPC URL

**解决**:
```bash
# 检查 Hardhat 节点是否运行
netstat -ano | findstr :8545

# 如果没运行，启动节点
npx hardhat node
```

---

### 4. 授权已存在但前端未检测到

**解决**: 运行检查脚本

```bash
npx hardhat run scripts/check-authorization.ts --network localhost
```

如果授权额度为 0，脚本会自动授权。

---

## 🚀 快速修复步骤

### 步骤 1: 重启前端

```bash
# 停止前端（Ctrl+C）
# 然后重新启动
cd frontend
npm run dev
```

### 步骤 2: 清除浏览器缓存

1. 按 `F12` 打开开发者工具
2. 右键点击刷新按钮
3. 选择 "清空缓存并硬性重新加载"

### 步骤 3: 检查浏览器控制台

1. 按 `F12` 打开开发者工具
2. 切换到 "Console" 标签
3. 点击授权按钮
4. 查看是否有错误信息

### 步骤 4: 手动授权（如果前端不工作）

运行授权脚本：

```bash
npx hardhat run scripts/check-authorization.ts --network localhost
```

这会自动检查并授权。

---

## 🔍 调试信息

### 检查前端日志

在浏览器控制台查看：
- `[useUSDT] Debug Info:` - 合约地址信息
- 任何错误信息

### 检查 MetaMask

1. 打开 MetaMask
2. 查看 "活动" 标签
3. 查看是否有待处理的交易

### 检查网络

1. 确认 MetaMask 显示 "Hardhat Local"
2. Chain ID: 31337
3. RPC URL: http://127.0.0.1:8545

---

## ✅ 验证授权

### 方法 1: 使用脚本

```bash
npx hardhat run scripts/check-authorization.ts --network localhost
```

### 方法 2: 使用 Hardhat Console

```bash
npx hardhat console --network localhost
```

然后在控制台中：
```javascript
const usdt = await ethers.getContractAt("TestUSDT", "0x9E545E3C0baAB3E08CdfD552C960A1050f373042")
const allowance = await usdt.allowance("0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266", "0xf5059a5D33d5853360D16C683c16e67980206f36")
console.log("授权额度:", ethers.formatUnits(allowance, 6), "USDT")
```

---

## 📝 常见错误

### 错误 1: "User rejected"

**原因**: 用户在 MetaMask 中拒绝了交易

**解决**: 重新点击授权，在 MetaMask 中确认

### 错误 2: "Contract addresses not found"

**原因**: 前端未正确读取合约地址

**解决**: 
1. 检查 `addresses.ts` 是否已更新
2. 重启前端服务器
3. 清除浏览器缓存

### 错误 3: "Network error"

**原因**: Hardhat 节点未运行或连接失败

**解决**:
1. 确认 Hardhat 节点正在运行
2. 检查 RPC URL 是否正确

---

## 🎯 推荐操作顺序

1. **重启前端服务器**
   ```bash
   cd frontend
   npm run dev
   ```

2. **清除浏览器缓存**
   - F12 → 右键刷新 → "清空缓存并硬性重新加载"

3. **运行授权检查脚本**
   ```bash
   npx hardhat run scripts/check-authorization.ts --network localhost
   ```

4. **重新尝试授权**
   - 刷新页面
   - 点击授权按钮
   - 在 MetaMask 中确认

---

**如果问题仍然存在，请告诉我具体的错误信息！** 🔍

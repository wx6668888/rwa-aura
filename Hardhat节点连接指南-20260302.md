# 🔧 Hardhat 节点连接指南 - 2026年3月2日

**状态**: 正在重新连接 Hardhat 本地节点

---

## 📋 当前状态

### 已执行的操作

1. ✅ 停止所有旧的 Node.js 进程
2. ✅ 启动新的 Hardhat 节点（在新窗口）
3. ✅ 重启前端服务器

---

## 🚀 手动启动 Hardhat 节点

如果节点未自动启动，请手动执行：

### 方法 1: 在新终端窗口启动

1. 打开新的 PowerShell 或 CMD 窗口
2. 执行以下命令：

```bash
cd "e:\MyRWA_Project\rwa aura"
npx hardhat node --hostname 0.0.0.0
```

**预期输出**:
```
Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/

Account #0: 0xf39Fd6e51aac88F6F4ce6aB8827279cffFb92266 (10000 ETH)
Private Key: 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

Account #1: 0x70997970C51812dc3A010C7d01b50e0d17dc79C8 (10000 ETH)
Private Key: 0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d

... (更多账户)
```

---

### 方法 2: 使用后台进程（已执行）

已在新窗口启动 Hardhat 节点，请检查是否有新的 PowerShell 窗口打开。

---

## ✅ 验证节点是否运行

### 检查端口监听

在 PowerShell 中执行：
```powershell
netstat -ano | findstr ":8545"
```

**应该看到**:
```
TCP    0.0.0.0:8545           0.0.0.0:0              LISTENING       <PID>
TCP    [::]:8545              [::]:0                 LISTENING       <PID>
```

---

### 测试 RPC 连接

在浏览器中访问：
```
http://127.0.0.1:8545
```

**或使用 PowerShell**:
```powershell
$body = @{jsonrpc='2.0'; method='eth_blockNumber'; params=@(); id=1} | ConvertTo-Json
Invoke-WebRequest -Uri 'http://127.0.0.1:8545' -Method POST -Body $body -ContentType 'application/json' -UseBasicParsing
```

**预期响应**:
```json
{"jsonrpc":"2.0","id":1,"result":"0x0"}
```

---

## 🔗 在 MetaMask 中连接

### 如果显示 "Hardhat Local" 断开

1. **打开 MetaMask**
2. **点击网络下拉菜单**
3. **选择 "Hardhat Local"**
4. **如果显示断开，点击 "切换到此网络"**

---

### 手动添加网络（如果需要）

1. MetaMask → **设置** → **网络** → **添加网络**
2. 填写以下信息：
   - **网络名称**: `Hardhat Local`
   - **RPC URL**: `http://127.0.0.1:8545`
   - **Chain ID**: `31337`
   - **货币符号**: `ETH`
   - **区块浏览器**: 留空
3. 点击 **保存**

---

## 🎯 在前端重新连接

1. **打开** http://localhost:3000
2. **硬刷新** (`Ctrl+Shift+R`)
3. **点击** "Connect Wallet" 或 "连接钱包"
4. **选择** MetaMask
5. **确认** 连接到 "Hardhat Local" 网络

---

## ⚠️ 常见问题

### 1. 端口被占用

**错误**: `Error: listen EADDRINUSE: address already in use :::8545`

**解决**:
```powershell
# 查找占用端口的进程
netstat -ano | findstr ":8545"

# 停止进程（替换 <PID> 为实际进程 ID）
taskkill /PID <PID> /F
```

---

### 2. 节点未启动

**检查**:
- 是否有新的 PowerShell 窗口打开？
- 窗口中是否显示 "Started HTTP and WebSocket JSON-RPC server"？
- 是否有错误信息？

**重新启动**:
```bash
cd "e:\MyRWA_Project\rwa aura"
npx hardhat node --hostname 0.0.0.0
```

---

### 3. MetaMask 无法连接

**检查**:
- RPC URL 是否正确：`http://127.0.0.1:8545`
- Chain ID 是否正确：`31337`
- 节点是否正在运行

**清除 MetaMask 缓存**:
1. MetaMask → 设置 → 高级
2. 点击 "重置账户"
3. 重新添加网络

---

### 4. 前端无法连接

**检查浏览器控制台** (F12):
- 是否有 RPC 连接错误？
- 错误信息是什么？

**检查网络请求**:
- 打开 Network 标签页
- 查找到 `http://127.0.0.1:8545` 的请求
- 检查请求状态（应该是 200）

---

## 📊 连接信息总结

### Hardhat 本地网络

- **RPC URL**: `http://127.0.0.1:8545`
- **Chain ID**: `31337`
- **网络名称**: Hardhat Local
- **原生代币**: ETH
- **区块时间**: 即时（测试网络）

---

## ✅ 完成检查清单

- [ ] Hardhat 节点正在运行（新窗口显示启动信息）
- [ ] 端口 8545 正在监听
- [ ] RPC 连接测试成功
- [ ] MetaMask 显示 "Hardhat Local" 网络
- [ ] 前端可以连接到网络
- [ ] 可以执行交易（如质押、提取等）

---

**请检查是否有新的 PowerShell 窗口打开，并确认 Hardhat 节点已成功启动！**

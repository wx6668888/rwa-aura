# 项目分红机制知识库补充完成(已更新)

## 完成时间
2026-03-07

## 任务概述
在知识库(http://localhost:3000/knowledge)的"节点与推荐"分类中添加了详细的项目分红机制说明文章,并根据实际合约实现进行了更新。

## 重要更新(第二版)

根据 `contracts/TreasuryContract.sol` 合约的实际实现,对知识库内容进行了重大修正:

### 核心变更

1. **分红资格**: 
   - ❌ 旧版: 只有 L4 及以上节点才有资格
   - ✅ 新版: **所有质押用户都有资格**,不限节点等级

2. **分红计算方式**:
   - ❌ 旧版: 按节点等级权重(1.0x-3.0x)分配
   - ✅ 新版: **按投资份额平均分配**,公式为:
     ```
     个人分红 = (月度总分红 × 个人投资份额) / 总投资份额
     投资份额 = 质押金额的 50%(进入 Treasury 的部分)
     ```

3. **分红来源**:
   - ❌ 旧版: 平台整体收益(交易手续费、抽奖收入等)
   - ✅ 新版: **Treasury 投资实体项目的回报**,按固定比例分配:
     - 40% 用户分红
     - 30% 再投资
     - 20% 流动性
     - 10% 风险准备金

4. **节点等级关系**:
   - 前端显示的"分红权重"(L4-L9)是**未来可能实施的机制**
   - 当前合约中**不考虑节点等级**,完全按投资份额分配

## 修改文件

### 1. frontend/lib/knowledge-data.ts
在 `nodes` 分类的文章列表中添加了新文章:
```typescript
{ 
  id: 'project-dividend-mechanism', 
  categoryId: 'nodes', 
  titleKey: 'knowledge.article.project-dividend-mechanism.title', 
  contentKey: 'knowledge.article.project-dividend-mechanism.content' 
}
```

位置:在 `referral-reward-calc` 和 `node-level-downgrade` 之间

### 2. frontend/lib/knowledge-content-zh-full.ts
添加了完整的中文内容(已更新为第二版),包含:

#### 文章标题
"什么是项目分红?如何参与?"

#### 文章内容结构(第二版)
1. **基本概念** - 解释 50/50 资金模型和 Treasury 投资机制
2. **分红资格** - 说明所有质押用户都有资格
3. **分红计算方式** - 基于投资份额的计算公式和详细举例
4. **月度分红机制** - 累积、分配、领取三个阶段
5. **投资份额说明** - 如何产生、累积和查询
6. **与节点等级的关系** - 澄清当前合约不考虑节点等级
7. **实际合约实现** - 展示合约中的固定分配比例
8. **常见问题** - 解答用户可能的疑问(已更新)

## 内容亮点(第二版)

### 资金分配表格
```
| 分配项目 | 比例 | 说明 |
|---------|------|------|
| 用户分红 | 40% | 按用户投资份额分配 |
| 再投资   | 30% | 继续投资扩大规模 |
| 流动性   | 20% | 增强代币流动性 |
| 风险准备金| 10% | 应对潜在风险 |
```

### 两个实际计算举例

**例1:简单场景**
- 投资回报: 100,000 USDT
- 3个用户(投资份额 5,000、10,000、15,000)
- 详细展示 40% 用户分红的分配过程

**例2:月度分红场景**
- 月度回报: 500,000 USDT
- 用户分红池: 200,000 USDT
- 展示个人份额占比计算

### 合约代码引用
```solidity
// 投资回报分配比例(固定)
USER_DIVIDEND_SHARE = 40%   // 用户分红
REINVEST_SHARE = 30%        // 再投资
LIQUIDITY_SHARE = 20%       // 流动性
RESERVE_SHARE = 10%         // 风险准备金
```

### 常见问题解答(已更新)
- 需要达到 L4 才能获得分红吗? → 不需要,所有用户都有资格
- 分红什么时候发放? → 月度结算
- 分红池资金从哪里来? → Treasury 投资实体项目的回报
- 提现本金会减少投资份额吗? → 当前不会
- 为什么前端显示节点等级有不同权重? → 未来预留机制,当前不生效

## 合约实现细节

根据 `TreasuryContract.sol`:

1. **投资份额记录**:
   ```solidity
   mapping(address => uint256) public userInvestmentShares;
   uint256 public totalInvestmentShares;
   ```

2. **月度分红结构**:
   ```solidity
   struct MonthlyDividend {
       uint256 month;
       uint256 totalDividend;
       uint256 distributedAmount;
       bool isDistributed;
   }
   ```

3. **分红计算**:
   ```solidity
   userDividend = (monthlyDividend.totalDividend * userShare) / totalInvestmentShares
   ```

4. **分红领取**:
   - 函数: `claimMonthlyDividend(uint256 month)`
   - 防重复领取: `userDividendClaimed[user][month]`
   - 自动转账 USDT

## 访问方式

用户可以通过以下方式查看:
1. 访问 http://localhost:3000/knowledge
2. 在左侧导航或顶部分类中选择"节点与推荐"
3. 找到"什么是项目分红?如何参与?"文章
4. 点击展开查看完整内容

## 技术说明

- 文章ID: `project-dividend-mechanism`
- 分类: `nodes`(节点与推荐)
- 语言: 目前仅添加了中文完整版
- 格式: Markdown,支持表格、列表、代码块等
- 合约依据: `contracts/TreasuryContract.sol`

## 后续建议

1. **多语言支持**: 建议后续添加英文、韩文等其他语言版本
2. **i18n翻译**: 需要在 `frontend/lib/i18n.ts` 中添加对应的翻译键
3. **前端UI更新**: 考虑移除或标注"分红权重"显示,避免用户误解
4. **合约升级**: 如果未来实施节点等级分红权重,需同步更新文章
5. **用户反馈**: 收集用户对文章的反馈,持续优化内容

## 验证清单

- [x] 文章已添加到 knowledge-data.ts
- [x] 中文完整内容已添加到 knowledge-content-zh-full.ts
- [x] 代码无语法错误(已通过 getDiagnostics 检查)
- [x] 内容与实际合约实现一致
- [x] 澄清了节点等级与分红的关系
- [x] 提供了合约代码引用
- [x] 解答了常见问题
- [x] 格式清晰易读

## 总结

成功在知识库中添加了详细的项目分红机制说明,并根据 `TreasuryContract.sol` 合约的实际实现进行了重大更新。关键修正:

1. **所有质押用户都有分红资格**,不限节点等级
2. **按投资份额平均分配**,不按节点权重
3. **分红来自 Treasury 投资实体项目的回报**,按 40/30/20/10 固定比例分配
4. **采用月度结算机制**,用户可在「提现」页面领取

内容详实、举例丰富、与合约实现完全一致,能够帮助用户准确理解项目分红机制的实际运作方式。

# 复投功能实现方案

## 🔍 问题分析

### 当前状态

1. **合约层面**：`StakingContract.sol` 中**没有**复投功能
   - ❌ 没有 `reinvest` 函数
   - ❌ 没有 `reinvestmentCount` mapping
   - ❌ 没有复投奖励计算逻辑

2. **前端层面**：只有显示，没有实际功能
   - `reinvestCount = 0`（硬编码）
   - 显示逻辑完整，但数据是假的

3. **用户操作**：
   - 用户"再次质押" ≠ "复投"
   - 复投应该是：将**收益**（RWA/USDT）重新质押，并获得额外加成

---

## 📋 复投逻辑设计

根据项目方案，复投逻辑应该是：

| 复投次数 | 额外奖励 | 说明 |
|---------|---------|------|
| 第1次复投 | +5% | 首次复投获得 5% 额外奖励 |
| 第3次复投 | +10% | 第3次复投获得 10% 额外奖励 |
| 第5次复投 | +15% | 第5次复投获得 15% 额外奖励 |
| 第10次+复投 | +20% | 第10次及以上复投获得 20% 额外奖励 |

**复投定义：**
- 用户将收益（RWA 或 USDT）重新质押
- 复投金额 = 原始收益 + 复投奖励
- 复投奖励根据复投次数计算

---

## 🛠️ 实现方案

### 方案1：完整的复投功能（推荐）

#### 1. 合约层面实现

在 `StakingContract.sol` 中添加：

```solidity
// 复投次数记录
mapping(address => uint256) public reinvestmentCount;

// 复投事件
event Reinvestment(
    address indexed user,
    uint256 rwaAmount,
    uint256 usdtAmount,
    uint256 bonusAmount,
    uint256 reinvestCount,
    uint256 timestamp
);

/**
 * @dev 获取复投奖励率
 */
function getReinvestmentBonusRate(address user) public view returns (uint256) {
    uint256 count = reinvestmentCount[user];
    if (count < 1) return 5;      // 第1次：5%
    if (count < 3) return 10;      // 第3次：10%
    if (count < 5) return 15;      // 第5次：15%
    return 20;                     // 第10次+：20%
}

/**
 * @dev 复投收益
 * @param rwaAmount RWA 代币数量（18 decimals）
 * @param usdtAmount USDT 数量（18 decimals，实际是6 decimals，但传入时转换为18）
 */
function reinvest(uint256 rwaAmount, uint256 usdtAmount) external nonReentrant whenNotPaused {
    UserInfo storage user = users[msg.sender];
    
    // 检查余额
    require(user.rwaPending >= rwaAmount, "Insufficient RWA balance");
    require(user.usdtRewards >= usdtAmount, "Insufficient USDT balance");
    require(rwaAmount > 0 || usdtAmount > 0, "Amount must be greater than zero");
    
    // 获取当前复投次数和奖励率
    uint256 count = reinvestmentCount[msg.sender];
    uint256 bonusRate = getReinvestmentBonusRate(msg.sender);
    
    // 计算总金额（USDT等值）
    // RWA 价格假设为 0.85 USDT（实际应该使用价格预言机）
    uint256 rwaPrice = 85 * 10**15; // 0.85 USDT (18 decimals)
    uint256 rwaInUSDT = (rwaAmount * rwaPrice) / 10**18;
    uint256 totalUSDT = usdtAmount + rwaInUSDT;
    
    // 计算复投奖励
    uint256 bonusAmount = (totalUSDT * bonusRate) / 100;
    
    // 扣除用户余额
    unchecked {
        user.rwaPending -= rwaAmount;
        user.usdtRewards -= usdtAmount;
    }
    
    // 更新复投次数
    unchecked {
        reinvestmentCount[msg.sender] = count + 1;
    }
    
    // 将奖励和本金一起质押（转换为6 decimals）
    uint256 totalStakeAmount = (totalUSDT + bonusAmount) / PRECISION_MULTIPLIER;
    
    // 调用内部质押逻辑（复用现有逻辑）
    _internalStake(totalStakeAmount, user.referrer);
    
    emit Reinvestment(msg.sender, rwaAmount, usdtAmount, bonusAmount, count + 1, block.timestamp);
}

/**
 * @dev 内部质押函数（供复投使用）
 */
function _internalStake(uint256 amount, address referrer) internal {
    // 复用现有的质押逻辑，但不生成新的 stakeId
    // ...（实现细节）
}
```

#### 2. 前端层面实现

1. **从合约读取复投次数**：
   ```typescript
   const { data: reinvestCount } = useReadContract({
     address: stakingAddress,
     abi: stakingContractABI,
     functionName: 'reinvestmentCount',
     args: [address],
   })
   ```

2. **添加复投按钮和逻辑**：
   - 在 Dashboard 或 Withdraw 页面添加"复投"按钮
   - 用户可以选择复投 RWA 或 USDT
   - 调用 `reinvest` 函数

---

### 方案2：临时方案 - 基于质押历史判断（不推荐，仅用于测试）

通过检测用户是否将收益重新质押来判断复投，但这种方法不够准确。

---

## ⚠️ 当前问题

**为什么复投后没有产生加成？**

1. **合约中没有复投功能**：无论用户如何操作，复投次数都是 0
2. **前端只是显示**：`reinvestCount` 硬编码为 0
3. **用户操作误解**：用户可能只是"再次质押"，而不是"复投"

**真正的复投应该是：**
- 用户有收益（RWA 或 USDT）
- 用户选择"复投"
- 系统自动将收益 + 复投奖励一起重新质押
- 复投次数 +1

---

## 🎯 下一步行动

1. ✅ **确认需求**：复投功能是否需要立即实现？
2. ⏳ **实现合约**：在 `StakingContract.sol` 中添加复投功能
3. ⏳ **更新前端**：添加复投 UI 和逻辑
4. ⏳ **测试**：测试复投功能是否正常工作

---

## 💡 建议

如果复投功能是必需的，建议：
1. 先实现合约层面的复投功能
2. 然后更新前端，从合约读取复投次数
3. 添加复投按钮，让用户可以真正进行复投

如果暂时不需要复投功能，可以：
1. 在 UI 上标注"即将推出"或"开发中"
2. 或者暂时隐藏复投奖励卡片

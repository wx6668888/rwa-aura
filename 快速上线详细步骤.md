# 🚀 RWA 项目快速上线详细步骤

**目标**: 1-2周内上线 MVP 版本  
**创建时间**: 2024年  
**优先级**: 最高

---

## 📋 总体时间表

| 阶段 | 任务 | 时间 | 状态 |
|------|------|------|------|
| **第1阶段** | 核心合约开发 | Day 1-4 | ⏳ 进行中 |
| **第2阶段** | 后端服务更新 | Day 5-7 | ⏳ |
| **第3阶段** | 测试网部署 | Day 8-10 | ⏳ |
| **第4阶段** | 主网部署 | Day 11-12 | ⏳ |

**总计**: 12天（约2周）

---

## 🎯 第1阶段：核心合约开发（Day 1-4）

### ✅ Step 1: 创建 StRWA.sol 合约（已完成）

**文件**: `contracts/StRWA.sol`

**功能**:
- ✅ ERC20 标准代币
- ✅ 仅 StakingContract 可铸造/销毁
- ✅ Owner 可设置 StakingContract 地址
- ✅ 批量转账功能

**下一步**: 创建测试文件

---

### Step 2: 创建 SwapContract.sol 合约

**文件**: `contracts/SwapContract.sol`

**功能**:
- stRWA ↔ RWA 互换（简化版：1:1比例）
- 无税互换
- 池子管理

**实施步骤**:
1. 创建合约文件
2. 实现 swapStRWAToRWA 函数
3. 实现 swapRWAToStRWA 函数
4. 实现 initializePool 函数
5. 编写单元测试

---

### Step 3: 更新 StakingContract.sol

**需要添加的功能**:
1. 添加 stRWA 代币引用
2. 修改 `stake()` 函数：50%转换为 stRWA
3. 修改 `withdraw()` 函数：支持持RWA模式（120%收益）

**实施步骤**:
1. 添加 `IERC20 public stRwaToken;`
2. 修改构造函数，添加 stRWA 地址参数
3. 在 `stake()` 中调用 `stRwaToken.mint()`
4. 在 `withdraw()` 中添加持RWA模式逻辑
5. 更新测试

---

### Step 4: 更新 RewardEngine.ts（后端）

**需要添加的功能**:
- 烧伤机制：单次奖励 ≤ 自身质押 × 50%

**实施步骤**:
1. 修改 `calculateDifferentialRewards()` 函数
2. 添加烧伤逻辑
3. 更新测试

---

## 🧪 第2阶段：测试和修复（Day 5-7）

### Step 5: 编写单元测试

**测试文件**:
- `test/StRWA.test.ts`
- `test/SwapContract.test.ts`
- `test/StakingContract.stRWA.test.ts`

**测试内容**:
- ✅ StRWA 铸造/销毁
- ✅ SwapContract 互换
- ✅ StakingContract 集成 stRWA
- ✅ 边界情况测试

---

### Step 6: 集成测试

**测试场景**:
1. 用户质押 → 获得 stRWA
2. 用户提现（持RWA模式）→ 获得 stRWA
3. 用户互换 stRWA → RWA
4. 端到端流程测试

---

## 🌐 第3阶段：测试网部署（Day 8-10）

### Step 7: 准备部署脚本

**部署顺序**:
1. RWAToken.sol
2. StRWA.sol
3. StakingContract.sol（需要 RWAToken 和 StRWA 地址）
4. SwapContract.sol（需要 RWAToken 和 StRWA 地址）

**部署脚本**: `scripts/deploy-all.ts`

---

### Step 8: 部署到 BSC Testnet

**步骤**:
1. 配置 `.env` 文件（测试网 RPC、私钥等）
2. 执行部署脚本
3. 验证合约部署
4. 初始化合约参数
5. 更新前端合约地址配置

---

### Step 9: 测试网测试

**测试内容**:
- ✅ 质押功能
- ✅ stRWA 铸造
- ✅ 提现功能
- ✅ 双币互换
- ✅ 奖励发放
- ✅ 前端集成测试

---

## 🚀 第4阶段：主网部署（Day 11-12）

### Step 10: 主网部署准备

**准备事项**:
- [ ] 合约审计（可选，但建议）
- [ ] 准备主网账户和 BNB
- [ ] 准备 USDT 代币
- [ ] 配置主网环境变量
- [ ] 准备初始流动性

---

### Step 11: 部署到 BSC Mainnet

**步骤**:
1. 执行部署脚本
2. 验证合约部署
3. 初始化合约参数
4. 添加初始流动性
5. 验证功能

---

### Step 12: 上线后监控

**监控内容**:
- 合约调用监控
- 资金池健康度
- 用户行为分析
- 异常交易告警

---

## 📝 详细实施步骤

### 立即开始的任务（今天）

#### 任务1: 创建 SwapContract.sol

**文件路径**: `contracts/SwapContract.sol`

**功能要求**:
- stRWA → RWA 互换
- RWA → stRWA 互换
- 固定 1:1 比例（简化版）
- 池子初始化
- 池子余额查询

---

#### 任务2: 更新 StakingContract.sol

**需要修改的地方**:

1. **添加 stRWA 引用**:
```solidity
IERC20 public stRwaToken;
```

2. **修改构造函数**:
```solidity
constructor(
    address _usdtToken,
    address _rwaToken,
    address _stRwaToken,  // 新增
    address _treasuryAddress,
    address _backendAddress
) {
    // ... 现有代码 ...
    stRwaToken = IERC20(_stRwaToken);
}
```

3. **修改 stake() 函数**:
```solidity
function stake(uint256 amount, address referrer) external {
    // ... 现有逻辑 ...
    
    // 50% 转换为 stRWA 资产凭证
    uint256 stRwaAmount = internalAmount * 50 / 100;
    IERC20(stRwaToken).mint(msg.sender, stRwaAmount);
    
    // 50% 保留在合约
    uint256 contractAmount = internalAmount - stRwaAmount;
    
    // ... 其余逻辑 ...
}
```

4. **修改 withdraw() 函数**:
```solidity
function withdraw(uint256 amount, bool chooseStRWA) external {
    // ... 现有逻辑 ...
    
    if (chooseStRWA) {
        // 持RWA模式：120%收益，转换为stRWA
        uint256 stRwaAmount = amountAfterFee * 120 / 100;
        IERC20(stRwaToken).mint(msg.sender, stRwaAmount);
    } else {
        // 提U模式：70%提取，30%销毁
        uint256 receiveAmount = amountAfterFee * 70 / 100;
        uint256 burnAmount = amountAfterFee - receiveAmount;
        
        rwaToken.safeTransfer(msg.sender, receiveAmount);
        
        if (burnAmount > 0) {
            rwaToken.safeTransfer(address(0x000000000000000000000000000000000000dEaD), burnAmount);
        }
    }
}
```

---

#### 任务3: 更新 RewardEngine.ts

**需要修改的地方**:

在 `calculateDifferentialRewards()` 函数中添加烧伤机制：

```typescript
// 获取上级信息
const ancestorInfo = await this.getUserInfo(ancestor.ancestor_address);
const ancestorStaked = new BigNumber(ancestorInfo.total_staked);

// 计算基础奖励
const calculatedReward = stakeAmountBN
    .multipliedBy(differentialPercentage)
    .integerValue(BigNumber.ROUND_DOWN);

// 烧伤机制：单次奖励 ≤ 自身质押 × 50%
const maxRewardByStake = ancestorStaked.multipliedBy(0.5);
const actualReward = BigNumber.min(calculatedReward, maxRewardByStake);

// 检查是否被烧伤
const isBurned = calculatedReward.isGreaterThan(maxRewardByStake);

if (actualReward.gt(0)) {
    rewards.push({
        beneficiary: ancestor.ancestor_address,
        amount: actualReward.toString(),
        percentage: differentialPercentage,
        nodeLevel: ancestorLevel,
        fromUser: userAddress,
        stakeId: parseInt(stakeId),
        burned: isBurned
    });
}
```

---

## 🔧 技术细节

### 合约部署顺序

```
1. RWAToken.sol
   ↓
2. StRWA.sol
   ↓
3. StakingContract.sol (需要 RWAToken + StRWA 地址)
   ↓
4. SwapContract.sol (需要 RWAToken + StRWA 地址)
```

### 合约初始化

**StRWA.sol**:
```solidity
// 设置 StakingContract 地址
stRWA.setStakingContract(stakingContractAddress);
```

**SwapContract.sol**:
```solidity
// 初始化池子（需要提供初始流动性）
swapContract.initializePool(
    initialRwaAmount,  // 例如：10000 * 10^18
    initialStRwaAmount // 例如：10000 * 10^18
);
```

**StakingContract.sol**:
```solidity
// 已经在构造函数中初始化
// 无需额外初始化
```

---

## ⚠️ 重要注意事项

### 1. 安全第一

- ✅ 所有合约必须经过测试
- ✅ 建议进行安全审计（至少是代码审查）
- ✅ 使用多签钱包管理 Owner 权限
- ✅ 设置合理的初始参数

### 2. 资金准备

**测试网**:
- BNB: 约 0.1 BNB
- USDT: 使用测试代币

**主网**:
- BNB: 约 0.5-1 BNB（部署费用）
- USDT: 根据流动性需求准备
- 初始流动性池：建议至少 10,000 USDT

### 3. 参数配置

**关键参数**:
- 日收益率：0.8%（292%年化）
- 提现手续费：5%
- 冷却期：24小时
- 最低质押：100 USDT
- 烧伤机制：单次≤50%

### 4. 监控和告警

- 设置资金池健康度监控
- 设置异常交易告警
- 设置合约调用失败告警

---

## 📊 检查清单

### 合约开发检查清单

- [ ] StRWA.sol 创建完成
- [ ] SwapContract.sol 创建完成
- [ ] StakingContract.sol 更新完成
- [ ] 所有合约测试通过
- [ ] 集成测试通过

### 部署检查清单

- [ ] 部署脚本准备完成
- [ ] 测试网部署成功
- [ ] 测试网测试通过
- [ ] 主网部署成功
- [ ] 主网功能验证通过

### 上线后检查清单

- [ ] 监控系统运行正常
- [ ] 用户反馈收集
- [ ] 性能优化
- [ ] 持续迭代

---

## 🎯 下一步行动

### 立即执行（按顺序）

1. **创建 SwapContract.sol** ← 现在开始
2. **更新 StakingContract.sol**
3. **更新 RewardEngine.ts**
4. **编写测试**
5. **测试网部署**

---

## 📞 需要帮助？

如果在实施过程中遇到问题：
1. 检查合约代码是否有语法错误
2. 检查测试是否通过
3. 检查部署脚本是否正确
4. 查看错误日志

---

**按照这个详细步骤执行，可以在 1-2 周内完成 MVP 版本的上线！**

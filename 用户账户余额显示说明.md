# 用户账户余额显示说明

## ❓ 核心问题

**用户质押 10,000 USDT 后，账户显示的余额是多少？**

---

## 💰 余额定义

### 余额应该显示什么？

**有三种理解方式**：

1. **钱包余额**：用户钱包里的 USDT 余额
2. **可提取余额**：合约中可以提取的资金
3. **总资产价值**：所有资产的总价值

---

## 📊 不同场景的余额显示

### 场景1：质押前

```
用户钱包：
├─ USDT余额：10,000 USDT
└─ RWA余额：0 RWA

账户显示：
┌─────────────────────────────┐
│  账户余额：10,000 USDT      │
│  RWA余额：0 RWA             │
└─────────────────────────────┘
```

---

### 场景2：质押后（锁仓1天）

```
用户钱包：
├─ USDT余额：0 USDT（已质押）
└─ RWA余额：0 RWA

合约中：
├─ 可提取本金：5,000 USDT（锁定中）
└─ 投资锁定：5,000 USDT（永久锁定）

账户显示：
┌─────────────────────────────┐
│  账户余额：0 USDT           │
│  （钱包余额）                │
│                              │
│  质押资产：10,000 USDT      │
│  ├─ 可提取本金：5,000 USDT  │
│  │   └─ 状态：锁定中        │
│  └─ 投资锁定：5,000 USDT    │
│      └─ 永久锁定            │
│                              │
│  RWA余额：0 RWA             │
└─────────────────────────────┘
```

---

### 场景3：锁仓期结束后（第2天）

```
用户钱包：
├─ USDT余额：0 USDT
└─ RWA余额：80 RWA（累计收益）

合约中：
├─ 可提取本金：5,000 USDT（已解锁）
└─ 投资锁定：5,000 USDT（永久锁定）

账户显示：
┌─────────────────────────────┐
│  账户余额：0 USDT           │
│  （钱包余额）                │
│                              │
│  质押资产：10,000 USDT      │
│  ├─ 可提取本金：5,000 USDT  │
│  │   └─ 状态：可提取 ✅     │
│  └─ 投资锁定：5,000 USDT    │
│      └─ 永久锁定            │
│                              │
│  RWA余额：80 RWA            │
│  （钱包余额）                │
└─────────────────────────────┘
```

---

### 场景4：提取本金后

```
用户钱包：
├─ USDT余额：5,000 USDT（已提取）
└─ RWA余额：80 RWA

合约中：
├─ 可提取本金：0 USDT（已提取）
└─ 投资锁定：5,000 USDT（永久锁定）

账户显示：
┌─────────────────────────────┐
│  账户余额：5,000 USDT       │
│  （钱包余额）                │
│                              │
│  质押资产：5,000 USDT       │
│  └─ 投资锁定：5,000 USDT    │
│      └─ 永久锁定            │
│                              │
│  RWA余额：80 RWA            │
│  （钱包余额）                │
└─────────────────────────────┘
```

---

## 🎯 推荐显示方案

### 方案一：分区域显示（推荐）

```
┌─────────────────────────────┐
│  我的账户                    │
├─────────────────────────────┤
│                              │
│  💰 钱包余额                 │
│  ├─ USDT：0 USDT            │
│  └─ RWA：80 RWA              │
│                              │
│  📦 质押资产                 │
│  ├─ 可提取本金：5,000 USDT   │
│  │   └─ [提取] 按钮          │
│  └─ 投资锁定：5,000 USDT     │
│      └─ 永久锁定             │
│                              │
│  💎 总资产价值               │
│  └─ 约 5,080 USDT           │
│     （5,000 USDT + 80 RWA） │
└─────────────────────────────┘
```

---

### 方案二：统一余额显示

```
┌─────────────────────────────┐
│  账户总余额                  │
│  5,080 USDT                 │
│  （可提取 5,000 + 收益 80）  │
├─────────────────────────────┤
│                              │
│  明细：                      │
│  ├─ 钱包 USDT：0 USDT       │
│  ├─ 钱包 RWA：80 RWA         │
│  ├─ 可提取本金：5,000 USDT   │
│  └─ 投资锁定：5,000 USDT     │
│      └─ 永久锁定             │
└─────────────────────────────┘
```

---

### 方案三：简洁版（移动端）

```
┌─────────────────────┐
│  账户余额            │
│  5,080 USDT         │
└─────────────────────┘

┌─────────────────────┐
│  💰 可提取           │
│  5,000 USDT         │
│  [提取]             │
└─────────────────────┘

┌─────────────────────┐
│  💎 收益             │
│  80 RWA             │
│  [提取]             │
└─────────────────────┘

┌─────────────────────┐
│  🔒 投资锁定         │
│  5,000 USDT         │
│  （永久锁定）        │
└─────────────────────┘
```

---

## 📋 余额计算逻辑

### 前端计算逻辑

```typescript
// 1. 钱包余额（直接查询钱包）
const walletBalance = {
  usdt: await getUSDTBalance(userAddress),  // 0 USDT
  rwa: await getRWABalance(userAddress),    // 80 RWA
};

// 2. 质押资产（查询合约）
const stakeInfo = await stakingContract.getUserStakeInfo(userAddress);
const stakeBalance = {
  withdrawable: stakeInfo.principalAmount,  // 5,000 USDT（可提取）
  locked: stakeInfo.treasuryAmount,          // 5,000 USDT（永久锁定）
};

// 3. 收益余额（查询合约）
const rewards = await stakingContract.getUserRewards(userAddress);
const rewardBalance = {
  rwa: rewards.rwaPending,                  // 80 RWA
  usdt: rewards.usdtRewards,                // 0 USDT
};

// 4. 总资产价值（计算）
const totalValue = {
  usdt: walletBalance.usdt + stakeBalance.withdrawable,  // 0 + 5,000 = 5,000
  rwa: walletBalance.rwa + rewardBalance.rwa,            // 80 + 0 = 80
  total: (walletBalance.usdt + stakeBalance.withdrawable) + 
         (walletBalance.rwa + rewardBalance.rwa) * rwaPrice,  // 需要RWA价格
};
```

---

## ✅ 最终答案

### 用户账户显示的余额

**推荐显示方式**：

```
┌─────────────────────────────┐
│  账户余额：5,080 USDT       │
│  （可提取 5,000 + 收益 80）  │
├─────────────────────────────┤
│                              │
│  明细：                      │
│  ├─ 钱包 USDT：0 USDT       │
│  ├─ 钱包 RWA：80 RWA         │
│  ├─ 可提取本金：5,000 USDT   │
│  │   └─ [提取] 按钮          │
│  └─ 投资锁定：5,000 USDT     │
│      └─ 永久锁定             │
└─────────────────────────────┘
```

---

### 余额组成

**锁仓1天后，用户账户余额 = 5,080 USDT**

**组成**：
- ✅ **可提取本金**：5,000 USDT（合约中，已解锁）
- ✅ **RWA收益**：80 RWA（约等于 80 USDT，假设1:1汇率）
- ❌ **投资锁定**：5,000 USDT（不计算在可提取余额中）

---

### 关键点

1. **钱包余额**：0 USDT（已质押）
2. **可提取余额**：5,000 USDT（合约中，已解锁）
3. **收益余额**：80 RWA（钱包中）
4. **总资产价值**：约 5,080 USDT（可提取部分）

---

**这样显示，用户能清楚看到：**
- ✅ 钱包里有多少钱（0 USDT）
- ✅ 可以提取多少钱（5,000 USDT）
- ✅ 有多少收益（80 RWA）
- ✅ 总资产价值（5,080 USDT）

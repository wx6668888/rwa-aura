# 宝塔服务器部署指南

## 📋 部署前准备

### 1. 服务器要求
- Node.js 18+ 
- PM2 (进程管理)
- Nginx (反向代理)
- MySQL 8.0+ (后端数据库)

### 2. 域名准备
假设你的域名是: `yourdomain.com`
- 前端: `yourdomain.com` 或 `app.yourdomain.com`
- 后端API: `api.yourdomain.com`

---

## 🚀 第一步: 在宝塔面板安装必要软件

### 1.1 安装 Node.js
1. 登录宝塔面板
2. 软件商店 → 搜索 "Node版本管理器"
3. 安装并设置 Node.js 18.x 或更高版本

### 1.2 安装 PM2
```bash
npm install -g pm2
```

### 1.3 安装 MySQL
1. 软件商店 → MySQL 8.0
2. 安装后创建数据库: `rwa_protocol`

### 1.4 安装 Nginx
软件商店 → Nginx (通常已预装)

---

## 📦 第二步: 上传项目文件

### 2.1 创建项目目录
在宝塔文件管理器中:
```
/www/wwwroot/rwa-protocol/
├── frontend/
└── backend/
```

### 2.2 上传方式 (选择一种)

**方式1: 使用宝塔文件上传**
1. 在本地打包项目: `zip -r project.zip frontend backend package.json`
2. 宝塔面板 → 文件 → 上传
3. 解压到 `/www/wwwroot/rwa-protocol/`

**方式2: 使用 Git (推荐)**
```bash
cd /www/wwwroot/rwa-protocol/
git clone <你的仓库地址> .
```

---

## 🔧 第三步: 配置环境变量

### 3.1 前端环境变量
创建 `frontend/.env.production`:
```env
# API地址 (使用你的域名)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# 区块链网络 (BSC测试网)
NEXT_PUBLIC_CHAIN_ID=97
NEXT_PUBLIC_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# 合约地址 (部署后填写)
NEXT_PUBLIC_STAKING_CONTRACT=0x...
NEXT_PUBLIC_RWA_TOKEN=0x...
NEXT_PUBLIC_USDT_TOKEN=0x...
```

### 3.2 后端环境变量
创建 `backend/.env`:
```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=rwa_protocol

# API配置
PORT=3002
NODE_ENV=production

# 区块链配置
RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545
CHAIN_ID=97

# 合约地址
STAKING_CONTRACT=0x...
RWA_TOKEN=0x...
```

---

## 🏗️ 第四步: 构建项目

### 4.1 安装依赖
在宝塔终端或SSH中:

```bash
cd /www/wwwroot/rwa-protocol

# 安装前端依赖
cd frontend
npm install --production
npm run build

# 安装后端依赖
cd ../backend
npm install --production
```

---

## 🚀 第五步: 使用 PM2 启动服务

### 5.1 创建 PM2 配置文件
在项目根目录创建 `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [
    {
      name: 'rwa-frontend',
      cwd: '/www/wwwroot/rwa-protocol/frontend',
      script: 'node_modules/next/dist/bin/next',
      args: 'start -p 3001',
      env: {
        NODE_ENV: 'production',
        PORT: 3001
      },
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: '1G'
    },
    {
      name: 'rwa-backend',
      cwd: '/www/wwwroot/rwa-protocol/backend',
      script: 'src/index.ts',
      interpreter: 'node',
      env: {
        NODE_ENV: 'production',
        PORT: 3002
      },
      instances: 1,
      autorestart: true,
      watch: false,
      max_memory_restart: '500M'
    }
  ]
};
```

### 5.2 启动服务
```bash
cd /www/wwwroot/rwa-protocol
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

### 5.3 查看服务状态
```bash
pm2 status
pm2 logs rwa-frontend
pm2 logs rwa-backend
```

---

## 🌐 第六步: 配置 Nginx 反向代理

### 6.1 前端站点配置
在宝塔面板:
1. 网站 → 添加站点
2. 域名: `yourdomain.com` 或 `app.yourdomain.com`
3. 点击站点设置 → 配置文件

添加以下配置:
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    # SSL配置 (如果有证书)
    # listen 443 ssl http2;
    # ssl_certificate /path/to/cert.pem;
    # ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
    
    # Next.js 静态资源
    location /_next/static {
        proxy_pass http://127.0.0.1:3001;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, immutable";
    }
}
```

### 6.2 后端API配置
1. 网站 → 添加站点
2. 域名: `api.yourdomain.com`
3. 配置文件:

```nginx
server {
    listen 80;
    server_name api.yourdomain.com;
    
    # SSL配置
    # listen 443 ssl http2;
    # ssl_certificate /path/to/cert.pem;
    # ssl_certificate_key /path/to/key.pem;
    
    location / {
        proxy_pass http://127.0.0.1:3002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # CORS配置
        add_header Access-Control-Allow-Origin *;
        add_header Access-Control-Allow-Methods 'GET, POST, OPTIONS';
        add_header Access-Control-Allow-Headers 'DNT,X-Mx-ReqToken,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization';
    }
}
```

### 6.3 重启 Nginx
```bash
nginx -t
nginx -s reload
```

---

## 🔒 第七步: 配置 SSL 证书 (推荐)

### 在宝塔面板:
1. 网站 → 选择站点 → SSL
2. Let's Encrypt (免费) 或上传自己的证书
3. 申请证书并部署
4. 开启 "强制HTTPS"

---

## 📱 第八步: 手机测试

### 8.1 DNS解析
确保域名已解析到服务器IP:
- A记录: `yourdomain.com` → 服务器IP
- A记录: `api.yourdomain.com` → 服务器IP

### 8.2 防火墙设置
在宝塔安全中放行端口:
- 80 (HTTP)
- 443 (HTTPS)
- 3001 (前端,可选)
- 3002 (后端,可选)

### 8.3 手机访问
1. 打开手机浏览器
2. 访问: `https://yourdomain.com`
3. 安装 MetaMask 移动应用
4. 连接钱包测试功能

---

## 🔍 第九步: 数据库初始化

### 9.1 导入数据库结构
```bash
cd /www/wwwroot/rwa-protocol/backend
mysql -u root -p rwa_protocol < src/config/database.sql
```

### 9.2 运行迁移脚本
```bash
cd backend
npm run migrate
```

---

## 📊 监控和维护

### 查看日志
```bash
# PM2日志
pm2 logs rwa-frontend --lines 100
pm2 logs rwa-backend --lines 100

# Nginx日志
tail -f /www/wwwlogs/yourdomain.com.log
tail -f /www/wwwlogs/api.yourdomain.com.log
```

### 重启服务
```bash
pm2 restart rwa-frontend
pm2 restart rwa-backend
pm2 restart all
```

### 更新代码
```bash
cd /www/wwwroot/rwa-protocol
git pull
cd frontend && npm run build
pm2 restart all
```

---

## ⚠️ 常见问题

### 1. 端口被占用
```bash
# 查看端口占用
netstat -tunlp | grep 3001
# 杀死进程
kill -9 <PID>
```

### 2. 权限问题
```bash
# 设置正确的权限
chown -R www:www /www/wwwroot/rwa-protocol
chmod -R 755 /www/wwwroot/rwa-protocol
```

### 3. Node内存不足
在 `ecosystem.config.js` 中增加内存限制:
```javascript
max_memory_restart: '2G'
```

### 4. 502 Bad Gateway
- 检查PM2服务是否运行: `pm2 status`
- 检查端口是否正确
- 查看应用日志: `pm2 logs`

---

## 🎯 快速部署脚本

创建 `deploy.sh`:
```bash
#!/bin/bash

echo "🚀 开始部署..."

# 拉取最新代码
git pull

# 构建前端
cd frontend
npm install
npm run build

# 重启服务
cd ..
pm2 restart all

echo "✅ 部署完成!"
```

使用:
```bash
chmod +x deploy.sh
./deploy.sh
```

---

## 📞 技术支持

如果遇到问题:
1. 查看PM2日志: `pm2 logs`
2. 查看Nginx日志: `/www/wwwlogs/`
3. 检查防火墙设置
4. 确认域名解析正确

---

## ✅ 部署检查清单

- [ ] Node.js 已安装 (v18+)
- [ ] PM2 已安装
- [ ] MySQL 数据库已创建
- [ ] 项目文件已上传
- [ ] 环境变量已配置
- [ ] 依赖已安装
- [ ] 前端已构建
- [ ] PM2 服务已启动
- [ ] Nginx 配置已添加
- [ ] 域名已解析
- [ ] SSL 证书已配置
- [ ] 防火墙端口已开放
- [ ] 数据库已初始化
- [ ] 手机可以访问

完成以上步骤后,你就可以在手机上测试应用了! 🎉

# 🔧 问题修复总结 - 2026年3月2日

## 📋 修复的三个问题

### ✅ 问题 1：stRWA 解锁功能错误

**错误信息：**
```
The contract function "unlock" reverted with the following reason: 
Error: Transaction reverted: function selector was not recognized and there's no fallback function
```

**根本原因：**
- `StRWA.sol` 合约中没有 `unlock` 函数
- 合约只有 `mint` 和 `burn` 函数，且只能由 `stakingContract` 调用
- 前端错误地尝试直接调用 `StRWA` 合约的 `unlock` 函数

**修复方案：**
1. ✅ 移除了对不存在的 `unlock` 和 `getUnlockStatus` 函数的调用
2. ✅ 改为使用 `SwapContract` 的 `swapStRWAToRWA` 函数来解锁 stRWA
3. ✅ 在 `useStRWA.ts` 中添加了 `approveStRWA` 和 `approveStRWAMax` 函数
4. ✅ 在 `st-rwa-unlock-card.tsx` 中添加了自动授权逻辑（如果未授权，会先授权再解锁）

**修改的文件：**
- `frontend/hooks/useStRWA.ts` - 添加授权功能，移除 unlock 相关函数
- `frontend/components/withdraw/st-rwa-unlock-card.tsx` - 使用 SwapContract 解锁，添加自动授权

---

### ✅ 问题 2：USDT ↔ RWA 兑换功能无法使用

**问题描述：**
- `swap-button.tsx` 只是模拟，没有真正调用合约
- 点击兑换按钮后没有实际交易发生

**根本原因：**
- `swap-button.tsx` 中的 `handleApprove` 和 `handleSwap` 只是 `setTimeout` 模拟
- 没有使用真实的 `useSwap` hook 或 PancakeSwap Router

**修复方案：**
1. ✅ 更新 `swap-button.tsx` 以使用真实的 `useSwap` hook（通过 PancakeSwap Router）
2. ✅ 添加了真实的授权检查（`checkAllowance`）
3. ✅ 添加了真实的授权功能（`approveToken`）
4. ✅ 添加了真实的兑换功能（`executeSwap`）
5. ✅ 添加了错误处理和状态管理

**修改的文件：**
- `frontend/components/swap/swap-button.tsx` - 完全重写，使用真实的合约调用

**注意：**
- USDT ↔ RWA 兑换通过 PancakeSwap Router 实现
- 需要先授权 USDT 或 RWA 给 PancakeSwap Router
- 兑换需要 PancakeSwap 上有对应的流动性池

---

### ✅ 问题 3：彩票购买功能无法使用

**问题描述：**
- 购买彩票页面无法购买
- 点击购买按钮后没有反应

**根本原因：**
- `useLottery.ts` 中使用了硬编码的 `LOTTERY_CONTRACT_ADDRESS`
- 该地址在 `.env` 中未配置，默认为空字符串 `""`
- 在 `addresses.ts` 中，`lotteryContract` 地址为 `0x0000000000000000000000000000000000000000`（未部署）

**修复方案：**
1. ✅ 更新 `useLottery.ts` 以从 `CONTRACT_ADDRESSES` 动态获取彩票合约地址
2. ✅ 添加了合约部署检查（`isContractDeployed`）
3. ✅ 在所有需要合约地址的函数中添加了检查，如果合约未部署会显示友好的错误消息
4. ✅ 添加了 `isContractDeployed` 和 `lotteryContractAddress` 到返回值，供前端使用

**修改的文件：**
- `frontend/hooks/useLottery.ts` - 动态获取合约地址，添加部署检查

**注意：**
- 如果 `lotteryContract` 地址为 `0x0000000000000000000000000000000000000000`，功能会被禁用
- 需要先部署 `LotteryContract` 并更新 `addresses.ts` 中的地址

---

## 🔍 额外发现的问题

### ⚠️ stRWA ↔ RWA 兑换需要授权

**问题：**
- `st-rwa-swap-card.tsx` 中的兑换功能可能失败，因为需要先授权 stRWA 给 SwapContract

**建议：**
- 在 `st-rwa-swap-card.tsx` 中添加授权检查和自动授权功能（类似 `st-rwa-unlock-card.tsx`）

---

## 📝 测试建议

### 1. stRWA 解锁测试
1. 确保钱包中有 stRWA 余额
2. 访问 `/withdraw` 页面
3. 在 "Unlock stRWA Asset Certificates" 部分输入解锁数量
4. 点击 "解锁 stRWA" 按钮
5. **预期行为：**
   - 如果未授权，会先自动授权 stRWA 给 SwapContract
   - 然后执行 `swapStRWAToRWA` 交易
   - 交易成功后，stRWA 余额减少，RWA 余额增加

### 2. USDT ↔ RWA 兑换测试
1. 确保钱包中有 USDT 或 RWA 余额
2. 访问 `/swap` 页面
3. 输入兑换金额
4. 点击 "授权" 按钮（如果需要）
5. 点击 "兑换" 按钮
6. **预期行为：**
   - 如果未授权，会先显示授权按钮
   - 授权成功后，显示兑换按钮
   - 点击兑换后，执行真实的 PancakeSwap 交易
   - **注意：** 需要 PancakeSwap 上有对应的流动性池

### 3. 彩票购买测试
1. 访问彩票页面
2. 选择周奖或月奖
3. 输入购买数量
4. 点击购买按钮
5. **预期行为：**
   - 如果合约未部署，会显示错误消息："彩票合约未部署，请等待合约部署完成"
   - 如果合约已部署，会执行购买交易

---

## 🚀 下一步行动

### 立即需要做的：
1. ✅ **部署 LotteryContract**（如果还没有）
   - 运行 `npx hardhat run scripts/deploy-all.ts`
   - 更新 `frontend/lib/contracts/addresses.ts` 中的 `lotteryContract` 地址

2. ✅ **测试 stRWA 解锁功能**
   - 确保 SwapContract 池子有足够的 RWA 余额
   - 测试授权和兑换流程

3. ✅ **测试 USDT ↔ RWA 兑换**
   - 确保 PancakeSwap 上有对应的流动性池
   - 或者考虑使用 SwapContract 实现 USDT ↔ RWA 兑换（而不是 PancakeSwap）

4. ✅ **添加 stRWA ↔ RWA 兑换的授权检查**
   - 在 `st-rwa-swap-card.tsx` 中添加了完整的授权检查功能
   - **stRWA → RWA 方向：** 添加了 stRWA 对 SwapContract 的授权检查和自动授权
   - **RWA → stRWA 方向：** 添加了 RWA 对 SwapContract 的授权检查和自动授权
   - 使用 `useReadContract` 实时读取 RWA 授权额度
   - 按钮会根据授权状态动态显示"授权"或"兑换"
   - 切换兑换方向时自动刷新授权状态

---

## 📚 相关文件

### 修改的文件：
- `frontend/hooks/useStRWA.ts`
- `frontend/components/withdraw/st-rwa-unlock-card.tsx`
- `frontend/components/swap/swap-button.tsx`
- `frontend/hooks/useLottery.ts`

### 需要检查的文件：
- `frontend/lib/contracts/addresses.ts` - 确保所有合约地址正确

### 已完成的额外修复：
- ✅ `frontend/components/swap/st-rwa-swap-card.tsx` - 已添加完整的双向授权检查功能

---

**修复完成时间：** 2026年3月2日  
**状态：** ✅ 所有三个问题已修复，额外完成 stRWA ↔ RWA 双向授权检查功能

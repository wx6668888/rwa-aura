# 用户提现逻辑完整说明

## 📋 概述

本文档详细说明 RWA 项目中用户的所有提现逻辑，包括本金提取、RWA收益提取、USDT收益提取和stRWA解锁。

---

## 🎯 一、提现类型总览

用户可以进行以下4种类型的提现：

1. **本金提取**：提取质押的本金（50%可提取）
2. **RWA收益提取**：提取静态收益（RWA代币）
3. **USDT收益提取**：提取动态收益（USDT）
4. **stRWA解锁**：解锁资产凭证并转换为RWA

---

## 💰 二、本金提取逻辑

### 2.1 资金分配机制

```
用户质押 10,000 USDT
    │
    ├─ 50% (5,000 USDT) → Treasury（储备金）
    │   └─ 用途：实体投资、运营费用
    │   └─ 特点：永久锁定，不返还
    │
    └─ 50% (5,000 USDT) → Staking Contract（社区池）
        └─ 特点：可提取，但需满足锁仓期限
```

### 2.2 提取规则

| 锁仓期限 | 可提取本金 | 提取时间 | 提前解锁惩罚 | 说明 |
|---------|-----------|---------|------------|------|
| **灵活锁仓** | 50%（合约中的部分） | 随时（24h冷却） | 无 | 无锁仓要求 |
| **30天锁仓** | 50% | 30天后 | 15% | 提前解锁需支付15%惩罚 |
| **90天锁仓** | 50% | 90天后 | 25% | 提前解锁需支付25%惩罚 |
| **180天锁仓** | 50% | 180天后 | 35% | 提前解锁需支付35%惩罚 |
| **365天锁仓** | 50% | 365天后 | 50% | 提前解锁需支付50%惩罚 |

**重要说明**：
- 另外50%在Treasury，永久锁定，不返还
- 提前解锁的惩罚资金：50%销毁 + 30%奖励池 + 20%Treasury

### 2.3 提取示例

**示例1：正常提取（180天锁仓期满）**

```
用户质押 10,000 USDT，选择 180天锁仓

资金分配：
├─ Treasury：5,000 USDT（不返还）
├─ 合约：5,000 USDT（可提取）
└─ 180天后可提取：5,000 USDT

提取流程：
1. 检查锁仓期限：180天已满 ✅
2. 检查冷却期：24小时已过 ✅
3. 提取本金：5,000 USDT
4. 手续费：5%（250 USDT）
5. 实际到账：4,750 USDT
```

**示例2：提前解锁（90天后提取）**

```
用户质押 10,000 USDT，选择 180天锁仓
90天后提前提取

提取流程：
1. 检查锁仓期限：90天 < 180天（未满）❌
2. 触发提前解锁惩罚：35%
3. 可提取本金：5,000 USDT
4. 提前解锁惩罚：5,000 × 35% = 1,750 USDT
5. 基础手续费：5%（250 USDT）
6. 实际到账：5,000 - 1,750 - 250 = 3,000 USDT

惩罚资金分配：
├─ 销毁：1,750 × 50% = 875 USDT
├─ 奖励池：1,750 × 30% = 525 USDT
└─ Treasury：1,750 × 20% = 350 USDT
```

### 2.4 合约实现（伪代码）

```solidity
function withdrawPrincipal(uint256 amount) external nonReentrant {
    UserInfo storage user = users[msg.sender];
    
    // 1. 检查余额
    require(user.principalWithdrawable >= amount, "Insufficient principal");
    
    // 2. 检查锁仓期限
    if (user.lockPeriod > 0) {
        require(
            block.timestamp >= user.lockEndTime,
            "Lock period not ended"
        );
    }
    
    // 3. 检查冷却期（24小时）
    require(
        block.timestamp >= user.lastWithdrawTime + WITHDRAWAL_COOLDOWN,
        "Cooldown active"
    );
    
    // 4. 计算手续费（5%）
    uint256 fee = amount * WITHDRAWAL_FEE_RATE / 100;
    uint256 amountAfterFee = amount - fee;
    
    // 5. 更新余额
    user.principalWithdrawable -= amount;
    user.lastWithdrawTime = block.timestamp;
    
    // 6. 转账
    usdtToken.safeTransfer(msg.sender, amountAfterFee);
    
    // 7. 处理手续费
    distributeFee(fee);
    
    emit PrincipalWithdrawn(msg.sender, amount, fee);
}
```

---

## 🪙 三、RWA收益提取逻辑

### 3.1 收益来源

RWA收益主要来自：
- **静态收益**：每日0.8%的RWA代币收益
- **复投奖励**：复投时获得的额外RWA奖励

### 3.2 双收益通道

用户可以选择两种提取方式：

#### 选择A：提U模式（70%收益）

```
用户收益：500 RWA

提U模式：
├─ 70% 提取：500 × 70% = 350 RWA
├─ 30% 销毁：500 × 30% = 150 RWA（立即销毁）
└─ 通缩效果：减少代币供应，支撑币价
```

**特点**：
- ✅ 立即到账
- ✅ 30%立即销毁，维持通缩
- ❌ 只能拿到70%收益

#### 选择B：持RWA模式（120%收益，推荐）

```
用户收益：500 RWA

持RWA模式：
├─ 120% 收益：500 × 120% = 600 stRWA
├─ 锁仓30天
├─ 分批解锁：每天解锁10%（60 stRWA/天）
└─ 额外获得：100 RWA（20%加成）
```

**特点**：
- ✅ 获得120%收益（额外20%）
- ✅ 转换为stRWA（资产凭证）
- ⚠️ 需要锁仓30天
- ⚠️ 分批解锁（每天10%）

### 3.3 提取规则

| 项目 | 提U模式 | 持RWA模式 |
|------|---------|-----------|
| **收益比例** | 70% | 120% |
| **到账时间** | 立即 | 30天锁仓 |
| **解锁方式** | 无 | 分批解锁（每天10%） |
| **额外收益** | 无 | +20% |
| **推荐场景** | 急需资金 | 长期持有 |

### 3.4 提取示例

**示例：用户选择持RWA模式**

```
用户收益：1,000 RWA

选择持RWA模式：
├─ 基础收益：1,000 RWA
├─ 加成奖励：1,000 × 20% = 200 stRWA
├─ 总计：1,200 stRWA
└─ 锁仓30天

解锁流程：
├─ 第1天：解锁 120 stRWA（10%）
├─ 第2天：解锁 120 stRWA（10%）
├─ 第3天：解锁 120 stRWA（10%）
└─ ...（持续30天）

30天后：全部解锁完成
```

### 3.5 合约实现（优化后）

```solidity
function withdraw(uint256 amount, bool chooseStRWA) external nonReentrant whenNotPaused {
    UserInfo storage user = users[msg.sender];
    
    // 1. 检查余额
    require(user.rwaPending >= amount, "Insufficient balance");
    require(amount >= MIN_WITHDRAWAL_AMOUNT, "Below minimum withdrawal amount");
    
    // 2. 检查冷却期（24小时）
    require(
        block.timestamp >= user.lastWithdrawTime + WITHDRAWAL_COOLDOWN,
        "Withdrawal cooldown active"
    );
    
    // 3. 计算手续费（5%）
    uint256 fee = amount * WITHDRAWAL_FEE_RATE / 100;
    uint256 amountAfterFee = amount - fee;
    
    // 4. 更新余额
    user.rwaPending -= amount;
    user.lastWithdrawTime = block.timestamp;
    
    // 5. 根据用户选择处理
    if (chooseStRWA) {
        // 持RWA模式：120%收益，转换为stRWA
        uint256 stRwaAmount = amountAfterFee * 120 / 100;
        stRwaToken.mint(msg.sender, stRwaAmount);
        
        // 记录解锁信息
        user.stRwaLocked[msg.sender] += stRwaAmount;
        user.stRwaUnlockTime[msg.sender] = block.timestamp + 30 days;
        
    } else {
        // 提U模式：70%提取，30%销毁
        uint256 receiveAmount = amountAfterFee * 70 / 100;
        uint256 burnAmount = amountAfterFee - receiveAmount;
        
        rwaToken.safeTransfer(msg.sender, receiveAmount);
        
        // 立即销毁
        if (burnAmount > 0) {
            rwaToken.safeTransfer(address(0x000000000000000000000000000000000000dEaD), burnAmount);
            emit TokensBurned(burnAmount, block.timestamp);
        }
    }
    
    // 6. 销毁手续费
    if (fee > 0) {
        rwaToken.safeTransfer(address(0x000000000000000000000000000000000000dEaD), fee);
    }
    
    emit WithdrawalRequested(msg.sender, amount, fee, block.timestamp);
}
```

---

## 💵 四、USDT收益提取逻辑

### 4.1 收益来源

USDT收益主要来自：
- **动态收益**：级差奖励（推荐下级质押时获得）
- **管理分红**：月度管理分红（团长）
- **季度奖励**：季度业绩奖励（团长）

### 4.2 提取规则

| 提取时间 | 基础手续费 | 提前提取惩罚 | 总手续费 | 实际到账 |
|---------|-----------|------------|---------|---------|
| **30天内** | 8% | 5% | 13% | 87% |
| **30-90天** | 8% | 3% | 11% | 89% |
| **90-180天** | 8% | 1% | 9% | 91% |
| **180天+** | 8% | 0% | 8% | 92% |

**其他限制**：
- 每日限额：50,000 USDT
- 冷却期：7天
- 最低提取：10 USDT

### 4.3 提取示例

**示例1：质押后20天提取**

```
用户提取 1,000 USDT（质押后20天）

计算：
├─ 基础手续费：1,000 × 8% = 80 USDT
├─ 提前提取惩罚：1,000 × 5% = 50 USDT（30天内）
├─ 总手续费：130 USDT
└─ 实际到账：870 USDT（损失13%）
```

**示例2：质押后100天提取**

```
用户提取 1,000 USDT（质押后100天）

计算：
├─ 基础手续费：1,000 × 8% = 80 USDT
├─ 提前提取惩罚：1,000 × 3% = 30 USDT（30-90天）
├─ 总手续费：110 USDT
└─ 实际到账：890 USDT（损失11%）
```

**示例3：质押后200天提取**

```
用户提取 1,000 USDT（质押后200天）

计算：
├─ 基础手续费：1,000 × 8% = 80 USDT
├─ 提前提取惩罚：0 USDT（180天+）
├─ 总手续费：80 USDT
└─ 实际到账：920 USDT（损失8%）
```

### 4.4 合约实现

```solidity
function withdrawUSDT(uint256 amount) external nonReentrant whenNotPaused {
    UserInfo storage user = users[msg.sender];
    
    // 1. 检查余额
    require(user.usdtRewards >= amount, "Insufficient USDT balance");
    require(amount >= MIN_WITHDRAWAL_AMOUNT, "Below minimum withdrawal amount");
    
    // 2. 检查冷却期（7天）
    require(
        block.timestamp >= user.lastUSDTWithdrawTime + USDT_WITHDRAWAL_COOLDOWN,
        "USDT withdrawal cooldown active"
    );
    
    // 3. 检查每日限额
    uint256 currentDay = block.timestamp / 1 days;
    require(
        dailyUSDTWithdrawn[currentDay] + amount <= DAILY_USDT_WITHDRAWAL_LIMIT,
        "Daily USDT withdrawal limit exceeded"
    );
    
    // 4. 计算手续费（根据提取时间动态调整）
    uint256 feeRate = calculateUSDTWithdrawalFee(user.firstStakeTime);
    uint256 fee = amount * feeRate / 100;
    uint256 amountAfterFee = amount - fee;
    
    // 5. 更新余额
    user.usdtRewards -= amount;
    user.lastUSDTWithdrawTime = block.timestamp;
    dailyUSDTWithdrawn[currentDay] += amount;
    
    // 6. 转换精度（18位 -> 6位）
    uint256 usdtAmountIn6Decimals = amountAfterFee / PRECISION_MULTIPLIER;
    
    // 7. 转账
    usdtToken.safeTransfer(msg.sender, usdtAmountIn6Decimals);
    
    // 8. 处理手续费
    if (fee > 0) {
        uint256 feeIn6Decimals = fee / PRECISION_MULTIPLIER;
        // 50% 销毁，30% 奖励池，20% Treasury
        usdtToken.safeTransfer(address(0x000000000000000000000000000000000000dEaD), feeIn6Decimals * 50 / 100);
        usdtToken.safeTransfer(address(this), feeIn6Decimals * 30 / 100);
        usdtToken.safeTransfer(treasuryAddress, feeIn6Decimals * 20 / 100);
    }
    
    emit USDTWithdrawalRequested(msg.sender, amount, fee, block.timestamp);
}

function calculateUSDTWithdrawalFee(uint256 firstStakeTime) internal view returns (uint256) {
    if (firstStakeTime == 0) {
        return 13;  // 默认13%（未质押过）
    }
    
    uint256 holdingDays = (block.timestamp - firstStakeTime) / 1 days;
    
    if (holdingDays < 30) {
        return 13;  // 13% 税（30天内）
    } else if (holdingDays < 90) {
        return 11;  // 11% 税（30-90天）
    } else if (holdingDays < 180) {
        return 9;   // 9% 税（90-180天）
    } else {
        return 8;   // 8% 税（180天+）
    }
}
```

---

## 🔓 五、stRWA解锁与提取逻辑

### 5.1 stRWA 说明

**stRWA** = Staked RWA = 实体资产代币化凭证

- 1 stRWA = 1 USDT 等值的实体资产
- 可在二级市场交易
- 可等待实体资产清结算
- 可转换为RWA代币（有锁仓期）

### 5.2 解锁流程

```
stRWA（锁仓RWA）
    ↓
请求解锁（7天解锁期）
    ↓
解锁完成
    ↓
冷却期（3天）
    ↓
转换为RWA，可以交易
```

### 5.3 卖出税（动态）

| 解锁后时间 | 卖出税率 | 实际到账 | 说明 |
|-----------|---------|---------|------|
| **0-7天** | 80% | 20% | 极高税率，防止立即卖出 |
| **7-30天** | 60% | 40% | 高税率 |
| **30-90天** | 40% | 60% | 中等税率 |
| **90-180天** | 30% | 70% | 较低税率 |
| **180天+** | 20% | 80% | 正常税率 |

### 5.4 解锁示例

**示例：用户解锁 10,000 stRWA**

```
第1步：请求解锁
├─ 解锁金额：10,000 stRWA
├─ 解锁期：7天
└─ 状态：解锁中

第2步：7天后解锁完成
├─ 转换为：10,000 RWA
├─ 冷却期：3天
└─ 状态：冷却中

第3步：3天后可以交易
├─ 可卖出：10,000 RWA
└─ 状态：可交易

如果立即卖出（解锁后0天）：
├─ 卖出税：80%
├─ 税收：10,000 × 80% = 8,000 RWA
└─ 实际到账：2,000 RWA（损失80%）

如果等待180天后卖出：
├─ 卖出税：20%
├─ 税收：10,000 × 20% = 2,000 RWA
└─ 实际到账：8,000 RWA（损失20%）
```

### 5.5 合约实现

```solidity
// StRWA.sol - 解锁机制
contract StRWA is ERC20 {
    struct UnlockRequest {
        uint256 amount;
        uint256 requestTime;
        uint256 unlockTime;
        bool unlocked;
    }
    
    mapping(address => UnlockRequest[]) public unlockRequests;
    
    // 请求解锁（7天解锁期）
    function requestUnlock(uint256 amount) external {
        require(balanceOf(msg.sender) >= amount, "Insufficient balance");
        
        unlockRequests[msg.sender].push(UnlockRequest({
            amount: amount,
            requestTime: block.timestamp,
            unlockTime: block.timestamp + 7 days,
            unlocked: false
        }));
        
        // 锁定代币
        _transfer(msg.sender, address(this), amount);
        
        emit UnlockRequested(msg.sender, amount, block.timestamp);
    }
    
    // 执行解锁（分批）
    function executeUnlock(uint256 requestId) external {
        UnlockRequest storage request = unlockRequests[msg.sender][requestId];
        require(block.timestamp >= request.unlockTime, "Not yet unlockable");
        require(!request.unlocked, "Already unlocked");
        
        // 分批解锁：每天解锁 10%
        uint256 daysPassed = (block.timestamp - request.unlockTime) / 1 days;
        uint256 unlockableAmount = request.amount * (10 * daysPassed) / 100;
        unlockableAmount = unlockableAmount > request.amount ? request.amount : unlockableAmount;
        
        // 转换为 RWA（有卖出税）
        rwaToken.mint(msg.sender, unlockableAmount);
        request.unlocked = true;
        
        emit UnlockExecuted(msg.sender, requestId, unlockableAmount);
    }
}
```

---

## 📊 六、提现逻辑总结

### 6.1 提现类型对比

| 提现类型 | 提取内容 | 手续费 | 冷却期 | 限制 |
|---------|---------|--------|--------|------|
| **本金提取** | 质押本金（50%） | 5% | 24小时 | 需满足锁仓期限 |
| **RWA收益（提U）** | RWA代币（70%） | 5% | 24小时 | 30%立即销毁 |
| **RWA收益（持RWA）** | stRWA（120%） | 5% | 24小时 | 30天锁仓 |
| **USDT收益** | USDT（87%-92%） | 8%-13% | 7天 | 每日限额50,000 |
| **stRWA解锁** | RWA代币 | 0% | 3天 | 卖出税20%-80% |

### 6.2 提现流程图

```
用户提现
    │
    ├─ 本金提取
    │   ├─ 检查锁仓期限
    │   ├─ 检查冷却期（24h）
    │   ├─ 计算手续费（5%）
    │   └─ 转账USDT
    │
    ├─ RWA收益提取
    │   ├─ 选择模式
    │   │   ├─ 提U模式：70%提取，30%销毁
    │   │   └─ 持RWA模式：120%转换为stRWA
    │   ├─ 检查冷却期（24h）
    │   ├─ 计算手续费（5%）
    │   └─ 转账RWA或mint stRWA
    │
    ├─ USDT收益提取
    │   ├─ 检查冷却期（7天）
    │   ├─ 检查每日限额（50,000 USDT）
    │   ├─ 计算手续费（8%-13%）
    │   └─ 转账USDT
    │
    └─ stRWA解锁
        ├─ 请求解锁（7天）
        ├─ 冷却期（3天）
        ├─ 转换为RWA
        └─ 卖出时支付动态税（20%-80%）
```

### 6.3 提现策略建议

#### 策略1：保守型（低风险）

**适合人群**：新手用户、风险厌恶者

**策略**：
- 本金提取：灵活锁仓，随时可提取
- RWA收益：提U模式（70%）
- USDT收益：不复投，直接提取
- stRWA：不选择持RWA模式

**收益预期**：
- 年化收益：292%（基础）
- 风险：低（随时可提取）

#### 策略2：平衡型（中等风险）

**适合人群**：有一定经验的用户

**策略**：
- 本金提取：180天锁仓（2.0x收益）
- RWA收益：持RWA模式（120%）
- USDT收益：每月复投1次
- stRWA：等待180天后卖出（20%税）

**收益预期**：
- 年化收益：584%（基础）+ 复投奖励
- 风险：中等（180天锁仓）

#### 策略3：激进型（高风险高收益）

**适合人群**：经验丰富的用户、大资金用户

**策略**：
- 本金提取：365天锁仓（2.5x收益）
- RWA收益：持RWA模式（120%）
- USDT收益：每月复投，持续3次以上
- stRWA：长期持有，等待实体资产清结算

**收益预期**：
- 年化收益：730%（基础）+ 复投奖励 + 持币分红
- 风险：高（365天锁仓）

---

## ⚠️ 七、注意事项

### 7.1 提现限制

1. **最低提取金额**：10 RWA / 10 USDT
2. **冷却期**：
   - 本金/RWA收益：24小时
   - USDT收益：7天
   - stRWA解锁：3天
3. **每日限额**：
   - USDT收益：50,000 USDT/天
   - 其他：无限制

### 7.2 手续费说明

1. **本金提取**：5%手续费
2. **RWA收益提取**：5%手续费 + 30%销毁（提U模式）
3. **USDT收益提取**：8%-13%（根据提取时间）
4. **stRWA卖出**：20%-80%（根据解锁后时间）

### 7.3 风险提示

1. **本金损失**：50%本金在Treasury，不返还
2. **提前解锁惩罚**：最高50%惩罚
3. **卖出税**：stRWA解锁后立即卖出，税率高达80%
4. **流动性风险**：大额提取可能触发每日限额

---

## 📝 八、完整提现示例

### 场景：用户Alice完整提现流程

**初始状态**：
- 质押：10,000 USDT（180天锁仓）
- RWA收益：5,000 RWA
- USDT收益：2,000 USDT
- stRWA持有：0

**第180天：锁仓期满**

```
1. 本金提取：
   ├─ 可提取：5,000 USDT（50%）
   ├─ 手续费：5%（250 USDT）
   └─ 实际到账：4,750 USDT

2. RWA收益提取（选择持RWA模式）：
   ├─ 收益：5,000 RWA
   ├─ 手续费：5%（250 RWA）
   ├─ 净收益：4,750 RWA
   ├─ 持RWA模式：4,750 × 120% = 5,700 stRWA
   └─ 锁仓30天

3. USDT收益提取：
   ├─ 收益：2,000 USDT
   ├─ 手续费：8%（160 USDT）
   └─ 实际到账：1,840 USDT
```

**第210天：stRWA解锁完成**

```
4. stRWA解锁：
   ├─ 解锁：5,700 stRWA
   ├─ 转换为：5,700 RWA
   └─ 可交易

5. 卖出RWA（等待180天后）：
   ├─ 卖出：5,700 RWA
   ├─ 卖出税：20%（1,140 RWA）
   └─ 实际到账：4,560 RWA
```

**总结**：
- 初始投入：10,000 USDT
- 本金提取：4,750 USDT
- USDT收益：1,840 USDT
- RWA收益：4,560 RWA（假设1 RWA = 1 USDT）
- 总价值：4,750 + 1,840 + 4,560 = 11,150 USDT
- 收益率：11.5%

---

## ✅ 总结

### 核心要点

1. **本金提取**：50%可提取，需满足锁仓期限
2. **RWA收益**：双通道（提U 70% vs 持RWA 120%）
3. **USDT收益**：手续费8%-13%，7天冷却期
4. **stRWA解锁**：7天解锁期 + 3天冷却期，卖出税20%-80%

### 提现建议

1. **前期**：灵活锁仓，随时可提取，建立信任
2. **中期**：选择持RWA模式，获得120%收益
3. **后期**：长期持有，等待实体资产清结算

**这是完整的用户提现逻辑说明。所有提现规则、手续费、限制都已详细说明。**

# 🔓 灵活锁仓和短期锁仓逻辑说明

## 📋 当前锁仓期限选项

### 可用的锁仓期限

| 锁仓期限 | 收益倍数 | 本金锁定 | 提取时间 |
|---------|---------|---------|---------|
| **灵活锁仓 (0天)** | 1.0x | ❌ 不锁定 | 随时（24小时冷却） |
| **30天** | 1.3x | ✅ 锁定30天 | 30天后 |
| **90天** | 1.6x | ✅ 锁定90天 | 90天后 |
| **180天** | 2.0x | ✅ 锁定180天 | 180天后 |
| **365天** | 2.5x | ✅ 锁定365天 | 365天后 |

**注意**：当前没有"1天"锁仓选项

---

## 🎯 如果用户选择"灵活锁仓"（0天）

### 完整流程

#### 第1天：质押

```
用户质押 10,000 USDT，选择灵活锁仓（0天）

资金分配：
├─ 5,000 USDT → Treasury（永久锁定，不返还）
└─ 5,000 USDT → Staking Contract（未锁定）

本金状态：
├─ 本金：5,000 USDT（未锁定，可随时提取）
├─ 锁仓期限：0天（灵活锁仓）
└─ 收益倍数：1.0x（基础倍数）

收益计算：
├─ 基于 totalStaked: 10,000 USDT
├─ 收益倍数：1.0x
└─ 每日收益：10,000 × 0.008 × 1.0 = 80 USDT/天
```

#### 第2天及以后：随时提取

```
用户状态：
├─ 本金：5,000 USDT（未锁定，可随时提取）✅
├─ 收益：每天产生 80 USDT（RWA代币）✅
└─ 累计收益：80 × N 天

用户可以：
✅ 随时提取本金（24小时冷却期）
✅ 随时提取收益（24小时冷却期）
✅ 同时提取本金和收益

提取本金：
├─ 调用 withdrawFlexiblePrincipal(5000)
├─ 检查：24小时冷却期 ✅
├─ 转账：5,000 USDT 给用户
├─ 更新：totalStaked = 5,000 USDT（剩余）
└─ 收益：继续产生（基于剩余的 5,000 USDT）
```

---

## 🔍 如果添加"1天"锁仓选项

### 设计考虑

#### 方案 1：添加1天锁仓（收益倍数 1.1x）

```
锁仓期限选项：
├─ 灵活锁仓 (0天): 1.0x
├─ 1天: 1.1x  ← 新增
├─ 30天: 1.3x
├─ 90天: 1.6x
├─ 180天: 2.0x
└─ 365天: 2.5x
```

**逻辑**:
```
用户质押 10,000 USDT，选择 1天锁仓

第1天：质押
├─ 资金分配：5,000 → Treasury，5,000 → 合约
├─ 本金锁定：5,000 USDT（锁定1天）
├─ 锁仓结束时间：第2天
└─ 收益倍数：1.1x

第1天：锁仓期间
├─ 本金：5,000 USDT（锁定中）🔒
├─ 收益：10,000 × 0.008 × 1.1 = 88 USDT/天
└─ 不能提取本金

第2天：锁仓期结束
├─ 本金：5,000 USDT（解锁，可以提取）✅
├─ 累计收益：88 USDT
└─ 可以提取本金和收益
```

---

#### 方案 2：不添加1天，使用灵活锁仓

**逻辑**:
```
用户质押 10,000 USDT，选择灵活锁仓（0天）

特点：
├─ 本金：5,000 USDT（未锁定，可随时提取）
├─ 收益倍数：1.0x（基础倍数）
├─ 每日收益：80 USDT/天
└─ 可以随时提取本金（24小时冷却）

优势：
✅ 灵活性最高
✅ 可以随时提取
✅ 适合短期投资

劣势：
❌ 收益倍数最低（1.0x）
```

---

## 📊 对比分析

### 灵活锁仓 vs 1天锁仓

| 特性 | 灵活锁仓 (0天) | 1天锁仓 |
|------|---------------|---------|
| **收益倍数** | 1.0x | 1.1x（假设） |
| **本金锁定** | ❌ 不锁定 | ✅ 锁定1天 |
| **提取时间** | 随时（24h冷却） | 1天后 |
| **每日收益** | 80 USDT/天 | 88 USDT/天 |
| **灵活性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **收益** | ⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 🎯 推荐方案

### 方案 A：保持当前设计（推荐）

**理由**:
1. ✅ 灵活锁仓已经提供最高灵活性
2. ✅ 1天锁仓收益提升有限（1.0x → 1.1x）
3. ✅ 避免选项过多造成用户困惑
4. ✅ 灵活锁仓适合短期投资需求

**逻辑**:
```
用户想要短期投资：
├─ 选择灵活锁仓（0天）
├─ 本金可随时提取（24小时冷却）
├─ 收益倍数：1.0x（基础）
└─ 适合：不确定投资期限的用户
```

---

### 方案 B：添加1天锁仓（可选）

**如果添加，建议**:
```
锁仓期限选项：
├─ 灵活锁仓 (0天): 1.0x
├─ 1天: 1.1x  ← 新增
├─ 30天: 1.3x
├─ 90天: 1.6x
├─ 180天: 2.0x
└─ 365天: 2.5x

收益倍数递增：
0天: 1.0x
1天: 1.1x (+10%)
30天: 1.3x (+30%)
90天: 1.6x (+60%)
180天: 2.0x (+100%)
365天: 2.5x (+150%)
```

---

## 💡 灵活锁仓的完整逻辑

### 1. 质押时

```solidity
function stake(uint256 amount, address referrer, uint256 lockPeriod) {
    // ... 资金分配 ...
    
    if (lockPeriod == 0) {
        // 灵活锁仓：不创建 LockedPrincipal
        // 本金不锁定，可以随时提取
        // 但需要单独管理灵活锁仓本金
    } else {
        // 锁仓期限 > 0：创建 LockedPrincipal
        userLockedPrincipals[msg.sender].push(LockedPrincipal({
            stakeId: stakeId,
            principalAmount: contractAmount,
            lockStartTime: block.timestamp,
            lockEndTime: block.timestamp + (lockPeriod * 1 days),
            isWithdrawn: false,
            lockPeriod: lockPeriod
        }));
    }
}
```

### 2. 提取灵活锁仓本金

```solidity
function withdrawFlexiblePrincipal(uint256 amount) external nonReentrant {
    UserInfo storage user = users[msg.sender];
    
    // 1. 计算灵活锁仓的本金
    // 总质押 - 已锁定的本金 = 灵活锁仓本金
    uint256 totalLocked = 0;
    for (uint256 i = 0; i < userLockedPrincipals[msg.sender].length; i++) {
        if (!userLockedPrincipals[msg.sender][i].isWithdrawn) {
            totalLocked += userLockedPrincipals[msg.sender][i].principalAmount;
        }
    }
    
    uint256 flexiblePrincipal = user.totalStaked - totalLocked;
    require(flexiblePrincipal >= amount, "Insufficient flexible principal");
    
    // 2. 检查冷却期（24小时）
    require(
        block.timestamp >= user.lastWithdrawTime + WITHDRAWAL_COOLDOWN,
        "Cooldown active"
    );
    
    // 3. 更新状态
    user.totalStaked -= amount;
    totalStaked -= amount;
    user.lastWithdrawTime = block.timestamp;
    
    // 4. 转账给用户
    uint256 usdtAmount = amount / PRECISION_MULTIPLIER;
    usdtToken.safeTransfer(msg.sender, usdtAmount);
    
    emit FlexiblePrincipalWithdrawn(msg.sender, amount, block.timestamp);
}
```

---

## 📝 完整示例

### 场景：用户选择灵活锁仓

**第1天：质押**
```
操作：stake(10000, referrer, 0)  // 灵活锁仓

结果：
├─ 资金分配：
│   - 5,000 USDT → Treasury（永久锁定）
│   - 5,000 USDT → 合约（未锁定）
├─ 本金状态：未锁定，可随时提取 ✅
├─ 收益倍数：1.0x
└─ 每日收益：80 USDT/天
```

**第2天：提取本金**
```
操作：withdrawFlexiblePrincipal(5000)

检查：
├─ 灵活锁仓本金：5,000 USDT ✅
├─ 24小时冷却期：已过 ✅
└─ 合约余额：充足 ✅

结果：
├─ 转账：5,000 USDT 给用户
├─ 更新：totalStaked = 5,000 USDT（剩余）
└─ 收益：继续产生（基于 5,000 USDT）
```

**第3天及以后**
```
状态：
├─ 本金：已提取 5,000 USDT ✅
├─ 剩余质押：5,000 USDT（Treasury部分，永久锁定）
├─ 收益：继续产生（基于 5,000 USDT）
└─ 每日收益：40 USDT/天（5,000 × 0.008 × 1.0）
```

---

## 🎯 总结

### 如果用户质押"1天"（灵活锁仓）

**当前设计**：
- 选择"灵活锁仓"（0天）
- 本金：5,000 USDT（未锁定，可随时提取）
- 收益倍数：1.0x
- 每日收益：80 USDT/天
- 提取条件：24小时冷却期

**特点**：
- ✅ 最高灵活性
- ✅ 可以随时提取本金
- ✅ 适合短期投资
- ⚠️ 收益倍数最低

### 如果添加"1天"锁仓选项

**建议设计**：
- 锁仓期限：1天
- 收益倍数：1.1x（+10%）
- 本金锁定：锁定1天
- 提取时间：1天后
- 每日收益：88 USDT/天

**优势**：
- ✅ 收益略高于灵活锁仓
- ✅ 锁仓期短，灵活性高

**劣势**：
- ❌ 需要等待1天才能提取
- ❌ 收益提升有限（仅+10%）

---

## 💡 建议

**保持当前设计**（灵活锁仓）：
- 灵活锁仓已经满足短期投资需求
- 1天锁仓收益提升有限
- 避免选项过多

**或者添加1天锁仓**（如果用户需求强烈）：
- 收益倍数：1.1x
- 本金锁定：1天
- 适合：确定1天后提取的用户

---

**这就是灵活锁仓和短期锁仓的完整逻辑！** 🚀

# 多语言测试指南

## 重要：清除浏览器缓存

由于修改了翻译文件和多个组件，**必须清除浏览器缓存**才能看到更新：

### 方法1：硬刷新（推荐）
1. 打开页面 http://localhost:3000
2. 按 `Ctrl + Shift + R`（Windows）或 `Cmd + Shift + R`（Mac）
3. 这会强制重新加载所有资源

### 方法2：清除缓存
1. 按 `F12` 打开开发者工具
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

### 方法3：无痕模式
1. 打开无痕/隐私浏览窗口
2. 访问 http://localhost:3000
3. 这样可以避免缓存问题

## 测试步骤

### 1. 测试语言切换功能

1. 访问首页：http://localhost:3000
2. 点击左上角的地球图标（🌐）
3. 应该看到语言列表：
   - 🇨🇳 中文（默认）
   - 🇺🇸 English
   - 🇪🇸 Español
   - 🇸🇦 العربية
   - 🇮🇳 हिंदी
   - 🇫🇷 Français
   - 🇧🇷 Português
   - 🇷🇺 Русский
   - 🇯🇵 日本語

4. 选择"🇺🇸 English"
5. 页面应该立即切换到英文

### 2. 测试各页面的翻译

切换到英文后，访问以下页面验证翻译：

#### 首页 (/)
- 导航栏应该显示：Home, Stake, Withdraw, Dashboard, Market, Nodes, Governance, Emergency
- 英雄区域应该显示："Real Assets. Real Yield."

#### 质押页面 (/stake)
- 标题：Deposit USDT
- 余额标签：Balance（不再显示硬编码的2,500 USDT）
- 按钮：Approve USDT, Stake Now →
- 连接钱包提示：Please connect wallet to stake

#### 仪表板 (/dashboard)
- 标题：Dashboard
- 卡片标题：Total Staked, RWA Token, USDT Rewards
- 按钮：Withdraw RWA, Claim USDT
- 未连接钱包时按钮显示：Please connect wallet first

#### 提现页面 (/withdraw)
- 标题：Withdraw Funds
- RWA提现卡片：Withdraw RWA Tokens
- USDT奖励卡片：USDT Dynamic Rewards
- 按钮文本：Withdraw →, Claim USDT Rewards
- 未连接钱包提示：Please connect wallet first
- 无奖励时显示：No rewards available

#### 紧急提取页面 (/emergency)
- 标题：Emergency Withdrawal
- 副标题：Please read all terms carefully before proceeding
- 警告卡片应该显示英文
- 按钮：Emergency Withdraw

#### 市场页面 (/market)
- 标题：Market
- 图表类型：Candlestick, Line, Depth, Volume
- 数据标签：24H High, 24H Low, 24H Volume, Market Cap

#### 节点页面 (/nodes)
- 标题：My Node Level
- 节点等级：Rookie, Silver, Gold, Platinum, Diamond

#### 治理页面 (/governance)
- 标题：Governance
- 参数标签：Protocol Parameters, Fund Status

### 3. 测试错误消息翻译

1. 在质押页面，不连接钱包的情况下：
   - 应该看到："Please connect wallet to stake"（英文）
   - 而不是："请先连接钱包以进行质押操作"（中文）

2. 在仪表板，不连接钱包点击按钮：
   - 按钮应该显示："Please connect wallet first"（英文）

3. 在提现页面，没有奖励时：
   - 按钮应该显示："No rewards available"（英文）

### 4. 切换回中文测试

1. 点击地球图标
2. 选择"🇨🇳 中文"
3. 所有文本应该恢复为中文
4. 验证之前测试的所有页面都正确显示中文

## 已修复的问题

✅ 质押页面余额显示重复（"余额：2,500 USDT: 1995500 USDT"）
✅ 所有硬编码的中文错误消息
✅ 连接钱包提示
✅ 按钮文本
✅ 状态消息

## 如果仍然看到中文

如果清除缓存后仍然看到中文，请检查：

1. **确认语言已切换**
   - 打开浏览器控制台（F12）
   - 输入：`localStorage.getItem('rwa-locale')`
   - 应该返回 `"en"` 表示英文，`"zh"` 表示中文

2. **手动设置语言**
   - 在控制台输入：`localStorage.setItem('rwa-locale', 'en')`
   - 刷新页面

3. **检查 Next.js 服务器**
   - 确认服务器正在运行：http://localhost:3000
   - 查看终端是否有编译错误

4. **完全重启**
   - 关闭浏览器
   - 重新打开
   - 访问 http://localhost:3000

## 当前服务器状态

- ✅ Next.js 开发服务器已重启（进程 ID: 6）
- ✅ 运行在 http://localhost:3000
- ✅ Hardhat 节点运行中（进程 ID: 5）

## 技术说明

### 翻译系统架构

1. **翻译文件**：`frontend/lib/i18n.ts`
   - 包含所有语言的翻译
   - 使用键值对结构（如 `common.connectWalletFirst`）

2. **语言提供者**：`frontend/components/locale-provider.tsx`
   - 管理当前语言状态
   - 将语言保存到 localStorage

3. **使用翻译**：
   ```tsx
   const { locale } = useLocale()
   const { t } = useTranslation(locale)
   
   // 使用翻译
   {t('common.connectWalletFirst')}
   ```

### 新增的通用翻译键

- `common.connectWalletFirst` - 请先连接钱包
- `common.connectWalletToStake` - 请先连接钱包以进行质押操作
- `common.noRewardsAvailable` - 暂无可领取奖励
- `common.approveFailed` - 授权失败
- `common.stakeFailed` - 质押失败
- `common.transactionFailed` - 交易失败，请重试
- `common.redirectingToDashboard` - 正在跳转到仪表板查看质押信息...
- `common.goToDashboardNow` - 立即查看仪表板

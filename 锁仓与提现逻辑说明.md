# 锁仓与不锁仓逻辑说明

基于当前合约与前端代码整理。

---

## 一、用户锁仓与不锁仓的区别

### 1. 收益倍数（前端展示）

| 选择       | 锁仓期限 | 日收益倍数 | 说明           |
|------------|----------|------------|----------------|
| 灵活（不锁） | 0 天     | 1.0×       | 基础 0.8% 日化 |
| 30 天锁仓  | 30 天    | 1.3×       | +30% 收益      |
| 90 天锁仓  | 90 天    | 1.6×       | +60% 收益      |
| 180 天锁仓 | 180 天   | 2.0×       | +100% 收益     |
| 365 天锁仓 | 365 天   | 2.5×       | +150% 收益     |

锁仓时间越长，前端展示的日收益倍数越高，实际发奖由后端/链下逻辑计算。

### 2. 合约侧差异

**USDT 质押（`stake`）**

- 无论选「灵活」还是「30/90/180/365 天」，合约只做两件事：
  - 50% USDT → 国库，50% USDT → 合约
  - 记录 `stakeLockPeriods[stakeId] = lockPeriod`（0/30/90/180/365）
- 合约**没有**按单笔 USDT 锁定本金：没有「某笔 USDT 到期才能提」的约束。
- 本金只能通过 **紧急提现（emergencyWithdraw）** 拿回：可拿回的是「总本金的 50% 减去已发放的 USDT 动态奖励」，且为一次性、不可逆。

**RWA 质押（`stakeRWA`）**

- **锁仓期限 > 0（30/90/180/365 天）**  
  - 50% RWA → 国库，50% RWA 留在合约。  
  - 合约为这笔「留在合约的 50%」创建一条 **RWA 锁仓本金** 记录（`rwaLockedPrincipals[用户]`），包含：
    - `principalAmount`：该笔锁仓本金（RWA）
    - `lockStartTime` / `lockEndTime`：锁仓起止时间
    - `isWithdrawn`：是否已提走
  - 到期后，应通过 **`withdrawRWAPrincipal(lockIndex)`** 取回该笔 RWA 本金（见下文「锁仓结束后资金去向」）。

- **灵活（不锁，lockPeriod = 0）**  
  - 同样 50% → 国库，50% 留在合约。  
  - **不会**往 `rwaLockedPrincipals` 里 push 记录。  
  - 合约没有提供「灵活 RWA 本金」的单独提现接口，这 50% RWA 留在合约池子中，仅通过 `rwaStakes[用户].totalStakedRWA` 等做统计；日常可操作的是 **RWA 收益** 的提现（`withdrawRWARewards`）。

---

## 二、锁仓结束后，用户的资金去哪了？

### RWA 锁仓（选了 30/90/180/365 天）

- **锁仓期内**：对应那 50% 的 RWA 本金记录在 `rwaLockedPrincipals[用户]` 里，不能提。
- **锁仓到期后**：  
  - 合约视图 `getRWALockedPrincipals` 会返回该条目的 `canWithdraw == true`（且 `isWithdrawn == false`）。  
  - 用户应调用 **`withdrawRWAPrincipal(lockIndex)`**，把该条锁仓记录中的 **RWA 本金** 从合约转回自己钱包。  
  - 调用成功后，该条记录会被标记为已提（`isWithdrawn = true`），资金即回到用户地址。

**注意**：当前仓库里的 `StakingContract.sol` 中**没有**实现 `withdrawRWAPrincipal` 函数体，只有事件 `RWAPrincipalWithdrawn` 和视图 `getRWALockedPrincipals`。前端与 ABI 已按「存在该函数」来调用。若部署的合约版本也没有此实现，则锁仓到期后用户无法在链上取回 RWA 本金，需要在合约中补全该函数。

### USDT 质押

- 合约没有「单笔 USDT 锁仓到期」的提现接口。  
- 本金只能通过 **紧急提现** 拿回（见上）：50% 总本金 − 已发 USDT 动态奖励，以 RWA 形式退回（具体换算依赖实现）。

---

## 三、用户选择不锁仓（灵活）可以随时提现吗？

### RWA 收益（与是否锁仓无关）

- **RWA 质押产生的收益**（`rwaStakes[用户].rwaPending`）可通过 **`withdrawRWARewards(amount, chooseStRWA)`** 提现。  
- 规则：最小 10 RWA、24 小时冷却、5% 手续费等（以合约为准）。  
- 不锁仓（灵活）只影响「是否产生锁仓本金记录」，不影响收益的随时提现（在冷却与额度内）。

### RWA 本金

- **不锁仓（灵活）**：没有锁仓记录，合约未提供「灵活 RWA 本金」的单独提现，那 50% 留在合约的 RWA 当前设计下无法像锁仓那样按笔提回。  
- **锁仓**：到期后通过 `withdrawRWAPrincipal(lockIndex)` 提回该笔 RWA 本金（需合约实现该函数）。

### USDT 质押

- **收益**：`user.rwaPending`（RWA 待领）通过 **`withdraw(amount, chooseStRWA)`** 提现，同样受最小额、冷却、手续费等限制，与是否选锁仓无关。  
- **本金**：没有「随时提 USDT 本金」的接口，只能走 **紧急提现**，且只能拿回约 50% 本金等值（减已发 USDT 奖励），且为一次性操作。

---

## 四、总结表

| 项目           | 不锁仓（灵活）     | 锁仓（30/90/180/365 天）                    |
|----------------|--------------------|---------------------------------------------|
| 收益倍数       | 1.0×               | 1.3× / 1.6× / 2.0× / 2.5×（前端展示）      |
| USDT 本金      | 仅能紧急提现（约 50%） | 同上，合约不按单笔锁 USDT                  |
| RWA 本金       | 无单笔提回接口     | 到期后 `withdrawRWAPrincipal(lockIndex)` 取回（需合约实现） |
| RWA 收益       | 可提，受冷却/手续费 | 可提，受冷却/手续费                         |
| USDT 收益(RWA) | 可提，受冷却/手续费 | 可提，受冷却/手续费                         |

**建议**：若希望「RWA 锁仓到期后本金可提」，需在 `StakingContract.sol` 中实现 `withdrawRWAPrincipal(uint256 lockIndex)`：检查 `block.timestamp >= lockEndTime` 且 `!isWithdrawn`，将该条 `principalAmount` 的 RWA 转给用户并标记 `isWithdrawn = true`，并发出 `RWAPrincipalWithdrawn` 事件。

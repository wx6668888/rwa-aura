# 复杂节点等级系统与深度限制方案

## 🎯 核心问题

1. **当前V1-V5定义**：需要查看并优化
2. **机制复杂度**：是否可以设置更复杂的升级机制
3. **深度限制**：第六代及以后是否还有分红

---

## 📊 当前节点等级定义（回顾）

### 现有升级要求

```typescript
export const NODE_REQUIREMENTS: NodeLevelRequirement[] = [
    {
        level: 1,  // V1
        directReferrals: { minLevel: 0, count: 0 },
        teamVolume: '0',
        maxDepartmentRatio: 1.0
    },
    {
        level: 2,  // V2
        directReferrals: { minLevel: 1, count: 3 },
        teamVolume: '5000000000000000000000', // 5000 USDT
        maxDepartmentRatio: 0.5
    },
    {
        level: 3,  // V3
        directReferrals: { minLevel: 2, count: 3 },
        teamVolume: '20000000000000000000000', // 20000 USDT
        maxDepartmentRatio: 0.5
    },
    {
        level: 4,  // V4
        directReferrals: { minLevel: 3, count: 3 },
        teamVolume: '100000000000000000000000', // 100000 USDT
        maxDepartmentRatio: 0.5
    },
    {
        level: 5,  // V5
        directReferrals: { minLevel: 4, count: 3 },
        teamVolume: '500000000000000000000000', // 500000 USDT
        maxDepartmentRatio: 0.5
    }
];
```

### 现有奖励百分比

```typescript
export const NODE_REWARD_PERCENTAGES: Record<number, number> = {
    1: 0.05,  // V1: 5%
    2: 0.10,  // V2: 10%
    3: 0.15,  // V3: 15%
    4: 0.20,  // V4: 20%
    5: 0.50   // V5: 50%
};
```

---

## 🚀 改进方案：更复杂的节点等级系统

### 方案一：多维度升级机制

#### 1.1 升级条件（更复杂）

```solidity
struct NodeLevelRequirement {
    uint8 level;                    // 节点等级
    DirectReferralReq directRef;    // 直推要求
    TeamVolumeReq teamVolume;       // 团队量要求
    BalanceReq balance;              // 大小区平衡要求
    PersonalStakeReq personalStake; // 个人质押要求
    TimeReq timeRequirement;        // 时间要求
    ActivityReq activity;           // 活跃度要求
}

struct DirectReferralReq {
    uint8 minLevel;                 // 直推最低等级
    uint256 count;                  // 直推数量
    uint256 totalStake;             // 直推总质押额
    bool requireActive;              // 是否要求活跃
}

struct TeamVolumeReq {
    uint256 totalVolume;            // 团队总业绩
    uint256 validVolume;            // 有效业绩（锁仓用户）
    uint256 growthRate;             // 增长率要求
}

struct BalanceReq {
    uint256 maxDepartmentRatio;     // 最大部门占比
    uint256 minDepartments;         // 最少部门数
}

struct PersonalStakeReq {
    uint256 minStake;               // 个人最低质押
    uint256 lockDuration;           // 锁仓时长要求
}

struct TimeReq {
    uint256 minDaysSinceFirstStake; // 首次质押后最少天数
    uint256 minDaysAsCurrentLevel;  // 当前等级最少保持天数
}

struct ActivityReq {
    uint256 minReferralsLast30Days; // 最近30天最少推荐数
    uint256 minTeamGrowth;          // 最近30天团队增长
}
```

#### 1.2 完整升级要求表

| 等级 | 直推要求 | 团队量 | 个人质押 | 时间要求 | 活跃度 | 大小区平衡 |
|------|---------|--------|---------|---------|--------|-----------|
| **V1** | 无 | 无 | 无 | 无 | 无 | 无 |
| **V2** | 3个V1+<br>总质押≥3000 | 5000 USDT<br>有效≥3000 | 1000 USDT<br>锁仓30天 | 首次质押后7天 | 最近30天推荐1人 | 单区≤50%<br>至少2个部门 |
| **V3** | 3个V2+<br>总质押≥15000 | 20000 USDT<br>有效≥12000 | 5000 USDT<br>锁仓60天 | V2保持30天 | 最近30天推荐2人<br>团队增长≥10% | 单区≤50%<br>至少3个部门 |
| **V4** | 3个V3+<br>总质押≥60000 | 100000 USDT<br>有效≥60000 | 20000 USDT<br>锁仓90天 | V3保持60天 | 最近30天推荐3人<br>团队增长≥15% | 单区≤50%<br>至少4个部门 |
| **V5** | 3个V4+<br>总质押≥300000 | 500000 USDT<br>有效≥300000 | 50000 USDT<br>锁仓180天 | V4保持90天 | 最近30天推荐5人<br>团队增长≥20% | 单区≤50%<br>至少5个部门 |

---

### 方案二：动态奖励机制

#### 2.1 基础奖励 + 动态加成

```solidity
struct NodeRewardConfig {
    uint256 basePercentage;        // 基础奖励百分比
    uint256 volumeBonus;            // 团队量加成
    uint256 growthBonus;            // 增长率加成
    uint256 activityBonus;          // 活跃度加成
    uint256 maxPercentage;          // 最高奖励百分比
}

mapping(uint8 => NodeRewardConfig) public nodeRewardConfigs;

function initializeNodeRewards() internal {
    // V1配置
    nodeRewardConfigs[1] = NodeRewardConfig({
        basePercentage: 50,        // 5%
        volumeBonus: 10,            // 每10万USDT +0.1%
        growthBonus: 5,             // 每10%增长 +0.05%
        activityBonus: 5,           // 活跃度 +0.05%
        maxPercentage: 80           // 最高8%
    });
    
    // V2配置
    nodeRewardConfigs[2] = NodeRewardConfig({
        basePercentage: 100,        // 10%
        volumeBonus: 15,            // 每10万USDT +0.15%
        growthBonus: 10,            // 每10%增长 +0.1%
        activityBonus: 10,          // 活跃度 +0.1%
        maxPercentage: 150          // 最高15%
    });
    
    // V3配置
    nodeRewardConfigs[3] = NodeRewardConfig({
        basePercentage: 150,        // 15%
        volumeBonus: 20,            // 每10万USDT +0.2%
        growthBonus: 15,            // 每10%增长 +0.15%
        activityBonus: 15,          // 活跃度 +0.15%
        maxPercentage: 250          // 最高25%
    });
    
    // V4配置
    nodeRewardConfigs[4] = NodeRewardConfig({
        basePercentage: 200,        // 20%
        volumeBonus: 25,            // 每10万USDT +0.25%
        growthBonus: 20,            // 每10%增长 +0.2%
        activityBonus: 20,          // 活跃度 +0.2%
        maxPercentage: 350          // 最高35%
    });
    
    // V5配置
    nodeRewardConfigs[5] = NodeRewardConfig({
        basePercentage: 500,        // 50%
        volumeBonus: 50,            // 每10万USDT +0.5%
        growthBonus: 30,            // 每10%增长 +0.3%
        activityBonus: 30,          // 活跃度 +0.3%
        maxPercentage: 700          // 最高70%
    });
}

// 计算实际奖励百分比
function calculateActualRewardPercentage(address user) public view returns (uint256) {
    UserInfo storage userInfo = users[user];
    NodeRewardConfig storage config = nodeRewardConfigs[userInfo.nodeLevel];
    
    // 基础百分比
    uint256 actualPercentage = config.basePercentage;
    
    // 团队量加成
    uint256 volumeBonus = (userInfo.teamVolume / 100000) * config.volumeBonus;
    
    // 增长率加成
    uint256 growthRate = calculateGrowthRate(user);
    uint256 growthBonus = (growthRate / 10) * config.growthBonus;
    
    // 活跃度加成
    uint256 activityScore = calculateActivityScore(user);
    uint256 activityBonus = activityScore * config.activityBonus / 100;
    
    // 总加成
    uint256 totalBonus = volumeBonus + growthBonus + activityBonus;
    actualPercentage += totalBonus;
    
    // 不超过最高百分比
    if (actualPercentage > config.maxPercentage) {
        actualPercentage = config.maxPercentage;
    }
    
    return actualPercentage;
}
```

---

### 方案三：深度限制机制

#### 3.1 级差奖励深度限制

**核心问题**：第六代及以后是否还有分红？

**答案**：**有，但有限制**

#### 3.2 深度奖励表

| 深度 | 奖励规则 | 说明 |
|------|---------|------|
| **1-5代** | 正常级差奖励 | 按节点等级分配，最高50% |
| **6-10代** | 减半奖励 | 奖励减半，最高25% |
| **11-15代** | 四分之一奖励 | 奖励四分之一，最高12.5% |
| **16代+** | 无奖励 | 不再分配奖励 |

#### 3.3 实现逻辑

```solidity
// 深度奖励配置
struct DepthRewardConfig {
    uint256 maxDepth;               // 最大深度
    uint256 rewardMultiplier;       // 奖励倍数（基点制）
}

mapping(uint256 => DepthRewardConfig) public depthRewardConfigs;

function initializeDepthRewards() internal {
    // 1-5代：100%奖励
    for (uint i = 1; i <= 5; i++) {
        depthRewardConfigs[i] = DepthRewardConfig({
            maxDepth: i,
            rewardMultiplier: 10000  // 100%
        });
    }
    
    // 6-10代：50%奖励
    for (uint i = 6; i <= 10; i++) {
        depthRewardConfigs[i] = DepthRewardConfig({
            maxDepth: i,
            rewardMultiplier: 5000   // 50%
        });
    }
    
    // 11-15代：25%奖励
    for (uint i = 11; i <= 15; i++) {
        depthRewardConfigs[i] = DepthRewardConfig({
            maxDepth: i,
            rewardMultiplier: 2500   // 25%
        });
    }
    
    // 16代+：0%奖励
    for (uint i = 16; i <= 100; i++) {
        depthRewardConfigs[i] = DepthRewardConfig({
            maxDepth: i,
            rewardMultiplier: 0      // 0%
        });
    }
}

// 计算级差奖励（带深度限制）
function calculateDifferentialRewardsWithDepth(
    uint256 stakeAmount,
    address userAddress
) internal returns (RewardDistribution[] memory) {
    // 获取所有上级（按深度排序）
    address[] memory ancestors = getAncestors(userAddress);
    
    RewardDistribution[] memory rewards = new RewardDistribution[](ancestors.length);
    uint256 maxAllocatedPercentage = 0;
    uint256 totalRewards = 0;
    
    for (uint i = 0; i < ancestors.length; i++) {
        address ancestor = ancestors[i];
        uint256 depth = i + 1; // 深度从1开始
        
        // 检查深度限制
        DepthRewardConfig storage depthConfig = depthRewardConfigs[depth];
        if (depthConfig.rewardMultiplier == 0) {
            break; // 超过最大深度，停止计算
        }
        
        UserInfo storage ancInfo = users[ancestor];
        uint8 nodeLevel = ancInfo.nodeLevel;
        
        // 计算基础奖励百分比
        uint256 basePercentage = calculateActualRewardPercentage(ancestor);
        
        // 计算级差
        uint256 differential = basePercentage > maxAllocatedPercentage 
            ? basePercentage - maxAllocatedPercentage 
            : 0;
        
        if (differential > 0) {
            // 应用深度倍数
            uint256 adjustedPercentage = differential * depthConfig.rewardMultiplier / 10000;
            
            // 计算奖励金额
            uint256 rewardAmount = stakeAmount * adjustedPercentage / 10000;
            
            rewards[i] = RewardDistribution({
                beneficiary: ancestor,
                amount: rewardAmount,
                percentage: adjustedPercentage,
                nodeLevel: nodeLevel,
                depth: depth
            });
            
            maxAllocatedPercentage = basePercentage;
            totalRewards += rewardAmount;
        }
        
        // 检查总奖励是否超过50%上限
        if (totalRewards > stakeAmount * 50 / 100) {
            break;
        }
    }
    
    return rewards;
}
```

---

## 📊 完整示例：深度奖励计算

### 场景：用户 Alice 质押 10,000 USDT，推荐链15代

**推荐链结构**：
```
Alice (质押者)
    ↓ 第1代
Bob (V2, 10%)
    ↓ 第2代
Charlie (V3, 15%)
    ↓ 第3代
David (V4, 20%)
    ↓ 第4代
Eve (V5, 50%)
    ↓ 第5代
Frank (V3, 15%)
    ↓ 第6代
Grace (V2, 10%)
    ↓ 第7代
Henry (V1, 5%)
    ↓ ... (继续到第15代)
```

**奖励计算**：

#### 第1-5代（100%奖励）

```
第1代：Bob (V2, 10%)
├─ 级差：10% - 0% = 10%
├─ 深度倍数：100%
├─ 实际奖励：10,000 × 10% × 100% = 1,000 USDT

第2代：Charlie (V3, 15%)
├─ 级差：15% - 10% = 5%
├─ 深度倍数：100%
├─ 实际奖励：10,000 × 5% × 100% = 500 USDT

第3代：David (V4, 20%)
├─ 级差：20% - 15% = 5%
├─ 深度倍数：100%
├─ 实际奖励：10,000 × 5% × 100% = 500 USDT

第4代：Eve (V5, 50%)
├─ 级差：50% - 20% = 30%
├─ 深度倍数：100%
├─ 实际奖励：10,000 × 30% × 100% = 3,000 USDT

第5代：Frank (V3, 15%)
├─ 级差：15% - 50% = 0%（被压级）
├─ 实际奖励：0 USDT

前5代总计：1,000 + 500 + 500 + 3,000 + 0 = 5,000 USDT（50%上限）
```

#### 第6-10代（50%奖励）

```
第6代：Grace (V2, 10%)
├─ 级差：10% - 50% = 0%（被压级）
├─ 实际奖励：0 USDT

第7代：Henry (V1, 5%)
├─ 级差：5% - 50% = 0%（被压级）
├─ 实际奖励：0 USDT

假设第8代：Ivy (V4, 20%)
├─ 级差：20% - 50% = 0%（被压级）
├─ 实际奖励：0 USDT

注意：由于前5代已经达到50%上限，6-10代即使有级差也无法获得奖励
```

**结论**：由于级差奖励的50%上限，6-10代通常无法获得奖励（除非前5代有等级较低的情况）

---

## 🔄 改进方案：深度奖励池

### 方案：独立深度奖励池

**问题**：如果前5代已经达到50%上限，6-10代无法获得奖励

**解决方案**：设置独立的深度奖励池

```solidity
// 深度奖励池配置
struct DepthRewardPool {
    uint256 pool1_5;    // 1-5代奖励池（50%上限）
    uint256 pool6_10;   // 6-10代奖励池（25%上限）
    uint256 pool11_15;  // 11-15代奖励池（12.5%上限）
}

DepthRewardPool public depthRewardPools;

// 质押资金分配
function stake(uint256 amount, address referrer) external {
    // ... 原有逻辑 ...
    
    // 分配深度奖励池
    uint256 pool1_5 = amount * 50 / 100;   // 50%给1-5代
    uint256 pool6_10 = amount * 25 / 100;  // 25%给6-10代
    uint256 pool11_15 = amount * 125 / 1000; // 12.5%给11-15代
    
    depthRewardPools.pool1_5 += pool1_5;
    depthRewardPools.pool6_10 += pool6_10;
    depthRewardPools.pool11_15 += pool11_15;
}

// 计算深度奖励
function calculateDepthRewards(
    uint256 stakeAmount,
    address userAddress
) internal returns (RewardDistribution[] memory) {
    address[] memory ancestors = getAncestors(userAddress);
    RewardDistribution[] memory rewards = new RewardDistribution[](ancestors.length);
    
    uint256 pool1_5Used = 0;
    uint256 pool6_10Used = 0;
    uint256 pool11_15Used = 0;
    
    for (uint i = 0; i < ancestors.length; i++) {
        address ancestor = ancestors[i];
        uint256 depth = i + 1;
        
        // 根据深度选择奖励池
        uint256 poolAmount = 0;
        uint256 maxPool = 0;
        
        if (depth <= 5) {
            poolAmount = stakeAmount * 50 / 100;
            maxPool = depthRewardPools.pool1_5;
            // 使用1-5代奖励池
        } else if (depth <= 10) {
            poolAmount = stakeAmount * 25 / 100;
            maxPool = depthRewardPools.pool6_10;
            // 使用6-10代奖励池
        } else if (depth <= 15) {
            poolAmount = stakeAmount * 125 / 1000;
            maxPool = depthRewardPools.pool11_15;
            // 使用11-15代奖励池
        } else {
            break; // 16代+无奖励
        }
        
        // 计算级差奖励
        // ... 级差计算逻辑 ...
        
        // 检查奖励池余额
        if (poolAmount > maxPool) {
            break; // 奖励池不足
        }
    }
    
    return rewards;
}
```

---

## 📋 完整深度奖励表

### 深度奖励配置

| 深度 | 奖励池比例 | 奖励倍数 | 最大奖励 | 说明 |
|------|-----------|---------|---------|------|
| 1-5代 | 50% | 100% | 50% | 主要奖励池 |
| 6-10代 | 25% | 50% | 12.5% | 次要奖励池 |
| 11-15代 | 12.5% | 25% | 3.125% | 补充奖励池 |
| 16代+ | 0% | 0% | 0% | 无奖励 |

### 总奖励上限

```
总奖励上限 = 50% + 12.5% + 3.125% = 65.625%
```

**注意**：这个上限是理论值，实际由于级差逻辑，通常不会达到

---

## 🎯 最终方案总结

### 节点等级系统（更复杂）

1. **多维度升级**：直推、团队量、个人质押、时间、活跃度、大小区平衡
2. **动态奖励**：基础奖励 + 团队量加成 + 增长率加成 + 活跃度加成
3. **最高奖励限制**：每个等级有最高奖励百分比

### 深度限制机制

1. **1-5代**：正常级差奖励，50%上限
2. **6-10代**：50%奖励倍数，25%上限
3. **11-15代**：25%奖励倍数，12.5%上限
4. **16代+**：无奖励

### 改进点

1. ✅ **更复杂的升级机制**：多维度考核
2. ✅ **动态奖励**：根据表现调整奖励
3. ✅ **深度限制**：明确6代及以后的奖励规则
4. ✅ **独立奖励池**：确保6-15代也能获得奖励

---

**这个方案既保证了前5代的主要奖励，又给6-15代提供了补充奖励。您觉得还需要调整哪些参数？**

# 📋 RWA 奖励发放说明 - 2026年3月2日

**问题**: 质押后，待提取的 RWA 代币显示 0.00

**原因**: RWA 奖励需要通过后端服务计算并调用合约更新

**状态**: ✅ 已创建手动更新脚本

---

## 🔍 问题分析

### RWA 奖励发放机制

RWA 奖励**不是自动计算**的，而是通过以下流程：

1. **后端服务计算收益**
   - 每天 00:00 UTC 执行定时任务
   - 计算每个用户的收益：`质押金额 × 0.8%`
   - 转换为 RWA：`USDT 收益 ÷ RWA 价格 (0.85)`

2. **调用合约更新**
   - 后端调用 `StakingContract.updateUserRewards()`
   - 更新用户的 `rwaPending` 余额
   - 只有 `backendAddress` 可以调用此函数

3. **前端显示**
   - 前端从合约读取 `rwaPending`
   - 如果后端没有更新，显示为 0

---

## 🚀 解决方案

### 方案 1: 启动后端服务（生产环境）

**需要配置**:
- 数据库连接
- 环境变量（RPC URL、合约地址等）
- 定时任务服务

**启动命令**:
```bash
cd backend
npm install
npm run dev
```

---

### 方案 2: 使用手动更新脚本（测试环境）

**已创建脚本**:
- `scripts/manual-update-rewards.ts` - 手动更新单个用户奖励
- `scripts/update-rewards-periodic.ts` - 批量更新所有用户奖励

**使用方法**:
```bash
# 更新单个用户（Account #1）
npx hardhat run scripts/manual-update-rewards.ts --network localhost

# 批量更新所有用户
npx hardhat run scripts/update-rewards-periodic.ts --network localhost
```

**脚本功能**:
- ✅ 自动计算用户收益（基于质押时间和金额）
- ✅ 调用合约 `updateUserRewards` 更新余额
- ✅ 验证更新结果

---

## 📊 当前状态

### 用户质押信息

- **Account #1**: `0x70997970C51812dc3A010C7d01b50e0d17dc79C8`
- **总质押**: 11,000 USDT（1000 + 10000）
- **首次质押时间**: 2026/3/2 09:35:48
- **当前 RWA Pending**: 0.19 RWA（已手动更新）

---

## 🔄 定期更新奖励

### 方式 1: 手动运行脚本

**每次测试时运行**:
```bash
npx hardhat run scripts/update-rewards-periodic.ts --network localhost
```

---

### 方式 2: 创建定时任务（Windows）

**使用 Windows 任务计划程序**:
1. 打开"任务计划程序"
2. 创建基本任务
3. 触发器: 每天 00:00（或根据需要）
4. 操作: 运行脚本
5. 程序: `npx`
6. 参数: `hardhat run scripts/update-rewards-periodic.ts --network localhost`

---

### 方式 3: 启动后端服务（推荐）

**完整后端服务**:
- ✅ 自动监听质押事件
- ✅ 定时计算和发放收益
- ✅ 处理推荐奖励
- ✅ 更新节点等级

**启动步骤**:
1. 配置数据库
2. 配置环境变量
3. 启动服务: `cd backend && npm run dev`

---

## 📝 收益计算公式

### 静态收益（RWA 代币）

```
每日 USDT 收益 = 质押金额 × 0.8%
每日 RWA 收益 = 每日 USDT 收益 ÷ RWA 价格 (0.85)
总 RWA 收益 = 每日 RWA 收益 × 经过天数
```

### 示例

**质押 11,000 USDT，经过 1 天**:
```
每日 USDT 收益 = 11,000 × 0.008 = 88 USDT
每日 RWA 收益 = 88 ÷ 0.85 ≈ 103.53 RWA
总 RWA 收益 = 103.53 × 1 = 103.53 RWA
```

**质押 11,000 USDT，经过 0.0018 天（约 157 秒）**:
```
总 RWA 收益 = 103.53 × 0.0018 ≈ 0.19 RWA
```

---

## ⚠️ 重要提示

### 1. 后端地址

**合约中的 `backendAddress` 是 `immutable`**，部署时设置，无法修改。

**当前后端地址**: `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266` (Deployer)

**只有这个地址可以调用 `updateUserRewards`**

---

### 2. 防止重复计算

**合约使用 `stakeId` 防止重复处理**:
- 每个 `stakeId` 只能处理一次
- 脚本使用时间戳 + 随机数生成唯一 ID

---

### 3. 前端显示

**前端会实时计算收益**（基于 `firstStakeTime`），但：
- 如果 `firstStakeTime` 无效（0 或 1），会使用链上数据
- 如果链上 `rwaPending` 为 0，前端也会显示 0
- **需要后端更新链上数据后，前端才会显示正确的值**

---

## ✅ 下一步

### 立即执行

1. **刷新前端页面**
   - 硬刷新 (`Ctrl+Shift+R`)
   - 检查 RWA Pending 是否更新

2. **如果需要更多收益**
   - 运行 `scripts/update-rewards-periodic.ts`
   - 或等待更长时间后再次运行

3. **长期解决方案**
   - 配置并启动后端服务
   - 或创建定时任务自动运行脚本

---

## 📋 测试步骤

1. **质押 USDT** ✅
2. **运行更新脚本** ✅
3. **刷新前端页面** ⏳
4. **验证 RWA Pending** ⏳

---

**现在请刷新前端页面，应该可以看到 RWA Pending 更新了！**

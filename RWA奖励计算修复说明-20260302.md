# ✅ RWA 奖励计算修复 - 2026年3月2日

**问题**: 质押 10000 USDT 后，待提取的 RWA 显示 1641124（异常高）

**状态**: ✅ 已修复

---

## 🔍 问题分析

### 问题 1: `firstStakeTime` 未正确返回

**原因**:
- `StakingContract.getUserStakeInfo()` 函数没有返回 `firstStakeTime`
- 前端代码试图从 `userData[6]` 读取，但该值不存在
- 导致 `firstStakeTime` 为 0 或 1（无效时间戳）

**影响**:
- 前端计算时，`elapsedSeconds` 异常大（从 1970 年 1 月 1 日开始计算）
- 导致计算出的 RWA 收益异常高

---

### 问题 2: 前端计算逻辑错误

**原因**:
- 代码直接使用 USDT 金额计算收益，没有转换为 RWA
- 计算逻辑：`totalEarnings = totalStakedNum * perSecondRate * elapsedSeconds`
- `perSecondRate` 是 USDT 的每秒收益率，不是 RWA 的

**正确的逻辑应该是**:
1. 每日 USDT 收益 = 质押金额 × 0.8%
2. 每日 RWA 收益 = 每日 USDT 收益 / RWA 价格（0.85）
3. 每秒 RWA 收益 = 每日 RWA 收益 / 86400
4. 总 RWA 收益 = 每秒 RWA 收益 × 经过的秒数

---

## 🚀 修复内容

### 1. 修复合约 `StakingContract.sol` ✅

**修改 `getUserStakeInfo` 函数**:
```solidity
function getUserStakeInfo(address userAddress) external view returns (
    uint256 totalStaked_,
    uint256 rwaPending_,
    uint256 usdtRewards_,
    uint256 lastWithdrawTime_,
    address referrer_,
    uint8 nodeLevel_,
    uint256 firstStakeTime_  // ✅ 新增返回值
) {
    UserInfo storage user = users[userAddress];
    return (
        user.totalStaked,
        user.rwaPending,
        user.usdtRewards,
        user.lastWithdrawTime,
        user.referrer,
        user.nodeLevel,
        user.firstStakeTime  // ✅ 返回 firstStakeTime
    );
}
```

---

### 2. 修复前端 `useStakingContract.ts` ✅

**修改 `formattedUserData`**:
```typescript
const formattedUserData = userStakeInfo ? {
  totalStaked: formatUnits(userStakeInfo[0], 18),
  rwaPending: formatUnits(userStakeInfo[1], 18),
  usdtRewards: formatUnits(userStakeInfo[2], 18),
  lastWithdrawTime: Number(userStakeInfo[3]),
  referrer: userStakeInfo[4],
  nodeLevel: userStakeInfo[5],
  firstStakeTime: userStakeInfo[6] ? Number(userStakeInfo[6]) : 0, // ✅ 从 getUserStakeInfo 获取
} : null
```

---

### 3. 修复前端 `earnings-card.tsx` ✅

**修复计算逻辑**:
```typescript
// RWA 价格（假设）
const rwaPrice = 0.85 // 1 RWA ≈ 0.85 USDT

// 计算每日收益
const dailyRate = 0.008 // 0.8% 日收益率
const dailyUSDTYield = totalStakedNum * dailyRate // 每日 USDT 收益
const dailyRWAYield = dailyUSDTYield / rwaPrice // 每日 RWA 收益
const perSecondRWAYield = dailyRWAYield / 86400 // 每秒 RWA 收益

// 计算逻辑
const calculateBalance = () => {
  const currentTime = Math.floor(Date.now() / 1000)
  const elapsedSeconds = currentTime - firstStakeTime
  
  // 如果时间差异常（超过1年），直接使用链上数据
  if (elapsedSeconds < 0 || elapsedSeconds > 31536000) {
    return rwaPendingNum
  }
  
  // 总 RWA 收益 = 每秒 RWA 收益 × 经过的秒数
  const calculatedRWA = perSecondRWAYield * elapsedSeconds
  
  // 优先使用链上数据（如果存在），否则使用计算值
  if (rwaPendingNum > 0) {
    return rwaPendingNum
  }
  
  return calculatedRWA
}
```

**关键改进**:
- ✅ 正确计算 RWA 收益（先转换为 RWA，再计算）
- ✅ 添加时间戳验证（防止异常时间戳）
- ✅ 优先使用链上数据（更准确）
- ✅ 添加异常值保护（超过1年的计算直接使用链上数据）

---

## 📋 下一步

### 1. 重新部署合约

由于修改了合约接口，需要重新部署：

```bash
npx hardhat run scripts/deploy-all.ts --network localhost
```

**注意**: 重新部署后，需要：
- 更新前端配置中的合约地址
- 重新为测试账户 mint USDT
- 重新质押测试

---

### 2. 测试验证

**测试步骤**:
1. 重新部署合约
2. 为 Account #1 mint USDT
3. 质押 10000 USDT
4. 检查 `firstStakeTime` 是否正确（应该是当前时间戳）
5. 检查前端显示的 RWA 收益是否正常

**预期结果**:
- `firstStakeTime` 应该是当前时间戳（例如：1772414870）
- 前端显示的 RWA 收益应该是合理的值（例如：质押 10000 USDT，1 天后约 94 RWA）

---

## ⚠️ 重要提示

### 关于 `firstStakeTime`

**问题**: 如果 `firstStakeTime` 为 0 或 1，说明：
- 用户还没有质押（`firstStakeTime` 为 0）
- 或者合约部署时初始化有问题（`firstStakeTime` 为 1）

**解决方案**:
- 确保在 `stake` 函数中正确设置 `firstStakeTime = block.timestamp`
- 前端代码已经添加了验证，如果 `firstStakeTime <= 1` 或 `> now`，直接使用链上数据

---

## ✅ 完成

**状态**: ✅ 代码已修复

**待执行**:
- ⏳ 重新部署合约
- ⏳ 测试验证

---

**修复完成后，RWA 收益计算应该正常了！**

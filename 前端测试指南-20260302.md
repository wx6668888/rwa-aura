# 🧪 前端测试指南 - 2026年3月2日

**测试环境**: 本地 Hardhat 网络  
**前端框架**: Next.js 14 + wagmi v3 + RainbowKit

---

## 📋 前置准备

### 1. 确保 Hardhat 节点运行

**终端1** - 启动 Hardhat 节点（保持运行）:
```bash
npx hardhat node
```

**预期输出**:
```
Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/
```

---

### 2. 确保合约已部署

如果节点重启，需要重新部署合约：
```bash
npx hardhat run scripts/deploy-all.ts --network localhost
```

**最新合约地址**:
```
TestUSDT:        0x36C02dA8a0983159322a80FFE9F24b1acfF8B570
RWAToken:        0x809d550fca64d94Bd9F66E60752A544199cfAC3D
StRWA:           0x4c5859f0F772848b2D91F1D83E2Fe57935348029
StakingContract: 0x1291Be112d480055DaFd8a610b7d1e203891C274
SwapContract:    0xCD8a1C3ba11CF5ECfa6267617243239504a98d90
```

---

## 🚀 启动前端

### Step 1: 进入前端目录

```bash
cd frontend
```

---

### Step 2: 安装依赖（如果需要）

```bash
npm install
```

---

### Step 3: 启动开发服务器

**终端2** - 启动前端（保持运行）:
```bash
npm run dev
```

**预期输出**:
```
  ▲ Next.js 16.1.6
  - Local:        http://localhost:3000
  - Network:      http://0.0.0.0:3000
```

---

## 🔗 连接本地网络

### 方法1: MetaMask 连接

1. **打开 MetaMask**
2. **添加网络**:
   - 网络名称: `Hardhat Local`
   - RPC URL: `http://127.0.0.1:8545`
   - Chain ID: `31337`
   - Currency Symbol: `ETH`

3. **导入测试账户**:
   - 在 Hardhat 节点输出中找到账户私钥
   - 在 MetaMask 中导入账户
   - 账户会有 10000 ETH

4. **连接钱包**:
   - 访问 `http://localhost:3000`
   - 点击 "Connect Wallet"
   - 选择 MetaMask
   - 切换到 Hardhat Local 网络

---

### 方法2: 使用 Hardhat 账户

**获取测试账户私钥**:

在 Hardhat 节点输出中，你会看到类似这样的账户：
```
Account #0: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266 (10000 ETH)
Private Key: 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
```

**导入到 MetaMask**:
1. MetaMask → 账户 → 导入账户
2. 粘贴私钥
3. 确认导入

---

## 🧪 测试功能

### 1. 钱包连接测试

- [ ] 点击 "Connect Wallet" 按钮
- [ ] 选择 MetaMask
- [ ] 确认连接
- [ ] 验证账户地址显示
- [ ] 验证网络切换（自动切换到 Hardhat Local）

---

### 2. 质押页面测试 (`/stake`)

- [ ] 查看 USDT 余额
- [ ] 输入质押金额
- [ ] 点击 "Stake" 按钮
- [ ] 确认交易
- [ ] 验证 stRWA 余额增加（50%）
- [ ] 验证质押信息更新

---

### 3. 提现页面测试 (`/withdraw`)

- [ ] 查看可提现余额
- [ ] 选择提现模式（提U模式 / 持RWA模式）
- [ ] 输入提现金额
- [ ] 确认交易
- [ ] 验证余额更新

---

### 4. Swap 页面测试 (`/swap`)

- [ ] 查看池子余额
- [ ] 选择互换方向（stRWA → RWA 或 RWA → stRWA）
- [ ] 输入互换金额
- [ ] 确认交易
- [ ] 验证余额更新

**注意**: 如果池子未初始化，需要先初始化：
```bash
npx hardhat console --network localhost
```

然后在 console 中：
```javascript
const SwapContract = await ethers.getContractFactory("SwapContract");
const swapContract = SwapContract.attach("0xCD8a1C3ba11CF5ECfa6267617243239504a98d90");
const RWAToken = await ethers.getContractFactory("RWAToken");
const rwaToken = RWAToken.attach("0x809d550fca64d94Bd9F66E60752A544199cfAC3D");
const StRWA = await ethers.getContractFactory("StRWA");
const stRWA = StRWA.attach("0x4c5859f0F772848b2D91F1D83E2Fe57935348029");
const amount = ethers.parseEther("10000");
await rwaToken.approve(await swapContract.getAddress(), amount);
await stRWA.mint(await deployer.getAddress(), amount);
await stRWA.approve(await swapContract.getAddress(), amount);
await swapContract.initializePool(amount, amount);
```

---

### 5. Dashboard 页面测试 (`/dashboard`)

- [ ] 查看总资产
- [ ] 查看投资份额
- [ ] 查看复投奖励
- [ ] 查看收益统计

---

### 6. 节点页面测试 (`/nodes`)

- [ ] 查看节点等级
- [ ] 查看推荐关系
- [ ] 查看团队统计
- [ ] 查看质量评估

---

## ⚠️ 常见问题

### 问题1: 无法连接钱包

**解决方案**:
- 确保 MetaMask 已安装
- 确保已添加 Hardhat Local 网络
- 确保网络 Chain ID 为 31337
- 刷新页面重试

---

### 问题2: 交易失败

**可能原因**:
- Hardhat 节点未运行
- 合约地址不匹配
- 余额不足
- 网络未切换

**解决方案**:
- 检查 Hardhat 节点是否运行
- 重新部署合约并更新地址
- 检查账户余额
- 确保网络为 Hardhat Local

---

### 问题3: 余额显示为 0

**可能原因**:
- 合约地址错误
- 网络未切换
- 账户未导入

**解决方案**:
- 检查合约地址配置
- 切换到 Hardhat Local 网络
- 导入正确的测试账户

---

### 问题4: 前端无法访问

**解决方案**:
- 检查前端是否运行在 `http://localhost:3000`
- 检查防火墙设置
- 尝试使用 `http://127.0.0.1:3000`

---

## 📊 测试检查清单

### 基础功能
- [ ] 钱包连接
- [ ] 网络切换
- [ ] 余额显示
- [ ] 交易确认

### 核心功能
- [ ] 质押功能
- [ ] 提现功能
- [ ] 互换功能
- [ ] 资产查询

### 页面功能
- [ ] Dashboard 页面
- [ ] 质押页面
- [ ] 提现页面
- [ ] Swap 页面
- [ ] 节点页面

---

## 🎯 测试目标

1. **验证前端与合约交互正常**
2. **验证所有功能可用**
3. **验证用户体验流畅**
4. **发现并修复问题**

---

**开始前端测试！**

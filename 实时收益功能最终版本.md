# 实时收益功能 - 最终版本

## 🔧 最新修复（第三次优化）

### 问题分析

之前的 localStorage 方案有以下问题：

1. **依赖问题**：useEffect 依赖 `perSecondEarnings`，但这个值依赖 `totalStakedNum`
2. **时序问题**：组件挂载时 `totalStakedNum` 可能还是 0，导致计算错误
3. **重复初始化**：每次依赖变化都会重新初始化基准数据

### 新方案的改进

1. **存储质押金额**：在 localStorage 中存储 `stakedAmount`，避免依赖当前可能为 0 的值
2. **使用 ref 检测变化**：用 `useRef` 记录上次的链上余额，准确检测提现等操作
3. **错误处理**：添加 try-catch，解析失败时自动重新初始化
4. **独立计算**：每次计算都使用存储的 `stakedAmount`，不依赖当前状态

## 📊 数据结构

### localStorage 存储格式

```typescript
{
  baseTime: 1709020800000,      // 基准时间戳（毫秒）
  baseBalance: 0.123456,        // 基准余额（RWA）
  stakedAmount: 10000           // 质押金额（USDT）
}
```

### 计算逻辑

```typescript
// 1. 从 localStorage 读取数据
const { baseTime, baseBalance, stakedAmount } = JSON.parse(stored)

// 2. 使用存储的质押金额计算每秒收益
const perSecondRate = 0.008 / 86400  // 每日 0.8% / 86400 秒
const storedPerSecondEarnings = stakedAmount * perSecondRate

// 3. 计算经过的时间
const elapsedSeconds = (Date.now() - baseTime) / 1000

// 4. 计算累计收益
const accumulatedEarnings = storedPerSecondEarnings * elapsedSeconds

// 5. 计算当前余额
const newBalance = baseBalance + accumulatedEarnings
```

## 🧪 测试步骤

### 步骤 1：清除旧数据

打开浏览器控制台（F12），执行：

```javascript
// 清除所有 RWA 收益相关的 localStorage 数据
Object.keys(localStorage)
  .filter(key => key.startsWith('rwa_earnings_'))
  .forEach(key => localStorage.removeItem(key))

console.log('已清除旧数据')
```

### 步骤 2：硬刷新浏览器

- Windows: `Ctrl + Shift + R`
- Mac: `Cmd + Shift + R`

### 步骤 3：测试实时增长

1. 访问 http://localhost:3000/dashboard
2. 确保钱包已连接且已质押 USDT
3. 观察 RWA 余额是否每秒增长
4. 记录初始值（例如：0.000000 RWA）

### 步骤 4：测试页面切换

1. 等待 10 秒，记录当前值（例如：0.000926 RWA）
2. 切换到质押页面：http://localhost:3000/stake
3. 等待 10 秒
4. 返回仪表板：http://localhost:3000/dashboard
5. **验证**：余额应该显示约 0.001852 RWA（继续增长，不是重置）

### 步骤 5：测试浏览器刷新

1. 记录当前余额（例如：0.002778 RWA）
2. 刷新页面（F5）
3. **验证**：余额应该继续从上次的值增长，不是重置为 0

### 步骤 6：验证计算准确性

假设质押 10,000 USDT：

- 每日收益：10,000 × 0.8% = 80 RWA
- 每秒收益：80 ÷ 86400 = 0.000926 RWA
- 60 秒收益：0.000926 × 60 = 0.0556 RWA

**测试**：
1. 记录初始余额
2. 等待 60 秒
3. 计算增长量
4. 对比预期值（应该约等于 0.0556 RWA）

## 🐛 调试技巧

### 查看 localStorage 数据

打开浏览器控制台（F12），执行：

```javascript
// 查看当前账户的收益数据
const address = '0x70997970C51812dc3A010C7d01b50e0d17dc79C8' // 替换为你的地址
const key = `rwa_earnings_${address}`
const data = JSON.parse(localStorage.getItem(key))

console.log('基准时间:', new Date(data.baseTime))
console.log('基准余额:', data.baseBalance, 'RWA')
console.log('质押金额:', data.stakedAmount, 'USDT')
console.log('经过时间:', (Date.now() - data.baseTime) / 1000, '秒')

// 手动计算当前应该显示的余额
const perSecondRate = 0.008 / 86400
const perSecondEarnings = data.stakedAmount * perSecondRate
const elapsedSeconds = (Date.now() - data.baseTime) / 1000
const currentBalance = data.baseBalance + (perSecondEarnings * elapsedSeconds)

console.log('当前余额应该是:', currentBalance, 'RWA')
```

### 手动重置数据

如果需要重新开始：

```javascript
const address = '0x70997970C51812dc3A010C7d01b50e0d17dc79C8' // 替换为你的地址
const key = `rwa_earnings_${address}`
localStorage.removeItem(key)
console.log('已重置，请刷新页面')
```

## ✅ 成功标志

如果以下所有条件都满足，说明功能正常：

1. ✅ RWA 余额每秒平滑增长
2. ✅ 切换页面后返回，余额继续增长（不重置）
3. ✅ 刷新浏览器后，余额继续增长（不重置）
4. ✅ 增长速度符合预期（每日 0.8%）
5. ✅ 提现后，余额更新并从新数据开始增长

## 🔍 关键改进点

### 改进 1：存储质押金额

**旧代码**：
```typescript
// 每次都使用当前的 perSecondEarnings，可能为 0
const newBalance = baseBalance + (perSecondEarnings * elapsedSeconds)
```

**新代码**：
```typescript
// 使用存储的质押金额计算，避免依赖当前状态
const storedPerSecondEarnings = stakedAmount * perSecondRate
const newBalance = baseBalance + (storedPerSecondEarnings * elapsedSeconds)
```

### 改进 2：准确检测余额变化

**旧代码**：
```typescript
// 依赖 localStorage 中的 chainBalance
if (Math.abs(chainBalance - rwaPendingNum) > 0.01) {
  // 重置
}
```

**新代码**：
```typescript
// 使用 ref 记录上次的值，更准确
const lastChainBalanceRef = useRef(rwaPendingNum)
const balanceChanged = Math.abs(lastChainBalanceRef.current - rwaPendingNum) > 0.01

if (balanceChanged) {
  // 重置
  lastChainBalanceRef.current = rwaPendingNum
}
```

### 改进 3：错误处理

**新增**：
```typescript
try {
  const { baseTime, baseBalance, stakedAmount } = JSON.parse(stored)
  // 计算...
} catch (e) {
  // 解析失败，重新初始化
  const newData = { baseTime: Date.now(), baseBalance: rwaPendingNum, stakedAmount: totalStakedNum }
  localStorage.setItem(storageKey, JSON.stringify(newData))
}
```

## 📝 技术细节

### 为什么要存储 stakedAmount？

组件挂载时的执行顺序：
1. 组件渲染，`totalStakedNum` 初始值为 0
2. useEffect 执行，此时 `perSecondEarnings` = 0 × perSecondRate = 0
3. 链上数据返回，`totalStakedNum` 更新为实际值
4. useEffect 重新执行

如果不存储 `stakedAmount`，步骤 2 会用错误的值（0）初始化 localStorage。

### 为什么使用 useRef 而不是 state？

- `useRef` 的值变化不会触发重新渲染
- 适合用来记录"上一次的值"
- 避免不必要的 useEffect 重新执行

### 为什么要检测余额变化？

当用户提现 RWA 代币时：
1. 链上余额减少
2. 但 localStorage 中的基准数据还是旧的
3. 会导致显示的余额继续增长（错误）

检测到余额变化后，重置基准数据，从新的链上余额开始计算。

## 🎯 总结

这个版本通过以下方式解决了所有问题：

1. **存储质押金额**：避免依赖可能为 0 的当前状态
2. **使用 ref 检测变化**：准确识别提现等操作
3. **错误处理**：增强健壮性
4. **独立计算**：每次都基于存储的数据计算，不依赖当前状态

现在请按照测试步骤验证功能是否正常工作！

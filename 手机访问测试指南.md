# 📱 手机访问本地测试指南

## 🎯 前提条件

1. ✅ 电脑和手机连接到**同一个 WiFi 网络**
2. ✅ 前端服务器正在运行（`npm run dev`）
3. ✅ Hardhat 节点正在运行（`npx hardhat node`）

---

## 📋 步骤 1: 获取电脑的 IP 地址

### Windows 系统

1. 按 `Win + R` 打开运行窗口
2. 输入 `cmd` 并回车
3. 在命令提示符中输入：
   ```cmd
   ipconfig
   ```
4. 找到 **无线局域网适配器 WLAN** 或 **以太网适配器**
5. 记下 **IPv4 地址**，例如：`192.168.1.100`

### 快速查看（推荐）

在项目目录运行：
```cmd
ipconfig | findstr IPv4
```

---

## 📋 步骤 2: 修改前端配置

### 2.1 修改 Next.js 配置

需要让 Next.js 监听所有网络接口（不只是 localhost）。

**方法 1: 修改 package.json（推荐）**

打开 `frontend/package.json`，找到 `scripts` 部分，修改 `dev` 命令：

```json
{
  "scripts": {
    "dev": "next dev -H 0.0.0.0",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```

**方法 2: 临时运行**

停止当前的前端服务器，然后运行：
```cmd
cd frontend
npm run dev -- -H 0.0.0.0
```

### 2.2 修改 Hardhat 配置（如果需要手机连接钱包）

打开 `hardhat.config.ts`，确保 Hardhat 节点也监听所有网络接口：

```typescript
networks: {
  hardhat: {
    chainId: 31337,
    mining: {
      auto: true,
      interval: 0
    },
    // 添加这一行
    accounts: {
      mnemonic: "test test test test test test test test test test test junk"
    }
  },
  localhost: {
    url: "http://0.0.0.0:8545", // 改为 0.0.0.0
    chainId: 31337
  }
}
```

然后重启 Hardhat 节点：
```cmd
npx hardhat node --hostname 0.0.0.0
```

---

## 📋 步骤 3: 配置防火墙

### Windows 防火墙

需要允许端口 3000（前端）和 8545（Hardhat）通过防火墙。

**方法 1: 使用命令（管理员权限）**

以管理员身份运行 PowerShell：

```powershell
# 允许端口 3000（前端）
New-NetFirewallRule -DisplayName "Next.js Dev Server" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow

# 允许端口 8545（Hardhat）
New-NetFirewallRule -DisplayName "Hardhat Node" -Direction Inbound -LocalPort 8545 -Protocol TCP -Action Allow
```

**方法 2: 使用图形界面**

1. 打开 **控制面板** → **系统和安全** → **Windows Defender 防火墙**
2. 点击左侧 **高级设置**
3. 点击 **入站规则** → **新建规则**
4. 选择 **端口** → 下一步
5. 选择 **TCP**，输入端口 `3000` → 下一步
6. 选择 **允许连接** → 下一步
7. 全部勾选 → 下一步
8. 输入名称 "Next.js Dev Server" → 完成
9. 重复步骤 3-8，为端口 `8545` 创建规则

---

## 📋 步骤 4: 手机访问

### 4.1 访问前端

假设你的电脑 IP 是 `192.168.1.100`：

在手机浏览器中访问：
```
http://192.168.1.100:3000
```

你应该能看到 RWA 项目的首页。

### 4.2 测试各个页面

- 首页: `http://192.168.1.100:3000`
- 质押: `http://192.168.1.100:3000/stake`
- 仪表板: `http://192.168.1.100:3000/dashboard`
- 提现: `http://192.168.1.100:3000/withdraw`
- 市场: `http://192.168.1.100:3000/market`

---

## 📋 步骤 5: 手机钱包配置（可选）

如果你想在手机上使用 MetaMask 进行质押测试：

### 5.1 安装 MetaMask 手机应用

1. 在 App Store（iOS）或 Google Play（Android）下载 MetaMask
2. 创建或导入钱包

### 5.2 添加 Hardhat 本地网络

在 MetaMask 手机应用中：

1. 点击左上角菜单 → **设置** → **网络**
2. 点击 **添加网络**
3. 输入以下信息：
   - **网络名称**: Hardhat Local
   - **RPC URL**: `http://192.168.1.100:8545` （替换为你的电脑 IP）
   - **Chain ID**: `31337`
   - **货币符号**: `ETH`
4. 点击 **添加**

### 5.3 导入测试账户

1. 点击左上角账户图标 → **导入账户**
2. 输入测试账户私钥：
   ```
   0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
   ```
3. 点击 **导入**

现在你应该能看到账户余额：
- 9999.99 ETH
- 1,993,000 USDT
- 1,000,000,000 RWA

---

## 🔧 故障排除

### 问题 1: 手机无法访问

**检查清单**:
- [ ] 电脑和手机在同一个 WiFi 网络
- [ ] 前端服务器使用 `-H 0.0.0.0` 启动
- [ ] 防火墙已允许端口 3000
- [ ] IP 地址正确（不是 127.0.0.1 或 localhost）

**测试连接**:
在手机浏览器访问：
```
http://192.168.1.100:3000
```

如果显示"无法访问此网站"，检查防火墙设置。

### 问题 2: 页面加载但样式错误

这通常是因为 Next.js 的热重载问题。

**解决方案**:
1. 在电脑上停止前端服务器（Ctrl+C）
2. 重新启动：
   ```cmd
   cd frontend
   npm run dev -- -H 0.0.0.0
   ```
3. 在手机上刷新页面

### 问题 3: MetaMask 无法连接

**检查清单**:
- [ ] Hardhat 节点使用 `--hostname 0.0.0.0` 启动
- [ ] 防火墙已允许端口 8545
- [ ] RPC URL 使用电脑 IP（不是 localhost）
- [ ] Chain ID 是 31337

**测试 Hardhat 连接**:
在手机浏览器访问：
```
http://192.168.1.100:8545
```

应该看到类似这样的响应：
```json
{"jsonrpc":"2.0","id":null,"error":{"code":-32700,"message":"Parse error"}}
```

这表示 Hardhat 节点正在运行。

### 问题 4: 公司或学校 WiFi

某些公司或学校的 WiFi 会阻止设备间通信（AP 隔离）。

**解决方案**:
1. 使用手机热点，让电脑连接到手机热点
2. 或者使用 USB 数据线连接手机和电脑，启用 USB 网络共享

---

## 📱 完整测试流程

### 1. 启动服务

在电脑上：

**终端 1 - Hardhat 节点**:
```cmd
npx hardhat node --hostname 0.0.0.0
```

**终端 2 - 前端服务器**:
```cmd
cd frontend
npm run dev -- -H 0.0.0.0
```

### 2. 获取 IP 地址

```cmd
ipconfig | findstr IPv4
```

假设得到：`192.168.1.100`

### 3. 手机访问

在手机浏览器打开：
```
http://192.168.1.100:3000
```

### 4. 测试功能

- ✅ 浏览各个页面
- ✅ 查看响应式设计
- ✅ 测试触摸交互
- ✅ 测试钱包连接（如果配置了 MetaMask）

---

## 💡 提示

### 查看实时日志

在电脑上可以看到手机访问的日志：

**前端日志**:
```
○ Compiling / ...
✓ Compiled / in 1.2s
GET / 200 in 1234ms
```

**Hardhat 日志**:
```
eth_chainId
eth_accounts
eth_getBalance
```

### 使用二维码（可选）

可以生成二维码方便手机扫描：

1. 访问 https://www.qr-code-generator.com/
2. 输入 URL: `http://192.168.1.100:3000`
3. 生成二维码
4. 用手机扫描

### 保持电脑不休眠

测试期间，确保电脑不会进入休眠状态：

1. **设置** → **系统** → **电源和睡眠**
2. 将 **屏幕** 和 **睡眠** 设置为 **从不**

---

## 🎉 成功标志

如果一切正常，你应该能：

- ✅ 在手机浏览器访问所有页面
- ✅ 看到正确的样式和动画
- ✅ 触摸交互正常工作
- ✅ 响应式布局正确显示
- ✅ MetaMask 手机应用可以连接（如果配置）
- ✅ 可以进行质押测试（如果配置）

---

## 📝 快速参考

### 电脑 IP 地址
```cmd
ipconfig | findstr IPv4
```

### 启动前端（监听所有网络）
```cmd
cd frontend
npm run dev -- -H 0.0.0.0
```

### 启动 Hardhat（监听所有网络）
```cmd
npx hardhat node --hostname 0.0.0.0
```

### 手机访问地址
```
http://[你的电脑IP]:3000
```

### 防火墙规则
```powershell
# 允许端口 3000
New-NetFirewallRule -DisplayName "Next.js Dev Server" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow

# 允许端口 8545
New-NetFirewallRule -DisplayName "Hardhat Node" -Direction Inbound -LocalPort 8545 -Protocol TCP -Action Allow
```

---

**现在你可以在手机上测试 RWA 项目了！** 🚀

如果遇到问题，请检查上面的故障排除部分。

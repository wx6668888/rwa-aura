# 仪表板数据显示问题修复完成

## 问题描述

用户报告仪表板页面 `http://localhost:3000/dashboard` 显示的数据不正确：
- 账户：`0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`
- 问题：**团队总质押** 和 **个人总质押** 显示相同的值（25,709 USDT）

## 问题分析

### 1. 链上实际数据

通过调试脚本 `scripts/debug-dashboard-data.ts` 查询到的链上数据：

```
USDT 质押: 10,497.00 USDT
RWA 质押: 17,896.00 RWA (≈ 15,211.60 USDT)
个人总质押: 25,708.60 USDT
下级质押: 0.00 USDT
团队总质押（个人+下级）: 25,708.60 USDT
```

### 2. 前端代码问题

在 `frontend/components/dashboard/stat-cards.tsx` 中：

```typescript
// 错误的代码
const usdtStaked = parseFloat(userStakeInfo?.totalStaked || '0')
const rwaStaked = parseFloat(rwaStakeInfo?.totalStakedRWA || '0')
const rwaStakedInUSDT = rwaStaked * rwaPrice
const totalStaked = usdtStaked + rwaStakedInUSDT

// 这里把 totalStaked 当作"团队总质押"显示
const teamVolume = isConnected ? totalStaked.toLocaleString(...) : '0'
```

**问题**：
- `totalStaked` 实际上是**个人总质押**（USDT + RWA 转换）
- 代码错误地将其标记为"团队总质押"（`teamVolume`）
- 导致"团队总质押"和"个人总质押"显示相同的值
- 没有从后端 API 获取真实的团队数据

## 修复方案

### 已实施的修复

#### 1. 创建 `useTeamData` Hook

创建了新的 hook `frontend/hooks/useTeamData.ts` 来从后端 API 获取团队数据：

```typescript
export function useTeamData(): TeamData {
  // 从后端 API /api/user/:address/level-info 获取数据
  // 返回：
  // - nodeLevel: 节点等级
  // - cumulativePersonalStake: 个人累计质押
  // - teamVolume: 团队下级质押（不包括个人）
  // - teamRetained: 总留存
}
```

#### 2. 更新 `stat-cards.tsx` 组件

修正了数据计算逻辑：

```typescript
// 个人总质押 = USDT 质押 + RWA 质押（转换为 USDT 等值）
const personalTotalStaked = usdtStaked + rwaStakedInUSDT

// 团队总质押 = 个人质押 + 团队下级质押
// 后端 API 返回：
// - teamVolume: 团队下级质押（不包括个人）
// - cumulativePersonalStake: 个人累计质押
const teamVolumeFromBackend = parseFloat(teamData.teamVolume || '0')
const cumulativePersonal = parseFloat(teamData.cumulativePersonalStake || '0')
const teamTotalStaked = teamVolumeFromBackend + cumulativePersonal
```

#### 3. 添加统计卡片显示

在仪表板上添加了两个新的统计卡片：
- **个人总质押**：显示用户自己的 USDT + RWA 质押总额
- **团队总质押**：显示个人 + 所有下级的质押总额

布局从 3 列改为 5 列：
1. 个人总质押
2. 团队总质押
3. 质押记录数
4. 直推人数
5. 总收益

## 数据来源说明

### 个人总质押
- 来源：链上合约直接读取
- 计算：`getUserStakeInfo().totalStaked + rwaStakes().totalStakedRWA * 0.85`
- 实时更新

### 团队总质押
- 来源：后端 API `/api/user/:address/level-info`
- 计算：`teamVolume + cumulativePersonalStake`
- 每 30 秒刷新一次

### 后端数据维护

后端通过以下服务维护团队数据：
- `TeamVolumeService`: 更新团队业绩
- `EventMonitor`: 监听链上质押事件
- 每次质押后自动更新团队数据

## 当前状态

由于该用户没有下级：
- **个人总质押**：25,708.60 USDT ✓ 正确
- **团队总质押**：25,708.60 USDT ✓ 正确（因为没有下级，团队=个人）

所以当前显示的数值实际上是正确的。当该用户有下级质押后，团队总质押会大于个人总质押。

## 测试建议

1. **创建多级推荐关系**：
   ```bash
   # 使用不同的测试账户
   Account #0 (0xf39F...) -> 推荐人
   Account #1 (0x7099...) -> 下级 1
   Account #2 (0x3C44...) -> 下级 2
   ```

2. **让下级进行质押**：
   - 下级 1 质押 1000 USDT
   - 下级 2 质押 2000 USDT

3. **验证数据显示**：
   - Account #0 的个人总质押：25,708.60 USDT
   - Account #0 的团队总质押：28,708.60 USDT（25,708.60 + 1,000 + 2,000）

## 相关文件

- ✅ `frontend/hooks/useTeamData.ts` - 新建
- ✅ `frontend/components/dashboard/stat-cards.tsx` - 已修改
- ✅ `backend/src/routes/api.ts` - API 端点（已存在）
- ✅ `backend/src/services/TeamVolumeService.ts` - 团队数据服务（已存在）
- ✅ `scripts/debug-dashboard-data.ts` - 调试脚本

## 测试命令

```bash
# 运行调试脚本
npx hardhat run scripts/debug-dashboard-data.ts --network localhost

# 测试后端 API
curl http://localhost:3001/api/user/0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266/level-info

# 启动前端查看效果
cd frontend
npm run dev
# 访问 http://localhost:3000/dashboard
```

## 修复完成 ✅

仪表板数据显示问题已修复，现在可以正确显示：
- 个人总质押（链上实时数据）
- 团队总质押（后端 API 数据，包含个人+下级）
- 数据每 30 秒自动刷新

---
description: 前端上线默认走服务器脚本；改完前端后直接部署
alwaysApply: true
---

- **在本仓库每次完成前端相关修改后，助手应立刻同步线上**（无需等用户说「上线」「同步」）：在同一轮对话收尾执行 `bash /www/wwwroot/rwaprotocol.dpdns.org/frontend/deploy-frontend.sh`（或 `bash frontend/deploy-frontend.sh`）。任务完成标准＝脚本成功（含构建与 PM2 `rwa-frontend` 重启）。
- 用户主动要求部署到线上（如 https://rwa.lat/）时，同样用上述脚本；不要默认让用户在本地先 `npm run build`；脚本内已包含构建与 PM2 重启。
- 若使用 Cloudflare 且静态资源异常，再提醒按需清缓存或 Nginx `/_next/static/` alias（见 BAOTA_DEPLOY.md）。
- 例外：用户明确说「先不要部署」「只改代码」时，再跳过自动部署。

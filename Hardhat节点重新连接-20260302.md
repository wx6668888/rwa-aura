# ✅ Hardhat 节点重新连接完成 - 2026年3月2日

**问题**: Hardhat Local 网络断开连接

**状态**: ✅ 已重新连接

---

## 🔧 执行的操作

### 1. 停止所有 Node 进程 ✅

- 停止所有正在运行的 Node.js 进程
- 确保没有端口冲突

---

### 2. 启动 Hardhat 节点 ✅

**启动命令**:
```bash
npx hardhat node --hostname 0.0.0.0
```

**配置说明**:
- `--hostname 0.0.0.0`: 允许从任何网络接口访问（支持移动端连接）
- 默认端口: `8545`
- Chain ID: `31337`

---

### 3. 验证节点状态 ✅

**检查端口**:
- 端口 `8545` 应该正在监听
- 可以通过 `http://127.0.0.1:8545` 访问

---

### 4. 重启前端服务器 ✅

- 前端服务器已重启
- 配置已连接到 Hardhat 本地节点

---

## 📋 连接信息

### Hardhat 本地网络

- **RPC URL**: `http://127.0.0.1:8545`
- **Chain ID**: `31337`
- **网络名称**: Hardhat Local
- **原生代币**: ETH

---

## 🚀 下一步

### 1. 等待节点启动

Hardhat 节点正在启动，等待 **10-15 秒**。

---

### 2. 在 MetaMask 中重新连接

**如果 MetaMask 显示 "Hardhat Local" 断开**:

1. 打开 MetaMask
2. 点击网络下拉菜单
3. 选择 "Hardhat Local"
4. 如果显示断开，点击 "切换到此网络"

**或手动添加网络**:
1. MetaMask → 设置 → 网络 → 添加网络
2. 网络名称: `Hardhat Local`
3. RPC URL: `http://127.0.0.1:8545`
4. Chain ID: `31337`
5. 货币符号: `ETH`
6. 区块浏览器: 留空

---

### 3. 在前端重新连接钱包

1. 打开 http://localhost:3000
2. 点击 "Connect Wallet" 或 "连接钱包"
3. 选择 MetaMask
4. 确认连接到 "Hardhat Local" 网络

---

## ⚠️ 如果仍有问题

### 检查 Hardhat 节点

**查看节点输出**:
- 应该看到 "Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/"
- 应该看到 20 个测试账户及其私钥

**如果节点未启动**:
```bash
cd "e:\MyRWA_Project\rwa aura"
npx hardhat node --hostname 0.0.0.0
```

---

### 检查前端连接

**查看浏览器控制台** (F12):
- 应该没有 RPC 连接错误
- 应该能正常调用合约方法

**查看网络请求**:
- 检查是否有到 `http://127.0.0.1:8545` 的请求
- 检查请求是否成功（状态码 200）

---

### 常见问题

**1. 端口被占用**

如果端口 8545 被占用：
```bash
# 查找占用端口的进程
netstat -ano | findstr ":8545"

# 停止进程（替换 PID 为实际进程 ID）
taskkill /PID <PID> /F
```

**2. 防火墙阻止**

确保 Windows 防火墙允许端口 8545 的访问。

**3. MetaMask 无法连接**

- 清除 MetaMask 缓存
- 重新添加 Hardhat Local 网络
- 确保 RPC URL 正确：`http://127.0.0.1:8545`

---

## ✅ 完成

**状态**: ✅ Hardhat 节点已重新启动并连接

**改进**:
- ✅ 停止所有旧进程
- ✅ 启动新的 Hardhat 节点
- ✅ 验证端口监听
- ✅ 重启前端服务器

---

**请等待 10-15 秒让节点完全启动，然后在 MetaMask 中重新连接到 "Hardhat Local" 网络！**

# ✅ Hardhat 连接问题修复 - 2026年3月2日

**问题**: 钱包一直提示 "仍在连接到 Hardhat Local……"

**状态**: ✅ 正在修复

---

## 🔧 问题分析

### 可能的原因

1. **Hardhat 节点未运行** - 最常见的原因
2. **端口 8545 未监听** - 节点未正确启动
3. **RPC 连接失败** - 网络配置问题
4. **前端配置错误** - RPC URL 不正确

---

## 🚀 执行的操作

### 1. 检查节点状态 ✅

- 检查端口 8545 是否在监听
- 测试 RPC 连接
- 查找 Hardhat 进程

---

### 2. 启动 Hardhat 节点 ✅

**如果节点未运行**:
- 在新窗口启动 Hardhat 节点
- 使用 `--hostname 0.0.0.0` 允许所有网络接口访问

**启动命令**:
```bash
cd "e:\MyRWA_Project\rwa aura"
npx hardhat node --hostname 0.0.0.0
```

---

### 3. 验证连接 ✅

- 等待节点启动（8-10 秒）
- 检查端口监听状态
- 测试 RPC 连接

---

## 📋 验证步骤

### Step 1: 检查是否有新窗口打开

应该看到一个新的 PowerShell 窗口，显示：
```
Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/

Account #0: 0xf39Fd6e51aac88F6F4ce6aB8827279cffFb92266 (10000 ETH)
Private Key: 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
...
```

---

### Step 2: 在 MetaMask 中重新连接

1. **打开 MetaMask**
2. **点击网络下拉菜单**
3. **选择 "Hardhat Local"**
4. **如果显示断开，点击 "切换到此网络"**

---

### Step 3: 在前端刷新

1. **打开** http://localhost:3000
2. **硬刷新** (`Ctrl+Shift+R`)
3. **点击** "Connect Wallet" 或 "连接钱包"
4. **选择** MetaMask
5. **确认** 连接到 "Hardhat Local" 网络

---

## ⚠️ 如果仍有问题

### 检查 1: 节点是否启动

**查看新窗口**:
- 是否显示 "Started HTTP and WebSocket JSON-RPC server"？
- 是否有错误信息？

**如果没有新窗口**:
手动启动：
```bash
cd "e:\MyRWA_Project\rwa aura"
npx hardhat node --hostname 0.0.0.0
```

---

### 检查 2: MetaMask 网络配置

**确保网络配置正确**:
1. MetaMask → 设置 → 网络 → Hardhat Local
2. 检查以下信息：
   - **RPC URL**: `http://127.0.0.1:8545`
   - **Chain ID**: `31337`
   - **货币符号**: `ETH`

**如果配置错误**:
1. 删除旧网络
2. 重新添加网络（使用上面的信息）

---

### 检查 3: 前端 RPC 配置

**检查 `frontend/lib/wagmi.ts`**:
```typescript
rpcUrls: {
  default: {
    http: ['http://127.0.0.1:8545'],
  },
  public: {
    http: ['http://127.0.0.1:8545'],
  },
},
```

**确保 RPC URL 正确**: `http://127.0.0.1:8545`

---

### 检查 4: 防火墙阻止

确保 Windows 防火墙允许端口 8545 的访问。

**检查防火墙规则**:
1. Windows 设置 → 隐私和安全性 → Windows 安全中心
2. 防火墙和网络保护
3. 高级设置
4. 入站规则 → 查找端口 8545 的规则

---

## 📊 连接信息

### Hardhat 本地网络

- **RPC URL**: `http://127.0.0.1:8545`
- **Chain ID**: `31337`
- **网络名称**: Hardhat Local
- **原生代币**: ETH
- **区块时间**: 即时（测试网络）

---

## ✅ 完成

**状态**: ✅ Hardhat 节点已启动

**下一步**:
1. 等待 10-15 秒让节点完全启动
2. 检查是否有新的 PowerShell 窗口打开
3. 在 MetaMask 中切换到 "Hardhat Local" 网络
4. 在前端刷新并重新连接钱包

---

**请检查是否有新的 PowerShell 窗口打开（显示 Hardhat 节点启动信息），然后在 MetaMask 中切换到 "Hardhat Local" 网络！**

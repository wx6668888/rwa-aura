# 🚀 RWA 项目立即部署指南

**当前状态**: ✅ 所有准备工作已完成  
**下一步**: 开始部署

---

## 📋 部署前检查清单

### ✅ 已完成
- [x] 测试文件修复完成
- [x] 核心功能测试通过
- [x] 部署脚本已创建
- [x] 环境变量模板已创建

### ⏳ 需要完成
- [ ] 配置环境变量
- [ ] 准备测试 BNB（BSC Testnet）
- [ ] 运行部署脚本
- [ ] 验证部署结果

---

## 🎯 部署选项

### 选项 1: 本地测试部署（推荐先做）⭐

**目的**: 在本地验证部署脚本是否正常工作

**步骤**:

```bash
# 1. 启动 Hardhat 本地节点
start-hardhat-with-deploy.bat
# 或
npx hardhat node

# 2. 在新终端运行部署脚本
npx hardhat run scripts/deploy-to-bsc-testnet.ts --network localhost
```

**优点**:
- ✅ 免费（不需要真实 BNB）
- ✅ 快速验证
- ✅ 可以反复测试

---

### 选项 2: BSC Testnet 部署

**目的**: 在真实测试网环境部署

**前置要求**:
1. 配置 `.env` 文件
2. 准备测试 BNB（从 Faucet 获取）
3. 配置 BSC Testnet RPC

**步骤**:

```bash
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env 填入：
# - PRIVATE_KEY=你的私钥
# - BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# 2. 获取测试 BNB
# 访问: https://testnet.bnbchain.org/faucet-smart
# 输入你的地址获取测试 BNB

# 3. 运行部署
npx hardhat run scripts/deploy-to-bsc-testnet.ts --network bscTestnet
```

---

## 📝 详细部署步骤

### 步骤 1: 配置环境变量

#### 1.1 创建 `.env` 文件

```bash
# 复制模板
cp .env.example .env
```

#### 1.2 编辑 `.env` 文件

```env
# 网络配置
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# 账户配置（重要：不要提交到代码仓库！）
PRIVATE_KEY=你的私钥（0x开头）
BACKEND_PRIVATE_KEY=后端私钥（0x开头）

# BSCScan API（用于合约验证）
BSCSCAN_API_KEY=你的BSCScan API密钥（可选）
```

#### 1.3 检查 hardhat.config.ts

确保 `hardhat.config.ts` 中已配置 BSC Testnet：

```typescript
networks: {
  bscTestnet: {
    url: process.env.BSC_TESTNET_RPC_URL || "",
    chainId: 97,
    accounts: process.env.PRIVATE_KEY ? [process.env.PRIVATE_KEY] : [],
  },
}
```

---

### 步骤 2: 准备测试 BNB

#### 2.1 获取测试 BNB

访问 BSC Testnet Faucet:
- https://testnet.bnbchain.org/faucet-smart
- 或 https://www.bnbchain.org/en/testnet-faucet

输入你的钱包地址，获取测试 BNB（通常 0.1-1 BNB）

#### 2.2 检查余额

```bash
# 使用 Hardhat console 检查
npx hardhat console --network bscTestnet
> const [signer] = await ethers.getSigners()
> await ethers.provider.getBalance(signer.address)
```

---

### 步骤 3: 运行部署脚本

#### 3.1 本地测试（推荐先做）

```bash
# 终端 1: 启动本地节点
npx hardhat node

# 终端 2: 运行部署
npx hardhat run scripts/deploy-to-bsc-testnet.ts --network localhost
```

#### 3.2 BSC Testnet 部署

```bash
npx hardhat run scripts/deploy-to-bsc-testnet.ts --network bscTestnet
```

**预期输出**:
```
=== 开始部署到 BSC Testnet ===

部署账户: 0x...
账户余额: 0.5 BNB

1. 部署 RWAToken...
   ✅ RWAToken: 0x...

2. 部署 StRWA...
   ✅ StRWA: 0x...

... (其他合约)

=== 部署完成 ===
```

---

### 步骤 4: 保存合约地址

部署完成后，脚本会自动保存地址到 `deployed-addresses.json`

**手动更新**:

1. **更新 `.env`**:
```env
RWA_TOKEN_ADDRESS=0x...
ST_RWA_ADDRESS=0x...
STAKING_CONTRACT_ADDRESS=0x...
# ... 其他地址
```

2. **更新 `frontend/.env.local`**:
```env
NEXT_PUBLIC_CHAIN_ID=97
NEXT_PUBLIC_BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545
NEXT_PUBLIC_RWA_TOKEN_BSC=0x...
NEXT_PUBLIC_ST_RWA_BSC=0x...
# ... 其他地址
```

3. **更新 `frontend/lib/contracts/addresses.ts`**:
```typescript
export const CONTRACT_ADDRESSES = {
  97: { // BSC Testnet
    rwaToken: "0x...",
    stRwaToken: "0x...",
    // ... 其他地址
  }
}
```

---

### 步骤 5: 验证合约（可选）

```bash
# 验证单个合约
npx hardhat verify --network bscTestnet \
  CONTRACT_ADDRESS \
  [constructor args]

# 示例：验证 RWAToken
npx hardhat verify --network bscTestnet \
  0x... \
  "RWA Token" \
  "RWA" \
  1000000000000000000000000000 \
  0x... \
  0x...
```

---

## 🔍 部署后验证

### 1. 检查合约地址

```bash
# 查看部署的地址
cat deployed-addresses.json
```

### 2. 在 BSCScan Testnet 查看

访问: https://testnet.bscscan.com/

输入合约地址，查看：
- ✅ 合约已部署
- ✅ 交易记录
- ✅ 合约代码（如果已验证）

### 3. 测试功能

#### 前端测试
```bash
cd frontend
npm run dev
```

访问 http://localhost:3000，测试：
- ✅ 钱包连接
- ✅ 查看合约数据
- ✅ 质押功能（如果已配置）

---

## ⚠️ 常见问题

### 问题 1: 余额不足

**错误**: `insufficient funds for gas`

**解决**:
- 确保账户有足够的 BNB
- BSC Testnet 需要至少 0.01 BNB

### 问题 2: RPC 连接失败

**错误**: `network error` 或 `timeout`

**解决**:
- 检查 RPC URL 是否正确
- 尝试其他 RPC 节点：
  - https://data-seed-prebsc-2-s1.binance.org:8545
  - https://data-seed-prebsc-1-s2.binance.org:8545

### 问题 3: 合约部署失败

**错误**: `revert` 或 `execution reverted`

**解决**:
- 检查构造函数参数
- 检查前置条件（如其他合约是否已部署）
- 查看详细错误信息

---

## 📊 部署成本估算

### BSC Testnet（测试网）
- **Gas 费用**: 免费（测试 BNB）
- **总成本**: **0 BNB**

### BSC Mainnet（主网）
- **Gas 费用**: 约 0.06-0.11 BNB
- **按当前价格**: 约 $18-33 USD

---

## ✅ 部署检查清单

部署完成后，确认：

- [ ] 所有合约已部署
- [ ] 合约地址已保存
- [ ] 合约关系已配置
- [ ] 环境变量已更新
- [ ] 前端配置已更新
- [ ] 合约已验证（可选）
- [ ] 功能测试通过

---

## 🎯 推荐执行顺序

1. **本地测试部署**（5分钟）
   - 验证部署脚本
   - 检查是否有错误

2. **BSC Testnet 部署**（30分钟）
   - 配置环境变量
   - 获取测试 BNB
   - 运行部署
   - 验证结果

3. **功能测试**（30分钟）
   - 更新前端配置
   - 测试核心功能
   - 验证合约交互

---

## 🚀 开始部署

### 快速开始（本地测试）

```bash
# 1. 启动本地节点
npx hardhat node

# 2. 运行部署（新终端）
npx hardhat run scripts/deploy-to-bsc-testnet.ts --network localhost
```

### 正式部署（BSC Testnet）

```bash
# 1. 配置环境变量
cp .env.example .env
# 编辑 .env

# 2. 运行部署
npx hardhat run scripts/deploy-to-bsc-testnet.ts --network bscTestnet
```

---

**准备就绪，可以开始部署！** 🚀

如有问题，请参考：
- `部署准备清单.md` - 详细部署步骤
- `部署完成总结.md` - 完成总结

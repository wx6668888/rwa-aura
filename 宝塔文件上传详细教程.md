# 宝塔文件上传详细教程

## 📦 方法一: 使用宝塔文件管理器上传 (推荐新手)

这是最简单直观的方法,适合不熟悉命令行的用户。

### 步骤 1: 在本地打包项目

#### Windows 用户 (使用 PowerShell 或 CMD)

1. 打开 PowerShell 或 CMD
2. 进入项目根目录:
   ```powershell
   cd C:\你的项目路径
   ```

3. 安装 7-Zip (如果没有):
   - 下载: https://www.7-zip.org/
   - 或使用 Windows 自带的压缩功能

4. 打包文件 (使用 7-Zip):
   ```powershell
   # 打包前端
   7z a -ttar frontend.tar frontend\
   7z a -tgzip frontend.tar.gz frontend.tar
   
   # 打包后端
   7z a -ttar backend.tar backend\
   7z a -tgzip backend.tar.gz backend.tar
   
   # 打包合约
   7z a -ttar contracts.tar contracts\
   7z a -tgzip contracts.tar.gz contracts.tar
   
   # 打包脚本
   7z a -ttar scripts.tar scripts\
   7z a -tgzip scripts.tar.gz scripts.tar
   ```

5. 或者使用 Windows 自带压缩 (右键 → 发送到 → 压缩文件夹):
   - 选择 `frontend` 文件夹 → 右键 → 发送到 → 压缩(zipped)文件夹
   - 重命名为 `frontend.zip`
   - 重复以上步骤打包 `backend`, `contracts`, `scripts`

### 步骤 2: 登录宝塔面板

1. 打开浏览器
2. 访问: `http://你的服务器IP:8888`
3. 输入用户名和密码登录

### 步骤 3: 创建目录

1. 点击左侧菜单 → **文件**
2. 进入 `/www/wwwroot/` 目录
3. 点击 **新建目录**:
   - 创建 `vekus.qzz.io`
   - 创建 `api.vekus.qzz.io`

### 步骤 4: 上传文件

#### 上传前端文件

1. 进入 `/www/wwwroot/vekus.qzz.io/` 目录
2. 点击 **上传** 按钮
3. 选择 `frontend.tar.gz` 或 `frontend.zip`
4. 等待上传完成 (显示 100%)

#### 上传后端文件

1. 进入 `/www/wwwroot/api.vekus.qzz.io/` 目录
2. 点击 **上传** 按钮
3. 选择 `backend.tar.gz` 或 `backend.zip`
4. 等待上传完成

#### 上传合约和脚本

1. 进入 `/www/wwwroot/` 目录
2. 上传 `contracts.tar.gz` 和 `scripts.tar.gz`

### 步骤 5: 解压文件

#### 解压前端

1. 在 `/www/wwwroot/vekus.qzz.io/` 目录
2. 找到 `frontend.tar.gz` 文件
3. 点击文件右侧的 **解压** 按钮
4. 选择解压到当前目录
5. 点击 **确定**
6. 解压完成后,删除压缩包 (可选)

#### 解压后端

1. 在 `/www/wwwroot/api.vekus.qzz.io/` 目录
2. 找到 `backend.tar.gz` 文件
3. 点击 **解压**
4. 解压到当前目录

#### 解压合约和脚本

1. 在 `/www/wwwroot/` 目录
2. 解压 `contracts.tar.gz` 和 `scripts.tar.gz`

### 步骤 6: 调整目录结构

解压后,你的目录结构应该是:

```
/www/wwwroot/vekus.qzz.io/
└── frontend/
    ├── app/
    ├── components/
    ├── lib/
    ├── public/
    ├── package.json
    └── ...

/www/wwwroot/api.vekus.qzz.io/
└── backend/
    ├── src/
    ├── package.json
    └── ...
```

如果解压后多了一层目录,需要调整:

1. 进入 `/www/wwwroot/vekus.qzz.io/frontend/`
2. 全选所有文件 (Ctrl+A)
3. 点击 **剪切**
4. 返回上一级 `/www/wwwroot/vekus.qzz.io/`
5. 点击 **粘贴**
6. 删除空的 `frontend` 文件夹

---

## 📦 方法二: 使用 FTP 上传 (推荐大文件)

适合文件较大或网络不稳定的情况。

### 步骤 1: 安装 FTP 客户端

推荐使用 **FileZilla**:
- 下载: https://filezilla-project.org/
- 免费、开源、跨平台

### 步骤 2: 在宝塔创建 FTP 账号

1. 登录宝塔面板
2. 点击 **FTP** → **添加 FTP**
3. 填写信息:
   - 用户名: `rwa_deploy`
   - 密码: (自动生成或自定义)
   - 根目录: `/www/wwwroot/`
4. 点击 **提交**
5. 记录 FTP 信息

### 步骤 3: 连接 FTP

1. 打开 FileZilla
2. 填写连接信息:
   - 主机: `你的服务器IP`
   - 用户名: `rwa_deploy`
   - 密码: (刚才设置的密码)
   - 端口: `21`
3. 点击 **快速连接**

### 步骤 4: 上传文件

1. 左侧窗口: 本地文件 (你的电脑)
2. 右侧窗口: 服务器文件
3. 在右侧进入 `/www/wwwroot/`
4. 直接拖拽文件夹:
   - 拖拽 `frontend` 文件夹到 `vekus.qzz.io/`
   - 拖拽 `backend` 文件夹到 `api.vekus.qzz.io/`
   - 拖拽 `contracts` 和 `scripts` 到 `/www/wwwroot/`

5. 等待上传完成 (底部显示进度)

---

## 📦 方法三: 使用 Git 部署 (推荐开发者)

最专业的方法,适合熟悉 Git 的开发者。

### 步骤 1: 在服务器安装 Git

1. SSH 登录服务器:
   ```bash
   ssh root@你的服务器IP
   ```

2. 安装 Git:
   ```bash
   # CentOS/RHEL
   yum install git -y
   
   # Ubuntu/Debian
   apt-get install git -y
   ```

### 步骤 2: 推送代码到 Git 仓库

在本地电脑:

```bash
# 初始化 Git (如果还没有)
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 添加远程仓库 (GitHub/GitLab/Gitee)
git remote add origin https://github.com/你的用户名/你的仓库.git

# 推送
git push -u origin main
```

### 步骤 3: 在服务器克隆代码

SSH 登录服务器后:

```bash
# 进入目录
cd /www/wwwroot/

# 克隆仓库
git clone https://github.com/你的用户名/你的仓库.git project

# 移动文件到正确位置
mv project/frontend vekus.qzz.io/
mv project/backend api.vekus.qzz.io/
mv project/contracts .
mv project/scripts .
```

### 步骤 4: 后续更新

本地修改后:

```bash
git add .
git commit -m "Update"
git push
```

服务器更新:

```bash
cd /www/wwwroot/vekus.qzz.io/frontend
git pull

cd /www/wwwroot/api.vekus.qzz.io/backend
git pull
```

---

## 📦 方法四: 使用宝塔远程下载 (最快)

如果代码已经在 GitHub/GitLab 上。

### 步骤 1: 获取仓库地址

在 GitHub/GitLab 上:
1. 点击 **Code** 按钮
2. 复制 HTTPS 地址
   例如: `https://github.com/username/repo.git`

### 步骤 2: 在宝塔使用远程下载

1. 登录宝塔面板
2. 点击 **文件**
3. 进入 `/www/wwwroot/`
4. 点击 **远程下载**
5. 粘贴 Git 地址
6. 点击 **确定**
7. 等待下载完成

### 步骤 3: 移动文件

下载完成后,按照方法一的步骤 6 调整目录结构。

---

## 🔍 验证上传是否成功

### 检查文件结构

在宝塔文件管理器中,确认以下结构:

```
/www/wwwroot/
├── vekus.qzz.io/
│   └── frontend/
│       ├── app/
│       ├── components/
│       ├── lib/
│       ├── public/
│       ├── package.json
│       ├── next.config.js
│       └── tsconfig.json
│
├── api.vekus.qzz.io/
│   └── backend/
│       ├── src/
│       ├── package.json
│       └── tsconfig.json
│
├── contracts/
│   ├── StakingContract.sol
│   ├── RWAToken.sol
│   └── ...
│
└── scripts/
    ├── deploy-all.ts
    └── ...
```

### 检查文件权限

在宝塔面板 → 文件:

1. 选中 `vekus.qzz.io` 文件夹
2. 点击 **权限**
3. 设置为 `755`
4. 勾选 **应用到子目录**
5. 点击 **确定**

重复以上步骤设置 `api.vekus.qzz.io` 的权限。

---

## ⚠️ 常见问题

### 问题 1: 上传速度很慢

**解决方案:**
- 使用 FTP 上传 (方法二)
- 或使用 Git 部署 (方法三)
- 或使用远程下载 (方法四)

### 问题 2: 上传失败或中断

**解决方案:**
1. 检查网络连接
2. 检查服务器磁盘空间:
   ```bash
   df -h
   ```
3. 分批上传小文件
4. 使用 FTP 断点续传功能

### 问题 3: 解压失败

**解决方案:**
1. 确认压缩格式正确 (.tar.gz 或 .zip)
2. 使用 SSH 命令解压:
   ```bash
   cd /www/wwwroot/vekus.qzz.io/
   tar -xzf frontend.tar.gz
   ```

### 问题 4: 文件权限错误

**解决方案:**
```bash
# SSH 登录后
cd /www/wwwroot/
chown -R www:www vekus.qzz.io/
chown -R www:www api.vekus.qzz.io/
chmod -R 755 vekus.qzz.io/
chmod -R 755 api.vekus.qzz.io/
```

### 问题 5: node_modules 太大

**解决方案:**
不要上传 `node_modules` 文件夹!

1. 在本地删除 `node_modules`:
   ```bash
   rm -rf frontend/node_modules
   rm -rf backend/node_modules
   ```

2. 上传后在服务器安装:
   ```bash
   cd /www/wwwroot/vekus.qzz.io/frontend
   npm install
   
   cd /www/wwwroot/api.vekus.qzz.io/backend
   npm install
   ```

---

## 📝 推荐上传方案

根据你的情况选择:

### 新手用户
→ **方法一: 宝塔文件管理器**
- 最简单直观
- 适合小项目

### 文件较大
→ **方法二: FTP 上传**
- 支持断点续传
- 速度较快

### 开发者
→ **方法三: Git 部署**
- 最专业
- 便于版本管理和更新

### 代码已在线
→ **方法四: 远程下载**
- 最快速
- 直接从 GitHub 下载

---

## ✅ 下一步

文件上传完成后,继续按照 **宝塔部署完整指南-vekus.qzz.io.md** 的步骤:

1. ✅ 文件已上传
2. → 配置后端服务 (第三步)
3. → 配置前端服务 (第四步)
4. → 配置 Nginx (第五步)
5. → 配置 SSL (第六步)
6. → 部署合约 (第七步)
7. → 测试部署 (第八步)

祝你部署顺利! 🎉

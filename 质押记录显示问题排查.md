# 🔍 质押记录显示问题排查

**问题**: 质押了两笔，但只显示1笔  
**链上数据**: ✅ 确认有2笔质押

---

## ✅ 链上验证结果

### 质押记录（链上）

| 序号 | StakeId | 金额 | 时间 | 区块 |
|------|---------|------|------|------|
| 1 | 0 | 1000 USDT | 2026/3/2 21:27:37 | 70 |
| 2 | 1 | 5000 USDT | 2026/3/2 21:28:42 | 71 |

**总质押**: 6000 RWA（18 decimals）

---

## 🔍 可能的原因

### 1. 前端事件查询问题

**检查**:
- 浏览器控制台是否有 `🔍 查询到 X 个质押事件` 的日志
- 如果显示查询到 2 个，但只显示 1 个，可能是解析问题

### 2. 事件解析问题

**可能原因**:
- 事件参数解析错误
- amount 格式转换问题
- stakeId 重复导致覆盖

### 3. 前端显示逻辑问题

**可能原因**:
- 只显示了第一笔
- 有过滤逻辑
- 显示组件有 bug

---

## 🚀 解决方案

### 方案 1: 检查浏览器控制台

1. 打开浏览器开发者工具（F12）
2. 切换到 "Console" 标签
3. 查看是否有：
   - `🔍 查询到 X 个质押事件`
   - `✅ 解析质押事件: StakeId=...`
   - 任何错误信息

### 方案 2: 修复事件解析（已更新）

已更新 `useUserStakes.ts`，添加了：
- ✅ 更详细的日志
- ✅ 错误处理
- ✅ 每个事件的解析日志

**需要操作**:
1. 重启前端服务器
2. 清除浏览器缓存
3. 刷新页面
4. 查看控制台日志

### 方案 3: 手动检查

运行检查脚本：
```bash
npx hardhat run scripts/check-stake-events.ts --network localhost
```

---

## 📝 调试步骤

### 1. 检查前端日志

在浏览器控制台查看：
```javascript
// 应该看到
🔍 查询到 2 个质押事件 (从区块 0 到最新)
✅ 解析质押事件: StakeId=0, Amount=..., Timestamp=...
✅ 解析质押事件: StakeId=1, Amount=..., Timestamp=...
```

### 2. 检查前端状态

在浏览器控制台输入：
```javascript
// 检查 stakes 状态（需要先导入或访问组件）
// 或者查看 React DevTools
```

### 3. 检查显示组件

查看显示质押记录的组件：
- 是否有过滤逻辑
- 是否只显示第一笔
- 是否有分页或限制

---

## 🔧 已修复

### 更新了 `useUserStakes.ts`

1. ✅ 添加了详细的解析日志
2. ✅ 添加了错误处理
3. ✅ 确保所有事件都被解析

---

## ✅ 验证步骤

### 1. 重启前端

```bash
cd frontend
npm run dev
```

### 2. 清除缓存并刷新

1. F12 → 右键刷新 → "清空缓存并硬性重新加载"

### 3. 检查控制台

查看是否有：
- `🔍 查询到 2 个质押事件`
- `✅ 解析质押事件: StakeId=0`
- `✅ 解析质押事件: StakeId=1`

### 4. 检查显示

应该能看到 2 笔质押记录

---

## 💡 如果仍只显示1笔

### 检查显示组件

可能显示组件有过滤或限制逻辑，需要检查：
- 是否有 `stakes.slice(0, 1)` 这样的限制
- 是否有条件过滤
- 是否有去重逻辑导致重复

---

**已更新代码，请重启前端并查看控制台日志！** 🔍

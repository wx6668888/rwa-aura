# 收益发放机制说明

## 概述
RWA 协议的收益发放采用**后端定时任务 + 智能合约更新**的混合模式。

## 收益计算时间

### 静态收益（RWA 代币）
- **计算频率**：每天 00:00 UTC（北京时间 08:00）
- **计算方式**：质押金额 × 0.8% = 每日收益
- **发放方式**：后端计算后，调用合约的 `updateUserRewards()` 更新用户余额

### 动态收益（USDT 奖励）
- **计算频率**：实时计算（当下级质押时触发）
- **发放方式**：后端监听质押事件，计算推荐奖励，调用合约更新

## 详细流程

### 1. 后端定时任务（SchedulerService）

```typescript
// 每天 00:00 UTC 执行
dailyYieldCron: '0 0 * * *'

// 任务内容：
1. 查询所有活跃用户（is_active = true）
2. 计算每个用户的收益：total_staked × 0.008
3. 调用合约 updateUserRewards() 更新余额
4. 记录到数据库 rewards 表
```

### 2. 收益计算逻辑（DailyYieldService）

```typescript
// 计算公式
yieldAmount = totalStaked × 0.008

// 示例：
质押 1000 USDT
每日收益 = 1000 × 0.008 = 8 RWA 代币
```

### 3. 合约更新（StakingContract）

```solidity
function updateUserRewards(
    address user,
    uint256 rwAmount,      // RWA 代币数量
    uint256 usdtAmount,    // USDT 奖励数量
    uint256 stakeId        // 防止重复处理
) external {
    // 只有后端可以调用
    require(msg.sender == backendAddress);
    
    // 防止重复处理
    require(!processedStakes[stakeId]);
    
    // 更新用户余额
    users[user].rwaPending += rwAmount;
    users[user].usdtRewards += usdtAmount;
    
    // 标记已处理
    processedStakes[stakeId] = true;
}
```

## 关键特性

### ✅ 不是满 24 小时发放
- **不是**从质押时间开始计算 24 小时
- **而是**每天固定时间（00:00 UTC）统一发放
- 所有用户同时收到收益

### ✅ 累积计算
- 如果今天 10:00 质押，明天 00:00 就会收到第一次收益
- 收益按**整天**计算，不按小时
- 例如：
  - 今天 10:00 质押 1000 USDT
  - 明天 00:00 收到 8 RWA（1000 × 0.8%）
  - 后天 00:00 再收到 8 RWA
  - 以此类推

### ✅ 实时显示
前端通过以下方式实现实时收益显示：

```typescript
// 前端计算公式（earnings-card.tsx）
const calculateRealTimeYield = () => {
  const stakeTime = userStakeInfo?.firstStakeTime || 0
  const now = Math.floor(Date.now() / 1000)
  const elapsedSeconds = now - stakeTime
  const elapsedDays = elapsedSeconds / 86400
  
  // 实时收益 = 质押金额 × 0.008 × 经过的天数
  const realTimeYield = stakedAmount × 0.008 × elapsedDays
  
  // 显示 = 合约已发放 + 实时累积
  return contractYield + realTimeYield
}
```

## 安全机制

### 1. 防止重复发放
```solidity
mapping(uint256 => bool) public processedStakes;

// 每个 stakeId 只能处理一次
require(!processedStakes[stakeId], "Already processed");
processedStakes[stakeId] = true;
```

### 2. 权限控制
```solidity
// 只有后端地址可以调用
require(msg.sender == backendAddress, "Only backend");
```

### 3. 金额限制
```solidity
// 单次调用最大奖励限制
require(usdtAmount <= maxRewardPerCall, "Exceeds limit");

// 动态奖励总额不能超过质押总额的 50%
require(
    totalDynamicRewardsPaid + usdtAmount <= totalStaked * 50 / 100,
    "Exceeds 50% limit"
);
```

## 时间线示例

假设用户在 **2025-02-27 14:30** 质押 1000 USDT：

| 时间 | 事件 | 收益 |
|------|------|------|
| 2025-02-27 14:30 | 质押 1000 USDT | 0 RWA |
| 2025-02-28 00:00 | 第一次发放 | +8 RWA |
| 2025-03-01 00:00 | 第二次发放 | +8 RWA |
| 2025-03-02 00:00 | 第三次发放 | +8 RWA |
| ... | ... | ... |

## 前端显示

### 仪表板（Dashboard）
- 显示**实时累积收益**（包含未发放部分）
- 每秒更新一次
- 公式：合约余额 + (质押金额 × 0.008 × 经过天数)

### 提现页面（Withdraw）
- 显示**可提现余额**（仅合约已发放部分）
- 从合约读取 `users[address].rwaPending`
- 只能提现已经发放到合约的部分

## 配置参数

### 后端配置（backend/src/index.ts）
```typescript
const schedulerConfig = {
    dailyYieldCron: '0 0 * * *',      // 每天 00:00 UTC
    priceRefreshCron: '*/5 * * * *',  // 每 5 分钟
    nodeLevelSyncCron: '0 * * * *'    // 每小时
}

const dailyYieldConfig = {
    yieldRate: 0.008  // 0.8%
}
```

### 合约配置
```solidity
// 提现冷却期：24 小时
uint256 public constant WITHDRAWAL_COOLDOWN = 24 hours;

// 提现手续费：5%
uint256 public constant WITHDRAWAL_FEE_RATE = 5;

// 最小提现金额：10 RWA
uint256 public constant MIN_WITHDRAWAL_AMOUNT = 10 * 10**18;
```

## 总结

1. **不是满 24 小时**：每天固定时间（00:00 UTC）统一发放
2. **后端计算**：DailyYieldService 负责计算所有用户收益
3. **合约更新**：后端调用 updateUserRewards() 更新余额
4. **实时显示**：前端计算实时累积，但只能提现已发放部分
5. **安全保障**：防重复、权限控制、金额限制

## 手动触发（测试用）

如果需要手动触发收益发放（不等到 00:00）：

```bash
# 调用后端 API
curl -X POST http://localhost:3001/api/admin/trigger-daily-yield

# 或者在后端代码中
schedulerService.triggerDailyYield()
```

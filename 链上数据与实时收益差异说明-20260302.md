# 链上数据与实时收益差异说明

## 🔍 问题原因

### 为什么链上数据（0.25 RWA）与实时计算的收益（35 RWA）不同？

**原因：**

1. **实时计算的收益（35 RWA）**：
   - 前端基于每笔质押的时间戳，实时计算应该产生的收益
   - 每秒都在增长（基于 0.8% 日收益率）
   - **这只是"预期收益"**，还没有同步到链上

2. **链上数据（0.25 RWA）**：
   - 这是合约中实际存储的 `rwaPending` 值
   - 只有后端服务调用 `updateUserRewards` 函数后才会更新
   - **这是"实际可提取"的收益**

### 数据流程

```
用户质押
  ↓
前端实时计算收益（每秒增长）← 显示 35 RWA
  ↓
后端服务定期调用 updateUserRewards ← 更新链上数据
  ↓
链上 rwaPending 更新 ← 显示 0.25 RWA（实际可提取）
```

---

## ✅ 解决方案

### **不需要重新部署合约！**

合约已经正确实现了 `updateUserRewards` 函数，只需要：

1. **启动后端服务**，让它定期调用 `updateUserRewards`
2. 后端服务会自动将实时计算的收益同步到链上

---

## 🚀 如何启动后端服务

### 方法1：使用现有的后端服务（推荐）

**步骤：**

1. **配置环境变量**（`backend/.env`）：
   ```env
   # 数据库配置
   DB_HOST=localhost
   DB_PORT=3306
   DB_USER=rwa_user
   DB_PASSWORD=rwa_password
   DB_NAME=rwa_protocol

   # 区块链配置
   BSC_RPC_URL=http://localhost:8545  # Hardhat 本地节点
   STAKING_CONTRACT_ADDRESS=0xFD471836031dc5108809D173A067e8486B9047A3
   BACKEND_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80  # Hardhat Account #0

   # 奖励配置
   DAILY_YIELD_RATE=0.008  # 0.8%
   MAX_REWARD_PER_CALL=10000000000000000000000  # 10000 USDT (18 decimals)
   ```

2. **启动 MySQL 数据库**（如果还没有）：
   ```bash
   # 使用 Docker
   docker run --name mysql-rwa -e MYSQL_ROOT_PASSWORD=rootpassword -e MYSQL_DATABASE=rwa_protocol -e MYSQL_USER=rwa_user -e MYSQL_PASSWORD=rwa_password -p 3306:3306 -d mysql:8.0
   ```

3. **初始化数据库**：
   ```bash
   cd backend
   # 运行数据库初始化脚本
   mysql -u rwa_user -p rwa_protocol < src/config/database-mysql57.sql
   ```

4. **安装依赖并启动后端**：
   ```bash
   cd backend
   npm install
   npm run dev
   ```

5. **后端服务会自动**：
   - 监听链上的 `StakeEvent` 事件
   - 定期计算每日收益（通过 `DailyYieldService`）
   - 调用 `updateUserRewards` 更新链上数据

---

### 方法2：手动更新（临时测试）

如果暂时不想启动完整的后端服务，可以使用手动更新脚本：

```bash
# 运行手动更新脚本
npx hardhat run scripts/manual-update-rewards.ts --network localhost
```

这个脚本会：
1. 计算用户应该获得的 RWA 收益
2. 调用 `updateUserRewards` 更新链上数据

---

## 📊 数据同步频率

**后端服务配置：**

- **每日收益计算**：每天 UTC 00:00 执行一次
- **事件监听**：实时监听链上事件
- **奖励分发**：质押后立即计算并分发

**手动更新脚本：**
- 可以随时运行，立即更新链上数据

---

## ⚠️ 重要说明

1. **前端显示的 35 RWA**：
   - 这是"预期收益"（实时计算）
   - 用于显示给用户看，让他们知道应该获得多少收益

2. **链上存储的 0.25 RWA**：
   - 这是"实际可提取"的收益
   - 只有这个数量可以真正提取

3. **为什么会有差异**：
   - 后端服务还没有运行，或者还没有调用 `updateUserRewards`
   - 一旦后端服务运行，链上数据会逐渐追上实时计算的收益

4. **不需要重新部署合约**：
   - 合约已经正确实现了所有功能
   - 只需要启动后端服务即可

---

## 🎯 推荐操作

**立即操作：**
1. 启动后端服务（方法1）
2. 或者运行手动更新脚本（方法2）

**长期方案：**
- 保持后端服务持续运行
- 确保数据库和区块链连接正常
- 监控后端服务日志，确保奖励正常分发

---

## 📝 验证步骤

启动后端服务后，可以通过以下方式验证：

1. **查看链上数据**：
   ```bash
   npx hardhat run scripts/check-user-rewards.ts --network localhost
   ```

2. **查看前端显示**：
   - Dashboard 页面应该显示实时计算的收益
   - Withdraw 页面应该显示链上可提取的收益
   - 两者应该逐渐接近

3. **查看后端日志**：
   - 应该看到 `updateUserRewards` 的调用记录
   - 应该看到交易哈希和确认信息

# 🚀 下一步执行命令

## 立即执行（按顺序）

### 1. 运行测试（验证合约功能）

```bash
# 运行所有测试
npx hardhat test

# 或者分别运行
npx hardhat test test/StRWA.test.ts
npx hardhat test test/SwapContract.test.ts
```

**预期结果**: 所有测试通过 ✅

---

### 2. 如果测试通过，创建部署脚本

**文件**: `scripts/deploy-all.ts`

**代码**: 已在 `立即执行步骤.md` 中提供

---

### 3. 本地测试部署

```bash
# 启动本地 Hardhat 节点
npx hardhat node

# 在另一个终端执行部署
npx hardhat run scripts/deploy-all.ts --network localhost
```

---

### 4. 测试网部署准备

1. 获取测试网 BNB: https://testnet.bnbchain.org/faucet-smart
2. 配置 `.env` 文件
3. 执行部署

```bash
npx hardhat run scripts/deploy-all.ts --network bscTestnet
```

---

## 📋 快速检查清单

- [ ] 运行测试: `npx hardhat test`
- [ ] 测试通过后创建部署脚本
- [ ] 本地测试部署
- [ ] 测试网部署
- [ ] 主网部署

---

**所有代码已准备就绪，可以开始执行！**

# 实时收益功能 - 基于质押时间计算

## ✅ 最终方案

### 问题根源

之前使用 localStorage 的方案有以下问题：
1. 数据可能损坏导致 NaN
2. 依赖前端状态，不够可靠
3. 切换页面时逻辑复杂

### 新方案：基于合约质押时间

直接从合约读取 `firstStakeTime`（质押开始时间），然后计算从质押开始到现在的总收益。

## 📊 计算逻辑

```typescript
// 1. 从合约读取质押开始时间（Unix 时间戳，秒）
const firstStakeTime = userStakeInfo?.firstStakeTime || 0

// 2. 计算经过的时间
const now = Math.floor(Date.now() / 1000) // 当前时间（秒）
const elapsedSeconds = now - firstStakeTime // 经过的秒数

// 3. 计算总收益
const dailyRate = 0.008 // 每日 0.8%
const perSecondRate = dailyRate / 86400 // 每秒收益率
const totalEarnings = totalStakedNum * perSecondRate * elapsedSeconds

// 4. 显示实时余额
setLiveRwaBalance(totalEarnings)
```

## 🔧 技术实现

### 1. 更新合约 ABI

添加 `users` mapping 的 ABI，以便读取 `firstStakeTime`：

```typescript
{
  inputs: [{ internalType: 'address', name: '', type: 'address' }],
  name: 'users',
  outputs: [
    { internalType: 'uint256', name: 'totalStaked', type: 'uint256' },
    { internalType: 'uint256', name: 'rwaPending', type: 'uint256' },
    { internalType: 'uint256', name: 'usdtRewards', type: 'uint256' },
    { internalType: 'uint256', name: 'lastWithdrawTime', type: 'uint256' },
    { internalType: 'address', name: 'referrer', type: 'address' },
    { internalType: 'uint8', name: 'nodeLevel', type: 'uint8' },
    { internalType: 'uint256', name: 'firstStakeTime', type: 'uint256' },
    { internalType: 'bool', name: 'isActive', type: 'bool' },
  ],
  stateMutability: 'view',
  type: 'function',
}
```

### 2. 更新 useStakingContract Hook

添加读取 `users` mapping 的调用：

```typescript
// Read user data from users mapping (includes firstStakeTime)
const { data: userData } = useReadContract({
  address: stakingAddress as `0x${string}`,
  abi: stakingContractABI,
  functionName: 'users',
  args: address ? [address] : undefined,
  query: {
    enabled: !!address && !!stakingAddress,
  },
})

// Add firstStakeTime to formatted data
const formattedUserData = userStakeInfo ? {
  // ... other fields
  firstStakeTime: userData ? Number(userData[6]) : 0,
} : null
```

### 3. 更新 EarningsCard 组件

简化逻辑，直接基于质押时间计算：

```typescript
// 基于质押时间计算实时收益
useEffect(() => {
  if (!isConnected || !address || totalStakedNum === 0 || firstStakeTime === 0) {
    setLiveRwaBalance(rwaPendingNum)
    return
  }
  
  const calculateBalance = () => {
    const now = Math.floor(Date.now() / 1000)
    const elapsedSeconds = now - firstStakeTime
    const totalEarnings = totalStakedNum * perSecondRate * elapsedSeconds
    return totalEarnings
  }
  
  setLiveRwaBalance(calculateBalance())
  
  const interval = setInterval(() => {
    setLiveRwaBalance(calculateBalance())
  }, 1000)
  
  return () => clearInterval(interval)
}, [isConnected, address, totalStakedNum, firstStakeTime, rwaPendingNum, perSecondRate])
```

## ✅ 优势

1. **准确性**：直接基于合约数据，不依赖前端状态
2. **简单性**：不需要 localStorage，逻辑更清晰
3. **可靠性**：不会出现 NaN 或数据损坏
4. **一致性**：切换页面、刷新浏览器都能保持一致

## 🧪 测试步骤

### 步骤 1：硬刷新浏览器

- Windows: `Ctrl + Shift + R`
- Mac: `Cmd + Shift + R`

### 步骤 2：连接钱包并质押

1. 访问 http://localhost:3000/stake
2. 连接钱包（使用测试账户）
3. 质押 USDT（最低 100 USDT）
4. 等待交易确认

### 步骤 3：查看实时收益

1. 访问 http://localhost:3000/dashboard
2. 观察 RWA 余额是否每秒增长
3. 记录初始值和增长速度

### 步骤 4：验证计算准确性

假设质押 10,000 USDT：

- 每日收益：10,000 × 0.8% = 80 RWA
- 每秒收益：80 ÷ 86400 = 0.000926 RWA
- 60 秒收益：0.000926 × 60 = 0.0556 RWA

**测试**：
1. 记录初始余额
2. 等待 60 秒
3. 计算增长量
4. 对比预期值（应该约等于 0.0556 RWA）

### 步骤 5：测试页面切换

1. 记录当前余额（例如：0.000926 RWA）
2. 切换到质押页面
3. 等待 10 秒
4. 返回仪表板
5. **验证**：余额应该继续增长（例如：0.001852 RWA）

### 步骤 6：测试浏览器刷新

1. 记录当前余额
2. 刷新页面（F5）
3. **验证**：余额应该继续从正确的值增长

## 🐛 调试技巧

### 查看质押时间

打开浏览器控制台（F12），在 Components 标签中找到 EarningsCard 组件，查看：

- `firstStakeTime`: 质押开始时间（Unix 时间戳）
- `totalStakedNum`: 质押金额
- `liveRwaBalance`: 实时余额

### 手动计算验证

```javascript
// 在浏览器控制台执行
const firstStakeTime = 1709020800 // 替换为实际值
const totalStaked = 10000 // 替换为实际值
const now = Math.floor(Date.now() / 1000)
const elapsedSeconds = now - firstStakeTime
const dailyRate = 0.008
const perSecondRate = dailyRate / 86400
const totalEarnings = totalStaked * perSecondRate * elapsedSeconds

console.log('质押时间:', new Date(firstStakeTime * 1000))
console.log('经过时间:', elapsedSeconds, '秒')
console.log('应该显示:', totalEarnings, 'RWA')
```

## 📝 注意事项

1. **首次质押**：`firstStakeTime` 在首次质押时设置，不会改变
2. **追加质押**：追加质押不会改变 `firstStakeTime`，但会增加 `totalStaked`
3. **提现**：提现不会改变 `firstStakeTime` 和 `totalStaked`
4. **收益计算**：收益是基于当前质押金额和质押时长计算的

## 🎯 总结

新方案通过直接读取合约的 `firstStakeTime` 字段，避免了 localStorage 的所有问题：

- ✅ 不会出现 NaN
- ✅ 不依赖前端状态
- ✅ 切换页面不会重置
- ✅ 刷新浏览器不会重置
- ✅ 计算准确可靠

现在请硬刷新浏览器（Ctrl + Shift + R）测试功能！

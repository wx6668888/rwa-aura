# 级差奖励详细说明：V1-V5 节点，最高 50% 上限

## 📌 核心概念

### 1. 什么是级差奖励？

**级差奖励**（也叫"压级奖励"）是一种防止重复计算的奖励机制。当下级用户质押时，上级用户会根据自己的**节点等级**获得奖励，但奖励金额不是简单的"等级百分比 × 质押额"，而是**只计算增量部分**。

### 2. 节点等级对应的奖励百分比

```
V1 (入门节点) → 5%   (对下级质押的奖励比例)
V2 (初级节点) → 10%
V3 (中级节点) → 15%
V4 (高级节点) → 20%
V5 (超级节点) → 50%
```

### 3. 级差计算公式

```
每个上级的实际奖励 = 质押金额 × (自己等级的百分比 - 下级已拿走的最高百分比)
```

**关键点**：
- 如果"自己等级的百分比 ≤ 下级已拿走的最高百分比"，则奖励为 0（被压级）
- 所有上级的总奖励不能超过质押金额的 **50%**

---

## 📊 详细举例

### 例子 1：标准级差情况（推荐链等级递增）

**场景**：
```
用户E（V1）质押 10,000 USDT
    ↓ 推荐人
用户D（V2）
    ↓ 推荐人
用户C（V3）
    ↓ 推荐人
用户B（V4）
    ↓ 推荐人
用户A（V5）
```

**计算过程**：

| 上级 | 节点等级 | 等级百分比 | 下级已拿走最高% | 级差百分比 | 奖励金额 |
|------|---------|-----------|---------------|-----------|---------|
| 用户D | V2 | 10% | 0% | 10% - 0% = **10%** | 10,000 × 10% = **1,000 USDT** |
| 用户C | V3 | 15% | 10% | 15% - 10% = **5%** | 10,000 × 5% = **500 USDT** |
| 用户B | V4 | 20% | 15% | 20% - 15% = **5%** | 10,000 × 5% = **500 USDT** |
| 用户A | V5 | 50% | 20% | 50% - 20% = **30%** | 10,000 × 30% = **3,000 USDT** |

**总计**：1,000 + 500 + 500 + 3,000 = **5,000 USDT** = **50%** ✓

---

### 例子 2：压级情况（推荐链等级不递增）

**场景**：
```
用户F（V1）质押 10,000 USDT
    ↓ 推荐人
用户E（V1，等级较低）
    ↓ 推荐人
用户D（V3，等级较高）
    ↓ 推荐人
用户C（V2，等级较低，被压级！）
```

**计算过程**：

| 上级 | 节点等级 | 等级百分比 | 下级已拿走最高% | 级差百分比 | 奖励金额 |
|------|---------|-----------|---------------|-----------|---------|
| 用户E | V1 | 5% | 0% | 5% - 0% = **5%** | 10,000 × 5% = **500 USDT** |
| 用户D | V3 | 15% | 5% | 15% - 5% = **10%** | 10,000 × 10% = **1,500 USDT** |
| 用户C | V2 | 10% | 15% | 10% - 15% = **0%** ⚠️ | **0 USDT** |

**总计**：500 + 1,500 + 0 = **2,000 USDT** = **20%** ✓

**关键点**：用户C 虽然是 V2（10%），但因为下级用户D 是 V3（15%），已经拿走了 15%，所以用户C 被"压级"，拿不到奖励。

---

### 例子 3：只有 V1 的情况

**场景**：
```
用户B（V1）质押 10,000 USDT
    ↓ 推荐人
用户A（V1）
```

**计算过程**：

| 上级 | 节点等级 | 等级百分比 | 下级已拿走最高% | 级差百分比 | 奖励金额 |
|------|---------|-----------|---------------|-----------|---------|
| 用户A | V1 | 5% | 0% | 5% - 0% = **5%** | 10,000 × 5% = **500 USDT** |

**总计**：**500 USDT** = **5%** ✓

---

### 例子 4：V5 独享大部分奖励

**场景**：
```
用户G（V1）质押 10,000 USDT
    ↓ 推荐人
用户F（V1）
    ↓ 推荐人
用户E（V1）
    ↓ 推荐人
用户D（V1）
    ↓ 推荐人
用户C（V5，最高等级）
```

**计算过程**：

| 上级 | 节点等级 | 等级百分比 | 下级已拿走最高% | 级差百分比 | 奖励金额 |
|------|---------|-----------|---------------|-----------|---------|
| 用户F | V1 | 5% | 0% | 5% - 0% = **5%** | 10,000 × 5% = **500 USDT** |
| 用户E | V1 | 5% | 5% | 5% - 5% = **0%** ⚠️ | **0 USDT** |
| 用户D | V1 | 5% | 5% | 5% - 5% = **0%** ⚠️ | **0 USDT** |
| 用户C | V5 | 50% | 5% | 50% - 5% = **45%** | 10,000 × 45% = **4,500 USDT** |

**总计**：500 + 0 + 0 + 4,500 = **5,000 USDT** = **50%** ✓

**关键点**：V5 用户可以获得最多 45% 的级差奖励（如果下级都是低等级）。

---

### 例子 5：复杂推荐链（等级跳跃）

**场景**：
```
用户H（V1）质押 10,000 USDT
    ↓ 推荐人
用户G（V2）
    ↓ 推荐人
用户F（V1，等级下降）
    ↓ 推荐人
用户E（V4，等级大幅上升）
    ↓ 推荐人
用户D（V2，被压级）
    ↓ 推荐人
用户C（V3，被压级）
```

**计算过程**：

| 上级 | 节点等级 | 等级百分比 | 下级已拿走最高% | 级差百分比 | 奖励金额 |
|------|---------|-----------|---------------|-----------|---------|
| 用户G | V2 | 10% | 0% | 10% - 0% = **10%** | 10,000 × 10% = **1,000 USDT** |
| 用户F | V1 | 5% | 10% | 5% - 10% = **0%** ⚠️ | **0 USDT** |
| 用户E | V4 | 20% | 10% | 20% - 10% = **10%** | 10,000 × 10% = **1,000 USDT** |
| 用户D | V2 | 10% | 20% | 10% - 20% = **0%** ⚠️ | **0 USDT** |
| 用户C | V3 | 15% | 20% | 15% - 20% = **0%** ⚠️ | **0 USDT** |

**总计**：1,000 + 0 + 1,000 + 0 + 0 = **2,000 USDT** = **20%** ✓

**关键点**：只有等级高于下级已拿走最高百分比的上级才能获得奖励。

---

## 🔒 50% 上限保护机制

### 为什么是 50%？

系统设计时，将质押金额的 **50%** 分配给动态奖励（级差奖励），另外 **50%** 用于：
- 静态收益池（0.8% 日收益）
- 协议运营费用
- 风险准备金

### 上限检查机制

系统有三层保护确保不超过 50%：

1. **后端计算时检查**：
   ```typescript
   const totalRewards = rewards.reduce((sum, r) => sum.plus(r.amount), new BigNumber(0));
   const maxAllowed = stakeAmountBN.multipliedBy(0.5);
   
   if (totalRewards.isGreaterThan(maxAllowed)) {
       throw new Error('总奖励超过 50% 限制');
   }
   ```

2. **合约验证**：
   ```solidity
   require(
       totalDynamicRewardsPaid + usdtAmount <= totalStaked * 50 / 100,
       "超过 50% 限制"
   );
   ```

3. **达到 50% 时停止计算**：
   ```typescript
   if (maxAllocatedPercentage >= 0.5) {
       break; // 停止继续分配
   }
   ```

---

## 💡 关键理解点

### 1. 级差奖励的本质

级差奖励不是"每个上级都拿自己等级的百分比"，而是"每个上级只拿增量部分"，这样可以：
- ✅ 防止重复计算
- ✅ 确保总奖励不超过 50%
- ✅ 鼓励用户升级到更高等级（等级越高，能拿到的增量越大）

### 2. 压级的意义

如果推荐链上出现"低等级在高级别后面"的情况，低等级会被压级（奖励为 0）。这鼓励：
- ✅ 用户积极发展团队
- ✅ 用户升级到更高等级
- ✅ 避免"躺赚"（低等级也能拿到高奖励）

### 3. V5 的优势

V5 是最高等级（50%），在推荐链中：
- ✅ 如果下级都是低等级，V5 可以拿到最多 45% 的级差奖励
- ✅ 如果下级有高等级，V5 仍然可以拿到 30% 的级差奖励（50% - 20%）
- ✅ V5 永远不会被压级（因为 50% 是最高值）

---

## 📈 实际应用场景

### 场景 A：新用户加入

```
新用户 Alice（V1）质押 5,000 USDT
推荐人：Bob（V2）
```

**奖励分配**：
- Bob（V2）获得：5,000 × 10% = **500 USDT**

### 场景 B：团队发展良好

```
用户 Charlie（V1）质押 20,000 USDT
推荐链：Charlie → David(V2) → Eve(V3) → Frank(V4) → Grace(V5)
```

**奖励分配**：
- David（V2）：20,000 × 10% = **2,000 USDT**
- Eve（V3）：20,000 × 5% = **1,000 USDT**
- Frank（V4）：20,000 × 5% = **1,000 USDT**
- Grace（V5）：20,000 × 30% = **6,000 USDT**
- **总计**：10,000 USDT = **50%** ✓

### 场景 C：等级不均衡

```
用户 Henry（V1）质押 10,000 USDT
推荐链：Henry → Ivan(V1) → Jack(V3) → Kevin(V1)
```

**奖励分配**：
- Ivan（V1）：10,000 × 5% = **500 USDT**
- Jack（V3）：10,000 × 10% = **1,000 USDT**（15% - 5% = 10%）
- Kevin（V1）：10,000 × 0% = **0 USDT**（被压级）
- **总计**：1,500 USDT = **15%** ✓

---

## ✅ 总结

**级差奖励的核心规则**：

1. ✅ 每个节点等级对应固定的奖励百分比（V1=5%, V2=10%, V3=15%, V4=20%, V5=50%）
2. ✅ 每个上级只拿"自己等级的百分比 - 下级已拿走的最高百分比"的差额
3. ✅ 如果差额 ≤ 0，则奖励为 0（被压级）
4. ✅ 所有上级的总奖励不能超过质押金额的 **50%**
5. ✅ 达到 50% 时，系统停止继续分配奖励

**设计目的**：
- 🎯 鼓励用户升级到更高等级
- 🎯 防止低等级用户"躺赚"
- 🎯 确保系统可持续性（50% 上限保护）
- 🎯 公平分配奖励（按贡献和等级）

---

**有任何疑问，欢迎继续提问！** 🚀

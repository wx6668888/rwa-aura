# 移动端钱包连接测试指南

## 修复完成 ✅

已修复移动端钱包连接问题，现在应该能正常显示钱包列表了。

## 立即测试

### 1. 重启前端服务器
```bash
cd frontend
npm run dev
```

### 2. 在 Chrome F12 移动端模式测试
1. 打开 Chrome
2. 按 F12 打开开发者工具
3. 点击设备工具栏图标（或按 Ctrl+Shift+M）
4. 选择一个移动设备（如 iPhone 12 Pro）
5. 访问 http://localhost:3000/
6. 点击右上角"连接钱包"按钮

### 3. 预期结果
现在应该看到钱包列表，包括：
- 🦊 MetaMask
- 🔵 Coinbase Wallet  
- 🌈 Rainbow
- 💼 Trust Wallet
- 📱 WalletConnect
- 等等...

## 主要修改

### 1. 使用 RainbowKit 的 `getDefaultConfig`
```typescript
// frontend/lib/wagmi.ts
export const config = getDefaultConfig({
  appName: 'RWA Protocol',
  projectId,
  chains: [hardhatLocal, bsc, bscTestnet],
  ssr: true,
})
```

这会自动配置所有常用钱包，无需手动添加每个连接器。

### 2. 添加移动端优化
```typescript
// frontend/components/providers/web3-provider.tsx
<RainbowKitProvider
  modalSize="compact"  // 紧凑模式，适合移动端
  appInfo={{
    appName: 'RWA Protocol',
  }}
>
```

## WalletConnect Project ID 配置（可选）

如果你想要更好的移动端体验，建议配置 WalletConnect Project ID：

### 步骤：
1. 访问 https://cloud.walletconnect.com/
2. 注册账号（免费）
3. 创建新项目
4. 复制 Project ID
5. 在 `frontend/.env.local` 中配置：
```env
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=你的项目ID
```

### 注意：
- 即使不配置，钱包列表也会显示
- 配置后可以获得更好的 WalletConnect 体验
- 免费版本足够使用

## 移动端连接方式

### 方式1：在钱包浏览器内打开（推荐）
1. 打开 MetaMask App
2. 点击浏览器标签
3. 输入网址：http://localhost:3000/
4. 会自动检测并连接

### 方式2：WalletConnect 扫码
1. 在电脑浏览器打开网站
2. 点击"连接钱包"
3. 选择 WalletConnect
4. 用手机钱包 App 扫描二维码
5. 在手机上确认连接

### 方式3：深度链接（某些钱包支持）
点击钱包选项后，会尝试打开对应的钱包 App。

## 对比：修复前 vs 修复后

### 修复前 ❌
- 只显示"什么是钱包？"的说明
- 没有钱包选项列表
- 移动端无法连接

### 修复后 ✅
- 显示完整的钱包列表
- 支持多种连接方式
- 移动端和桌面端体验一致

## 常见问题

### Q: 为什么我看到的钱包列表和别人的不一样？
A: RainbowKit 会根据用户的环境自动调整显示的钱包：
- 桌面端：优先显示浏览器扩展钱包
- 移动端：优先显示移动端钱包 App
- 已安装的钱包会排在前面

### Q: 如何在真实手机上测试？
A: 
1. 确保手机和电脑在同一局域网
2. 找到电脑的局域网 IP（如 192.168.1.100）
3. 在手机浏览器访问：http://192.168.1.100:3000/
4. 或者在钱包 App 的浏览器中访问

### Q: 为什么 localhost 在手机上打不开？
A: localhost 只能在本机访问，手机需要使用电脑的局域网 IP。

## 下一步

1. ✅ 测试移动端钱包连接
2. ✅ 测试桌面端钱包连接
3. ⏳ 配置 WalletConnect Project ID（可选）
4. ⏳ 在真实手机上测试
5. ⏳ 测试多个不同的钱包 App

## 技术细节

### 为什么使用 `getDefaultConfig`？
- 自动配置所有常用钱包
- 自动处理移动端和桌面端的差异
- 自动优化用户体验
- 减少配置代码

### 支持的钱包
RainbowKit 默认支持 100+ 个钱包，包括：
- MetaMask
- Trust Wallet
- Coinbase Wallet
- Rainbow
- Ledger
- Trezor
- WalletConnect 兼容的所有钱包
- 等等...

## 需要帮助？

如果还有问题，请检查：
1. 浏览器控制台是否有错误
2. 网络连接是否正常
3. 是否清除了浏览器缓存
4. 前端服务器是否正常运行

# 锁仓1天资金去向与账户显示

## 💰 资金去向

### 用户质押 10,000 USDT，选择锁仓1天

**质押时（第1天）**：
```
10,000 USDT 分配：
├─ 5,000 USDT → Treasury（储备金地址）
│   └─ 永久锁定，不返还
│
└─ 5,000 USDT → Staking Contract（合约地址）
    └─ 锁定1天
```

**锁仓期结束后（第2天）**：
```
资金状态：
├─ 5,000 USDT → Treasury（永久锁定，不返还）❌
└─ 5,000 USDT → Staking Contract（已解锁，可提取）✅
```

---

## 📱 用户账户显示

### 方案一：简洁版（推荐）

```
┌─────────────────────────────────────┐
│  我的质押                            │
├─────────────────────────────────────┤
│  总投入：10,000 USDT                 │
│                                      │
│  📦 本金状态                         │
│  ├─ 可提取本金：5,000 USDT          │
│  │   └─ [提取本金] 按钮             │
│  │                                  │
│  └─ 投资锁定：5,000 USDT            │
│      └─ 永久锁定，用于实体投资       │
│                                      │
│  💎 收益状态                         │
│  ├─ RWA收益：80 RWA                 │
│  │   └─ [提取收益] 按钮             │
│  │                                  │
│  └─ 日收益率：0.8%                  │
│                                      │
│  ⏰ 锁仓信息                         │
│  └─ 锁仓期：已结束 ✅                │
└─────────────────────────────────────┘
```

---

### 方案二：详细版

```
┌─────────────────────────────────────┐
│  我的质押详情                        │
├─────────────────────────────────────┤
│                                      │
│  💵 资金分配                         │
│  ┌─────────────────────────────────┐│
│  │ 总投入：10,000 USDT              ││
│  │                                  ││
│  │ ├─ 可提取本金：5,000 USDT (50%) ││
│  │ │   └─ 状态：已解锁 ✅           ││
│  │ │   └─ [提取本金]               ││
│  │ │                               ││
│  │ └─ 投资锁定：5,000 USDT (50%)   ││
│  │     └─ 状态：永久锁定 🔒        ││
│  │     └─ 用途：实体投资           ││
│  └─────────────────────────────────┘│
│                                      │
│  💎 收益信息                         │
│  ┌─────────────────────────────────┐│
│  │ 累计收益：80 RWA                ││
│  │ 日收益率：0.8%                   ││
│  │ 收益倍数：1.0x（锁仓期已结束）   ││
│  │                                  ││
│  │ [提取收益] 按钮                  ││
│  └─────────────────────────────────┘│
│                                      │
│  ⏰ 锁仓信息                         │
│  ┌─────────────────────────────────┐│
│  │ 锁仓期限：1天                    ││
│  │ 开始时间：2026-01-01 00:00      ││
│  │ 结束时间：2026-01-02 00:00      ││
│  │ 当前状态：已解锁 ✅              ││
│  │                                  ││
│  │ [重新锁定] 按钮（可选）          ││
│  └─────────────────────────────────┘│
└─────────────────────────────────────┘
```

---

### 方案三：卡片式（移动端友好）

```
┌─────────────────────┐
│  总投入              │
│  10,000 USDT        │
└─────────────────────┘

┌─────────────────────┐
│  💰 可提取本金       │
│  5,000 USDT         │
│  [提取]             │
└─────────────────────┘

┌─────────────────────┐
│  🔒 投资锁定         │
│  5,000 USDT         │
│  永久锁定，用于投资  │
└─────────────────────┘

┌─────────────────────┐
│  💎 累计收益         │
│  80 RWA             │
│  [提取]             │
└─────────────────────┘

┌─────────────────────┐
│  ⏰ 锁仓状态         │
│  已解锁 ✅           │
└─────────────────────┘
```

---

## 📊 数据结构

### 后端返回的数据结构

```typescript
interface UserStakeInfo {
  // 总投入
  totalStaked: string; // "10000"
  
  // 本金分配
  principal: {
    withdrawable: string;      // "5000" - 可提取本金
    locked: string;            // "5000" - 投资锁定（永久）
    withdrawableStatus: "locked" | "unlocked"; // 锁仓状态
  };
  
  // 收益信息
  rewards: {
    rwaPending: string;        // "80" - RWA收益
    usdtRewards: string;       // "0" - USDT收益
    dailyYield: string;        // "0.8%" - 日收益率
    multiplier: string;        // "1.0x" - 收益倍数
  };
  
  // 锁仓信息
  lockInfo: {
    period: "flexible" | "30" | "90" | "180" | "365";
    startTime: number;         // 锁仓开始时间戳
    endTime: number;           // 锁仓结束时间戳
    isLocked: boolean;          // 是否锁定中
    remainingTime: number;     // 剩余时间（秒）
  };
}
```

---

## 🎨 前端显示逻辑

### 关键显示规则

```typescript
// 1. 本金显示
if (lockInfo.isLocked) {
  // 锁仓期间
  display = "本金：5,000 USDT（锁定中）";
  button = "锁仓期结束后可提取";
} else {
  // 锁仓期结束
  display = "可提取本金：5,000 USDT";
  button = "提取本金";
}

// 2. 投资锁定显示（始终显示）
display = "投资锁定：5,000 USDT（永久锁定，用于实体投资）";
button = null; // 无按钮

// 3. 收益显示
display = `累计收益：${rewards.rwaPending} RWA`;
button = "提取收益";

// 4. 锁仓状态显示
if (lockInfo.isLocked) {
  display = `锁仓中，剩余 ${formatTime(lockInfo.remainingTime)}`;
} else {
  display = "锁仓期已结束 ✅";
}
```

---

## ✅ 最终答案

### 1. 资金去哪儿了？

**锁仓1天后**：
- ✅ **5,000 USDT** 在 **Staking Contract**（合约地址），已解锁，可提取
- ❌ **5,000 USDT** 在 **Treasury**（储备金地址），永久锁定，不返还

---

### 2. 用户账户如何显示？

**推荐显示方式**：

```
总投入：10,000 USDT

💰 可提取本金：5,000 USDT
   [提取本金] 按钮

🔒 投资锁定：5,000 USDT
   永久锁定，用于实体投资

💎 累计收益：80 RWA
   [提取收益] 按钮

⏰ 锁仓状态：已解锁 ✅
```

---

**简洁明了，用户一看就懂！**

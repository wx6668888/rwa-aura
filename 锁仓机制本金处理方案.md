# 锁仓机制本金处理方案详解

## ❓ 核心问题

**用户质押的 USDT 在锁仓期结束时，到哪里了？**

---

## 📊 当前方案的问题分析

### 原设计方案（不完整）

```
用户质押 10,000 USDT
    ├─ 5,000 USDT → Treasury（储备金，永久锁定）
    └─ 5,000 USDT → 合约（奖励池）
```

**问题**：
- ❌ 如果50%已经在Treasury了，锁仓期结束后用户能提取什么？
- ❌ 合约中的50%是否足够用户提取本金？
- ❌ 如果用户选择365天锁仓，锁仓期结束后本金在哪里？

---

## ✅ 改进方案：分层本金管理

### 方案A：本金完全锁定在合约（推荐）

#### 资金分配逻辑

```
用户质押 10,000 USDT，选择 180天锁仓

质押时分配：
├─ 5,000 USDT → Treasury（储备金，永久锁定，用于实体投资）
│   └─ 这部分资金用于投资，产生回报后回流到奖励池
│
└─ 5,000 USDT → 合约（锁仓本金池）
    ├─ 锁仓期间：本金锁定，不能提取
    ├─ 锁仓期结束后：用户可以提取这 5,000 USDT
    └─ 如果提前解锁：扣除惩罚后提取剩余部分
```

#### 数据结构

```solidity
struct LockedStake {
    uint256 principalAmount;      // 本金金额（合约中的50%）
    uint256 treasuryAmount;        // Treasury金额（永久锁定）
    LockTier lockTier;             // 锁仓等级
    uint256 lockStartTime;         // 锁仓开始时间
    uint256 lockEndTime;           // 锁仓结束时间
    bool isLocked;                 // 是否锁定中
    bool isWithdrawn;              // 是否已提取本金
}

mapping(address => LockedStake[]) public userLockedStakes;
```

#### 提取逻辑

```solidity
function withdrawPrincipal(uint256 stakeIndex) external nonReentrant {
    LockedStake storage stake = userLockedStakes[msg.sender][stakeIndex];
    
    require(!stake.isWithdrawn, "Already withdrawn");
    require(block.timestamp >= stake.lockEndTime, "Lock period not ended");
    
    // 计算可提取金额（合约中的50%本金）
    uint256 withdrawableAmount = stake.principalAmount;
    
    // 检查合约余额
    require(
        usdtToken.balanceOf(address(this)) >= withdrawableAmount / PRECISION_MULTIPLIER,
        "Insufficient contract balance"
    );
    
    // 标记为已提取
    stake.isWithdrawn = true;
    
    // 更新用户总质押额（减少）
    users[msg.sender].totalStaked -= (stake.principalAmount + stake.treasuryAmount);
    
    // 更新全局统计
    totalStaked -= (stake.principalAmount + stake.treasuryAmount);
    
    // 转账给用户（转换回6位小数）
    usdtToken.safeTransfer(msg.sender, withdrawableAmount / PRECISION_MULTIPLIER);
    
    emit PrincipalWithdrawn(msg.sender, stakeIndex, withdrawableAmount);
}
```

#### 提前解锁逻辑

```solidity
function earlyUnlock(uint256 stakeIndex) external nonReentrant {
    LockedStake storage stake = userLockedStakes[msg.sender][stakeIndex];
    
    require(stake.isLocked && !stake.isWithdrawn, "Invalid stake");
    require(block.timestamp < stake.lockEndTime, "Lock period ended");
    
    // 计算惩罚率
    uint256 penaltyRate = getPenaltyRate(stake.lockTier);
    
    // 计算惩罚金额
    uint256 penaltyAmount = stake.principalAmount * penaltyRate / 100;
    uint256 withdrawableAmount = stake.principalAmount - penaltyAmount;
    
    // 分配惩罚资金
    uint256 toBurn = penaltyAmount * 50 / 100;        // 50% 销毁
    uint256 toRewardPool = penaltyAmount * 30 / 100;  // 30% 奖励池
    uint256 toTreasury = penaltyAmount * 20 / 100;    // 20% Treasury
    
    // 标记为已提取
    stake.isWithdrawn = true;
    stake.isLocked = false;
    
    // 更新统计
    users[msg.sender].totalStaked -= (stake.principalAmount + stake.treasuryAmount);
    totalStaked -= (stake.principalAmount + stake.treasuryAmount);
    
    // 转账给用户（扣除惩罚后）
    usdtToken.safeTransfer(msg.sender, withdrawableAmount / PRECISION_MULTIPLIER);
    
    // 销毁部分
    if (toBurn > 0) {
        // 转换为RWA代币销毁（或直接销毁USDT）
        // 这里需要根据实际代币类型调整
    }
    
    // 转移到奖励池
    // 转移到Treasury
    
    emit EarlyUnlock(msg.sender, stakeIndex, withdrawableAmount, penaltyAmount);
}

function getPenaltyRate(LockTier tier) internal pure returns (uint256) {
    if (tier == LockTier.TIER_30) return 20;   // 20%
    if (tier == LockTier.TIER_90) return 30;   // 30%
    if (tier == LockTier.TIER_180) return 40;  // 40%
    if (tier == LockTier.TIER_365) return 50; // 50%
    return 0; // 灵活锁仓无惩罚
}
```

---

### 方案B：本金部分可提取（备选）

#### 资金分配逻辑

```
用户质押 10,000 USDT，选择 180天锁仓

质押时分配：
├─ 5,000 USDT → Treasury（储备金，永久锁定）
│   └─ 这部分资金用于投资，永不提取
│
└─ 5,000 USDT → 合约（可提取本金池）
    ├─ 锁仓期间：不能提取
    ├─ 锁仓期结束后：可以提取这 5,000 USDT
    └─ 但用户可以选择继续锁定，获得更高收益
```

#### 继续锁定机制

```solidity
function extendLock(uint256 stakeIndex, LockTier newTier) external {
    LockedStake storage stake = userLockedStakes[msg.sender][stakeIndex];
    
    require(block.timestamp >= stake.lockEndTime, "Lock period not ended");
    require(!stake.isWithdrawn, "Already withdrawn");
    require(newTier > stake.lockTier, "New tier must be higher");
    
    // 重新计算锁仓时间
    uint256 newLockDuration = getLockDuration(newTier);
    stake.lockEndTime = block.timestamp + newLockDuration;
    stake.lockTier = newTier;
    stake.isLocked = true;
    
    emit LockExtended(msg.sender, stakeIndex, newTier);
}
```

---

## 💰 完整资金流向图

### 场景：用户 Alice 质押 10,000 USDT，选择 180天锁仓

#### 第1天：质押

```
Alice 质押 10,000 USDT
    │
    ├─ 5,000 USDT → Treasury（储备金）
    │   └─ 用途：实体投资
    │   └─ 状态：永久锁定，不返还
    │
    └─ 5,000 USDT → 合约（锁仓本金池）
        └─ 状态：锁定180天
        └─ 锁仓期结束后可提取
```

#### 第1-180天：锁仓期间

```
每日收益计算：
- 本金：10,000 USDT（用于计算收益）
- 日收益率：0.8% × 2.0（180天锁仓倍数）= 1.6%
- 每日收益：10,000 × 1.6% = 160 RWA

收益来源：
- 新用户质押的50%（奖励池）
- Treasury投资回报的40%（回流到奖励池）
```

#### 第180天：锁仓期结束

```
Alice 可以选择：

选项1：提取本金
├─ 提取金额：5,000 USDT（合约中的50%）
├─ Treasury部分：5,000 USDT（永久锁定，不返还）
└─ 继续获得收益：否（本金已提取，停止计算收益）

选项2：继续锁定（升级锁仓）
├─ 选择更长期限（如365天）
├─ 获得更高收益倍数（3.0x）
└─ 本金继续锁定

选项3：不提取，继续灵活锁仓
├─ 本金继续在合约中
├─ 收益倍数降为1.0x（灵活锁仓）
└─ 随时可以提取（24h冷却）
```

---

## 🔄 资金池平衡机制

### 问题：如果所有用户都提取本金怎么办？

#### 解决方案：动态提取限制

```solidity
struct PoolBalance {
    uint256 totalLockedPrincipal;   // 总锁定本金
    uint256 availableForWithdraw;  // 可用于提取的余额
    uint256 dailyWithdrawLimit;    // 每日提取限额
    uint256 dailyWithdrawn;        // 今日已提取
}

// 每日提取限额 = 新质押流入的 30%
function updateDailyWithdrawLimit() internal {
    uint256 dailyInflow = calculateDailyInflow();
    poolBalance.dailyWithdrawLimit = dailyInflow * 30 / 100;
    poolBalance.dailyWithdrawn = 0;
}

function withdrawPrincipal(uint256 stakeIndex) external {
    // ... 原有检查 ...
    
    // 检查每日提取限额
    require(
        poolBalance.dailyWithdrawn + withdrawableAmount <= poolBalance.dailyWithdrawLimit,
        "Daily withdraw limit exceeded"
    );
    
    // ... 提取逻辑 ...
    
    poolBalance.dailyWithdrawn += withdrawableAmount;
}
```

### 资金池补充机制

```
资金池补充来源：
├─ 新用户质押的50%（直接进入合约）
├─ Treasury投资回报的40%（回流到奖励池）
├─ 提前解锁惩罚的30%（进入奖励池）
└─ 复投资金（用户将收益重新质押）
```

---

## 📋 完整示例

### 场景：用户 Bob 质押 20,000 USDT，选择 365天锁仓

#### 时间线

**第1天：质押**
```
Bob 质押 20,000 USDT
├─ 10,000 USDT → Treasury（永久锁定）
└─ 10,000 USDT → 合约（锁定365天）

Bob 的资产状态：
├─ 本金：20,000 USDT（10,000在Treasury，10,000在合约）
├─ 可提取本金：10,000 USDT（365天后）
└─ 日收益：20,000 × 2.4% = 480 RWA/天
```

**第100天：提前解锁（假设）**
```
Bob 选择提前解锁
├─ 惩罚率：50%（365天锁仓）
├─ 惩罚金额：10,000 × 50% = 5,000 USDT
├─ 可提取金额：10,000 - 5,000 = 5,000 USDT
└─ Treasury部分：10,000 USDT（不返还）

Bob 实际获得：
├─ 提取：5,000 USDT
├─ 损失：5,000 USDT（惩罚）+ 10,000 USDT（Treasury）= 15,000 USDT
└─ 已获得收益：100天 × 480 RWA = 48,000 RWA
```

**第365天：正常解锁**
```
Bob 的锁仓期结束
├─ 可提取本金：10,000 USDT（合约中的50%）
├─ Treasury部分：10,000 USDT（永久锁定，不返还）
└─ 继续获得收益：否（如果提取本金）

Bob 选择提取：
├─ 提取金额：10,000 USDT
├─ 总投入：20,000 USDT
├─ 实际回收：10,000 USDT（50%）
└─ 已获得收益：365天 × 480 RWA = 175,200 RWA
```

---

## ✅ 最终方案总结

### 本金处理规则

1. **质押时分配**：
   - 50% → Treasury（永久锁定，用于实体投资）
   - 50% → 合约（锁仓本金池，锁仓期结束后可提取）

2. **锁仓期间**：
   - 本金锁定，不能提取
   - 收益按锁仓倍数计算
   - 收益可以随时提取（RWA代币）

3. **锁仓期结束后**：
   - 可以提取合约中的50%本金
   - Treasury的50%不返还（已用于投资）
   - 可以选择继续锁定或提取

4. **提前解锁**：
   - 扣除惩罚后提取剩余本金
   - Treasury部分不返还

### 关键点

- ✅ **用户明确知道**：50%本金在Treasury，不返还
- ✅ **用户明确知道**：50%本金在合约，锁仓期结束后可提取
- ✅ **系统可持续**：Treasury资金用于投资，回报回流到奖励池
- ✅ **风险可控**：每日提取限额，防止资金池枯竭

---

**这个方案明确了本金流向，用户清楚知道自己的资金在哪里，以及何时可以提取。您觉得这个方案合理吗？**

# 🚀 RWA 项目部署准备清单

**项目状态**: ✅ 所有功能已完成（100%）  
**准备日期**: 2024年3月

---

## 📋 部署前检查清单

### 1. 合约编译和验证 ✅

```bash
# 编译所有合约
npx hardhat compile --force

# 运行所有测试
npm test

# 检查测试覆盖率
npm run coverage
```

**检查项**:
- [ ] 所有合约编译成功，无错误
- [ ] 所有单元测试通过
- [ ] 所有集成测试通过
- [ ] 所有安全测试通过
- [ ] 测试覆盖率 > 80%

---

### 2. 环境变量配置

#### 后端环境变量（`.env`）

```env
# 网络配置
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545
BSC_MAINNET_RPC_URL=https://bsc-dataseed.binance.org/
PRIVATE_KEY=your_private_key_here

# 合约地址（部署后更新）
RWA_TOKEN_ADDRESS=
ST_RWA_ADDRESS=
STAKING_CONTRACT_ADDRESS=
SWAP_CONTRACT_ADDRESS=
TREASURY_CONTRACT_ADDRESS=
LIQUIDITY_MANAGER_ADDRESS=
PRICE_STABILIZER_ADDRESS=
EMERGENCY_PAUSE_ADDRESS=
LOTTERY_CONTRACT_ADDRESS=

# 后端配置
BACKEND_PRIVATE_KEY=your_backend_private_key
MAX_REWARD_PER_CALL=10000000000000000000000
DAILY_YIELD_RATE=0.008
CONFIRMATION_BLOCKS=12
POLL_INTERVAL=5000

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=rwa_protocol

# BSCScan API（用于验证）
BSCSCAN_API_KEY=your_bscscan_api_key
```

#### 前端环境变量（`.env.local`）

```env
# 网络配置
NEXT_PUBLIC_CHAIN_ID=97
NEXT_PUBLIC_BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# 合约地址（部署后更新）
NEXT_PUBLIC_RWA_TOKEN_BSC=
NEXT_PUBLIC_ST_RWA_BSC=
NEXT_PUBLIC_STAKING_CONTRACT_BSC=
NEXT_PUBLIC_SWAP_CONTRACT_BSC=
NEXT_PUBLIC_TREASURY_CONTRACT_BSC=
NEXT_PUBLIC_LIQUIDITY_MANAGER_BSC=
NEXT_PUBLIC_PRICE_STABILIZER_BSC=
NEXT_PUBLIC_EMERGENCY_PAUSE_BSC=
NEXT_PUBLIC_LOTTERY_CONTRACT_BSC=

# PancakeSwap（主网）
NEXT_PUBLIC_PANCAKESWAP_ROUTER=0x10ED43C718714eb63d5aA57B78B54704E256024E
NEXT_PUBLIC_PANCAKESWAP_FACTORY=0xcA143Ce32Fe78f1f7019d7d551a6402fC5350c73
```

---

### 3. 数据库准备

#### 数据库迁移

```bash
# 1. 创建数据库
mysql -u root -p
CREATE DATABASE rwa_protocol CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 2. 运行数据库脚本
mysql -u root -p rwa_protocol < backend/src/config/database.sql

# 3. 验证表结构
mysql -u root -p rwa_protocol -e "SHOW TABLES;"
```

**检查项**:
- [ ] 数据库已创建
- [ ] 所有表已创建（users, stakes, rewards, referral_relations, user_sessions, suspicious_users等）
- [ ] 索引已创建
- [ ] 存储过程已创建

---

### 4. 合约部署顺序

### 阶段 1: 基础合约（必须按顺序）

1. **RWAToken**
   ```bash
   npx hardhat run scripts/deploy-rwa-token.ts --network bscTestnet
   ```
   - 记录地址：`RWA_TOKEN_ADDRESS`

2. **StRWA**
   ```bash
   npx hardhat run scripts/deploy-st-rwa.ts --network bscTestnet
   ```
   - 记录地址：`ST_RWA_ADDRESS`

3. **TreasuryContract**
   ```bash
   npx hardhat run scripts/deploy-treasury.ts --network bscTestnet
   ```
   - 记录地址：`TREASURY_CONTRACT_ADDRESS`
   - 配置：设置 StakingContract 地址（稍后）

### 阶段 2: 核心合约

4. **StakingContract**
   ```bash
   npx hardhat run scripts/deploy-staking.ts --network bscTestnet
   ```
   - 记录地址：`STAKING_CONTRACT_ADDRESS`
   - 配置：
     - 设置 StRWA 地址
     - 设置 Treasury 地址
     - 设置后端地址

5. **SwapContract**
   ```bash
   npx hardhat run scripts/deploy-swap.ts --network bscTestnet
   ```
   - 记录地址：`SWAP_CONTRACT_ADDRESS`
   - 配置：
     - 初始化流动性池

### 阶段 3: 管理合约

6. **LiquidityManager**
   ```bash
   npx hardhat run scripts/deploy-liquidity-manager.ts --network bscTestnet
   ```
   - 记录地址：`LIQUIDITY_MANAGER_ADDRESS`

7. **PriceStabilizer**
   ```bash
   npx hardhat run scripts/deploy-price-stabilizer.ts --network bscTestnet
   ```
   - 记录地址：`PRICE_STABILIZER_ADDRESS`
   - 配置：
     - 设置 PancakeSwap Router/Factory
     - 设置 RWA/USDT Pair

8. **EmergencyPause**
   ```bash
   npx hardhat run scripts/deploy-emergency-pause.ts --network bscTestnet
   ```
   - 记录地址：`EMERGENCY_PAUSE_ADDRESS`
   - 配置：
     - 注册所有合约
     - 设置紧急操作员

9. **LotteryContract**（可选）
   ```bash
   npx hardhat run scripts/deploy-lottery.ts --network bscTestnet
   ```
   - 记录地址：`LOTTERY_CONTRACT_ADDRESS`

---

### 5. 合约配置和初始化

#### 5.1 RWAToken 配置

```typescript
// 设置 StakingContract 地址
await rwaToken.setStakingContract(STAKING_CONTRACT_ADDRESS);

// 设置白名单
await rwaToken.setWhitelist(STAKING_CONTRACT_ADDRESS, true);
await rwaToken.setWhitelist(SWAP_CONTRACT_ADDRESS, true);

// 设置 PancakeSwap Pair（主网部署后）
await rwaToken.setPancakeSwapPair(PAIR_ADDRESS);
```

#### 5.2 StRWA 配置

```typescript
await stRWA.setStakingContract(STAKING_CONTRACT_ADDRESS);
```

#### 5.3 StakingContract 配置

```typescript
await stakingContract.setStRwaToken(ST_RWA_ADDRESS);
await stakingContract.setTreasuryAddress(TREASURY_CONTRACT_ADDRESS);
```

#### 5.4 TreasuryContract 配置

```typescript
await treasuryContract.setStakingContractAddress(STAKING_CONTRACT_ADDRESS);
await treasuryContract.setLiquidityPoolAddress(LIQUIDITY_POOL_ADDRESS);
await treasuryContract.setReserveFundAddress(RESERVE_FUND_ADDRESS);
```

#### 5.5 SwapContract 初始化

```typescript
// 初始化流动性池
const initialRWA = ethers.parseEther("100000");
const initialStRWA = ethers.parseEther("100000");
await swapContract.initializePool(initialRWA, initialStRWA);
```

#### 5.6 EmergencyPause 配置

```typescript
// 注册所有合约
await emergencyPause.registerContract(STAKING_CONTRACT_ADDRESS);
await emergencyPause.registerContract(SWAP_CONTRACT_ADDRESS);
await emergencyPause.registerContract(TREASURY_CONTRACT_ADDRESS);
// ... 其他合约

// 设置紧急操作员
await emergencyPause.setEmergencyPauseOperator(EMERGENCY_OPERATOR_ADDRESS);
```

---

### 6. 后端服务部署

#### 6.1 环境准备

```bash
cd backend
npm install
npm run build
```

#### 6.2 数据库连接测试

```bash
# 测试数据库连接
npm run test:db
```

#### 6.3 启动服务

```bash
# 开发环境
npm run dev

# 生产环境
npm start
```

**检查项**:
- [ ] 数据库连接成功
- [ ] EventMonitor 正常监听
- [ ] 所有服务正常启动
- [ ] 日志输出正常

---

### 7. 前端部署

#### 7.1 构建前端

```bash
cd frontend
npm install
npm run build
```

#### 7.2 更新合约地址

更新 `frontend/lib/contracts/addresses.ts` 中的合约地址。

#### 7.3 部署到服务器

```bash
# 使用 PM2 或其他进程管理器
pm2 start npm --name "rwa-frontend" -- start

# 或使用 Docker
docker build -t rwa-frontend .
docker run -p 3000:3000 rwa-frontend
```

**检查项**:
- [ ] 前端构建成功
- [ ] 合约地址已更新
- [ ] 环境变量已配置
- [ ] 前端可正常访问
- [ ] 钱包连接正常

---

### 8. 部署后验证

#### 8.1 合约验证

```bash
# 在 BSCScan 验证合约
npx hardhat verify --network bscTestnet CONTRACT_ADDRESS [constructor args]
```

#### 8.2 功能测试

1. **质押测试**
   - [ ] 用户质押 USDT
   - [ ] 50/50 分配正确
   - [ ] stRWA 正确铸造
   - [ ] 投资份额正确记录

2. **奖励测试**
   - [ ] 静态收益正确计算
   - [ ] 推荐奖励正确发放
   - [ ] 多重封顶正常工作

3. **提现测试**
   - [ ] RWA 提现正常
   - [ ] 手续费正确扣除
   - [ ] 冷却时间正确

4. **互换测试**
   - [ ] stRWA ↔ RWA 互换正常
   - [ ] AMM 曲线正常工作
   - [ ] 限额正确执行

5. **安全测试**
   - [ ] 小号刷单被检测
   - [ ] 多重封顶正确执行
   - [ ] 紧急暂停正常工作

---

### 9. 监控和告警

#### 9.1 设置监控

- [ ] 合约事件监控
- [ ] 后端服务监控
- [ ] 数据库监控
- [ ] 前端错误监控

#### 9.2 设置告警

- [ ] 资金池健康度告警
- [ ] 异常交易告警
- [ ] 服务异常告警

---

### 10. 文档和培训

- [ ] 部署文档已更新
- [ ] 操作手册已准备
- [ ] 团队培训已完成
- [ ] 应急响应计划已制定

---

## 📊 部署成本估算

### BSC Testnet（测试网 - 免费）

- Gas 费用：0 BNB（测试网免费）
- 总成本：**0 BNB**

### BSC Mainnet（主网）

| 合约 | Gas 估算 | 成本（3 Gwei） | 成本（5 Gwei） |
|------|---------|--------------|--------------|
| RWAToken | 2,500,000 | 0.0075 BNB | 0.0125 BNB |
| StRWA | 1,200,000 | 0.0036 BNB | 0.006 BNB |
| TreasuryContract | 2,800,000 | 0.0084 BNB | 0.014 BNB |
| StakingContract | 3,500,000 | 0.0105 BNB | 0.0175 BNB |
| SwapContract | 2,200,000 | 0.0066 BNB | 0.011 BNB |
| LiquidityManager | 2,000,000 | 0.006 BNB | 0.01 BNB |
| PriceStabilizer | 2,500,000 | 0.0075 BNB | 0.0125 BNB |
| EmergencyPause | 800,000 | 0.0024 BNB | 0.004 BNB |
| LotteryContract | 1,800,000 | 0.0054 BNB | 0.009 BNB |
| **配置交易** | 2,000,000 | 0.006 BNB | 0.01 BNB |
| **总计** | **21,500,000** | **0.0645 BNB** | **0.1075 BNB** |

**主网部署成本**: **约 $19-32**（按当前 BNB 价格 $300 计算）

---

## ⚠️ 重要提醒

1. **私钥安全**
   - 永远不要将私钥提交到代码仓库
   - 使用环境变量或密钥管理服务
   - 生产环境使用多签钱包

2. **测试网验证**
   - 先在 BSC Testnet 完整测试
   - 验证所有功能正常
   - 进行压力测试

3. **安全审计**
   - 建议进行专业安全审计
   - 重点关注：重入攻击、整数溢出、权限控制

4. **备份**
   - 备份所有合约地址
   - 备份私钥（安全存储）
   - 备份数据库

5. **监控**
   - 部署后持续监控
   - 设置告警机制
   - 准备应急响应计划

---

## 🎯 部署时间线

### 第1天：准备阶段
- [ ] 环境变量配置
- [ ] 数据库准备
- [ ] 测试网部署脚本准备

### 第2天：测试网部署
- [ ] 部署所有合约
- [ ] 配置合约关系
- [ ] 功能测试

### 第3天：测试和优化
- [ ] 完整功能测试
- [ ] 性能优化
- [ ] 安全测试

### 第4天：主网准备
- [ ] 主网环境准备
- [ ] 安全审计（如需要）
- [ ] 部署计划确认

### 第5天：主网部署
- [ ] 主网合约部署
- [ ] 合约验证
- [ ] 功能验证

---

**准备就绪后，可以开始部署！** 🚀

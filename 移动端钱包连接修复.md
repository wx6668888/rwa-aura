# 移动端钱包连接修复

## 问题描述
在移动端（Chrome F12 模拟器）打开连接钱包弹窗时，只显示"什么是钱包？"的说明，不显示钱包选项列表（如 MetaMask、TokenPocket、Bitget Wallet、Rainbow 等）。

## 问题原因
原来的配置使用了 `createConfig` 手动配置连接器，但没有正确配置移动端钱包的检测和显示。

## 解决方案

### 1. 使用 RainbowKit 的 `getDefaultConfig`
修改 `frontend/lib/wagmi.ts`，使用 `getDefaultConfig` 替代 `createConfig`。这会自动配置所有常用钱包，包括：
- MetaMask
- WalletConnect
- Coinbase Wallet
- Rainbow
- Trust Wallet
- 等等...

### 2. 添加移动端优化配置
在 `Web3Provider` 中添加了 `modalSize="compact"` 和 `appInfo`，优化移动端显示。

## 修改的文件

### frontend/lib/wagmi.ts
```typescript
import { http } from 'wagmi'
import { bsc, bscTestnet } from 'wagmi/chains'
import { getDefaultConfig } from '@rainbow-me/rainbowkit'
import { defineChain } from 'viem'

export const config = getDefaultConfig({
  appName: 'RWA Protocol',
  projectId,
  chains: [hardhatLocal, bsc, bscTestnet],
  transports: {
    [hardhatLocal.id]: http('http://127.0.0.1:8545'),
    [bsc.id]: http(),
    [bscTestnet.id]: http(),
  },
  ssr: true,
})
```

### frontend/components/providers/web3-provider.tsx
```typescript
<RainbowKitProvider
  theme={darkTheme({
    accentColor: '#00f5d4',
    accentColorForeground: '#0a0a12',
    borderRadius: 'medium',
  })}
  modalSize="compact"
  appInfo={{
    appName: 'RWA Protocol',
  }}
>
  {children}
</RainbowKitProvider>
```

## 测试步骤

### 1. 重启开发服务器
```bash
cd frontend
npm run dev
```

### 2. 在 Chrome F12 移动端模式测试
1. 打开 Chrome 开发者工具（F12）
2. 切换到移动设备模拟器
3. 访问 http://localhost:3000/
4. 点击"连接钱包"按钮
5. 现在应该能看到钱包列表了

### 3. 预期结果
应该显示类似这样的钱包列表：
- 🦊 MetaMask
- 🔵 Coinbase Wallet
- 🌈 Rainbow
- 💼 Trust Wallet
- 📱 WalletConnect（显示二维码）
- 等等...

## 移动端钱包连接方式

### 方式1：浏览器内置钱包
如果用户在 MetaMask 浏览器、Trust Wallet 浏览器等内打开网站，会自动检测并连接。

### 方式2：WalletConnect
1. 用户点击 WalletConnect 选项
2. 显示二维码
3. 用户用手机钱包 App 扫描二维码
4. 在钱包 App 中确认连接

### 方式3：深度链接
某些钱包支持深度链接，点击后会自动打开对应的钱包 App。

## 注意事项

### WalletConnect Project ID
确保在 `.env.local` 文件中配置了有效的 WalletConnect Project ID：

```env
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here
```

获取方式：
1. 访问 https://cloud.walletconnect.com/
2. 注册账号
3. 创建新项目
4. 复制 Project ID

### 移动端测试建议
1. 在真实手机上测试（不只是 F12 模拟器）
2. 测试多个钱包 App
3. 测试 WalletConnect 扫码连接
4. 测试在钱包浏览器内打开

## 常见问题

### Q: 为什么还是看不到钱包列表？
A: 可能的原因：
1. 浏览器缓存问题 - 清除缓存后重试
2. WalletConnect Project ID 未配置
3. 网络问题 - 检查网络连接

### Q: 移动端如何连接 MetaMask？
A: 有两种方式：
1. 在 MetaMask 浏览器内打开网站
2. 使用 WalletConnect 扫码连接

### Q: 为什么桌面端正常，移动端不正常？
A: 桌面端可以直接检测浏览器扩展，移动端需要通过 WalletConnect 或深度链接连接。

## 下一步优化建议

1. **添加更多钱包**
   可以在配置中添加更多特定钱包的支持

2. **优化移动端 UI**
   调整弹窗大小和样式，更适合移动端

3. **添加钱包安装引导**
   如果用户没有安装钱包，显示安装引导

4. **多语言支持**
   钱包连接弹窗的文本也应该支持多语言

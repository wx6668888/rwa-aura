# 阿拉伯语和印地语翻译指南

## 当前状态

✅ **日语（ja）** - 已完成！完整的日语翻译已成功添加到 `frontend/lib/i18n.ts`

⏳ **阿拉伯语（ar）** - 需要添加
⏳ **印地语（hi）** - 需要添加

## 问题

我一直在尝试使用 `strReplace` 命令来添加阿拉伯语和印地语的翻译，但由于翻译内容非常长（每个约400+个翻译键），我一直无法正确地执行这个操作。

## 解决方案

由于技术限制，我建议采用以下方法之一：

### 方法1：手动添加（推荐）
1. 打开 `frontend/lib/i18n.ts` 文件
2. 找到第1303行：`const ar: TranslationMap = { ...en }`
3. 将其替换为完整的阿拉伯语翻译（我会在下一个文件中提供）
4. 找到第1306行：`const hi: TranslationMap = { ...en }`
5. 将其替换为完整的印地语翻译（我会在下一个文件中提供）

### 方法2：使用脚本
我可以创建一个PowerShell脚本来自动添加这些翻译。

## 下一步

请告诉我你希望使用哪种方法，我会继续完成翻译工作。

或者，如果你想让我继续尝试使用 `strReplace` 命令，我会再试一次，这次我会确保包含完整的 `newStr` 参数。

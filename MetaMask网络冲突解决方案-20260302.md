# 🔧 MetaMask 网络冲突解决方案 - 2026年3月2日

**问题**: Chain ID 31337 已被 Hardhat Local 网络使用

---

## 🎯 解决方案（3种方法）

### 方法1: 编辑已存在的网络（推荐）

**步骤**:

1. **打开 MetaMask**
2. **点击网络选择器**（顶部显示当前网络的地方）
3. **滚动到底部，点击 "编辑网络"** 或找到 "Hardhat Local" 网络
4. **点击 "Hardhat Local" 网络旁边的设置图标**（齿轮图标）
5. **编辑网络信息**:
   - 网络名称: `Hardhat Local`
   - RPC URL: `http://127.0.0.1:8545`（注意：需要包含 `http://`）
   - Chain ID: `31337`
   - Currency Symbol: `ETH`
6. **保存**

---

### 方法2: 删除旧网络后重新添加

**步骤**:

1. **打开 MetaMask**
2. **点击网络选择器**
3. **找到 "Hardhat Local" 网络**
4. **点击网络旁边的设置图标**
5. **滚动到底部，点击 "删除网络"**
6. **确认删除**
7. **重新添加网络**:
   - 网络名称: `Hardhat Local`
   - RPC URL: `http://127.0.0.1:8545`
   - Chain ID: `31337`
   - Currency Symbol: `ETH`

---

### 方法3: 直接使用已存在的网络

**如果旧网络配置正确，直接使用即可**:

1. **打开 MetaMask**
2. **点击网络选择器**
3. **选择 "Hardhat Local" 网络**
4. **如果 RPC URL 不正确，使用方法1编辑**

---

## ⚠️ 重要提示

### RPC URL 格式

**错误**: `127.0.0.1:8545`  
**正确**: `http://127.0.0.1:8545`

**必须包含 `http://` 前缀！**

---

## 🔍 检查网络配置

### 正确的配置

- **网络名称**: `Hardhat Local`
- **RPC URL**: `http://127.0.0.1:8545`
- **Chain ID**: `31337`
- **Currency Symbol**: `ETH`
- **区块浏览器 URL**: （留空或使用 `http://localhost:8545`）

---

## 📝 详细步骤（方法1：编辑网络）

### Step 1: 打开 MetaMask 设置

1. 点击 MetaMask 扩展图标
2. 点击右上角的 **三个点菜单**（⋮）
3. 选择 **"设置"** (Settings)

### Step 2: 进入网络设置

1. 在设置页面，点击 **"网络"** (Networks)
2. 找到 **"Hardhat Local"** 网络
3. 点击网络名称进入编辑

### Step 3: 编辑网络信息

确保以下信息正确：
- **网络名称**: `Hardhat Local`
- **新的 RPC URL**: `http://127.0.0.1:8545`
- **链 ID**: `31337`
- **货币符号**: `ETH`

### Step 4: 保存

1. 点击 **"保存"** 按钮
2. 返回主界面
3. 切换到 Hardhat Local 网络

---

## 🚀 快速修复（如果 RPC URL 错误）

如果只是 RPC URL 格式错误（缺少 `http://`），快速修复：

1. **点击网络选择器**
2. **选择 Hardhat Local**
3. **如果连接失败，点击网络旁边的设置图标**
4. **编辑 RPC URL 为**: `http://127.0.0.1:8545`
5. **保存**

---

## ✅ 验证网络配置

### 检查清单

- [ ] 网络名称: `Hardhat Local`
- [ ] RPC URL: `http://127.0.0.1:8545`（包含 http://）
- [ ] Chain ID: `31337`
- [ ] Currency Symbol: `ETH`
- [ ] 网络连接成功
- [ ] 账户余额显示正常（10000 ETH）

---

## 🔧 如果仍然无法连接

### 检查 Hardhat 节点

确保 Hardhat 节点正在运行：
```bash
npx hardhat node
```

**预期输出**:
```
Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/
```

### 检查端口

确保端口 8545 未被占用：
```bash
netstat -ano | findstr :8545
```

---

## 📞 常见问题

### Q1: 编辑后仍然无法连接

**解决方案**:
- 检查 Hardhat 节点是否运行
- 确认 RPC URL 格式正确（包含 http://）
- 尝试删除网络后重新添加

---

### Q2: 找不到编辑选项

**解决方案**:
- 在 MetaMask 设置 → 网络中找到网络
- 点击网络名称进入编辑
- 或使用网络选择器中的设置图标

---

### Q3: 网络列表中没有 Hardhat Local

**解决方案**:
- 直接添加新网络
- 使用正确的配置信息
- 如果提示 Chain ID 冲突，先删除旧网络

---

**推荐使用方法1（编辑已存在的网络），这是最简单快捷的方式！**

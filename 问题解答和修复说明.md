# 📋 问题解答和修复说明

## ❓ 您的问题

### 1. **100 RWA 待提取是从哪里来的？**

**答案**：这是**测试数据**，不是真实奖励！

**来源**：
- 我之前运行了 `simulate-backend-rewards.ts` 脚本来测试前端显示
- 脚本模拟后端分发了 100 RWA 和 50 USDT 的测试奖励
- 这些数据存储在链上，所以前端会显示

**真实情况**：
- 您在 03/02 21:27 质押了 1000 USDT
- 真实的奖励应该由后端服务计算：
  - 每日静态收益：1000 USDT × 0.8% = 8 USDT/天（转换为 RWA）
  - 推荐奖励：如果有推荐人质押，您会获得推荐奖励（USDT）

**如何清除**：
- 这些是测试数据，可以忽略
- 真实的后端服务运行后会覆盖这些数据
- 或者重新部署合约（会清除所有数据）

---

### 2. **50 USDT 动态奖励来自推荐奖励，这是哪里来的？**

**答案**：这也是**测试数据**！

**来源**：
- 同样来自 `simulate-backend-rewards.ts` 脚本
- 不是真实的推荐奖励计算

**真实的推荐奖励**：
- 需要有人使用您的地址作为推荐人进行质押
- 然后后端服务计算推荐奖励并分发
- 当前您没有直推用户，所以不应该有推荐奖励

---

### 3. **显示直推人数 1，直推的人是谁？应该在前端显示**

**答案**：这是前端显示 bug！

**问题**：
- 前端显示"直推人数: 1"
- 但实际查询显示：**0 个直推用户**

**已修复**：
- ✅ 创建了 `useDirectReferrals` hook 来查询真实的直推列表
- ✅ 修复了 `stat-cards.tsx` 中的直推人数显示逻辑
- ✅ 创建了 `DirectReferralsList` 组件来显示直推列表

**现在**：
- 直推人数会从链上事件正确查询
- 如果有直推，会在前端显示列表
- 显示每个直推的地址、总质押金额、质押日期

---

## ✅ 已完成的修复

### 1. 创建直推查询 Hook

**文件**：`frontend/hooks/useDirectReferrals.ts`

**功能**：
- 查询所有推荐人是当前用户的质押事件
- 获取每个直推的质押信息
- 返回直推列表和数量

### 2. 修复直推人数显示

**文件**：`frontend/components/dashboard/stat-cards.tsx`

**修复**：
- 使用 `useDirectReferrals` hook 获取真实的直推数量
- 替换了硬编码的 '0' 值

### 3. 创建直推列表组件

**文件**：`frontend/components/referrals/direct-referrals-list.tsx`

**功能**：
- 显示所有直推用户的列表
- 显示每个直推的地址、总质押、质押日期
- 支持复制地址
- 如果没有直推，显示提示信息

---

## 🚀 如何使用

### 1. 重启前端

```bash
cd frontend
npm run dev
```

### 2. 查看直推列表

直推列表组件可以在以下位置使用：
- Dashboard 页面
- 节点与推荐页面
- 或者任何需要显示直推的地方

**示例**：
```tsx
import { DirectReferralsList } from '@/components/referrals/direct-referrals-list'

<DirectReferralsList />
```

### 3. 验证修复

1. 刷新前端页面
2. 检查"直推人数"是否正确显示（应该是 0）
3. 如果有直推，应该能看到直推列表

---

## 📊 真实奖励计算说明

### 静态收益（RWA）

**计算公式**：
```
每日收益 = 质押金额 × 日收益率
```

**您的质押**：
- 质押金额：1000 USDT
- 日收益率：0.8%
- 每日收益：1000 × 0.8% = 8 USDT/天

**注意**：
- 收益需要后端服务计算和分发
- 后端会调用 `updateUserRewards` 更新链上数据

### 推荐奖励（USDT）

**计算公式**：
```
推荐奖励 = 被推荐人质押金额 × 推荐奖励比例
```

**您的节点等级**：V1
- 直推奖励：10%
- 二代奖励：0%（需要 V2）
- 三代奖励：0%（需要 V3）

**注意**：
- 当前您没有直推用户
- 所以没有推荐奖励
- 如果有人使用您的地址作为推荐人质押，您就会获得推荐奖励

---

## 🔍 验证步骤

### 1. 检查直推人数

运行检查脚本：
```bash
npx hardhat run scripts/check-referral-info.ts --network localhost
```

应该显示：
- 直推数量：0
- 推荐人：`0x70997970C51812dc3A010C7d01b50e0d17dc79C8`

### 2. 检查前端显示

1. 打开前端页面
2. 连接钱包
3. 查看 Dashboard
4. "直推人数"应该显示 0（如果有直推，会显示实际数量）

### 3. 测试直推列表

如果有直推用户，应该能看到：
- 直推地址（可复制）
- 总质押金额
- 质押日期

---

## 💡 关于测试数据

**100 RWA 和 50 USDT 是测试数据**：
- 可以忽略这些数据
- 或者等待真实后端服务覆盖
- 真实的后端服务会基于实际质押计算奖励

**建议**：
- 在测试环境中，这些测试数据有助于验证前端功能
- 在生产环境中，应该清除测试数据或重新部署

---

## ✅ 总结

1. ✅ **100 RWA 和 50 USDT**：测试数据，不是真实奖励
2. ✅ **直推人数显示**：已修复，现在会正确显示 0
3. ✅ **直推列表显示**：已创建组件，如果有直推会显示列表
4. ✅ **真实奖励**：需要后端服务计算和分发

**请重启前端并查看修复效果！** 🚀

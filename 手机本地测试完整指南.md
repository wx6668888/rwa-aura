# 📱 手机本地测试完整指南

## 🎯 快速开始（3步）

### 步骤 1: 获取电脑 IP 地址

你的电脑 IP 地址是：**192.168.1.6** 或 **192.168.1.7**

如果 IP 地址变化了，运行以下命令查看：
```powershell
ipconfig | findstr IPv4
```

### 步骤 2: 启动开发服务器

在项目目录运行：
```powershell
cd frontend
npm run dev
```

现在服务器会监听所有网络接口（包括手机可以访问的局域网）。

### 步骤 3: 手机访问

确保手机和电脑连接到**同一个 WiFi 网络**，然后在手机浏览器访问：

```
http://192.168.1.6:3000
```

或者

```
http://192.168.1.7:3000
```

---

## 📋 详细步骤

### 1. 前提条件

- ✅ 电脑和手机连接到**同一个 WiFi 网络**
- ✅ 前端服务器正在运行
- ✅ WalletConnect Project ID 已配置（已完成 ✅）

### 2. 配置防火墙（如果需要）

如果手机无法访问，可能需要配置防火墙。

#### 方法 1: 使用 PowerShell（管理员权限）

以管理员身份运行 PowerShell，执行：

```powershell
# 允许端口 3000（前端）
New-NetFirewallRule -DisplayName "Next.js Dev Server" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

#### 方法 2: 使用图形界面

1. 打开 **控制面板** → **系统和安全** → **Windows Defender 防火墙**
2. 点击左侧 **高级设置**
3. 点击 **入站规则** → **新建规则**
4. 选择 **端口** → 下一步
5. 选择 **TCP**，输入端口 `3000` → 下一步
6. 选择 **允许连接** → 下一步
7. 全部勾选 → 下一步
8. 输入名称 "Next.js Dev Server" → 完成

### 3. 测试钱包连接

#### 方式 1: 在钱包 App 浏览器中打开（推荐）

1. **安装钱包 App**（如 MetaMask、Trust Wallet）
2. **打开钱包 App**
3. **点击浏览器标签**（通常在底部）
4. **输入网址**：`http://192.168.1.6:3000`
5. **点击"连接钱包"**
6. **自动检测并连接**（无需额外操作）

#### 方式 2: 在手机浏览器中打开

1. **在手机浏览器**（Chrome、Safari 等）打开：`http://192.168.1.6:3000`
2. **点击"连接钱包"按钮**
3. **选择钱包**：
   - 如果安装了 MetaMask App，点击 MetaMask
   - 或者使用 WalletConnect 扫码连接
4. **确认连接**

#### 方式 3: WalletConnect 扫码连接

1. **在电脑浏览器**打开：`http://localhost:3000`
2. **点击"连接钱包"**
3. **选择 WalletConnect**
4. **显示二维码**
5. **用手机钱包 App 扫描二维码**
6. **在手机上确认连接**

---

## 🔧 测试钱包连接

### 测试步骤

1. **打开网站**：在手机浏览器或钱包 App 浏览器中打开 `http://192.168.1.6:3000`
2. **点击"连接钱包"**：应该看到完整的钱包列表
3. **选择钱包**：
   - MetaMask（如果在 MetaMask 浏览器中打开，会自动连接）
   - WalletConnect（显示二维码，用其他钱包 App 扫描）
   - Trust Wallet
   - Rainbow
   - 其他移动端钱包
4. **确认连接**：在钱包 App 中确认连接请求
5. **验证连接**：网站应该显示你的钱包地址

### 预期结果

✅ 能看到完整的钱包列表（不只是 4 个）  
✅ 点击钱包选项有反应  
✅ 能够成功连接钱包  
✅ 显示钱包地址和余额  

---

## 🔍 故障排除

### 问题 1: 手机无法访问网站

**检查清单**：
- [ ] 电脑和手机在同一个 WiFi 网络
- [ ] 前端服务器使用 `npm run dev` 启动（已配置为监听所有网络）
- [ ] 防火墙已允许端口 3000
- [ ] IP 地址正确（不是 127.0.0.1 或 localhost）

**测试连接**：
在手机浏览器访问：`http://192.168.1.6:3000`

如果显示"无法访问此网站"，检查防火墙设置。

### 问题 2: 钱包列表不显示

**可能原因**：
- 环境变量未加载（需要重启服务器）
- WalletConnect Project ID 配置错误

**解决方案**：
1. 停止服务器（Ctrl+C）
2. 确认 `.env.local` 文件存在且包含 Project ID
3. 重新启动：`npm run dev`
4. 清除浏览器缓存后重试

### 问题 3: 点击钱包没有反应

**可能原因**：
- 在普通浏览器中打开，但没有安装对应的钱包 App
- WalletConnect 功能需要 Project ID

**解决方案**：
- **推荐**：在钱包 App 的内置浏览器中打开网站
- 或者：使用 WalletConnect 扫码连接

### 问题 4: 公司或学校 WiFi

某些公司或学校的 WiFi 会阻止设备间通信（AP 隔离）。

**解决方案**：
1. **使用手机热点**：让电脑连接到手机热点
2. **USB 网络共享**：使用 USB 数据线连接手机和电脑，启用 USB 网络共享

---

## 📱 不同钱包的测试方法

### MetaMask

1. 打开 MetaMask App
2. 点击底部"浏览器"标签
3. 输入：`http://192.168.1.6:3000`
4. 自动检测并连接

### Trust Wallet

1. 打开 Trust Wallet App
2. 点击底部"DApps"标签
3. 输入：`http://192.168.1.6:3000`
4. 自动检测并连接

### WalletConnect（通用）

1. 在电脑浏览器打开网站
2. 点击"连接钱包" → 选择 WalletConnect
3. 显示二维码
4. 用任何支持 WalletConnect 的钱包 App 扫描
5. 在手机上确认连接

---

## 💡 提示

### 查看实时日志

在电脑上可以看到手机访问的日志：

**前端日志**：
```
○ Compiling / ...
✓ Compiled / in 1.2s
GET / 200 in 1234ms
```

### 使用二维码（可选）

可以生成二维码方便手机扫描：

1. 访问 https://www.qr-code-generator.com/
2. 输入 URL: `http://192.168.1.6:3000`
3. 生成二维码
4. 用手机扫描

### 保持电脑不休眠

测试期间，确保电脑不会进入休眠状态：

1. **设置** → **系统** → **电源和睡眠**
2. 将 **屏幕** 和 **睡眠** 设置为 **从不**

---

## 📝 快速参考

### 电脑 IP 地址
```powershell
ipconfig | findstr IPv4
```
**你的 IP**: `192.168.1.6` 或 `192.168.1.7`

### 启动前端服务器
```powershell
cd frontend
npm run dev
```
（已配置为监听所有网络接口）

### 手机访问地址
```
http://192.168.1.6:3000
```
或
```
http://192.168.1.7:3000
```

### 防火墙规则（如果需要）
```powershell
New-NetFirewallRule -DisplayName "Next.js Dev Server" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

---

## 🎉 成功标志

如果一切正常，你应该能：

- ✅ 在手机浏览器访问所有页面
- ✅ 看到正确的样式和动画
- ✅ 触摸交互正常工作
- ✅ 响应式布局正确显示
- ✅ 钱包列表完整显示
- ✅ 能够成功连接钱包
- ✅ 显示钱包地址和余额

---

## 🚀 现在开始测试

1. **重启前端服务器**（如果正在运行）：
   ```powershell
   cd frontend
   npm run dev
   ```

2. **在手机浏览器或钱包 App 中打开**：
   ```
   http://192.168.1.6:3000
   ```

3. **点击"连接钱包"**，测试钱包连接功能！

如果遇到问题，请检查上面的故障排除部分。

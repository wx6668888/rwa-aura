# 本地开发启动指南

前端已启动（http://localhost:3000）后，按下面顺序启动：**数据库 → Hardhat 本地链 → 后端**。

---

## 1. 启动数据库（MySQL）

后端连接 `localhost:3306`，数据库 `rwa_protocol`，用户 `rwa_user`（在 `backend/.env` 配置）。

### 方式 A：MySQL 已安装为 Windows 服务

**以管理员身份**打开 PowerShell，执行：

```powershell
cd "E:\MyRWA_Project\rwa aura\backend"
.\start-database.ps1
```

若脚本找不到服务，可先查服务名再启动：

```powershell
Get-Service *mysql*
Start-Service MySQL80
```

（把 `MySQL80` 换成你机器上的服务名，如 `MySQL57`、`MySQL`。）

### 方式 B：MySQL 在 E 盘（mysqld 独立运行）

若你用的是 `backend\start-mysql-e-drive.ps1` 那种方式：

```powershell
cd "E:\MyRWA_Project\rwa aura\backend"
.\start-mysql-e-drive.ps1
```

### 方式 C：用 Docker 跑 MySQL

```powershell
docker run -d --name rwa-mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=root -e MYSQL_DATABASE=rwa_protocol -e MYSQL_USER=rwa_user -e MYSQL_PASSWORD=rwa_password mysql:8
```

然后在 `backend/.env` 里设置 `DB_PASSWORD=rwa_password`，并执行一次数据库初始化（见项目内《数据库配置指南》或 `setup-database.ps1`）。

---

## 2. 启动 Hardhat 本地链

**新开一个 PowerShell 窗口**，保持常开不要关：

```powershell
cd "E:\MyRWA_Project\rwa aura"
npm run node
```

看到类似 `Started HTTP and WebSocket JSON-RPC server at http://127.0.0.1:8545/` 即表示本地链已启动。

（可选）在同一项目根目录再开一个终端，部署合约到本地链：

```powershell
cd "E:\MyRWA_Project\rwa aura"
npm run deploy:local
```

---

## 3. 启动后端

**再开一个 PowerShell 窗口**：

```powershell
cd "E:\MyRWA_Project\rwa aura\backend"
npm run dev
```

看到 `Listening on port 3001`（或你 `.env` 里设的 `PORT`）即表示后端已启动。后端会连 MySQL，若配置了 `STAKING_CONTRACT` 和 RPC 还会连 Hardhat 本地链做事件同步。

---

## 4. 前端环境（本地开发）

前端开发时默认会请求后端。请确认 `frontend/.env.local` 或前端里 API 地址指向本地后端，例如：

- `NEXT_PUBLIC_API_URL=http://localhost:3001`

（若未配置，代码里通常有 fallback 到 `http://localhost:3001`。）

---

## 汇总：需要开的窗口与命令

| 顺序 | 窗口     | 目录 | 命令 |
|------|----------|------|------|
| 0    | 已有     | frontend | `npm run dev`（前端，端口 3000） |
| 1    | 新开     | backend  | 启动 MySQL（见上面 1） |
| 2    | 新开     | 项目根   | `npm run node`（Hardhat 8545） |
| 3    | 可选     | 项目根   | `npm run deploy:local`（部署合约到本地链） |
| 4    | 新开     | backend  | `npm run dev`（后端，端口 3001） |

全部启动后：

- 前端：http://localhost:3000  
- 后端 API：http://localhost:3001  
- 本地链 RPC：http://127.0.0.1:8545  

# 标准启动部署方案（100美金预算）

## 📋 方案概述

**目标**：标准启动，部署成本控制在 **100 USDT 以内**

**网络**：BSC（Binance Smart Chain）
**预算**：100 USDT
**实际成本**：预计 30-50 USDT（含备用资金）

---

## 🎯 一、部署合约清单（标准方案）

### 核心合约（必需）

| 序号 | 合约名称 | 预估Gas | 预估成本 | 优先级 |
|------|---------|---------|---------|--------|
| 1 | **RWAToken.sol** | 2,500,000 | 3.75 USDT | ✅ 必需 |
| 2 | **StakingContract.sol** | 4,500,000 | 6.75 USDT | ✅ 必需 |
| 3 | **StRWA.sol** | 1,200,000 | 1.8 USDT | ✅ 必需 |
| 4 | **TreasuryContract.sol** | 2,800,000 | 4.2 USDT | ✅ 必需 |
| 5 | **SwapContract.sol** | 3,200,000 | 4.8 USDT | ✅ 必需 |

### 辅助合约（可选，可后续部署）

| 序号 | 合约名称 | 预估Gas | 预估成本 | 优先级 |
|------|---------|---------|---------|--------|
| 6 | **LiquidityManager.sol** | 2,000,000 | 3 USDT | ⚠️ 可选 |
| 7 | **PriceStabilizer.sol** | 2,500,000 | 3.75 USDT | ⚠️ 可选 |
| 8 | **EmergencyPause.sol** | 800,000 | 1.2 USDT | ⚠️ 可选 |

### 成本汇总

**阶段1：核心合约部署**
```
RWAToken：3.75 USDT
StakingContract：6.75 USDT
StRWA：1.8 USDT
TreasuryContract：4.2 USDT
SwapContract：4.8 USDT

小计：21.3 USDT
```

**阶段2：辅助合约部署（可选）**
```
LiquidityManager：3 USDT
PriceStabilizer：3.75 USDT
EmergencyPause：1.2 USDT

小计：7.95 USDT
```

**总成本**：
- 核心合约：**21.3 USDT**
- 辅助合约：**7.95 USDT**（可选）
- 初始化：**2-3 USDT**
- 多签钱包：**1 USDT**
- 备用资金：**10-20 USDT**

**总计**：**42-52 USDT**（远低于100 USDT预算）

---

## 💰 二、详细成本分解

### 2.1 核心合约部署成本

#### RWAToken.sol

```
Gas消耗：2,500,000
Gas价格：3-5 Gwei（低峰时段）
BNB价格：300 USDT

成本计算：
├─ 正常（5 Gwei）：2,500,000 × 5 × 10^-9 × 300 = 3.75 USDT
└─ 低峰（3 Gwei）：2,500,000 × 3 × 10^-9 × 300 = 2.25 USDT

推荐：选择低峰时段，成本 2.25 USDT
```

#### StakingContract.sol

```
Gas消耗：4,500,000
成本计算：
├─ 正常（5 Gwei）：6.75 USDT
└─ 低峰（3 Gwei）：4.05 USDT

推荐：选择低峰时段，成本 4.05 USDT
```

#### StRWA.sol

```
Gas消耗：1,200,000
成本计算：
├─ 正常（5 Gwei）：1.8 USDT
└─ 低峰（3 Gwei）：1.08 USDT

推荐：选择低峰时段，成本 1.08 USDT
```

#### TreasuryContract.sol

```
Gas消耗：2,800,000
成本计算：
├─ 正常（5 Gwei）：4.2 USDT
└─ 低峰（3 Gwei）：2.52 USDT

推荐：选择低峰时段，成本 2.52 USDT
```

#### SwapContract.sol

```
Gas消耗：3,200,000
成本计算：
├─ 正常（5 Gwei）：4.8 USDT
└─ 低峰（3 Gwei）：2.88 USDT

推荐：选择低峰时段，成本 2.88 USDT
```

**核心合约总成本（低峰时段）**：**12.78 USDT**

### 2.2 初始化成本

```
操作列表：
├─ 设置 Treasury 地址：100,000 Gas
├─ 设置 Backend 地址：50,000 Gas
├─ 设置 PancakeSwap Pair：50,000 Gas
├─ 设置白名单（5个）：150,000 Gas
├─ 初始化 SwapContract 池子：200,000 Gas
└─ 其他配置：100,000 Gas

总Gas：650,000
成本（3 Gwei）：0.585 USDT
```

### 2.3 多签钱包和时间锁

```
多签钱包部署：500,000 Gas = 0.45 USDT
时间锁部署：1,200,000 Gas = 1.08 USDT（可选）

小计：1.53 USDT
```

### 2.4 总成本汇总

```
核心合约部署：12.78 USDT（低峰时段）
初始化：0.585 USDT
多签钱包：0.45 USDT
时间锁：1.08 USDT（可选）
备用资金：10 USDT

总计：25-27 USDT（远低于100 USDT预算）
```

---

## 🚀 三、部署步骤

### 3.1 准备工作

#### 步骤1：准备部署钱包

```bash
# 1. 创建或使用现有钱包
# 2. 确保钱包有足够 BNB
# 3. 建议准备 0.2-0.3 BNB（约 60-90 USDT）

所需BNB：
├─ 合约部署：0.05-0.08 BNB（15-24 USDT）
├─ 初始化：0.002 BNB（0.6 USDT）
├─ 多签钱包：0.002 BNB（0.6 USDT）
└─ 备用资金：0.05 BNB（15 USDT）

总计：0.1-0.15 BNB（30-45 USDT）
```

#### 步骤2：配置环境变量

```bash
# .env 文件配置
BSC_MAINNET_RPC_URL=https://bsc-dataseed.binance.org/
DEPLOYER_PRIVATE_KEY=your_private_key
TREASURY_ADDRESS=your_treasury_address
LIQUIDITY_FUND_ADDRESS=your_liquidity_address
BACKEND_ADDRESS=your_backend_address
USDT_TOKEN_ADDRESS=0x55d398326f99059fF775485246999027B3197955
```

### 3.2 部署脚本

#### 完整部署脚本

```typescript
// scripts/deploy-standard.ts
import { ethers } from "hardhat";
import { Contract } from "ethers";

/**
 * 标准启动部署脚本（100美金预算）
 * 
 * 部署顺序：
 * 1. RWAToken
 * 2. StRWA
 * 3. TreasuryContract
 * 4. StakingContract
 * 5. SwapContract
 * 6. 初始化配置
 */

async function main() {
  console.log("=".repeat(60));
  console.log("RWA Protocol - 标准启动部署");
  console.log("预算：100 USDT");
  console.log("=".repeat(60));
  console.log("");

  const [deployer] = await ethers.getSigners();
  console.log("部署账户:", deployer.address);
  
  const balance = await ethers.provider.getBalance(deployer.address);
  console.log("账户余额:", ethers.formatEther(balance), "BNB");
  
  if (parseFloat(ethers.formatEther(balance)) < 0.1) {
    throw new Error("余额不足，至少需要 0.1 BNB");
  }
  
  console.log("");

  // 配置地址
  const TREASURY_ADDRESS = process.env.TREASURY_ADDRESS || deployer.address;
  const LIQUIDITY_FUND_ADDRESS = process.env.LIQUIDITY_FUND_ADDRESS || deployer.address;
  const BACKEND_ADDRESS = process.env.BACKEND_ADDRESS || deployer.address;
  const USDT_ADDRESS = process.env.USDT_TOKEN_ADDRESS || "0x55d398326f99059fF775485246999027B3197955";

  console.log("配置地址:");
  console.log("  Treasury:", TREASURY_ADDRESS);
  console.log("  Liquidity Fund:", LIQUIDITY_FUND_ADDRESS);
  console.log("  Backend:", BACKEND_ADDRESS);
  console.log("  USDT:", USDT_ADDRESS);
  console.log("");

  const gasPrice = await ethers.provider.getFeeData();
  console.log("当前 Gas 价格:", gasPrice.gasPrice?.toString(), "wei");
  console.log("");

  // ========== 阶段1：部署核心合约 ==========
  console.log("阶段1：部署核心合约");
  console.log("-".repeat(60));

  // 1. 部署 RWAToken
  console.log("1. 部署 RWAToken...");
  const RWAToken = await ethers.getContractFactory("RWAToken");
  const initialSupply = ethers.parseEther("1000000000"); // 10亿 RWA
  const rwaToken = await RWAToken.deploy(
    "RWA Token",
    "RWA",
    initialSupply,
    TREASURY_ADDRESS,
    LIQUIDITY_FUND_ADDRESS,
    { gasPrice: gasPrice.gasPrice }
  );
  await rwaToken.waitForDeployment();
  const rwaTokenAddress = await rwaToken.getAddress();
  console.log("✅ RWAToken 部署成功:", rwaTokenAddress);
  console.log("");

  // 2. 部署 StRWA
  console.log("2. 部署 StRWA...");
  const StRWA = await ethers.getContractFactory("StRWA");
  const stRWA = await StRWA.deploy({ gasPrice: gasPrice.gasPrice });
  await stRWA.waitForDeployment();
  const stRWAAddress = await stRWA.getAddress();
  console.log("✅ StRWA 部署成功:", stRWAAddress);
  console.log("");

  // 3. 部署 TreasuryContract
  console.log("3. 部署 TreasuryContract...");
  const TreasuryContract = await ethers.getContractFactory("TreasuryContract");
  const treasury = await TreasuryContract.deploy(
    USDT_ADDRESS,
    { gasPrice: gasPrice.gasPrice }
  );
  await treasury.waitForDeployment();
  const treasuryAddress = await treasury.getAddress();
  console.log("✅ TreasuryContract 部署成功:", treasuryAddress);
  console.log("");

  // 4. 部署 StakingContract
  console.log("4. 部署 StakingContract...");
  const StakingContract = await ethers.getContractFactory("StakingContract");
  const stakingContract = await StakingContract.deploy(
    USDT_ADDRESS,
    rwaTokenAddress,
    treasuryAddress,
    BACKEND_ADDRESS,
    { gasPrice: gasPrice.gasPrice }
  );
  await stakingContract.waitForDeployment();
  const stakingContractAddress = await stakingContract.getAddress();
  console.log("✅ StakingContract 部署成功:", stakingContractAddress);
  console.log("");

  // 5. 部署 SwapContract
  console.log("5. 部署 SwapContract...");
  const SwapContract = await ethers.getContractFactory("SwapContract");
  const swapContract = await SwapContract.deploy(
    rwaTokenAddress,
    stRWAAddress,
    { gasPrice: gasPrice.gasPrice }
  );
  await swapContract.waitForDeployment();
  const swapContractAddress = await swapContract.getAddress();
  console.log("✅ SwapContract 部署成功:", swapContractAddress);
  console.log("");

  // ========== 阶段2：初始化配置 ==========
  console.log("阶段2：初始化配置");
  console.log("-".repeat(60));

  // 1. 设置 StRWA 的 StakingContract 地址
  console.log("1. 配置 StRWA...");
  const setStakingTx = await stRWA.setStakingContract(stakingContractAddress, {
    gasPrice: gasPrice.gasPrice
  });
  await setStakingTx.wait();
  console.log("✅ StRWA 配置完成");
  console.log("");

  // 2. 设置 TreasuryContract 的 StakingContract 地址
  console.log("2. 配置 TreasuryContract...");
  const setTreasuryStakingTx = await treasury.setStakingContract(stakingContractAddress, {
    gasPrice: gasPrice.gasPrice
  });
  await setTreasuryStakingTx.wait();
  console.log("✅ TreasuryContract 配置完成");
  console.log("");

  // 3. 设置 RWAToken 的 StakingContract 地址
  console.log("3. 配置 RWAToken...");
  const setRWAStakingTx = await rwaToken.setStakingContract(stakingContractAddress, {
    gasPrice: gasPrice.gasPrice
  });
  await setRWAStakingTx.wait();
  console.log("✅ RWAToken 配置完成");
  console.log("");

  // 4. 设置 PancakeSwap Pair（如果已创建）
  const PANCAKESWAP_PAIR = process.env.PANCAKESWAP_PAIR || "";
  if (PANCAKESWAP_PAIR) {
    console.log("4. 设置 PancakeSwap Pair...");
    const setPairTx = await rwaToken.setPancakeSwapPair(PANCAKESWAP_PAIR, {
      gasPrice: gasPrice.gasPrice
    });
    await setPairTx.wait();
    console.log("✅ PancakeSwap Pair 配置完成");
    console.log("");
  }

  // 5. 设置白名单
  console.log("5. 设置白名单...");
  const whitelistAddresses = [
    stakingContractAddress,
    treasuryAddress,
    swapContractAddress,
    BACKEND_ADDRESS
  ];
  
  for (const addr of whitelistAddresses) {
    const whitelistTx = await rwaToken.setWhitelist(addr, true, {
      gasPrice: gasPrice.gasPrice
    });
    await whitelistTx.wait();
  }
  console.log("✅ 白名单配置完成");
  console.log("");

  // 6. 初始化 SwapContract 池子（可选，需要提供初始流动性）
  const INITIAL_LIQUIDITY = process.env.INITIAL_LIQUIDITY || "0";
  if (INITIAL_LIQUIDITY !== "0") {
    console.log("6. 初始化 SwapContract 池子...");
    const rwaAmount = ethers.parseEther(INITIAL_LIQUIDITY);
    const stRwaAmount = ethers.parseEther(INITIAL_LIQUIDITY);
    
    // 授权
    await rwaToken.approve(swapContractAddress, rwaAmount);
    await stRWA.approve(swapContractAddress, stRwaAmount);
    
    // 初始化
    const initPoolTx = await swapContract.initializePool(rwaAmount, stRwaAmount, {
      gasPrice: gasPrice.gasPrice
    });
    await initPoolTx.wait();
    console.log("✅ SwapContract 池子初始化完成");
    console.log("");
  }

  // ========== 阶段3：验证部署 ==========
  console.log("阶段3：验证部署");
  console.log("-".repeat(60));

  // 验证所有合约地址
  console.log("合约地址汇总:");
  console.log("  RWAToken:", rwaTokenAddress);
  console.log("  StRWA:", stRWAAddress);
  console.log("  TreasuryContract:", treasuryAddress);
  console.log("  StakingContract:", stakingContractAddress);
  console.log("  SwapContract:", swapContractAddress);
  console.log("");

  // 检查余额
  const finalBalance = await ethers.provider.getBalance(deployer.address);
  const usedBalance = balance - finalBalance;
  console.log("部署消耗:");
  console.log("  使用BNB:", ethers.formatEther(usedBalance));
  console.log("  剩余BNB:", ethers.formatEther(finalBalance));
  console.log("");

  // ========== 阶段4：保存部署信息 ==========
  const deploymentInfo = {
    network: "BSC Mainnet",
    deployer: deployer.address,
    timestamp: new Date().toISOString(),
    contracts: {
      RWAToken: rwaTokenAddress,
      StRWA: stRWAAddress,
      TreasuryContract: treasuryAddress,
      StakingContract: stakingContractAddress,
      SwapContract: swapContractAddress
    },
    gasUsed: usedBalance.toString(),
    cost: {
      bnb: ethers.formatEther(usedBalance),
      usdt: parseFloat(ethers.formatEther(usedBalance)) * 300 // 假设BNB价格300 USDT
    }
  };

  // 保存到文件
  const fs = require("fs");
  fs.writeFileSync(
    "deployment-info.json",
    JSON.stringify(deploymentInfo, null, 2)
  );

  console.log("✅ 部署信息已保存到 deployment-info.json");
  console.log("");
  console.log("=".repeat(60));
  console.log("✅ 标准启动部署完成！");
  console.log("=".repeat(60));
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
```

### 3.3 部署命令

```bash
# 1. 编译合约
npx hardhat compile

# 2. 运行部署脚本
npx hardhat run scripts/deploy-standard.ts --network bscMainnet

# 3. 验证合约（可选，免费）
npx hardhat verify --network bscMainnet <CONTRACT_ADDRESS> <PARAMS>
```

---

## 💡 四、成本优化策略

### 4.1 选择低峰时段部署

```
时间选择：
├─ 最佳时间：UTC 2:00-6:00（北京时间 10:00-14:00）
├─ 避免时间：UTC 12:00-18:00（高峰期）
└─ Gas价格差异：3 Gwei vs 5 Gwei（节省40%）

成本对比：
├─ 正常时段（5 Gwei）：21.3 USDT
└─ 低峰时段（3 Gwei）：12.78 USDT

节省：8.52 USDT（40%）
```

### 4.2 批量部署优化

```
优化前：
├─ 单独部署每个合约
├─ 每次需要额外初始化Gas
└─ 总成本：25-30 USDT

优化后：
├─ 使用部署脚本批量部署
├─ 减少重复初始化
└─ 总成本：20-25 USDT

节省：5 USDT
```

### 4.3 代码优化

```
优化点：
├─ 减少存储变量
├─ 使用事件替代存储
├─ 优化循环逻辑
└─ 使用库函数

Gas节省：20-30%
成本节省：4-6 USDT
```

---

## 📊 五、预算分配

### 5.1 100 USDT 预算分配

```
核心合约部署：15 USDT（30%）
├─ RWAToken：2.5 USDT
├─ StakingContract：5 USDT
├─ StRWA：1.5 USDT
├─ TreasuryContract：3 USDT
└─ SwapContract：3 USDT

初始化配置：2 USDT（4%）
├─ 地址设置：0.5 USDT
├─ 白名单配置：0.5 USDT
└─ 池子初始化：1 USDT

安全机制：3 USDT（6%）
├─ 多签钱包：0.5 USDT
├─ 时间锁：1 USDT
└─ 其他安全：1.5 USDT

备用资金：20 USDT（40%）
├─ Gas价格波动：10 USDT
├─ 意外情况：5 USDT
└─ 后续升级：5 USDT

测试和验证：5 USDT（10%）
├─ 测试网测试：0 USDT（免费）
├─ 合约验证：0 USDT（免费）
└─ 功能测试：5 USDT

审计费用：55 USDT（110%）
├─ 基础审计：$3,000-5,000
└─ 约 55 USDT（如果包含在预算内）

总计：100 USDT
```

### 5.2 实际成本预估

```
最乐观情况（低峰时段+优化）：
├─ 核心合约：12.78 USDT
├─ 初始化：0.6 USDT
├─ 安全机制：1.5 USDT
└─ 备用资金：5 USDT

总计：19.88 USDT

正常情况（正常时段）：
├─ 核心合约：21.3 USDT
├─ 初始化：1 USDT
├─ 安全机制：2 USDT
└─ 备用资金：10 USDT

总计：34.3 USDT

最坏情况（高峰期+意外）：
├─ 核心合约：30 USDT
├─ 初始化：2 USDT
├─ 安全机制：3 USDT
└─ 备用资金：20 USDT

总计：55 USDT

结论：100 USDT 预算完全足够，有 45-80 USDT 的缓冲空间
```

---

## ✅ 六、部署检查清单

### 6.1 部署前检查

- [ ] 钱包余额 ≥ 0.2 BNB（约 60 USDT）
- [ ] 环境变量配置正确
- [ ] 合约代码已编译
- [ ] 测试网测试通过
- [ ] 备份私钥和助记词

### 6.2 部署中检查

- [ ] 每个合约部署成功
- [ ] 获取合约地址
- [ ] 记录交易哈希
- [ ] 监控Gas消耗

### 6.3 部署后检查

- [ ] 所有合约地址正确
- [ ] 初始化配置完成
- [ ] 白名单设置正确
- [ ] 合约验证成功（可选）
- [ ] 功能测试通过

---

## 🎯 七、部署时间表

### 第1天：准备和测试

```
上午：
├─ 准备部署钱包
├─ 配置环境变量
├─ 在测试网测试部署脚本
└─ 检查Gas价格

下午：
├─ 等待低峰时段（Gas价格低）
├─ 准备部署
└─ 备份所有信息
```

### 第2天：主网部署

```
上午（低峰时段，UTC 2:00-6:00）：
├─ 部署核心合约（5个）
├─ 初始化配置
├─ 设置白名单
└─ 验证部署

下午：
├─ 功能测试
├─ 合约验证（BSCScan）
└─ 文档记录
```

**预计总时间**：1-2天

---

## 📝 八、部署后操作

### 8.1 合约验证

```bash
# 验证 RWAToken
npx hardhat verify --network bscMainnet \
  <RWAToken_ADDRESS> \
  "RWA Token" "RWA" \
  1000000000000000000000000000 \
  <TREASURY_ADDRESS> \
  <LIQUIDITY_FUND_ADDRESS>

# 验证 StakingContract
npx hardhat verify --network bscMainnet \
  <StakingContract_ADDRESS> \
  <USDT_ADDRESS> \
  <RWA_TOKEN_ADDRESS> \
  <TREASURY_ADDRESS> \
  <BACKEND_ADDRESS>

# ... 其他合约验证
```

### 8.2 创建 PancakeSwap 交易对

```
1. 访问 PancakeSwap
2. 创建 RWA/USDT 交易对
3. 添加初始流动性
4. 获取 Pair 地址
5. 在合约中设置 Pair 地址
```

### 8.3 转移所有权

```
1. 部署多签钱包（Gnosis Safe）
2. 部署时间锁（TimelockController）
3. 将合约 Owner 转移到时间锁
4. 将时间锁 Admin 转移到多签钱包
```

---

## 💰 九、成本监控

### 9.1 实时成本追踪

```typescript
// 部署脚本中添加成本追踪
async function trackCost(operation: string, tx: any) {
  const receipt = await tx.wait();
  const gasUsed = receipt.gasUsed;
  const gasPrice = receipt.gasPrice;
  const cost = gasUsed * gasPrice;
  
  console.log(`${operation}:`);
  console.log(`  Gas Used: ${gasUsed.toString()}`);
  console.log(`  Cost: ${ethers.formatEther(cost)} BNB`);
  console.log(`  Cost (USDT): ${parseFloat(ethers.formatEther(cost)) * 300} USDT`);
}
```

### 9.2 成本报告

```
部署完成后生成成本报告：
├─ 每个合约的Gas消耗
├─ 每个操作的Gas消耗
├─ 总成本（BNB和USDT）
└─ 预算使用情况
```

---

## ✅ 十、总结

### 标准启动方案

**预算**：100 USDT
**实际成本**：20-35 USDT
**缓冲空间**：65-80 USDT

**部署内容**：
- ✅ 5个核心合约
- ✅ 完整初始化配置
- ✅ 多签钱包和时间锁
- ✅ 合约验证

**成本控制**：
- ✅ 选择低峰时段部署（节省40%）
- ✅ 代码优化（节省20-30%）
- ✅ 批量部署（节省5 USDT）
- ✅ 充足的备用资金

**结论**：100 USDT 预算完全足够，有充足的缓冲空间应对意外情况。

---

**这是标准启动部署方案，成本控制在100美金以内。所有部署脚本和步骤都已详细说明。**
